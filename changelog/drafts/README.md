# Drafts Changelog

Chronological history for draft authoring, QA, and publish work.

Use `YYYY-MM.md` files by default. Split into `drafts/{domain}/YYYY-MM.md` only when a draft needs its own retained history.
