# Drafts Changelog - 2026-05

Chronological draft-authoring and draft-QA history migrated out of `Codex.md`.

<!-- Migrated from Codex.md on 2026-05-15 (Central Time). -->
<!-- Keep this file chronological. Keep durable decisions in Codex.md or ai-notes, not here. -->

- 2026-04-30 image aspect follow-up: `generic-media img[width][height]` must keep `height: auto` in global CSS so intrinsic `width`/`height` reserve aspect ratio without turning the image height attribute into a fixed rendered height when draft utility classes scale width.
- 2026-04-30 public asset cache follow-up: public image uploads should preserve long-lived immutable cache headers. The image-upload API now returns `Cache-Control` in direct upload presign headers and includes it in S3 `PutObject` params so optimized draft assets are cacheable at CloudFront/browser layers.
- 2026-05-01 Angora regression recovery: local `ngx-angora-css` debugging is connected by linking `node_modules/ngx-angora-css` to `../ngx-angora-css/dist/ngx-angora-css-library`. On `zoolandingpage.com.mx`, local dev/SSR checks show `btnBase` computed as `display:flex`, `align-items:center`, `padding:16px 24px`, `border-radius:8px`, with zero exact duplicate Angora rules. Local SSR Lighthouse after the final fix returned desktop `92/96/100/100` and mobile `100/100/100/100` for performance/accessibility/best-practices/SEO. `https://test.zoolandingpage.com.mx/` still showed stale/regressed signals during comparison, including duplicate Angora rules on desktop and incomplete mobile `btnBase` styles, until the fixed build is deployed.
- 2026-05-02 05:05 CT visual/performance closeout: `zoolandingpage.com.mx` process tabs broke because the component class collector only handled `config.classes` plus a few hand-listed dropdown/input keys; rich controls can put utilities in any `*Classes`/`*ClassName` property such as `containerClasses`, `tabButtonClasses`, and `panelClasses`. Keep `collectAllClassesFromComponents(...)` recursive over class-bearing keys so Angora receives utilities like `ank-gtc-lg-1fr__2fr`. Preserve critical CSS fallbacks for first-paint process-tab geometry, keep client bootstrap delayed until `load + 3000ms`, and release the static boot curtain at `load` when SSR already rendered content so Lighthouse and users see real SSR content while hydration still runs later or on first pointer/keyboard interaction. Final local SSR Lighthouse on `4312` returned desktop `100/96/100/100` and mobile `100/100/100/100` for performance/accessibility/best-practices/SEO; final Playwright audit found zero Angora diagnostics and zero duplicate rule groups across `zoolandingpage.com.mx`, `pamelabetancourt.com`, `erosbarajas.com`, and `alecfest-voliii.com`.
- 2026-05-02 06:10 CT dark-mode correction: the testing dark palettes are the reference palettes for `zoolandingpage.com.mx` and `alecfest-voliii.com`; do not replace them with a new brand interpretation. Zoo dark uses `#1a1a1a`, `#ffffff`, `#d8dadbff`, `#66b3ff`, `#225783ff`, `#2d2d2d`, `#d9dcdfff`, `#6cc3e6ff`, `#30a464ff`, and `#20673cff`. Alecfest dark uses `#070807`, `#ddd8ca`, `#eefb90`, `#ffd447`, `#97f23d`, `#12160f`, `#f4eedc`, `#fff8d6`, `#c1ff69`, and `#394f13`. Runtime theme changes must still write active and alternate palette tokens to `--ank-*` CSS variables on `document.documentElement`, and `ThemeService` must re-read saved theme after draft config/local-storage suffixes are available. `tools/tests/draft-dark-theme.spec.mjs` now guards the exact testing palettes plus practical contrast checks.
- 2026-05-02 loading curtain follow-up: the boot curtain is static critical HTML/CSS in `src/index.html`, then `LoadingCurtainService` configures it from `ui.loadingCurtain` once draft variables load and removes it after rendered classes are sent to Angora. Drafts may set `enabled`, `title`, `subtitle`, `logoUrl`, `background`, `foreground`, `accent`, `minVisibleMs`, and `exitDurationMs`; keep `minVisibleMs` conservative because it can hurt Lighthouse if it covers content after styles are ready. Final local checks at 2026-05-02 04:10 -06:00: `npm test` 308 passed, `npm run build` passed, local draft CSS audit on 4201/4300 had zero duplicate exact Angora rule groups for `zoolandingpage.com.mx`, `pamelabetancourt.com`, `erosbarajas.com`, and `alecfest-voliii.com`. SSR Lighthouse for `zoolandingpage.com.mx` returned `74/100/100/100`; remaining performance work is real TBT/hydration and SSR TTFB, not loader masking.
- 2026-05-04 CT Angora 1.6.1 release prep: reusable DOM-class and stylesheet readiness helpers moved into local `ngx-angora-css` as `collectRenderedDomClasses(...)`, `hasGeneratedCssRules()`, and `waitForCssReady(...)`; keep `zoolandingpage` delegating to those library APIs instead of reimplementing them in app services. Combo matching in the library should only treat a class as a combo instance when it starts with `${comboName}VAL`, so names such as `btnBase` and unrelated prefixes do not collide. Pamela draft palette regression now guards the Google Sites anchor color `#734332` plus the current local dark palette. Final local checks for this prep: `npm run angora:generate-css` against SSR `4315` produced 11 targets, 1235 normal rules, 24 responsive rules; Playwright desktop saw 1259 Angora stylesheet rules and 0 exact duplicate groups on all 11 routes; Playwright mobile saw no failed requests, console errors, overflow, or broken images on all 11 routes; Lighthouse mobile returned Zoo `100/100/100/92` and Pamela `100/95/100/100` for performance/accessibility/best-practices/SEO; `npm test` passed 315 app tests; `ngx-angora-css` library/app tests passed 45/5; production `npm audit --omit=dev` found 0 vulnerabilities in both repos; `npm publish --dry-run --json` produced `ngx-angora-css@1.6.1` with shasum `12d421956f78e735fd9186efa63a960831820bdc`.
- 2026-05-04 CT Angora draft audit follow-up: development bootstrap should not inherit the production `load + 3000ms` client delay because it makes local draft classes appear late even when SSR/Lighthouse still need deferred hydration in production. After render, `RuntimeService` must send both orchestrator-collected classes and actual DOM class tokens to `AngoraCombosService.updateRenderedDomClasses(...)`; this covers dynamic combo keys such as `payloadCodeBlock` and normalized host classes that do not appear in the authored component payload. Toast class bindings must normalize variable-driven Angora class strings before binding so generated rules and DOM tokens match. Treat `ng serve` Lighthouse as non-release evidence because HMR/dev bundles are about 14 MB and reported LCP around 75s in this run; use browser draft audits for local class readiness and production SSR Lighthouse for release performance. Local SSR remains invalid for `despacholegalastralex.com` and `music.lynxpardelle.com` until their runtime bundles are available to the SSR process, so keep validating those drafts on the dev server.
- 2026-05-04 CT Angora fallback cleanup: do not hardcode `ank-*` utilities or draft combo selectors such as `sectionBase`, `sectionTitle`, or `btnBaseVAL...` in `src/styles.scss`. Angora-owned classes must be generated by `ngx-angora-css` through the managed `angora-styles.css` and `angora-styles-responsive.css` sheets after runtime render/DOM-class replay. `styles.scss` may keep non-Angora global rules such as focus helpers, visually-hidden helpers, animation-name helpers, and `generic-media img[width][height]` intrinsic image sizing, but not Angora utility declarations. `tools/tests/critical-css.spec.mjs` now guards this rule.
- 2026-05-04 CT curtain/classes/overflow follow-up: the boot curtain must not begin its exit until `AngoraCombosService.waitForCssReady()` confirms generated rules in the managed Angora stylesheets and at least one paint has elapsed. Runtime post-render CSS refresh should combine orchestrator-collected classes plus document DOM classes into one explicit `cssCreate([...])` pass instead of running a premature full scan before render. For `zoolandingpage.com.mx`, keep the desktop header at the `xl` breakpoint because the full nav overflows at `lg`/992px; mobile header/menu remains visible below `xl`. Hero floating badges should use non-negative mobile offsets and only move outward at `xl` to avoid hidden or lateral-scrolling content on narrow screens.
- 2026-05-04 CT dev-server host follow-up: keep `architect.serve.options.allowedHosts` in `angular.json` aligned with local authoring URLs so both `localhost:4201` and `127.0.0.1:4201` can load drafts. The build target's `security.allowedHosts` is not enough for Vite/Angular dev-server host validation.
- 2026-05-04 CT static Angora performance closeout: do not move authored `ank-*` utilities back into `src/styles.scss`. For release-like local audits, generate Angora-owned CSS through `npm run angora:generate-css`, keep `/css/angora-styles.css` and `/css/angora-styles-responsive.css` linked from `src/index.html`, and keep those generated files out of the Angular `styles` bundle to avoid duplicating CSS. The local SSR draft API on `/runtime-bundle` and `/debug-workspace/*` lets production SSR audit all local drafts without falling back to public APIs. Final local SSR mobile Lighthouse on port `4315` after this change returned performance >= 94 across 11 audited routes; Playwright mobile QA saw 1259 Angora rules available at the first 900 ms sample, `docOverflowX: 0`, `tooWide: 0`, no broken images, no failed requests, and no bad responses. `drafts:smoke` still timed out after 304s without output in this run, so do not use it as evidence until the script is fixed or narrowed.
- 2026-05-06 CT Angora 1.6.1 deployment: after `ngx-angora-css@1.6.1` was published to npm, `zoolandingpage` was restored to the registry package and `node_modules/ngx-angora-css` was no longer a local junction. Keep `angora:restore-registry` pinned to `ngx-angora-css@1.6.1` until the next library release. Production builds should keep `optimization.fonts.inline` disabled so Angular does not need to fetch Google Fonts during build; do not work around certificate failures by disabling TLS verification. Drafts were pushed and published through the authoring API with `updatedBy: Codex`: `zoolandingpage.com.mx` `20260506T194500Z-1cdfad25c507`, `pamelabetancourt.com` `20260506T194501Z-49e9441cc891`, `erosbarajas.com` `20260506T194538Z-c33fa83cc70d`, `alecfest-voliii.com` `20260506T194544Z-9c01688a1790`, `despacholegalastralex.com` `20260506T194546Z-3ac20da84e2b`, and `music.lynxpardelle.com` `20260506T194551Z-47bbddbcd354`. Node `fetch` hit `UNABLE_TO_VERIFY_LEAF_SIGNATURE` against npm audit/config-authoring in this environment, while PowerShell using the Windows certificate store succeeded; GitHub pushes required repo-local `git config http.sslBackend schannel`.
- 2026-05-06 CT final testing draft audit: keep draft icon rendering independent from Google Material Icons. `generic-icon` now renders known Material-style and brand icons as local inline SVG paths, and `generic-card` delegates icon rendering through `generic-icon`; do not re-add a render-blocking Google Fonts Material Icons stylesheet just to display authored icon names. `generic-media` should not render `<img>`/media/link output when `src` resolves to an empty string because those empty image elements show up as broken images in browser QA. Zoo draft conversion hints were republished as `20260506T202328Z-14e73df8c397` with `updatedBy: codex-testing-audit` to avoid mobile overflow. Final remote Playwright smoke on 11 testing routes across desktop and mobile (`reports/release-audit/testing-css-smoke-20260506-final.json`) returned 0 failed requests, 0 console errors, 0 bad responses, 0 broken images, 0 overflow, no Google Fonts, and Angora CSS rules present at the first checkpoint (`cssReadyAtMs: 0`). Final remote Lighthouse mobile on all 11 routes (`reports/lighthouse/testing-20260506-final-mobile-rerun/summary.json`) returned performance scores from `94` to `100`, accessibility from `96` to `100`, best practices `100` on every route, SEO `92` to `100`, CLS `0` on every route, and TBT from `0 ms` to `90 ms`.
- 2026-05-06 CT Pamela visual fixes: runtime theme application must not depend on the optional Angular `REQUEST` token in browser hydration. `ThemeService` and `AppShellComponent` should use `isPlatformBrowser(...)` directly so SSR CSS variables, browser theme overrides, rendered-class replay, and scroll-driven value instructions all run in the right phase. Keep the managed `#zlp-theme-utility-overrides` style after theme sync so draft token utilities such as `ank-bg-bgColor`, `ank-text-titleColor`, `ank-color-white`, and `ank-text-inherit` resolve from active CSS variables instead of stale static Angora fallback colors. `collectAllClassesFromComponents(...)` should also extract `ank-*` classes embedded in `valueInstructions` so dynamic branches like Pamela's scrolled header state have generated CSS before interaction. Pamela was published through the config authoring API as version `20260506T223036Z-fa09e5eaa15b` with `updatedBy: codex-pamela-visual-fixes`; local SSR QA showed first render colors as `body rgb(244,231,225)`, text `rgb(91,77,71)`, H1/CTA `rgb(115,67,50)`, and scrolled header/link colors `rgb(255,255,255)`. Remote testing after `main-AHBBX3QK.js` deployed showed the same first-render palette, scrolled header/link colors `rgb(255,255,255)`, no console/request errors, no broken images, and no overflow. Remote Lighthouse for Pamela returned mobile `100/100/100/100` with LCP `1.1 s`, CLS `0`, TBT `0 ms`; desktop returned `99/100/100/100` with LCP `0.4 s`, CLS `0`, TBT `0 ms`.
- 2026-05-06 CT testing audit closeout: remove the unused Google Material Icons stylesheet from `src/index.html` unless a real `material-icons` or `mat-icon` usage is added; the unused external stylesheet and 129 kB font made Lighthouse treat Google Fonts as render-blocking and dropped remote Zoo mobile performance to `82` before removal. Testing deploy served the updated HTML without `fonts.googleapis.com` at 14:03:56 CT. Final remote Playwright smoke on `https://test.zoolandingpage.com.mx` for Zoo, Pamela, Eros, Alecfest, Astralex, and Music saw 0 failed requests, 0 console errors, 0 bad responses, 0 broken images, 0 overflow, and 1259 Angora rules on mobile. Final remote Lighthouse mobile: Zoo `99/96/100/100` with LCP `1.2 s`, CLS `0`, TBT `0 ms`; Pamela `100/95/100/100` with LCP `1.3 s`, CLS `0`, TBT `0 ms`. Broken remote draft images were fixed by uploading local files through presigned public asset uploads and replacing `/drafts/...` URLs with `https://assets.zoolandingpage.com.mx/...`; republished version IDs were Alecfest `20260506T195816Z-457f44354cda`, Astralex `20260506T195819Z-5169acbbcb8c`, Eros `20260506T195822Z-27d352498dc2`, and Music `20260506T195826Z-cf6a095e7a18`.
- 2026-05-06 CT Zoo visual fixes: when authored Zoo cards/process steps use Material icon names, add missing names to the local `generic-icon` SVG path map rather than restoring the external Material Icons webfont. The first Zoo pass added `filter_alt`, `cloud`, `saved_search`, `preview`, `rocket_launch`, `autorenew`, and `trending_up`. Zoo dark `successColor` should match the light WhatsApp CTA green `#198754`; this was published as draft version `20260506T212759Z-8be1f221a945` with `updatedBy: codex-zoo-visual-fixes`. Remote verification on testing found no leaked icon-name text, no broken images, no overflow, no console/request errors, and `Hablar por WhatsApp` computed as `rgb(25, 135, 84)` in both light and dark. Post-change Zoo mobile Lighthouse returned `99/100/100/100`, LCP `1.3 s`, CLS `0`, and TBT `0 ms`.
- 2026-05-07 16:09 CT Pamela SEO publish: `pamelabetancourt.com` was pushed and published through the config authoring API as version `20260507T220720Z-451a38ce5655` with `updatedBy: Codex` so `site.seo.defaultImage`, `openGraph.image`, and `twitter.image` use `https://assets.zoolandingpage.com.mx/pamelabetancourt.com/acerca-de-mi/images/pamela-betancourt-profile.webp` and `twitter.card` uses `summary_large_image`. In this environment, Node `fetch` in `tools/config-draft-sync.mjs` failed with `UNABLE_TO_VERIFY_LEAF_SIGNATURE` against both the stable and raw authoring endpoints; use PowerShell `Invoke-RestMethod` with the same documented `upsertDraft` and `publishDraft` payloads instead of disabling TLS verification.
- 2026-05-07 20:27 CT draft preview domain guardrail: production draft domains must ignore `draftDomain` query params so one public draft host cannot load another draft. Cross-draft preview through query params is allowed only on local hosts and `test.zoolandingpage.com.mx`; `debugWorkspace=true` follows the same host policy. Every draft should keep a skip-to-main link in the DOM for accessibility, but it must be visually hidden in its resting state; prefer the shared `.skipLink`/visually-hidden focus pattern over rendering the text persistently. Do not store deployment API keys or raw secret values in repo notes.
- 2026-05-07 CT debug workspace gating: local browser hosts (`localhost`, `127.0.0.1`, `::1`) should show debug workspace tools automatically, including the local draft registry. Non-local production/testing hosts should hide debug tools unless `debugWorkspace=true` is present, and even then they must not expose or refresh the draft registry. When the query flag is present, load the versioned `/drafts/_debug/debug-workspace/*.json` static assets from same-origin so QA does not depend on cross-origin API CORS or Node/Express-only routes. If browser bootstrap already completed before `RuntimeService.connect(...)`, connect must still configure debug workspace payloads so the panel does not mount empty. The debug workspace UI should use current draft theme tokens for Event Viewer text/background instead of fixed white text.
- 2026-05-07 CT music visual QA closeout: local draft visual QA can append `debugWorkspace=false` to hide the localhost debug workspace while preserving the default local auto-enable behavior when the query flag is absent. `music.lynxpardelle.com` visual loops need explicit loop bindings for nav, badges, cards, footer links, and modal text so authored templates do not render empty shells. The music draft was pushed and published as version `20260508T014834Z-a0eeea3b082a` with `updatedBy: Codex`; runtime-read confirmed that published version and kept `server/integrations.json` out of the browser bundle, while `/api-proxy/read` returned six filtered iTunes tracks. Production builds should set `apiProxyUrl` to the raw API Gateway proxy base (`[raw API Gateway proxy base]`) until the CloudFront `/api-proxy/read` front door stops intermittently resetting browser requests. In this environment, `music.lynxpardelle.com` did not resolve through DNS, so public browser QA was blocked at DNS; testing-host QA on `main-D4Q7GKKJ.js` passed three desktop/mobile browser passes with 6 tracks, no failed requests, no console errors, no overflow, and no debug overlay.
- 2026-05-07 CT navigation scroll behavior: internal client navigation now supports draft-configured scroll restoration through `runtime.navigation.scrollRestoration` with modes `preserve`, `top`, and `position`. Default remains `preserve` for existing drafts. `pamelabetancourt.com` sets `mode: "top"` so cross-page CTA/navigation clicks return to the top after the runtime refresh; local browser QA on `127.0.0.1:4201` covered 6 drafts, 22 route/viewport checks, and 3 repeated passes with 0 app-surface failures. Pamela navigation from `Home` to `Servicios` verified `scrollY` from `1526` before click to `0` after click.
- 2026-05-07 CT runtime API proxy deployment follow-up: `zoolanding-api-proxy` is a private GitHub repository at `LynxPardelle/zoolanding-api-proxy` and is deployed as SAM stack `zoolanding-api-proxy` in `us-east-1`. The raw API Gateway base and distribution identifiers are intentionally omitted here; the public API front door maps `https://api.zoolandingpage.com.mx/api-proxy/*` to the proxy origin. CORS is allowlisted for production, testing, and local QA origins, and proxy response filtering supports nested `allowedFields` paths such as `results.trackName` so drafts do not receive broad upstream records by default. `music.lynxpardelle.com` was republished as version `20260508T005634Z-c138b7dd3273` with the iTunes source enabled, server-only policy included, and analytics disabled because the draft has no consent UI.
- 2026-05-07 CT runtime API proxy design: drafts/sites must support multiple external API sources and actions, not a single integration. Keep public draft config limited to source/action IDs, mappings, refresh rules, and variable targets; put server-only upstream policies in `drafts/{domain}/server/integrations.json` so `config-authoring` can publish them with the normal draft package while `runtime-read` keeps them out of browser bundles. Real credentials belong in AWS Secrets Manager and are referenced only by `credentialRef`. The planned `zoolanding-api-proxy` should resolve `domain + sourceId/actionId`, load the published server-only policy, enforce method/request/response allowlists, and write frontend results into runtime variables such as `remote.*` and `remoteStatus.*`.
- 2026-05-07 CT runtime API proxy implementation follow-up: frontend runtime sources now use `RuntimeDataSourceService` to load multiple `site-config.json.runtime.dataSources[]` entries through `RuntimeApiProxyClientService`, map responses into `VariableStoreService` runtime overlays, and expose status under `remoteStatus.*`. Mutable draft actions use the `proxyAction` event handler with `runtime.apiActions[]` input allowlists. The local `zoolanding-api-proxy` repo is a no-secret SAM/Python 3.13 Lambda scaffold with tests for method/input/response allowlists, server-only `server/integrations.json` policy loading, Secrets Manager `credentialRef` lookup, and safe upstream errors. `music.lynxpardelle.com` contains a safe iTunes Search API policy for Lynx Pardelle songs plus static fallback variables; the draft source is disabled until `/api-proxy/read` is deployed to avoid local/testing CORS noise. Spotify/Tidal credentials were not added.
- 2026-05-08 01:04 CT API proxy CORS follow-up: `zoolanding-api-proxy` should not require a SAM parameter edit for every new draft domain. Keep platform origins in `AllowedCorsOrigins`, allow published draft origins dynamically from `zoolanding-config-registry`, and enforce that public draft origins may request only their own `domain` in proxy POST bodies. `test.zoolandingpage.com.mx` and local QA origins may preview other domains. For first-viewport draft identity assets, prefer same-origin app assets when a public CDN asset shows intermittent request resets during browser QA.
- 2026-05-09 16:26 CT Spotify runtime API decision: do not implement the Spotify album section for `music.lynxpardelle.com` while the project owner does not have Spotify Premium. The Spotify Developer Dashboard blocked Web API selection behind Premium, and Spotify docs state Development Mode apps require the app owner to have an active Spotify Premium subscription. If this is revisited later, use the existing runtime API proxy/Secrets Manager pattern and do not expose Spotify client secrets in draft config or browser payloads.
- 2026-05-09 16:38 CT TIDAL pause decision: keep the authored `music.lynxpardelle.com` TIDAL album section and server-only TIDAL policy in place for future reactivation, but do not render the section or call the runtime API while TIDAL catalog access is blocked. The draft pause is reversible by setting `tidalAlbumsSection.condition` back to a live condition/removing `all:false` and re-enabling the `lynx-tidal-albums` runtime data source.
- 2026-05-09 17:40 CT API proxy upstream compatibility: keep a default `User-Agent` on upstream proxy requests. PokeAPI returned `403` to Python `urllib` without a `User-Agent` and returned `200` with one; server-only static headers may override this default when an upstream API requires a different public header.
- 2026-05-09 17:40 CT PokeAPI demo closeout: `pokeapi-demo.zoolandingpage.com.mx` is the client-facing visual demo for runtime API drafts. It uses eight PokeAPI read sources through `zoolanding-api-proxy`, no secrets, server-only response allowlists, static fallback numbers/links, and container/media/text cards instead of `generic-card` images so the published testing build renders covers reliably. Published version: `20260509T233642Z-3feb20a40422`. Testing preview: `https://test.zoolandingpage.com.mx/?draftDomain=pokeapi-demo.zoolandingpage.com.mx&debugWorkspace=false`.
- 2026-05-09 22:55 CT parameterized runtime pages follow-up: runtime data sources can be scoped with `pageIds`, resolve input values from `literal`, `queryParam`, or `var` sources, and feed dynamic search suggestions into `search-box` configs through `valueInstructions`. `zoolanding-api-proxy` now supports server-owned `urlTemplate` values with allowlisted, scalar, percent-encoded placeholders so drafts can implement detail pages such as `/pokemon?name=charizard` without exposing arbitrary upstream URLs. `pokeapi-demo.zoolandingpage.com.mx` was republished as version `20260510T044815Z-42dfd6230702` with a PokeAPI search box and parameterized Pokemon detail route.
- 2026-05-09 23:12 CT PokeAPI testing deploy closeout: `zoolandingpage` was fast-forwarded and pushed through `main`, `dev`, and `test` at commit `0df759f`; testing served bundle `main-REKNAOOA.js` afterward. Remote QA on `https://test.zoolandingpage.com.mx` confirmed the PokeAPI home route and `/pokemon?name=charizard` detail route in desktop and mobile with no failed requests, bad responses, console errors, broken images, horizontal overflow, or visible raw theme icon text. Dynamic detail pages may initially SSR fallback values and then update after the proxy call; browser QA should wait for the target value or `SUCCESS` before capturing final evidence.
- 2026-05-09 CT TIDAL/runtime API follow-up: runtime data-source field mappings support `prefix` and `suffix` for safe display URL composition after proxy response filtering. The API proxy supports server-only static non-secret headers and `oauth2-client-credentials` auth, with real TIDAL credentials stored only in AWS Secrets Manager under a `credentialRef` such as `zoolanding/api/music/tidal`; never place client secrets in draft browser payloads or committed notes.
- 2026-05-10 19:08 CT PokeAPI catalog controls follow-up: loop configs now support reusable collection views with scope-driven filters, sort options, and pagination, including nested array paths such as `types.type.name` and `moves.move.name`. Runtime data sources still support multiple APIs per draft, but initial source loads run sequentially to avoid browser/proxy preflight bursts when a draft declares many sources. `pokeapi-demo.zoolandingpage.com.mx` was republished as version `20260511T010302Z-b0e0fc2e78a9` with paginated/sortable/filterable catalog controls, type/evolution/move filters, sun/moon theme icon switching, and a Charizard detail route that renders real move names from PokeAPI. `zoolandingpage` was pushed to `main`, `dev`, and `test` at commit `92db6ff`; testing served bundle `main-MBFHBYEI.js`. Local QA on `http://localhost:4201` and remote QA on `https://test.zoolandingpage.com.mx` covered desktop dark/light, Fire filter, Flamethrower filter, evolution filter, number-desc sort, Charizard moves, and mobile layout with 0 console errors, 0 failed requests, 0 bad responses, 0 horizontal overflow, and all proxy reads returning `200`.
- 2026-05-10 20:13 CT PokeAPI query-driven catalog follow-up: runtime data sources may declare `requiredInputKeys` so search/detail sources do not call the proxy until the required query/input exists. Loop bindings support `uriComponent`, `prefix`, and `suffix`, and loop view values can read query params so templates can link from item chips to new filtered views such as `/?move=mega-punch#move-matches`. `pokeapi-demo.zoolandingpage.com.mx` was republished as version `20260511T020320Z-6202d6af76cf`; the demo now searches beyond the initial eight Pokemon through the PokeAPI index, loads a single catalog result from `?pokemon=...`, hides catalog pagination during a single-result search, and opens move-result listings from Pokemon move links. Avoid `updateCombo(key, [])` for already-removed generated Angora combos because it can produce console noise; clear the registered combo map directly after confirming the key exists.
- 2026-05-10 22:00 CT PokeAPI catalog correction: movement links and filters should target the main `#pokemon-grid` list, not a separate related-attacks chip section. The demo uses the REST resource index (`/pokemon?limit=1500&offset=0`) for broad client-side name filtering because the hosted REST docs expose paginated lists plus get-by-id/name endpoints, not a general search query endpoint. Query-driven `move` and `type` routes mark mapped items with a server-source marker such as `catalogSource: "move"` or `"type"` so the list does not briefly show unrelated index cards while the narrower proxy source is loading. Keep the catalog apply action as a form submit that writes filter values into shareable query params. `pokeapi-demo.zoolandingpage.com.mx` was republished as version `20260511T040706Z-eec5a1f92c06` with `updatedBy: Codex`.
- 2026-05-11 19:10 CT PokeAPI numeric pagination closeout: add `pagination` as a reusable draft component instead of using numeric inputs for page navigation. Runtime mapper `metaFields` can expose API root metadata such as PokeAPI `count` next to `items`, allowing pagination totals to use upstream totals for default server-paginated reads and filtered collection length for query-specific reads. The PokeAPI demo now hides the old `Ver ataques` card action, moves move filtering into the main catalog list, puts `thunder-shock` near the top of the attack selector for demo QA, and uses <= 8px radii for catalog controls. Published draft version: `20260512T010858Z-55255e7a0d81`. Local QA on `http://127.0.0.1:4203/?draftDomain=pokeapi-demo.zoolandingpage.com.mx&debugWorkspace=false` ran three Playwright audit passes across desktop and mobile with 30 cases: default pagination, page size 8, `type=electric&move=thunder-shock`, `pokemon=pikachu`, page-2 numeric click, manual filter flow, mobile overflow, radius, no stale page input, and no browser console errors. Remote testing after the frontend deploy served `main-S3XZZEDP.js`; Playwright verified default pagination, `type=electric&move=thunder-shock`, numeric page-2 click on desktop, and mobile no-overflow with no console errors. Evidence: `Output/pokeapi-demo-qa/audit-final-summary.json` and `Output/pokeapi-demo-qa/remote-final-summary.json`.
- 2026-05-11 20:45 CT PokeAPI loading/detail closeout: PokeAPI list/type/move endpoints do not expose `evolves_from_species`; that field is available only through per-species detail reads, so the demo removed the visible Base/Evolution control instead of pretending the broad catalog can filter stage without a server-side enrichment/cache layer. `condition` DSL `any:` now works as an OR group, enabling API loading skeletons through `remoteStatus.*.state`; `link.scrollRestoration` and `navigateToUrl(..., top)` provide per-action top scrolling for Pokemon detail navigation without changing global draft scroll behavior. Published draft version: `20260512T024408Z-07f94c12a922`; frontend commit `9d7543a` was pushed to `main`, `dev`, and `test`, and testing later served `main-XAKQJ6IR.js`. Local focused Karma returned `31 SUCCESS`, `npm run build` passed, and local Playwright ran three desktop/mobile audit cycles covering skeletons, removed stage controls, filtered pagination, single-search pagination, detail scroll-top, no failed requests, no console errors, and no horizontal overflow. Remote Playwright after deploy covered default desktop/mobile, `type=electric&move=thunder-shock`, and click-through detail scroll-top with clean results. Evidence: `Output/pokeapi-demo-qa/20260511-loading-stage-audit/summary.json` and `Output/pokeapi-demo-qa/20260511-loading-stage-audit/remote-after-deploy-summary.json`.
- 2026-05-11 21:33 CT PokeAPI auto-search/detail skeleton follow-up: interaction scopes support optional `autoSubmit` config, either boolean-like or an object with `enabled`, `eventNames`, and `fieldIds`, so drafts can choose dropdown-driven automatic search while keeping text search on explicit submit. The PokeAPI demo enables auto-submit only for `type`, `attack`, `sort`, and `pageSize`, shows an authored mode hint, and leaves name search on the Search button. Runtime data-source startup now pre-marks every loadable source as `loading` before executing the sequential proxy calls, so query-driven filters can show skeletons immediately without reintroducing request bursts. The Pokemon detail page gates real detail sections on selected-source `success` plus item length and removes the `pikachu` fallback input, so the skeleton shows before the selected Pokemon loads. Published draft version: `20260512T034538Z-4f3fa36f6e09`; runtime-bundle verification confirmed the default and `pokemon-detail` pages on that published version. Local focused Karma returned `99 SUCCESS`, `npm run build` passed, and Playwright-core covered dropdown auto-search, manual text-search submit, immediate filtered skeleton state, detail skeleton state, no default Pikachu during loading, Charizard final state, no console errors, no failed requests, and no horizontal overflow. Evidence: `Output/pokeapi-demo-qa/20260511-autosearch-detail-skeleton/summary.json`.
- 2026-05-11 23:24 CT PokeAPI navigation-loading closeout: client-side draft navigation now pre-marks the active page runtime sources as `loading` before the runtime refresh begins, preventing a stale old render from flashing `Pagina 1 de 1 · 0 resultados` while a dropdown-driven filter waits on the API. Frontend commit `1004c3e` was pushed to `main`, `dev`, and `test`; testing served bundle `main-2KZQTKCP.js`. Remote Playwright-core QA completed three desktop/mobile cycles against `https://test.zoolandingpage.com.mx` with delayed `/api-proxy/read` calls and verified dropdown auto-search, button-only typed search, catalog skeletons, detail skeletons, no Pikachu fallback during detail loading, final Charizard state, no console errors, no failed requests, no bad responses, and no horizontal overflow. Evidence: `Output/pokeapi-demo-qa/20260511-autosearch-detail-skeleton/remote-after-navigation-fix-summary.json`. During deployment recovery, the Dokploy host disk-pressure incident was resolved with Docker cleanup and root-disk expansion, a pruned Lynx frontend image was rebuilt, all Swarm services returned to `1/1`, and the temporary SSH ingress rule was revoked.
- 2026-05-11 CT PokeAPI performance correction: avoid loading large default API indexes in-browser for every catalog click. Runtime data sources can now use `queryParamPageOffset` to convert `page` and `pageSize` query params into upstream `offset`, and `skipWhenQueryParams` to prevent broad default sources from running when narrower `pokemon`, `type`, or `move` sources are active. Loop pagination supports `applyWhenAnyQueryParam` so default server-paginated sources are not paginated twice, while query-filtered collections can still paginate client-side. For API-fed card loops with repeated child template loop configs, the loop materializer caches identical collection views per render so filters/sorts are computed once instead of once per child component. Interaction-scope configure/register effects should avoid no-op signal writes and use `untracked(...)` around registration/configuration effects to prevent `NG0103` change-detection loops. `pokeapi-demo.zoolandingpage.com.mx` was pushed and published as draft version `20260511T223646Z-2c0cd92d5baa`; the custom API domain reset during authoring operations, while raw fallback endpoints completed publish/runtime verification. Do not defer critical `generic-input` controls behind a zero-size viewport placeholder; PokeAPI catalog filters/pagination disappeared until `generic-input` rendered directly. For testing preview forms, include hidden native sticky fields such as `draftDomain` and `debugWorkspace` so SSR pre-hydration form submits do not leave the selected draft.
- 2026-05-12 12:01 CT PokeAPI dynamic type options closeout: avoid hardcoding short API enum lists in drafts when the upstream exposes a stable list endpoint. Runtime data-source mappers now support `prependItems` for local options such as `Todos` and `titleCase` for friendly API labels. `pokeapi-demo.zoolandingpage.com.mx` loads type options from PokeAPI `/type` through the server-only `pokeapiTypes` source, with a complete static fallback for loading or upstream failure. Published draft version: `20260512T174249Z-a3111c2c587b`; frontend commit `3c79188` was pushed to `main`, `dev`, and `test`, and testing served `main-JVSVVG5U.js`. Local and remote QA each ran three desktop/mobile cycles confirming the `pokeapiTypes` proxy read returned `200`, all 21 current PokeAPI types were visible, selecting `Fairy` updated the catalog, and there were no console errors, failed requests, bad responses, or horizontal overflow. Evidence: `Output/pokeapi-demo-qa/20260512-dynamic-types-local/summary.json` and `Output/pokeapi-demo-qa/20260512-dynamic-types-remote/summary.json`.
- 2026-05-12 12:47 CT PokeAPI special-type and curtain closeout: PokeAPI exposes `stellar`, `unknown`, and `shadow` from `/type`, but direct API checks showed all three have `pokemonCount: 0`; keep those special zero-Pokemon types out of the demo type dropdown unless a future UI explicitly explains them. The demo now requests only the 18 standard Pokemon types from the dynamic PokeAPI type source and keeps the static fallback aligned to the same set. The boot curtain early-release path now checks static stylesheet coverage for SSR-rendered classes before removing the curtain; when static CSS cannot yet cover the rendered draft, the curtain remains until Angular/Angora readiness removes it. Published draft version: `20260512T183326Z-2a869fdc8d7d`; frontend commit `43cce76` was pushed to `main`, `dev`, and `test`, and testing served `main-3NKXSM4W.js`. Remote Playwright QA ran three desktop/mobile cycles confirming the curtain stayed visible after `load`, the type dropdown showed `Todos` plus the 18 standard types without `Shadow`, `Unknown`, or `Stellar`, `Fairy` filtering returned `Pagina 1 de 22 · 88 resultados`, and there were no console errors, failed requests, bad responses, or horizontal overflow. Evidence: `Output/pokeapi-demo-qa/20260512-special-types-curtain/summary.json`.
- 2026-05-12 17:12 CT PokeAPI type-theme/redesign follow-up: runtime data-source mapper `lookup` values may now return object maps such as inline style tokens, and generic container/link/text/button components can bind dynamic `config.styles`. Keep API-specific labels, route values, and per-type styling in draft mapper lookups instead of `src/app`; production app code should stay generic. Angora combo payload values must remain arrays of class strings, not string scalars. `pokeapi-demo.zoolandingpage.com.mx` was published as version `20260512T232317Z-b47011634924`, frontend commit `8062be5` was pushed to `main`, `dev`, and `test`, and testing served `main-7KSLIC5R.js`. Evidence: `Output/pokeapi-demo-qa/20260512-type-theme-redesign-after-css/summary.json` and `Output/pokeapi-demo-qa/20260512-type-theme-redesign-remote/summary.json`.
- 2026-05-12 19:08 CT PokeAPI autocomplete/switch follow-up: `generic-input` now supports authored `controlType: "switch"` plus text input `autocompleteOptions`, and `interaction-scope.autoSubmit` can be gated by `enabledFieldId`/`enabledPath` so drafts can switch between dropdown auto-submit and button-applied search text without app-specific code. Keep typed text search out of auto-submit field IDs when authors want autocomplete-only typing. `pokeapi-demo.zoolandingpage.com.mx` was published as version `20260513T010827Z-8c16398e8fd8`; the draft hero no longer renders the old name-search box or `Ver criaturas` CTA, the catalog uses the switch-based auto-search mode, and the light palette uses the muted `#ebe3d4` background. Frontend commit `0adc759` was pushed to `main`, `dev`, and `test`; testing served `main-PQYXHFQO.js`. Local and remote QA each ran three desktop/mobile cycles covering autocomplete-only typing, switch-off manual filter apply, switch-on dropdown auto-search, light theme background, no empty `Ver ataques` card action, no console warnings, no bad responses, and no horizontal overflow. A stable remote hold check also returned no failed requests, no bad responses, no console warnings, 1350 autocomplete options, and zero broken images. Evidence: `Output/pokeapi-demo-qa/20260512-autocomplete-switch-local/summary.json` and `Output/pokeapi-demo-qa/20260512-autocomplete-switch-remote/summary.json`.
- 2026-05-12 21:41 CT PokeAPI search input guardrail: for `pokeapi-demo.zoolandingpage.com.mx`, text search must never auto-submit while typing. The authored auto-apply switch applies only selector-style fields (`type`, `attack`, `sort`, and `pageSize`), while `search` stays button-only with autocomplete suggestions. Published draft version: `20260513T033651Z-c5dfefe609eb`. Runtime-bundle verification confirmed `searchInAutoSubmit: false` and `autoSubmitFields: type,attack,sort,pageSize`; local and remote Playwright QA each ran three desktop/mobile cycles with typed `char` producing 0 `/api-proxy/read` calls, button search navigating to `pokemon=charmander`, selector auto-apply navigating to `type=fire`, 1350 autocomplete options, no console messages, no failed requests, no bad responses, and no horizontal overflow. Evidence: `Output/pokeapi-demo-qa/20260512-search-input-no-autosubmit/local-summary.json` and `Output/pokeapi-demo-qa/20260512-search-input-no-autosubmit/remote-summary.json`.
- 2026-05-13 01:06 CT PokeAPI SEO follow-up: the Lighthouse SEO blocker on the test preview came from `robots=noindex,nofollow` in the published draft config. `pokeapi-demo.zoolandingpage.com.mx` was pushed and published as draft version `20260513T063917Z-a0606ef1728b` with `robots=index,follow,max-image-preview:large`, explicit sitemap URLs for the root plus representative Pokemon detail pages, and `/pokemon` excluded when it has no `name` query. The public canonical host `pokeapi-demo.zoolandingpage.com.mx` now resolves through Route53 and Traefik to the Zoolandingpage testing service; no Dokploy API token or other secret was written to notes. App-level SEO work now supports `{{var:...}}` and `{{query:...}}` templates in SEO and JSON-LD, waits on only `ssr: true` runtime sources during SSR, escapes JSON-LD script content, and prevents unresolved templates from overriding fallback OG/Twitter images. Local verification: focused Karma specs returned `9 SUCCESS` and `68 SUCCESS`, `npm run build` passed, `npm run ssr:smoke` passed with 3 tests, local Lighthouse returned home `97/96/100/100` and Charizard detail `100/96/100/100` for performance/accessibility/best-practices/SEO, and three local browser QA cycles across desktop/mobile home/detail returned 12/12 clean checks. Remote verification after frontend commit `0d0c467`: testing served `main-2II4VBYZ.js`, root and Charizard detail returned resolved title/canonical/robots/OG/Twitter values with no head or JSON-LD templates, `sitemap.xml` listed explicit detail URLs without bare `/pokemon`, Lighthouse returned root `89/96/100/100` and Charizard detail `95/96/100/100`, and remote browser QA returned 12/12 clean checks. Evidence: `Output/pokeapi-demo-qa/20260513-seo/lighthouse-summary.json`, `Output/pokeapi-demo-qa/20260513-seo/browser-local-summary.json`, `Output/pokeapi-demo-qa/20260513-seo/remote/lighthouse-remote-final-summary.json`, and `Output/pokeapi-demo-qa/20260513-seo/remote/browser-remote-summary.json`.
- 2026-05-13 03:44 CT PokeAPI attack filter follow-up: for critical API-backed select controls, keep the dynamic `options.source` binding but author a complete fallback when deferred hydration could otherwise expose a short placeholder list before `/api-proxy/read` completes. `pokeapi-demo.zoolandingpage.com.mx` attack options now bind to `remote.pokemon.moveOptions.items` and carry a full 938-option fallback generated from PokeAPI `/move?limit=2000&offset=0`; the search input keeps `autocompleteMinLength: 3` and remains excluded from auto-submit. Published draft version: `20260513T091851Z-d50bad5f481b`. Remote Playwright-core QA ran three cycles on `https://test.zoolandingpage.com.mx`, confirming 938 attack options, `Shadow Sky` present, `Mega Punch` visually selected after navigating from Charizard detail, `Pagina 1 de 58 · 231 resultados`, no search API reads while typing, 3-character autocomplete suggestions, and zero mobile overflow. Evidence: `Output/pokeapi-demo-qa/20260513-attack-filter-audit/summary.json`.
- 2026-05-13 13:41 CT PokeAPI design polish closeout: `pokeapi-demo.zoolandingpage.com.mx` was pushed and published as draft version `20260513T194134Z-28891ce3529b` with a full visual pass over the home, catalog, and Pokemon detail templates. The draft now uses a quieter sage/light palette, tighter dark palette, compact top/detail bars, less-rounded controls, more deliberate card spacing, corrected action display classes, and the detail image caption no longer renders runtime status text such as `SUCCESS`. A static CSS collision was fixed by renaming generic PokeAPI section hooks such as `sectionEyebrow` to draft-prefixed hooks such as `pokeSectionEyebrow`; future drafts should avoid broad semantic class names when the shared static Angora stylesheet may also contain generated styles from other drafts. Verification included `node --test tools/tests/critical-css.spec.mjs`, `npm run build`, `tools/draft-smoke-check.mjs` against local SSR, and three browser QA cycles across desktop/mobile home, filtered catalog, and Charizard detail with no console errors, failed requests, bad responses, horizontal overflow, stuck loaders, or broken images. Evidence: `Output/pokeapi-demo-qa/20260513-design-review-after/browser-qa-3-cycles-final.json`.
- 2026-05-13 14:47 CT browser hydration loader follow-up: testing QA after the PokeAPI design publish found the boot curtain could remain visible over already rendered draft content when browser-only runtime services used the absence of optional `REQUEST` as part of the browser guard. Browser gating should rely on `PLATFORM_ID`/`isPlatformBrowser(...)`; keep `REQUEST` only for SSR request URL/header data. Apply the same rule to runtime config/data sources so browser navigation and query-driven API reads use `window.location` instead of a stale SSR request object. The runtime CSS-ready fallback was reduced to 4 seconds so the curtain does not block indefinitely if Angora readiness takes too long, while the existing static-CSS coverage guard still protects SSR pages from exposing unstyled content. An experimental static-stylesheet shortcut in `AngoraCombosService` was intentionally not kept because it did not measurably improve the PokeAPI demo and would have increased shared-runtime risk.
- 2026-05-13 15:58 CT boot curtain visual closeout: the boot curtain `leaving` state must make the fixed wrapper background transparent, not only move the split panels, so delayed DOM removal cannot leave an opaque overlay on top of rendered drafts. Static CSS coverage should ignore generic component hook classes that do not own visual styling, such as `btnIcon`; otherwise a harmless icon hook can prevent early curtain release even when all visible draft classes are covered. Local Playwright design QA after the fix completed 18/18 checks across three cycles for desktop/mobile home, filtered catalog, move-filtered catalog, and Charizard detail. Evidence: `Output/pokeapi-demo-qa/20260513-design-review-after/design-qa-3-cycles-final-stable-images.json`.
- 2026-05-13 CT Zoosite AI positioning: mention AI as a practical sales advantage only when it supports concrete outcomes such as faster content planning, clearer service pages, better copy iteration, SEO research support, measurement-driven improvements, and optional smart features. Avoid vague AI hype; keep the visible language simple and benefit-led.
- 2026-05-13 CT Zoosite campaign-readiness positioning: Zoosite should sell complete websites that are ready to receive traffic, convert visitors into contacts, and measure results from day one. Do not lead with what the team does not do; emphasize strengths such as analytics from launch, cloud architecture, fast pages, clear conversion paths, Google-ready structure, resilience, and the ability to support growth campaigns when the client is ready.
- 2026-05-13 CT Zoosite draft direction: `zoositioweb.com.mx` is the primary domain for the complete-website product; `zoosite.zoolandingpage.com.mx`, `sitiosweb.zoolandingpage.com.mx`, `quierounsitioweb.zoolandingpage.com.mx`, and `crearpaginaweb.zoolandingpage.com.mx` should be treated as aliases or supporting campaign domains. Position Zoosite as a product site for complete websites, not as the future consulting-company site. Use tiered plans with "from" pricing as the commercial model, organized by outcome in plain Spanish: professional presence, lead generation/trust, and custom growth. Keep visible copy free of technical jargon such as CTA, hero, SSR, schema, and API proxy; explain benefits with terms like contact buttons, measurement, cloud, Google, security, pages, forms, search, and integrations. Include a secondary ecosystem link to `zoolandingpage.com.mx` in the footer so the products start cross-linking without distracting from Zoosite conversion.
- 2026-05-13 CT Zoosite scope decision: build toward the large SEO hub option, not the compact site. The first implementation can be phased, but the information architecture should anticipate Google Ads traffic, campaign pages, sitelinks, conversion measurement, industry/location pages, guides, comparisons, examples, and plain-language service pages.
- 2026-05-14 CT draft workflow direction: do not add `.superpowers/` specs, plans, or companion artifacts to the repo. They are local planning artifacts only. Going forward, drafts should be reviewed in their own repositories and through the Lambda flow that creates, consumes, and updates them, rather than relying on repo-tracked `.superpowers` material in `zoolandingpage`.
- 2026-05-14 CT Zoosite ads landing decision: create one internal PyME-focused sales page first for Google Ads traffic, with direct messaging, benefits, plans, trust proof, FAQs, analytics events, and WhatsApp/project-review contact. Treat sector-specific ad pages as a planned phase 2, not a blocker for the first campaign page. The PyME page should be authored with reusable sections and copy patterns so future sector pages can specialize the same structure without rewriting from scratch.
- 2026-05-14 CT Zoosite cloud advantage decision: create a dedicated platform/technology page from the initial launch instead of only a supporting section. External research supports positioning cloud from day one as a business advantage around lower upfront infrastructure burden, pay-as-you-go scaling, faster launch/iteration, resilience/business continuity, measurement, security baselines, and future readiness for analytics and AI. Public copy must avoid overclaims such as guaranteed uptime, automatic security, or unlimited savings; include the necessary discipline around cost visibility, shared responsibility, privacy/data governance, backups, and provider lock-in in plain Spanish.
- 2026-05-14 CT Zoosite competitive pricing context: current public Mexican competitor examples include low-cost monthly pages around `$699/mes`, one-page or starter packages around `$3,990-$6,500 MXN`, multipage/business packages around `$5,490-$12,700 MXN`, and more customized PyME website packages around `$16,950 MXN` plus IVA. Use this as context only; Zoosite should position as premium-accessible because it includes analytics, cloud architecture, SEO structure, bilingual/theme support, and AI-assisted planning/copy, not as the cheapest option.
- 2026-05-14 CT Zoosite contact flow decision: use `WhatsApp guiado` as the first contact path. Primary buttons such as `Quiero mi sitio web` should open WhatsApp with page/plan-aware prefilled messages. `Revisar mi proyecto` can show a simple diagnostic prompt or section, but the initial submit/contact path remains WhatsApp until first-class forms and email/CRM integrations are productized. Track clicks by page, plan, sector, language, and theme where possible.
- 2026-05-14 CT Zoosite contact path: use the dual-contact route, with WhatsApp as the primary conversion action and a diagnostic/project-review action as the secondary route. Public buttons can use labels like `Quiero mi sitio web`, `Revisar mi proyecto`, and `Ver planes`, with analytics on WhatsApp clicks, plan clicks, industry-page clicks, language toggles, and theme toggles.
- 2026-05-14 CT Zoosite ecosystem link: connect to `zoolandingpage.com.mx` through the footer, not the header or a top banner. Present Zoolandingpage as a specialized product for campaign pages or individual landing pages, while Zoosite remains the complete-website product. Avoid making Zoolandingpage look like a cheaper substitute inside the primary Zoosite buying flow.
- 2026-05-14 CT Zoosite first industry package: start industry SEO pages with high-contact-intent service categories: consultorios, despachos, servicios locales, constructoras, inmobiliarias, and agencias. These should support specific copy around trust, contact capture, measurement, and clear service explanation.
- 2026-05-14 CT Zoosite footer decision: use the commercial footer plus site map on desktop: brand promise, primary contact action, main routes, industry links, resource links, legal/privacy links, ES/EN and light/dark controls, and `zoolandingpage.com.mx` as a product-sibling link. On mobile, keep the footer compact with the brand promise, primary contact action, product-sibling link, controls/legal row, and collapsible link groups so it does not consume excessive vertical space.
- 2026-05-14 CT Zoosite home order decision: use `Venta primero, confianza despues` for the home page structure: clear promise, measurable benefits, plans, industries, process, compact team proof, FAQ, and contact. Keep the team visible but secondary to selling complete websites, and reserve the Zoolandingpage product-sibling link for the footer.
- 2026-05-14 CT Zoosite home team cards: the compact home section should show Alec, Hector, Oswaldo, and Pamela with photos where available, a neutral person placeholder for Oswaldo, and one short achievement/title each. Draft titles to refine: Alec `Nube e inteligencia artificial para plataformas internacionales` with body copy mentioning `Bimbo`, `McGraw Hill`, and `Audio Enhancement`; Hector `Aplicaciones en la nube, datos e integraciones inteligentes`; Oswaldo `8+ anos creando sitios, tiendas y experiencias web`; Pamela `Marketing, diseno y analisis para conectar mensaje y mercado`.
- 2026-05-14 CT Zoosite implementation plan: Phase 1 planning was saved locally at `.superpowers/plan/feature-zoosite-seo-hub-1.md`. The plan implements the approved SEO hub foundation first: draft scaffold, shared shell/theme, 10 active Phase 1 routes, complete ES/EN content, route-level SEO, WhatsApp-first conversion, useful future previews, validation, browser QA, and closeout documentation. Do not implement Phase 2 sectors or Phase 3 resources until Phase 1 is verified and accepted.
- 2026-05-14 CT Zoosite implementation-planning answers: use the same WhatsApp number as `zoolandingpage.com.mx` for Zoosite; current repo docs record that number as `[existing public Zoolandingpage WhatsApp number]`, but implementation should verify the active Zoolandingpage contact config before publish. Phase 1 should include complete English content, not a short translation. Show future sector/resource previews, but make them useful and specific enough that they do not look like empty mockups; disable, mark upcoming, or avoid routing links until the destination content exists.
- 2026-05-14 CT Zoosite local draft copy/visual pass: `zoositioweb.com.mx` should use a pearl light background (`#ece7df`) and WhatsApp-like greens (`#128c7e` light, `#25d366` dark). Public copy should stay commercial and client-facing, avoiding explicit "confianza", guarantee/promise language, future feature hints, draft jargon, and negative framing. Home, Servicios, Planes, Plataforma, Contacto, PyMEs, Nosotros, and the three sector pages now use this tone locally; Servicios, Planes, Plataforma, and Contacto have distinct hero layouts. Internal draft navigation uses `runtime.navigation.scrollRestoration.mode = "top"`; the app validator accepts `runtime.navigation.scrollRestoration`. The skip link remains in the DOM but is hidden with `ank-d-none ank-display-none`. Local QA evidence: `.superpowers/qa/zoosite-all-routes/summary.json` covered all 10 routes in Spanish desktop/mobile plus navigation scroll and dark-theme green checks with 22 checks and 0 failures. Karma focused test timed out without useful output in this run; `npm run build` and draft package pack passed.
- 2026-05-14 CT Zoosite main promise decision: lead with the result-focused promise `Consigue mas clientes con un sitio profesional.` for the home page and adapt it for `/sitios-web-para-pymes`. Use the first supporting copy to connect complete websites with measurement from day one, Google-ready structure, clear contact paths, and credibility. Keep cloud architecture, resilience, analytics, and AI as visible differentiators below the promise and in a later dedicated section or page so the first screen stays commercial, not technical.
- 2026-05-14 CT Zoosite mobile footer decision: choose `Grupos plegables`. Keep brand, short promise, primary contact button, Zoolandingpage product-sibling link, language/theme controls, and privacy visible; put `Sitio`, `Sectores`, and `Recursos` links behind collapsible groups on phones.
- 2026-05-14 CT Zoosite navigation decision: use the balanced header option with `Inicio`, `Servicios`, `Sectores`, `Planes`, `Nosotros`, `Contacto`, visible ES/EN and light/dark controls, and a primary `Quiero mi sitio` action. Do not put `zoolandingpage.com.mx` in the header; place the product-sibling link in the footer.
- 2026-05-14 CT Zoosite Phase 1 local draft closeout: generated the first local draft payload set under `drafts/zoositioweb.com.mx` for 10 routes: `/`, `/servicios`, `/planes`, `/sitios-web-para-pymes`, `/plataforma`, `/nosotros`, `/contacto`, `/sectores/consultorios`, `/sectores/despachos`, and `/sectores/inmobiliarias`. Use clean local preview URLs with `draftDomain=zoositioweb.com.mx`, the matching `draftPageId`, `debugWorkspace=false`, and optionally `lang=es` or `lang=en`; otherwise the local dev server can show the debug Draft Workspace instead of the page. The Oswaldo team card uses the local placeholder asset `drafts/zoositioweb.com.mx/CVs_N_photos/oswaldo-placeholder.svg` until an approved photo is available. Local checks passed after fixing the stats template references and mapping card icons to the supported `generic-icon` SVG set: JSON/config/icon audit over 66 JSON files, draft pack via `tools/config-draft-sync.mjs`, and Playwright clean-preview QA across all 10 routes in Spanish and English on desktop and mobile. Keep the generator, QA evidence, and spec in `.superpowers/` as local-only material for the future Lambda/repo-specific draft review flow.
- 2026-05-14 CT Zoosite plan comparison decision: use a `Comparativa por resultado` table for plan comparisons. Public plan tables should explain business-fit, page count, contact paths, measurement, and growth potential in plain Spanish rather than a technical feature checklist. Preserve the `Presencia` `$3,000 MXN`, `Clientes` `$6,000 MXN`, and `Crecimiento` desde `$10,000 MXN` pricing, and phrase capabilities only as benefits the platform can currently support.
- 2026-05-14 CT Zoosite plan names: use `Presencia`, `Clientes`, and `Crecimiento` as the three main plan names. This keeps the offer outcome-led for PyMEs: look professional, convert visitors into contacts, and scale with more advanced content, integrations, and measurement.
- 2026-05-14 CT Zoosite platform navigation decision: make `Plataforma` visible in the main desktop navigation and include it in the mobile menu, grouped so the phone header does not become crowded. Update the initial commercial route set to include `/plataforma`. Keep `zoolandingpage.com.mx` out of the header and preserve it as a footer product-sibling link.
- 2026-05-14 CT Zoosite platform page decision: use `/plataforma` as the dedicated route for explaining the business value of the cloud-backed Zoosite platform. Frame it as `La base que hace crecer tu sitio`, covering cloud, measurement, stability, growth, data, AI readiness, cost discipline, and plain-language technical trust. In plan tables, later add an expandable `Detalles tecnicos` area per tier for cloud/resource limits such as monthly views or request allowances, but do not publish those limits until the team calculates real costs across all serving resources, including S3, Lambda, configuration delivery, rendering, analytics, and related infrastructure.
- 2026-05-14 CT Zoosite platform page structure decision: structure `/plataforma` with `Negocio primero`. Lead with the business problem and outcome: sites that are not measured, cannot scale, and are hard to improve. Then explain benefits such as measurement, stability, speed, growth, and how the platform helps PyMEs, freelancers, agencies, and larger companies. Keep architecture details, cost responsibility, shared responsibility, AI/data readiness, and technical implementation as supporting sections or accordions instead of the first sales block.
- 2026-05-14 CT Zoosite pricing strategy update: because the platform architecture can serve many sites through shared container/rendering and Lambda-backed configuration delivery, the first two tiers can be priced aggressively against competitors while remaining profitable. Use roughly `$500 MXN` per page as an internal pricing anchor for early tiers, but do not expose internal cost structure publicly. The `Crecimiento` tier can scale exponentially by scope, complexity, integrations, content volume, and custom work.
- 2026-05-14 CT Zoosite PyME landing page structure decision: use the `Conversion completa` option for `/sitios-web-para-pymes`. This page should be robust enough for Google Ads traffic: direct PyME promise, measurable benefits, WhatsApp/project-review contact actions, common business problems, included Zoosite value, plans, process, platform/team proof, FAQs, and a final contact close. Keep it commercial and conversion-led, not a short contact-only page and not a long editorial guide.
- 2026-05-14 CT Zoosite sector expansion decision: choose `Plantilla reutilizable + 3 sectores iniciales`. The first sector variants after the general PyME ads page should be `consultorios`, `despachos`, and `inmobiliarias`. Preserve the other first-package sectors (`servicios locales`, `constructoras`, and `agencias`) as follow-up pages after the first sector pattern is validated.
- 2026-05-14 CT Zoosite SEO hub execution decision: proceed with the broad SEO hub option as `Hub amplio por fases`, selected in the brainstorming companion as `c-phased`. Keep the full architecture for base commercial pages, sectors, guides, and comparisons, but implement Phase 1 first with the commercial foundation plus consultorios, despachos, and inmobiliarias. The working design spec was saved locally under `.superpowers/specs/2026-05-14-zoosite-seo-hub-design.md`; `.superpowers/` is local-only and must not be treated as repo-tracked source.
- 2026-05-14 CT Zoosite SEO hub priority: build the SEO hub with industry pages first. Keep comparison/guide content as a later blog or resources layer to support authority and education after the first commercial industry pages are in place.
- 2026-05-14 CT Zoosite SEO intent decision: use a page-level intent map for the initial SEO strategy. Assign each route a distinct search intent and conversion path to avoid internal cannibalization: home for brand/main offer and `sitios web para negocios`; `/sitios-web-para-pymes` for commercial PyME traffic and ads; `/servicios` for professional website-development service intent; `/plataforma` for cloud-backed trust and measurement intent; sector routes for industry-specific searches such as consultorios, despachos, and inmobiliarias. Add blog/guides later as supporting authority content, not as the initial commercial base.
- 2026-05-14 CT Zoosite SEO research closeout: focused SEO research was saved at `drafts/zoositioweb.com.mx/findings/2026-05-14-seo-research.md`. Key conclusions: keep `/sitios-web-para-pymes` as the first Google Ads page; use `/planes` for price comparison intent; use `/servicios` for professional website-development service intent; use `/plataforma` as trust/supporting proof, not the first paid-search destination; build first sector pages for consultorios, despachos, and inmobiliarias; avoid SEO guarantees, FAQ rich-result promises, email-delivering forms, login/portal claims, CRM/property portal promises, and sensitive-data collection until those product capabilities are implemented and reviewed.
- 2026-05-14 CT Zoosite SEO research scope decision: run a focused deep SEO research pass before finalizing metadata and page copy. Cover current search results, visible competitors, commercial keyword variants, sector-intent keywords, Google Ads landing-page readiness, page-level titles/descriptions, FAQ opportunities, and internal-linking structure. Keep the research focused on commercial pages and first sector routes rather than building a broad blog/content program in the initial launch.
- 2026-05-14 CT Zoosite sitemap decision: choose the large SEO hub option for `zoositioweb.com.mx`, not just the 10-page commercial base. The initial draft should support a stronger SEO footprint with base sales pages plus sector pages, location/country pages, comparison pages, and useful guides, while keeping content credible and not inventing unsupported claims.
- 2026-05-14 CT Zoosite theme palette decision: use the `Carbono + datos vivos` palette direction. Light mode should feel clean and premium with graphite text, green analytics accents, Google/cloud blue links, and coral action highlights. Dark mode should feel technical and high-contrast with the same accent logic. Implement both complete 18-token runtime palettes and wire visible theme/language controls.
- 2026-05-14 CT Zoosite tier pricing and capability boundary: set public starting prices to `Presencia` `$3,000 MXN`, `Clientes` `$6,000 MXN`, and `Crecimiento` desde `$10,000 MXN` to avoid cannibalizing the Zoolandingpage landing-page product. Public tier copy must describe only included benefits and avoid listing exclusions. `Presencia` should focus on simple contact/social links such as WhatsApp, phone, social profiles, map/location, or external booking links when provided. In `Clientes`, phrase the extra page as a public campaign/service page, not a login-protected internal page. Internally, do not promise email-delivering forms, email automation, login, accounts, or client portals until those app features are implemented and documented.
- 2026-05-14 CT Zoosite trust/team content: use platform/process proof as the primary trust section, plus a team section informed by the CV PDFs in `drafts/zoositioweb.com.mx/CVs_N_photos/`, the Pamela draft, and the user's role updates. Public copy can say the team combines cloud, AI, full-stack development, web delivery, design, marketing, ads support, and market/data analysis, but visible page copy should prefer plain Spanish such as nube, inteligencia artificial, desarrollo web completo, medicion, and publicidad en Google. Alec's update includes recent cloud/AI work and prior large/international company work with McGraw Hill, Bimbo, and Audio Enhancement. Hector's update includes work with Coppel (official spelling verified on `https://www.coppel.com/`) and recent cloud/AI focus. Oswaldo supports web development and publicidad en Google/marketing with Pamela; use a neutral person placeholder until his image is available. Pamela supports design, marketing, and market/data analysis. Do not publish CV contact details or private identifiers from source files. Use compact team cards with photos in home and richer profiles in `Nosotros`; CVs do not need to be updated.
- 2026-05-14 CT Zoosite URL structure decision: use Spanish descriptive routes with the ES/EN language selector for translated content. Initial commercial routes: `/`, `/servicios`, `/planes`, `/sitios-web-para-pymes`, `/nosotros`, and `/contacto`. Initial sector routes: `/sectores/consultorios`, `/sectores/despachos`, and `/sectores/inmobiliarias`. Later sector routes: `/sectores/servicios-locales`, `/sectores/constructoras`, and `/sectores/agencias`. Defer separate `/en/...` routes until a USA/English campaign justifies the extra canonical, sitemap, QA, and content work.
- 2026-05-14 CT Zoosite visual direction: use the `tecnologia premium y clara` direction as the visual base, with enough business-local warmth in copy and examples for PyMEs. The draft must support switchable light/dark themes and Spanish/English content, defaulting to Spanish unless a later decision changes it.
- 2026-05-14 22:46 CT Zoolandingpage alias update: `drafts/zoolandingpage.com.mx/site-config.json` now declares `zoolandingpage.com`, `sulandingpage.com.mx`, and `sulandingpage.com` alongside `test.zoolandingpage.com.mx`; `angular.json` build `security.allowedHosts` now includes those public hosts for deployment alignment. The draft was pushed and published through config authoring as version `20260515T045444Z-d975922a4b62` with `updatedBy: Codex`. Remote runtime-bundle checks resolved all three new aliases to `zoolandingpage.com.mx`, and live browser QA on `https://zoolandingpage.com/`, `https://sulandingpage.com.mx/`, and `https://sulandingpage.com/` passed desktop/mobile title, heading, canonical, `og:url`, no unresolved draft, no horizontal overflow, no broken images, no failed requests, and no console errors.
- 2026-05-15 CT Sulanding standalone draft authoring: `sulandingpage.com.mx` was promoted from alias status into its own local draft under `drafts/sulandingpage.com.mx`, while `drafts/zoolandingpage.com.mx/site-config.json` dropped the Sulanding aliases so the new domain owns its own route map. The active Sulanding Phase 1 route set is `/`, `/servicios`, `/planes`, `/landing-pages-para-pymes`, `/landing-pages-para-google-ads`, `/optimizacion-y-analitica`, `/nosotros`, `/preguntas-frecuentes`, `/contacto`, `/sectores/consultorios`, `/sectores/despachos`, and `/sectores/inmobiliarias`; all shared shell, route copy, canonicals, OG/Twitter metadata, and WhatsApp-first conversion flows were rewritten for the standalone landing-page brand without app changes. ES-only support removed the copied language toggles, draft-specific research was recorded at `drafts/sulandingpage.com.mx/findings/2026-05-15-sulanding-seo-research.md`, and the reusable cutover workflow was distilled into `ai-notes/how-to/promote-alias-into-standalone-draft.md`. Post-authoring cleanup removed the inactive copied folders `plataforma`, `sector-agencias`, `sector-constructoras`, and `sector-servicios-locales` from the Sulanding draft tree because they were not referenced by the final route map. Validation evidence: route-block JSON parsing during authoring, full-tree parse over 102 Sulanding JSON files, `tools/draft-smoke-check.mjs --domain=sulandingpage.com.mx --local-base-url=http://127.0.0.1:4200 --include-live=false` with 12 routes and 0 local failures (`reports/sulanding-smoke-2026-05-15.json`), and browser QA over all 12 active routes in desktop/mobile with 24 clean renders and 0 console/request/overflow failures (`reports/sulanding-browser-qa-2026-05-15.json`).
- 2026-05-15 CT Zoosite local design personality pass: revised `drafts/zoositioweb.com.mx` page component configuration so each active route has a clearer visual role without app-source changes. Home now reads more like a commercial storefront, Servicios like a process/workflow page, Planes like a comparison/pricing board, Plataforma like a technical foundation page, Contacto like an action-focused intake page, FAQs like a centralized help page, and sector pages use distinct industry accents. Card icon slots were filled through existing supported icons, and mobile CTA row spacing was adjusted in draft config to avoid overlap from wrapper height differences. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `npm run build`, `git diff -- src` empty, and Chrome CDP QA across 14 active routes in desktop/mobile with 28 checks and 0 failures (`.superpowers/qa/zoosite-design-personalities/summary.json`).
- 2026-05-15 09:44 CT Zoosite sector visual differentiation pass: used current sector website references to give the six Zoosite sector pages stronger individual layouts through draft configuration and `.superpowers` authoring scripts only. Consultorios now leans into calm appointment intake, despachos into formal practice-area structure, inmobiliarias into listing/search cues, servicios locales into service-area/contact speed, constructoras into project/portfolio proof, and agencias into case-study/portfolio rhythm. Shortened the constructoras hero and primary CTA copy after mobile QA found a 9px overflow. Validation evidence: full parse over 90 Zoosite JSON files, draft pack via `tools/config-draft-sync.mjs`, `npm run build`, `git diff -- src` empty, and Chrome CDP QA across 14 routes in desktop/mobile with 28 checks and 0 failures (`.superpowers/qa/zoosite-design-personalities/summary.json`).
- 2026-05-15 10:58 CT zoositioweb brand rename: updated the active `drafts/zoositioweb.com.mx` payload and local `.superpowers` authoring scripts so the public brand uses `zoositioweb` instead of the previous short name. Removed the short-name alias from `site-config.json`, updated visible header/loading/footer strings, SEO titles/descriptions/keywords, JSON-LD names, WhatsApp prefilled messages, localStorage keys, and QA readiness checks. Validation evidence: draft JSON parse, draft pack, `npm run build`, `git diff -- src` empty, and Chrome CDP QA across the 14 active routes in desktop/mobile.
- 2026-05-15 10:44 CT Draft GitHub backup rollout: created one private GitHub repository per domain draft and pushed sanitized `main` snapshots from each local draft repo: `LynxPardelle/draft-alecfest-voliii-com` (`62570cf`, 65 files), `LynxPardelle/draft-despacholegalastralex-com` (`7d6f679`, 17 files), `LynxPardelle/draft-erosbarajas-com` (`07d444f`, 18 files), `LynxPardelle/draft-music-lynxpardelle-com` (`e749e2c`, 21 files), `LynxPardelle/draft-pamelabetancourt-com` (`4dd9178`, 39 files), `LynxPardelle/draft-pokeapi-demo-zoolandingpage-com-mx` (`498c3a3`, 18 files), `LynxPardelle/draft-sulandingpage-com-mx` (`30eb9bf`, 80 files), `LynxPardelle/draft-zoolandingpage-com-mx` (`fd8ea06`, 15 files), and `LynxPardelle/draft-zoositioweb-com-mx` (`0588964`, 92 files). `_debug` stayed local because it is a debug workspace, not a domain draft. Per-draft `.gitignore` files keep local-only notes, findings, error reports, CV/photo folders, PDFs, logs, `.env*`, private keys, certificates, and local databases out of GitHub. Verification passed in three layers: pre-stage path/secret blocking, committed local `git ls-files` checks with 0 blocked tracked paths and 0 high-confidence secret hits, and remote GitHub tree checks with all repos `PRIVATE`, branch `main`, and 0 blocked remote paths.
- 2026-05-16 23:08 CT zoositioweb local restart QA: after restarting the workstation, the local dev server was relaunched on `127.0.0.1:4201` and the active `zoositioweb.com.mx` draft was reviewed across all 14 routes in desktop/mobile. Visual review found the mobile header menu trigger rendered as an empty square; the draft config now replaces it with visible wrapped mobile navigation chips, and the local Chrome CDP QA script now fails on visible blank links/buttons. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `git diff -- src` empty, and Chrome CDP QA with 28 checks and 0 failures (`.superpowers/qa/zoositioweb-design-personalities/summary.json`).
- 2026-05-17 00:12 CT zoositioweb home review pass: applied the first home-review corrections through draft configuration and `.superpowers` scripts only. Removed encapsulated eyebrow/kicker labels, muted the light secondary background, added measurable events to header/footer/home CTAs and sector links, gave hero accordions a visible open affordance, changed the home price stat to `desde $3,000 MXN`, merged sector cards into one SEO-oriented section, removed the footer WhatsApp CTA container, and redesigned the final home CTA so it is not nested into a card or overlapping the footer. The header now uses explicit desktop links plus a tested mobile hamburger menu, and footer groups remain compact without the removed Zoolandingpage/footer CTA block. Validation evidence: 90 draft JSON files parsed, `npm run build`, `git diff -- src` empty, in-app browser mobile menu open check, and Chrome CDP QA across all 14 routes in desktop/mobile with 28 checks and 0 failures (`.superpowers/qa/zoositioweb-design-personalities/summary.json`).
- 2026-05-17 00:40 CT zoositioweb home footer/sector refinement: applied the next home-review corrections through draft configuration and the local `.superpowers` generator only. The light secondary background changed to `#bcc1ba`, the home price stat now starts as `Desde $3,000 MXN`, sector navigation links became their own card-style links with SEO-oriented titles, the final home CTA now uses a single inner box without the green left rule, and the footer was simplified to useful route and sector columns with `Sectores` as the heading. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, targeted home style inspection, `npm run build`, `git diff -- src` empty, and Chrome CDP QA run three times across all 14 active routes in desktop/mobile with 28 checks and 0 failures each run (`.superpowers/qa/zoositioweb-design-personalities/summary.json`).
- 2026-05-17 00:56 CT zoositioweb home sector links: moved the six home sector route links into their corresponding sector cards and removed the separate bottom link grid from the sector section. Each sector card now carries its own route URL and visible link label in ES/EN. Validation evidence: 90 draft JSON files parsed, targeted browser inspection confirmed 6 card links and no `defaultSectorCtaLinks` render, Chrome CDP QA across all 14 active routes in desktop/mobile passed with 28 checks and 0 failures, `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-17 01:15 CT zoositioweb home annotation pass: applied the latest home annotations through draft configuration and the local `.superpowers` generator only. Hero accordion toggles now track both open and close events, the home language stat now says `Múltiples idiomas` with localized-content copy, the `Ver planes` and `Ir a contacto` secondary CTAs use outline-button styling through Angora utility classes, and footer sector links now use SEO-oriented `Sitio web para...` labels in ES/EN. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, in-app browser DOM inspection confirmed the visible home/footer text, Chrome CDP QA across all 14 active routes in desktop/mobile passed with 28 checks and 0 failures, `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-17 03:42 CT zoositioweb home hero/footer polish: corrected the home hero CTA row and footer layout through draft configuration and the local `.superpowers` generator only. Normalized hero CTA heights/alignment and widened the footer into a less crowded responsive grid with sector links arranged in columns. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, Chrome CDP QA across all 14 active routes in desktop/mobile passed with 28 checks and 0 failures, `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-17 03:56 CT zoositioweb native Angora outline fix: removed the draft-local `btnOutline` combo and switched the home secondary CTAs to the native Ngx Angora button utility `ank-btnOutline-linkColor-bgColor` plus layout utilities. This matches the `ngx-angora-css` library behavior verified in the sibling repo examples and `parseClass('ank-btnOutline-primary-success')` spec. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, in-app browser inspection confirmed both secondary CTAs use `ank-btnOutline-linkColor-bgColor` and no standalone `btnOutline` combo class, Chrome CDP QA across all 14 active routes in desktop/mobile passed with 28 checks and 0 failures, `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-17 04:03 CT zoositioweb outline border style: added `ank-bs-solid` to the home secondary CTAs while keeping the native Ngx Angora outline utility `ank-btnOutline-linkColor-bgColor`. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, Chrome CDP QA across all 14 active routes in desktop/mobile passed with 28 checks and 0 failures, `npm run build` passed, and `git diff -- src` stayed empty. Direct in-app browser inspection timed out and reset the browser REPL kernel during this pass, so the final visual evidence came from the CDP QA script plus JSON inspection.
- 2026-05-17 13:22 CT zoositioweb computed outline correction: browser computed-style inspection showed `ank-bs-solid` left the home secondary CTAs with `borderStyle: none` and `borderWidth: 0px`. Replaced it with `ank-borderStyle-solid ank-borderWidth-1px` while keeping `ank-btnOutline-linkColor-bgColor`; computed styles now show `borderStyle: solid`, `borderWidth: 1px`, and `borderColor: rgb(20, 99, 255)` for both `Ver planes` and `Ir a contacto`. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, Chrome CDP QA across all 14 active routes in desktop/mobile passed with 28 checks and 0 failures, `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-17 14:08 CT zoositioweb link-blue contrast pass: muted the draft link tokens from the bright blue to `#214c72` in light mode and `#6097b5` in dark mode, updating `linkColor`, `secondaryLinkColor`, and `infoColor` in the local draft config and home-review generator. The home secondary CTAs keep the native `ank-btnOutline-linkColor-bgColor` outline border, add a secondary-background hover, and set inline text color from `var(--ank-linkColor)` to avoid the generic link component inheriting low-contrast text during hover. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, Chrome CDP forced-hover contrast checked `4.90` in light hover and `4.80` in dark hover, Chrome CDP QA across all 14 active routes in desktop/mobile passed with 28 checks and 0 failures, `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-17 14:18 CT zoositioweb home CTA hover parity: restored the distinct final home `Quiero mi sitio web` WhatsApp message after confirming the issue was hover behavior, not the prefilled text. The hero outline CTA keeps a secondary-background hover, while the final outline CTA uses a base-background hover so the interaction remains visible inside the secondary final box; the local home-review generator now preserves that split. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `npm run build` passed, Puppeteer hover inspection confirmed hero hover `rgb(188, 193, 186)` and final hover `rgb(236, 231, 223)`, Chrome CDP QA across all 14 active routes in desktop/mobile passed with 28 checks and 0 failures, and `git diff -- src` stayed empty.
- 2026-05-17 22:53 CT zoositioweb servicios accordion affordance: added an `expand_more` icon to the `/servicios` hero-panel accordion buttons so labels like `Estructura` clearly read as expandable controls. The local page-personality script now preserves the same button config for future regeneration. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, in-app browser inspection confirmed the `/servicios` accordion buttons render SVG icons, `node --check` passed for the local script, and `git diff -- src` stayed empty.
- 2026-05-17 22:57 CT zoositioweb servicios hero spacing: added a small `8px` bottom margin to the `/servicios` hero lead paragraph so the CTA row has more breathing room without changing the copy. The local page-personality script now preserves the spacing. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, in-app browser inspection confirmed `marginBottom: 8px` and a `26px` gap to the buttons, and `git diff -- src` stayed empty.
- 2026-05-17 23:00 CT zoositioweb servicios AI-copy mention: updated the `/servicios` `Texto comercial` card in ES/EN to mention AI as support for organizing ideas and drafting copy, with explicit human review and commercial judgment. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, in-app browser inspection confirmed the Spanish card text, and `git diff -- src` stayed empty.
- 2026-05-17 23:05 CT zoositioweb servicios production-step icon: changed the `/servicios` process step 3 icon from `account_tree` to `developer_board` in ES/EN so `Producir el sitio` feels more technological. Validation evidence: targeted ES/EN i18n inspection confirmed only step 3 uses `developer_board`, 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, in-app browser inspection confirmed the rendered step 3 icon, and `git diff -- src` stayed empty.
- 2026-05-17 23:14 CT zoositioweb servicios final CTA container: changed the `/servicios` final CTA outer section background to the base page color and added a padded inner box with secondary background, border radius, and box sizing through draft configuration plus the local `.superpowers` page-personality script. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, in-app browser computed styles confirmed `section#contacto` background `rgb(236, 231, 223)` and `serviciosFinalInner` padding `52px`, and `git diff -- src` stayed empty.
- 2026-05-17 23:25 CT zoositioweb servicios process icon correction: replaced the unsupported `/servicios` process step 3 `developer_board` icon with the supported local SVG icon `auto_awesome` in ES/EN so the card no longer renders raw icon text. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, in-app browser inspection confirmed the `3. Producir el sitio` card renders an SVG path and no visible `developer_board` or `auto_awesome` text, and `git diff -- src` stayed empty.
- 2026-05-17 23:32 CT zoositioweb planes hero wrapper cleanup: removed the rendered `planesHeroInner` wrapper from the `/planes` hero by rendering `planesHeroCopy` directly inside the hero section, then centered the title, copy, and CTA row through draft configuration plus the local `.superpowers` page-personality script. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, in-app browser inspection confirmed `planesHeroInner-container` is absent, title text alignment is centered, hero copy alignment is centered, CTA `justify-content` is `center`, and `git diff -- src` stayed empty.
- 2026-05-17 23:54 CT zoositioweb CTA display-block correction: normalized draft CTA/link-button rendering so `/planes` hero buttons and shared interactive links use `ank-d-block` instead of inline/inline-block defaults where that display caused spacing issues. The local `.superpowers` scripts now preserve the block-link normalization for regenerated draft pages, while app source remains untouched. Validation evidence: 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the relevant `.superpowers` scripts, in-app browser inspection confirmed `/planes` hero CTA links compute to `display: block`, the CTA row keeps `8px` top/bottom spacing, no visible interactive links/buttons on `/planes` compute to inline/inline-block, and `git diff -- src` stayed empty.
- 2026-05-18 00:01 CT zoositioweb planes card stagger: adjusted the `/planes` comparison cards through draft configuration and the local page-personality script so the first card uses the validated Ngx Angora class `ank-transform-translateYSD2remED`, the second uses `ank-transform-translateYSDMIN2remED`, and the third stays unshifted. Removed the old inline `transform` styles from the cards so Angora classes control the layout. Validation evidence: `ngx-angora-css` `validateClass()` returned valid rules for both transform classes, 90 draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, in-app browser computed styles confirmed `matrix(..., 32)` for the first card, `matrix(..., -32)` for the second, no inline transform on those cards, and `git diff -- src` stayed empty.
- 2026-05-18 10:18 CT zoositioweb planes final-section redesign: after the user clarified the target was `/planes`, home was restored to its previous contained close and the `/planes` closing section before the footer was changed from an inner card-like block into a full-width secondary band with transparent inner layout, no inner borders, right-aligned CTA group, and the existing plan recommendation WhatsApp/contact CTAs preserved. The local page-personality script now preserves the `/planes` shape, while the home-review generator preserves home as it was. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the relevant scripts, `npm run build` passed, `git diff -- src` stayed empty, and focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures (`.superpowers/qa/zoositioweb-final-section-redesign/summary.json`).
- 2026-05-18 11:10 CT zoositioweb planes outline CTA style: updated the `/planes` `Conocer Zoolandingpage` button and hero `Ir a contacto` button to use the native Angora outline CTA utility `ank-btnOutline-linkColor-bgColor` with a secondary-background hover, preserving the links and tracking events. The local page-personality script now preserves that class set. Validation evidence: targeted browser inspection confirmed `a#planesZlpLink-link` and `a#planesHeroSecondary-link` include `ank-btnOutline-linkColor-bgColor`, compute `borderStyle: solid`, `borderWidth: 1px`, `borderColor: rgb(33, 76, 114)`, and `color: rgb(33, 76, 114)`; draft pack, `node --check`, `npm run build`, and `git diff -- src` passed.
- 2026-05-18 11:24 CT zoositioweb planes technology dropdowns: added a plain-language `Aspectos tecnológicos` / `Technology details` accordion to each `/planes` comparison card, covering responsive delivery, cloud infrastructure, initial measurement, WhatsApp intent messages, route/content growth, and future-integration readiness without publishing unsupported technical limits. The local page-personality script now preserves the accordions. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, `npm run build` passed, `git diff -- src` stayed empty, and focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures (`.superpowers/qa/zoositioweb-plans-tech-accordions/summary.json`).
- 2026-05-18 11:32 CT zoositioweb platform stacked hero: redesigned the `/plataforma` first viewport through draft configuration so the main hero copy and the `La base tecnológica trabaja para tus objetivos` panel render as two stacked sections: `#home` followed by the new `#base-tecnologica`, then `#evidencia`. The local page-personality script now preserves this structure. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, `npm run build` passed, `git diff -- src` stayed empty, and focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures (`.superpowers/qa/zoositioweb-platform-stacked-hero/summary.json`).
- 2026-05-18 12:15 CT zoositioweb platform integrated panel: changed the `/plataforma` `La base tecnológica trabaja para tus objetivos` block so it visually belongs to the `#base-tecnologica` band instead of reading as a highlighted card: the panel now matches the parent background and removes visible border, radius, and shadow. Added `expand_more` icons to the three accordion buttons so the controls read as expandable. The local page-personality script now preserves both changes. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, `npm run build` passed, `git diff -- src` stayed empty, and focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures (`.superpowers/qa/zoositioweb-platform-integrated-panel/summary.json`).
- 2026-05-18 12:19 CT zoositioweb platform final flattening: removed the card-like treatment from the `/plataforma` closing `#contacto` section by making `plataformaFinalInner` transparent with no border, no top accent, no shadow, and no inner padding, so the section reads as one flat band before the footer. The local page-personality script now preserves the override against the shared final-section rhythm. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, `npm run build` passed, `git diff -- src` stayed empty, and focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures (`.superpowers/qa/zoositioweb-platform-final-flat/summary.json`).
- 2026-05-18 12:24 CT zoositioweb platform hero outline CTA: updated the `/plataforma` hero secondary `Ver servicios` / `View services` button to use the native Angora outline utility `ank-btnOutline-linkColor-bgColor` with the same secondary-background hover pattern as the other outline CTAs. The local page-personality script now preserves the class set. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures confirming `borderStyle: solid`, `borderWidth: 1px`, `borderColor: rgb(33, 76, 114)`, and `color: rgb(33, 76, 114)` (`.superpowers/qa/zoositioweb-platform-hero-outline/summary.json`), `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-18 12:28 CT zoositioweb platform final outline CTA: updated the `/plataforma` final secondary `Ir a contacto` / `Go to contact` button to use the native Angora outline utility `ank-btnOutline-linkColor-bgColor` with a base-background hover so the interaction remains visible inside the flat secondary band. The local page-personality script now preserves the class set. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures confirming `borderStyle: solid`, `borderWidth: 1px`, `borderColor: rgb(33, 76, 114)`, `color: rgb(33, 76, 114)`, and hover background `rgb(236, 231, 223)` (`.superpowers/qa/zoositioweb-platform-final-outline/summary.json`), `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-18 12:31 CT zoositioweb FAQ hero outline CTA: updated the `/preguntas-frecuentes` hero secondary `Ir a contacto` / `Go to contact` button to use the native Angora outline utility `ank-btnOutline-linkColor-bgColor` with a base-background hover because the hero sits on the secondary band. The local page-personality script now preserves the class set. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures confirming `borderStyle: solid`, `borderWidth: 1px`, `borderColor: rgb(33, 76, 114)`, `color: rgb(33, 76, 114)`, and hover background `rgb(236, 231, 223)` (`.superpowers/qa/zoositioweb-faq-hero-outline/summary.json`), `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-18 12:34 CT zoositioweb FAQ final outline CTA: updated the `/preguntas-frecuentes` final secondary `Ver planes` / `View plans` button to use the native Angora outline utility `ank-btnOutline-linkColor-bgColor` with a base-background hover. The local page-personality script now preserves the class set. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures confirming `borderStyle: solid`, `borderWidth: 1px`, `borderColor: rgb(33, 76, 114)`, `color: rgb(33, 76, 114)`, and hover background `rgb(236, 231, 223)` (`.superpowers/qa/zoositioweb-faq-final-outline/summary.json`), `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-18 12:37 CT zoositioweb FAQ final background: changed the `/preguntas-frecuentes` closing `#contacto` section background from the secondary band to the base page background while keeping the inner closeout panel on the secondary background. The local page-personality script now preserves the section background. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures confirming `section#contacto` background `rgb(236, 231, 223)` and inner panel background `rgb(188, 193, 186)` (`.superpowers/qa/zoositioweb-faq-final-background/summary.json`), `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-18 12:40 CT zoositioweb contact hero outline CTA: updated the `/contacto` hero secondary `Ver planes` / `View plans` button to use the native Angora outline utility `ank-btnOutline-linkColor-bgColor` with a base-background hover because the button sits inside the secondary hero copy panel. The local page-personality script now preserves the class set. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures confirming `borderStyle: solid`, `borderWidth: 1px`, `borderColor: rgb(33, 76, 114)`, `color: rgb(33, 76, 114)`, and hover background `rgb(236, 231, 223)` (`.superpowers/qa/zoositioweb-contact-hero-outline/summary.json`), `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-18 12:44 CT zoositioweb contact final outline CTA: updated the `/contacto` final secondary `Ver planes` / `View plans` button to use the native Angora outline utility `ank-btnOutline-linkColor-bgColor` with a base-background hover. The local page-personality script now preserves the class set. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures confirming `borderStyle: solid`, `borderWidth: 1px`, `borderColor: rgb(33, 76, 114)`, `color: rgb(33, 76, 114)`, and hover background `rgb(236, 231, 223)` (`.superpowers/qa/zoositioweb-contact-final-outline/summary.json`), `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-18 13:04 CT zoositioweb contact final flattening: removed the card-like treatment from the `/contacto` closing `#contacto` section by making `contactoFinalInner` transparent with no border, no top accent, no shadow, no radius, and no inner padding, so the section reads as one flat band before the footer. The local page-personality script now preserves the override. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures confirming transparent inner background, no border, no shadow, and zero inner padding (`.superpowers/qa/zoositioweb-contact-final-flat/summary.json`), `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-18 13:39 CT zoositioweb contact final background/accent: changed the `/contacto` closing `#contacto` section from the secondary band to the base `bgColor` band and strengthened the secondary outline CTA by switching it to `ank-btnOutline-successColor-bgColor`, a secondary-background hover, high-contrast title-color text, and a success-color outline/shadow accent. The local page-personality script now preserves the section background and CTA class/style overrides. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, in-app browser inspection confirmed `section#contacto` background `rgb(236, 231, 223)`, CTA text `rgb(13, 20, 28)`, and CTA border/accent `rgb(18, 140, 126)`, focused browser QA repeated 3 times across desktop/mobile, ES/EN, and light/dark themes with 24 checks and 0 failures (`.superpowers/qa/zoositioweb-contact-final-bg-accent/summary.json`), `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-18 13:59 CT zoositioweb contact accordion affordance: added `expand_more` icons to the `/contacto` hero-panel accordion buttons (`Giro`, `Objetivo`, `Materiales`) so they read as expandable controls. The buttons now use the valid Angora `ank-justifyContent-spaceMINbetween` utility so the icon is pushed to the right; the reusable Angora layout constraint note now records this `space-between` token rule. The local page-personality script preserves the accordion button config. Validation evidence: 91 Zoosite draft JSON files parsed, draft pack via `tools/config-draft-sync.mjs`, `node --check` passed for the local script, in-app browser inspection confirmed each accordion button renders an SVG icon with no `expand_more` text leakage and computed `justify-content: space-between`, focused browser QA repeated 3 times across desktop/mobile and ES/EN with 12 checks and 0 failures (`.superpowers/qa/zoositioweb-contact-accordion-icons/summary.json`), `npm run build` passed, and `git diff -- src` stayed empty.
- 2026-05-18 CT zoositioweb 404 draft page: created a draft-level `/404` route with `pageId` `not-found`, ES/EN copy, recovery CTAs, useful route links, shared header/footer, and a sitemap exclusion for `/404`. Local runtime validation confirmed `/runtime-bundle?domain=zoositioweb.com.mx&path=%2F404&lang=es` resolves to `pageId: "not-found"` with status `200`, while an invented path such as `/no-existe-404-test` still resolves to `pageId: "default"` because the app/runtime currently falls back to `defaultPageId` for unmapped paths. A real automatic 404 response still needs app/runtime support before unknown URLs can render this draft page with HTTP 404 status. Browser QA covered desktop and mobile screenshots with no console errors or horizontal overflow.
- 2026-05-18 CT zoositioweb draft-only sticky footer: moved the short-page footer-bottom behavior out of app global CSS and into the Zoosite draft shared footer. `siteFooter` now uses draft-local sticky positioning (`position: sticky; top: 100vh`) so pages with short content, including `/404`, keep the footer at the viewport bottom without making content sections artificially taller. The temporary `flexGrow` on the 404 routes section was removed.
- 2026-05-18 16:40 CT zoositioweb sector final-section polish: updated the six sector routes (`/sectores/consultorios`, `/sectores/despachos`, `/sectores/inmobiliarias`, `/sectores/servicios-locales`, `/sectores/constructoras`, and `/sectores/agencias`) so their secondary CTAs use the native `ank-btnOutline-linkColor-bgColor` outline pattern and their final `#contacto` sections render as flat secondary bands with transparent inner layout, no inner border, no shadow, no radius, and right-aligned CTA groups. Validation evidence: `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed, Playwright QA covered all six sector routes in Spanish across desktop/mobile with 12 checks and 0 failures for horizontal overflow, outline CTA classes, and flat final sections, and `npm run build` passed. This pass was draft-only; existing app/runtime diffs from the configurable 404 work remain outside this visual-polish scope.
- 2026-05-18 16:54 CT zoositioweb sector hero semantics and typography: adjusted all six sector hero payloads so the `HeroCopy` block containing the `h1` renders before the supporting `HeroPanel`, preventing `h2` headings from appearing before the page `h1` in DOM order. Also reduced sector hero title sizing from the oversized 4.3rem desktop scale to existing draft title classes (`ank-fontSize-2_1rem ank-fontSize-md-3_2rem`) so long sector titles remain professional inside two-column hero cards. Validation evidence: Playwright checked all six sector routes in Spanish across desktop/mobile with 12 checks and 0 failures for `h1` before `h2` and horizontal overflow; a desktop screenshot smoke on `/sectores/despachos` confirmed the more balanced hero; 30 sector JSON files parsed; `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed; `npm run build` passed.
- 2026-05-18 17:29 CT zoositioweb sector-specific themes: researched common visual patterns for consultorios, despachos, inmobiliarias, servicios locales, constructoras, and agencias, then saved the source-backed findings at `drafts/zoositioweb.com.mx/findings/sector-design-research/2026-05-18-sector-theme-research.md`. Confirmed the runtime supports page-local complete `variables.theme` overrides, added complete light/dark themes to all six sector pages, and converted sector accent borders/gradients to read `var(--ank-accentColor)` so each sector palette controls the route mood. Validation evidence: 24 sector JSON files parsed, all six sector themes include complete 18-token light/dark palettes, `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed, `npm run build` passed, and Playwright/Chromium checked all six sector routes across desktop/mobile in Spanish with 12 checks and 0 failures for expected light/dark accent tokens, rendered `h1` content, and 0 horizontal overflow. This pass was draft-only and did not touch app/global styles.
- 2026-05-18 18:21 CT zoositioweb sector contrast, affordances, and event coverage: updated shared and page-level Zoosite draft CTAs that use `successColor` so they use `ank-color-onSuccessColor` plus `color: var(--ank-onSuccessColor)`, with `onSuccessColor` defined in shared and sector light/dark palettes. Added `expand_more` affordance icons to all draft accordion components and ensured component-level links, buttons, accordions, and dropdowns have `eventInstructions`, including `accordion_toggle`, `theme_toggle`, and `language_toggle` events. Validation evidence: 97 Zoosite draft JSON files parsed; component scan found 0 missing events for `link`, `button`, `accordion`, and `dropdown` components and 0 accordions missing icons; Playwright/Chromium checked all six sector routes across desktop/mobile and light/dark themes in Spanish with 24 checks and 0 failures for visible `a`/`button` contrast below 4.5:1, missing accordion icons, or horizontal overflow; `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed; `npm run build` passed. This pass was draft-only and did not touch app/global styles.
- 2026-05-18 18:47 CT zoositioweb Nosotros exposure and home expert proof: exposed `/nosotros` in shared desktop navigation, mobile navigation, footer links, and shared i18n; added a compact expert-proof `#equipo` section to home with Alec, Hector, Oswaldo, and Pamela; kept Oswaldo on the existing placeholder image; linked the section to `/nosotros`; and saved the planned-missing-pages inventory at `drafts/zoositioweb.com.mx/findings/2026-05-18-pending-pages-sections.md`. Validation evidence: 97 Zoosite draft JSON files parsed; component scan found 0 missing events for component-level `link`, `button`, `accordion`, and `dropdown` entries and 0 accordions missing `expand_more`; `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed; `npm run build` passed; in-app browser QA covered home and `/nosotros` in ES/EN with 4 checks and 0 failures; Edge/Chromium Playwright-core QA covered home and `/nosotros` across desktop/mobile in ES/EN with 8 checks and 0 failures for overflow, broken images, team copy, and team photos; `git diff --check`, nested draft `git diff --check`, and `git diff --name-only -- src` passed with no app/global style changes.
- 2026-05-18 19:06 CT zoositioweb team skill badges: replaced the home and `/nosotros` team grids with draft-local custom team cards so the old empty generic-card feature-icon square no longer renders. Each profile now has borderless skill badges with supported SVG icons: cloud/AI/architecture, cloud/AI/data, web/marketing/Google Ads, and design/marketing/analysis. Removed visible Oswaldo "photo pending" copy and changed the placeholder alt text to a temporary illustration label. No verified Oswaldo surname was present in local draft files or repo notes, so no surname was invented in public copy. Validation evidence: 97 Zoosite draft JSON files parsed; component scan found 0 missing events for component-level `link`, `button`, `accordion`, and `dropdown` entries and 0 accordions missing icons; no pending-photo strings remained in home/about i18n; `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed; `npm run build` passed; in-app browser inspection confirmed 12 team badge SVG icons and no pending-photo copy on home; Edge/Chromium Playwright-core QA covered home and `/nosotros` across desktop/mobile in ES/EN with 8 checks and 0 failures for overflow, broken images, badge labels, badge icons, empty boxes, generic icon-square containers, and pending-photo copy.
- 2026-05-18 21:00 CT zoositioweb Oswaldo profile correction: updated the home and `/nosotros` team cards so Oswaldo renders as `Oswaldo García`, uses `CVs_N_photos/oswaldo.jpeg`, and is positioned around web development, responsive work, and optimization instead of marketing or Google Ads. The current local scan of `CVs_N_photos` returned only four image files (`alec.jpg`, `hector.jpg`, `oswaldo.jpeg`, `pamelaBetancourt.webp`). Validation evidence: `npm run build` passed; Playwright-core QA covered home and `/nosotros` in ES/EN across desktop/mobile with 8 checks and 0 failures for overflow, broken images, Oswaldo image loading, forbidden marketing copy, and at least 3 badge SVGs per Oswaldo card.
- 2026-05-18 21:08 CT zoositioweb team positioning correction: updated home and `/nosotros` so Oswaldo keeps `Google Ads` but no longer carries marketing copy; Pamela remains the marketing profile. Changed Oswaldo's third badge icon to the supported `saved_search` SVG so no unsupported icon text leaks into the UI. Rewrote Hector Coronado's role and proof away from backend/fullstack language toward professional website and web app development for clearer SEO-oriented wording. Validation evidence: 97 Zoosite draft JSON files parsed; no forbidden Oswaldo marketing or Hector backend/fullstack copy in the targeted team files; `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed; `npm run build` passed; Playwright-core QA covered home and `/nosotros` across desktop/mobile in ES/EN with 8 checks and 0 failures for role text, Pamela marketing badge, Oswaldo Google Ads badge, supported SVG icons, broken images, console errors, and horizontal overflow.
- 2026-05-18 21:20 CT zoositioweb Nosotros AI/software repositioning: updated home and `/nosotros` team roles so Alec Montaño and Hector Coronado use `Desarrollo de software, Arquitectura de Nube, Ingeniería de Datos e IA`; Oswaldo García uses `Desarrollo web y Google Ads`; and Pamela Betancourt remains the only profile with marketing positioning. Reworked `/nosotros` hero, stats, work-method copy, SEO metadata, keywords, and structured data away from generic marketing positioning toward software, cloud, data, and AI. Added a post-contact `#pamelabetancourt` collaborator recommendation section with a measurable backlink to `https://pamelabetancourt.zoolandingpage.com.mx/` and no `nofollow`. Validation evidence: 97 Zoosite draft JSON files parsed; targeted JSON scan passed for roles, Google Ads badge, Pamela-only marketing, and backlink; `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed; `npm run build` passed; Playwright-core QA covered home and `/nosotros` across desktop/mobile in ES/EN with 8 checks and 0 failures for role text, marketing placement, referral order after contact, referral URL, supported SVG icons, broken images, console errors, and horizontal overflow.
- 2026-05-18 21:46 CT zoositioweb data-positioning pass: reinforced data as a core product advantage across home, Servicios, Planes, Plataforma, Contacto, Preguntas frecuentes, PyMEs, Nosotros, and the six sector pages. Updated ES/EN visible copy plus route SEO descriptions, Open Graph/Twitter descriptions, structured-data descriptions, and keywords around `datos propios`, first-party interaction data, analytics, data analysis, and measurement from day one. The wording focuses on site interaction signals collected through the team's own system, including pages consulted, section attention, important clicks, language choice, and WhatsApp actions, without promising unsupported integrations. Validation evidence: 97 Zoosite draft JSON files parsed; `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed; `npm run build` passed; `git -C drafts/zoositioweb.com.mx diff --check` passed with existing line-ending warnings only; Playwright-core QA covered 14 routes across ES/EN and desktop/mobile with 56 checks and 0 failures for visible data copy, HTTP status, console errors, horizontal overflow, and broken image URLs.
- 2026-05-18 22:32 CT zoositioweb SEO hardening and release-readiness audit: tightened route SEO metadata and structured data around complete websites, analytics, first-party data, cloud, WhatsApp conversion, and sector-specific page intent; added FAQ `mainEntity`, home `WebSite`/`WebPage`, plans `Service`/`OfferCatalog`, and noindex metadata for `/404`. Fixed `/servicios` hero DOM order so the `h1` renders before the supporting panel, updated language toggle accessible names to include visible `ES`/`EN`, and changed `/planes` visual benefit rows away from semantic `ul/li` because the runtime wrapper breaks list semantics. Validation evidence: 97 Zoosite JSON files parsed; component scan found 0 missing `eventInstructions` and 0 accordions missing icons; `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed; `npm run build` passed; in-app browser QA covered home, servicios, planes, nosotros, and contacto with 0 console errors, 0 broken images, 0 overflow, correct canonicals, data copy, and hydrated JSON-LD; final Playwright-core QA covered 14 public routes plus an unknown 404 across ES/EN and desktop/mobile with 60 checks, 0 retries, and 0 failures (`Output/zoositioweb-seo-audit/2026-05-19T04-31-12-990Z-final-retry/browser-qa-report.json`); final Lighthouse covered home, servicios, planes, plataforma, pymes, nosotros, contacto, FAQ, and consultorios with mobile plus key desktop runs, all scoring 100 in performance, accessibility, best practices, and SEO (`Output/zoositioweb-lighthouse/2026-05-19T04-25-34-284Z-final/lighthouse-summary.json`). This pass was draft-local for app behavior and did not edit `src/` or shared app styles; existing unrelated app/runtime diffs remained untouched.
- 2026-05-18 23:10 CT zoositioweb Google Ads checklist pass: applied the new Google Ads/GA4/SEO guide where safely possible inside the draft. Added a mobile-only fixed WhatsApp CTA root component with `whatsapp_click` tracking and a subtle existing `pulseSoftAnimation`; moved it out of the header after browser QA showed the header `backdrop-filter` constrained fixed positioning. Reinforced confirmed price clarity with `Desde $3,000 MXN` / `Starting at $3,000 MXN` on home, `/sitios-web-para-pymes`, and `/contacto`. Saved the platform/legal handoff at `drafts/zoositioweb.com.mx/findings/google-ads-readiness/2026-05-18-google-ads-ga4-seo-checklist.md`, including missing Google tag/dataLayer, GCLID persistence, legal pages, consent, SSR JSON-LD/hreflang, and robots/sitemap metadata. Validation evidence: 97 Zoosite JSON files parsed; `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed; `npm run build` passed; component scan found 0 missing link/button events and 0 accordions missing toggle icons; in-app browser QA covered home, PyME landing, contacto, planes, and 404 across mobile/desktop with 0 failures for overflow, H1 count, broken images, query preservation, and CTA visibility; Host-header checks confirmed `robots.txt` and `sitemap.xml` are dynamic but currently lack the documented handoff metadata; Lighthouse mobile on home, PyME landing, and contacto scored 99-100 performance, 100 accessibility, 100 best practices, and 100 SEO (`Output/zoositioweb-google-ads-readiness/2026-05-18T23-10-ct/lighthouse/summary.json`). This pass did not edit `src/` or app/global styles.

## 2026-05-18 23:44 CT - zoositioweb legal pages and confirmed Google Ads claims

- Added draft-local legal routes `/privacidad` and `/terminos` with bilingual i18n, page configs, structured data, and footer links.
- Updated Zoosite commercial copy for confirmed Google Ads claims: first test round in 5 days when base scope/materials are complete, prices before IVA, 100% invoiceable service, human developer support, and technology/data/cloud/AI consulting guidance.
- Updated the Google Ads/GA4/Search Console handoff with dynamic Google Analytics, dataLayer, Search Console verification, robots/sitemap, canonical, HTTPS, and consent tasks for the platform agent.
- Repaired leftover conflict markers in `drafts/zoositioweb.com.mx/not-found/components.json` before validation.
- Validation evidence: 107 Zoosite JSON files parsed; draft pack passed; `npm run build` passed; `git diff --check` and nested draft diff check passed with line-ending warnings only; Playwright-core QA covered home, PyME, planes, contacto, privacidad, and términos across ES/EN and mobile/desktop with 24 checks and 0 failures; Lighthouse mobile on `/privacidad` and `/terminos` scored 100 performance, 100 accessibility, 100 best practices, and 100 SEO (`Output/zoositioweb-legal-qa/2026-05-18T23-44-ct/lighthouse/summary.json`).

## 2026-05-19 11:38 CT - zoositioweb Google runtime draft config

- Reviewed the platform agent's Google Ads/Search Console/SEO runtime handoff and updated Zoosite's draft-local readiness note to mark app-level Google tag, Search Console, robots, sitemap, canonical, and SSR injection support as available.
- Prepared `drafts/zoositioweb.com.mx/site-config.json` for the new platform support without activating unverified third-party tags: canonical host and HTTPS flags are enabled, ad-attribution storage is declared, and `runtime.analytics.googleTag` remains disabled until real GA4, Google Ads, conversion label, optional GTM, and Search Console values are available.
- Updated durable Zoosite memory so future agents do not keep treating Google Analytics/Search Console support as pending platform work.
- Validation evidence: 107 Zoosite JSON files parsed; `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed; `git diff --check` and nested draft diff check passed with line-ending warnings only; `npm run build` passed; local SSR on port 4400 with `Host: zoositioweb.com.mx` returned HTTP 200 for home, stripped ad params from canonical, injected no Google tag or Search Console placeholder while IDs are absent, emitted robots disallows plus absolute sitemap, and emitted sitemap `lastmod`, `changefreq`, root `priority`, legal routes, and no `/404`.

## 2026-05-19 11:59 CT - alias-aware Google runtime draft scaffolds

- Added disabled Google runtime scaffolds and per-host alias override scaffolds to `drafts/zoolandingpage.com.mx/site-config.json`, `drafts/sulandingpage.com.mx/site-config.json`, and `drafts/zoositioweb.com.mx/site-config.json` without adding fake GA4, Google Ads, GTM, conversion-label, or Search Console verification values.
- The alias scaffolds set HTTPS/canonical host behavior per requested host so each primary domain and alias can later receive its own Google tag, Search Console meta/file verification, conversion mapping, and canonical origin without changing draft copy.
- Validation evidence: JSON parsing passed for the three draft `site-config.json` files and the shared schema; draft pack passed for `zoolandingpage.com.mx`, `sulandingpage.com.mx`, and `zoositioweb.com.mx`; `npm run build` passed; `node --test tools/tests/draft-local-context.spec.mjs` passed 7/7; local SSR host checks returned HTTP 200 with host-specific canonicals and no Google tag injection while disabled for `zoositioweb.com.mx`, `sitiosweb.zoolandingpage.com.mx`, `zoolandingpage.com`, and `sulanding.zoolandingpage.com.mx`.

## 2026-05-19 12:05 CT - alias canonical-to-primary policy

- Updated the alias scaffolds for `zoolandingpage.com.mx`, `sulandingpage.com.mx`, and `zoositioweb.com.mx` so configured aliases canonicalize to their primary `.com.mx` domain instead of self-canonicalizing.
- Added `zoositioweb.com` as an explicit alias/host override for `zoositioweb.com.mx`, with canonical origin `https://zoositioweb.com.mx`.
- Validation evidence: red-first alias policy test failed on the old self-canonical `zoolandingpage.com` value, then passed after the config update; JSON parsing passed for the three draft `site-config.json` files; `node --test tools/tests/draft-local-context.spec.mjs` passed 8/8; draft pack passed for all three domains; `npm run build` passed; local SSR with `x-forwarded-host` returned 301 redirects from checked aliases to the primary domains and 200 primary-domain canonicals with ad params stripped.

## 2026-05-19 16:52 CT - GA4 IDs and WhatsApp lead event mapping

- Enabled GA4 Google tag config for `zoolandingpage.com.mx` (`G-J9RDY0E194`), `zoolandingpage.com` (`G-1LWVZ3ZC1K`), `sulandingpage.com.mx` (`G-WTE1BKYC2W`), `sulandingpage.com` (`G-KS90EKT0CP`), `zoositioweb.com.mx` (`G-QRWR768FCM`), and `zoositioweb.com` (`G-GYQM5JGCGC`).
- Configured internal `whatsapp_click` to emit Google event `lead_conversion_whatsapp` with non-PII `pyme_id` values per draft. Extra production aliases without a dedicated supplied GA4 ID inherit the primary draft GA4 ID; `test.*` aliases remain disabled to avoid polluting analytics.
- Kept aliases canonicalized to the primary `.com.mx` domain through canonical links while setting alias `enforceCanonicalHost` to `false`, because a 301 redirect would prevent active alias GA4 tags from firing.
- No Google Ads `AW-...` conversion label, GTM ID, or Search Console verification value was added because none was provided in this handoff.
- Validation evidence: red-first focused Karma failed on missing `pyme_id`, then focused analytics/config Karma passed 71/71; three draft `site-config.json` files parsed; draft pack passed for all three domains; `node --test tools/tests/draft-local-context.spec.mjs` passed 9/9; `npm run build` passed; SSR host checks verified one expected `gtag.js` ID, `send_page_view:false`, ad-param-stripped canonical, and no Google tag on `test.zoositioweb.com.mx`.

## 2026-05-19 20:19 CT - GA4 central Zoosite property handoff

- Superseded the earlier per-property GA4 draft configuration after the external owner reported that `zoositioweb.com.mx` should be the central GA4 stream and that cross-domain measurement is configured in GA4.
- Updated `zoolandingpage.com.mx`, `zoolandingpage.com`, `sulandingpage.com.mx`, `sulandingpage.com`, `zoositioweb.com.mx`, and `zoositioweb.com` to use the central `G-QRWR768FCM` measurement ID while preserving per-draft non-PII `pyme_id` values.
- Kept `sendPageView: false`, GA4-only tracking, canonical links to the primary `.com.mx` domains, and `enforceCanonicalHost: false` on active aliases so the central tag can fire before any canonical signal is evaluated.
- No direct `AW-...` Google Ads tag, conversion label, GTM ID, or Search Console verification value was added.
- Validation evidence: JSON parsing passed for the three draft `site-config.json` files and shared schema; draft pack passed for `zoolandingpage.com.mx`, `sulandingpage.com.mx`, and `zoositioweb.com.mx`; focused analytics/config Karma passed 73/73; `node --test tools/tests/draft-local-context.spec.mjs` passed 9/9; `npm run build` passed; SSR host checks for all six requested domains returned one central `G-QRWR768FCM` tag, `send_page_view:false`, initialized `dataLayer`, and canonical URLs without ad parameters.

## 2026-05-19 20:32 CT - zoositioweb legal disclosure after GA4 handoff

- Consumed the GA/SEO handoff in `drafts/zoositioweb.com.mx/.agent-coordination/chat.md`, confirming that app/config plumbing is complete and the remaining work is production deploy, Tag Assistant validation, one real WhatsApp click test, and GA/Ads owner setup for `lead_conversion_whatsapp`.
- Updated `/privacidad` in Spanish and English so Google Analytics is described as active on the site, while Google Ads and Search Console remain future/configurable tools until enabled with real values.
- Updated the Zoosite Google Ads readiness note so it no longer says GA4 IDs are missing for Zoosite and now records the central `G-QRWR768FCM`, `send_page_view:false`, manual SPA pageviews, and `lead_conversion_whatsapp` mapping.
- Validation evidence: 107 Zoosite JSON files parsed; `node tools/config-draft-sync.mjs pack --domain=zoositioweb.com.mx` passed; `git diff --check` and nested draft diff check passed with line-ending warnings only; `npm run build` passed; local SSR production-mode host checks for `zoositioweb.com.mx` and `zoositioweb.com` confirmed central `G-QRWR768FCM`, no `AW-...`, `send_page_view:false`, `dataLayer`, and clean canonicals; local preview SSR confirmed the updated `/privacidad` disclosure in ES/EN.

## 2026-05-19 22:18 CT - production alias canonical redirect notes

- Updated local `site-config.json` for `zoolandingpage.com.mx`, `sulandingpage.com.mx`, and `zoositioweb.com.mx` so production aliases use `enforceCanonicalHost:true` toward the primary `.com.mx` domains, while `test.*` aliases remain renderable for QA.
- These local draft config edits were not published through the authoring API in this pass because the draft repos contain unrelated existing dirty changes; the deployed app-level alias logic still canonicalizes production aliases from the published top-level `aliases` list.
- Production validation for Zoosite confirmed `https://zoositioweb.com` returns 301 to `https://zoositioweb.com.mx/`, while `https://zoositioweb.com.mx` serves GA4 SSR HTML and the WhatsApp conversion event path works in browser-style validation.

## 2026-05-19 23:17 CT - zoositioweb contact-card WhatsApp analytics

- Configured the three `/contacto` option-card WhatsApp links to emit `whatsapp_click` from their component metadata instead of relying on a global link fallback.
- Added `target: "_blank"` and `rel: "noopener noreferrer"` metadata for those card links so the external WhatsApp destination does not replace the current page.
- Validation evidence: edited Zoosite contact JSON files parsed; local SSR/browser QA found 3 contact-option WhatsApp card links with `data-event-instructions`, `target="_blank"`, and `rel="noopener noreferrer"`; clicking the first card registered `whatsapp_click | cta | contacto-opcion-sitio-nuevo` in the draft Event Viewer.

## 2026-05-20 CT - GA4 alias redirect regression guard

- Found a local draft-workspace mismatch where active GA4 alias overrides for `zoolandingpage.com.mx` and `sulandingpage.com.mx` had drifted back toward canonical 301 behavior even though `drafts/` is not committed by this repo.
- Expanded the draft-local context test so every current active central-GA4 alias in the local Zoolandingpage, Sulandingpage, and Zoosite draft workspace is guarded against reintroducing a 301 that would prevent the alias tag from firing.

## 2026-05-20 18:06 CT - provisional Zoosite icon and wordmark

- Selected the wordmark-first provisional identity direction: `zoositioweb` as a solid wordmark, with a compact square Z favicon and teal rule. Rejected the earlier dot/accent-symbol exploration.
- Added local design artifacts under `Output/zoositioweb-logo-analysis/` and the app fallback SVG under `public/assets/brand/zoolandingpage-default-favicon.svg`.
- Uploaded public SVG assets to `https://assets.zoolandingpage.com.mx`: Zoosite favicon, Zoolandingpage favicon, and the Zoosite provisional wordmark. The local ignored draft configs for `zoolandingpage.com.mx` and `zoositioweb.com.mx` now point `site.icons` at their final public favicon URLs.
- Uploaded the Zoosite wordmark SEO share card to `https://assets.zoolandingpage.com.mx/zoositioweb.com.mx/shared/seo-images/zoositioweb-share-card-wordmark.svg` and updated Zoosite site/page SEO image fields to use that CDN URL.
- Validation evidence: public asset GET checks returned `200 image/svg+xml`; local Zoosite SSR HTML and hydrated desktop/mobile DOM showed the configured CDN favicon plus `theme-color:#128c7e`; home returned 200 and unknown route returned 404 across desktop/mobile with no non-analytics failed requests.
- Deployment evidence: `draft-zoositioweb-com-mx` PR #11 (`dev -> test`) and PR #12 (`test -> main`) were merged. GitHub Actions run `26198031012` deployed test successfully and run `26198071475` deployed production successfully. Runtime health returned production version `20260521T003024-production-85dbdb519555` for `zoositioweb.com.mx` and test version `20260521T002917-test-491e5098b649` for `test.zoositioweb.com.mx`.
