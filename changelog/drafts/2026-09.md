# Draft changelog - September 2026

## 2026-09-09 CT - Approved sleeping-pug PNG published to testing

- Draft commit `6bb1a0ef47ddce84d3a4afee362718b5e605eec5` selects the user-approved PNG with exact SHA-256 `e84e3131201ffe22a18a1ae4e69b643402935d7d91d60d3589fc7cbfb85e6167`; only one image source and the `nap` manifest entry changed, preserving the other ten assets and 14 payload files.
- Local and remote checks at 1440px and 390px loaded the approved 1536 × 1024 image, preserved the default `6 PE_L` result and found no overflow or errors. The 15-file deployment pack, three audits and public-safety validation passed.
- [Draft PR4](https://github.com/LynxPardelle/draft-pugestandar-com/pull/4) passed its guard and merged through `dev -> test` at `31ed714f3afecadd1211c5c9208105b87dc4eb5a`; [test run 34439654158](https://github.com/LynxPardelle/draft-pugestandar-com/actions/runs/34439654158) passed both jobs. Runtime HTTP 200 confirmed `published`/`test` version `test-31ed714f3afecadd1211c5c9208105b87dc4eb5a-34439654158-1`, with all 1,009 infographic and 861 original components matching local JSON exactly.

## 2026-09-09 CT - Pug infographic illustration correction published to testing

- Corrected the sleeping pug PNG to show four paws and redrew the character faces in all eight educational SVGs. Three visual and source audits passed; the SVG bodies, accessories, surrounding artwork and palette remained byte-identical. Nine replacement assets were uploaded under new keys and verified by HTTP 200 responses and exact source hashes.
- Draft commit `96dd94faea6e480c34ed75735cd165a33589f77a` changes only the SVG generator, nine asset-manifest entries and 21 image-source URLs in the infographic payload. All 14 other native payload files, original-page content, hero/explorer assets, copy and conversion logic remain unchanged.
- Local validation passed 33 browser checks at desktop 1440px and mobile 390px, including overflow checks at 320px, plus 486 numerical/inverse checks, 118 source/limit checks and the original 16 source factors. All 15 native payload files validate for deployment.
- [Draft PR3](https://github.com/LynxPardelle/draft-pugestandar-com/pull/3) passed its guard and merged through the protected `dev -> test` promotion at `7a896a5cc2da13539088754acbabbf3fff2d950a`. [Test publication run 34438960303](https://github.com/LynxPardelle/draft-pugestandar-com/actions/runs/34438960303) passed validation and deployment; production remains disabled.
- The live runtime returned HTTP 200 with `sourceStage: published`, environment `test`, and version `test-7a896a5cc2da13539088754acbabbf3fff2d950a-34438960303-1`. Both component sets matched local JSON exactly: 1,009 infographic components and 861 original components. Remote browser QA passed all 33 checks without reported errors at desktop 1440px and mobile 390px, including 320px overflow checks. All 13 calculator icons use the revised SVGs, the corrected sleeping pug is visible, hero/explorer remain unchanged, and `300 m -> 1 kPE_L`, inverse conversion and reset passed.

## 2026-09-09 CT - Pug estándar infographic variant published to testing

- Added `/infografico` as a second native page in the existing Pug draft, with 13 quantities and automatic/manual micro, milli, base, kilo, mega and giga PE prefixes. The illustrated variant adds three generated PNGs and eight reviewed educational SVGs under new asset keys; all eleven public images were verified without overwriting existing assets.
- Draft commit `593e71ad104e9ef106e3ce6cd71a67a1db3a7c25` contains 1,009 infographic components. SHA-256 comparisons confirmed that all nine original payload files remain unchanged.
- Local validation passed 486 numerical/inverse checks, 118 source-factor and SI-limit checks, 76 desktop/mobile browser checks without errors, and final navigation checks at both viewport widths. The deployment pack validates all 15 native payload files.
- [Draft PR2](https://github.com/LynxPardelle/draft-pugestandar-com/pull/2) passed its guard and merged through the protected `dev -> test` promotion at `a3cd696f64bf187c09222e087f4c7a7dbd43360b`. [Test publication run 34437404394](https://github.com/LynxPardelle/draft-pugestandar-com/actions/runs/34437404394) completed successfully in validation and deployment. The [infographic test route](https://test.zoolandingpage.com.mx/infografico?draftDomain=pugestandar.com) is published; no production or host configuration changed.
- The live runtime returned HTTP 200 with `sourceStage: published`, environment `test`, and version `test-a3cd696f64bf187c09222e087f4c7a7dbd43360b-34437404394-1`. Both published component sets matched local JSON exactly: 1,009 infographic components and 861 original components. Remote browser QA passed all 76 checks (38 desktop and 38 mobile), with no failures or reported errors. Remote visual checks passed on desktop and at mobile widths of 390 and 320px without broken clipping; repeated navigation to the same section passed against the published payload.

## 2026-09-09 CT - Pug estándar published to testing with verified runtime and browser QA

- Promoted the calculator through [draft PR1](https://github.com/LynxPardelle/draft-pugestandar-com/pull/1) to test merge `637f9d823521add6f1271e6c105ed89a96c3dcc1`. [Test publication run 34435291020](https://github.com/LynxPardelle/draft-pugestandar-com/actions/runs/34435291020) completed successfully in both validation and deployment jobs.
- Local acceptance passed 72 browser checks without reported findings, 16 source checks, and a ten-file packed payload check. The draft promotion guard passed.
- The live runtime returned HTTP 200 with `sourceStage: published`, environment `test`, and version `test-637f9d823521add6f1271e6c105ed89a96c3dcc1-34435291020-1`. All 861 returned components matched the local JSON exactly.
- Remote browser QA passed 72 of 72 checks at desktop 1440px and mobile 390px, with no failures, console errors, or network errors. Coverage included all 13 quantities, inverse conversion, reset, zero values, validation errors, auxiliary inputs, temperature, and FAQs. Horizontal-overflow checks also passed at 320, 390, 768, and 1440px.
- The calculator is published on the [shared testing preview](https://test.zoolandingpage.com.mx/?draftDomain=pugestandar.com). Production remains outside this release, and no production deployment or managed alias was created for Pug.

## 2026-09-09 CT - Pug estándar test-only bootstrap prepared

- Registered `pugestandar.com` in the [draft registry](../../docs/drafts-registry.json) with canonical local path `drafts/pugestandar.com`, repository [draft-pugestandar-com](https://github.com/LynxPardelle/draft-pugestandar-com), and exactly the `test` deployment environment. The existing draft entries and their environment scopes are unchanged.
- Created an independent repository with a reviewed 20-file bootstrap at commit `6d5f4b0b67631184e9458fd8415536a4b51e6db6`. It was initially private; GitHub's plan restriction required public visibility to enable branch protections, so only the audited bootstrap was made public. The `dev` branch is the default, `test` and `main` have active protections without bypass identities, and the required `guard` check is pinned to GitHub Actions.
- Configured only the test Environment and deployment role, verified the Environment's exact branch restriction and IAM trust, and matched the authoring endpoint to the live test stack. Production remains disabled in the repository configuration, with no Pug production Environment, deployment role, or managed alias.
- Appended the isolated Pug scope and authoring grant to the private test registry using conditional, versioned writes with exact readback. Test now contains 14 scopes and grants; all 13 prior bindings were preserved. Uploaded two generated project images under new domain-scoped asset keys without overwriting existing assets, and verified both public URLs returned HTTP 200.
- At this bootstrap checkpoint, calculator content is being authored and has not been published. The intended [shared testing preview](https://test.zoolandingpage.com.mx/?draftDomain=pugestandar.com) still requires the separate protected `dev -> test` draft promotion and desktop/mobile verification under the [draft lifecycle](../../docs/11-draft-lifecycle.md). This hub change records setup and ownership; it does not publish calculator content or deploy application code.

## 2026-09-07 CT - THN approved-design reconciliation (local only)

- Reconciled all 72 approved public JSON files into an independent draft checkout, retaining the reviewed delivery tooling. Mapped local media references to existing same-origin immutable assets only after matching their SHA-256 digests.
- Preserved explicit navigation and CTA text colors from the newer runtime-compatible baseline. Restored the requested centered lowercase Narrative heading, spacing and image layout without the removed introductory copy.
- Passed 17 draft tests, four integration contracts, and validate-only for all 72 files. Browser checks covered all 11 public routes in English and Spanish at 375 and 1280 pixels, with focused visual inspection of Home, Narrative and Journal.
- Original draft repositories and TEST remained unchanged. The public draft still has no active private Journal binding or authoring configuration; public-safety review and release provenance remain delivery gates.

## 2026-09-07 CT - The Hair Narrative Journal delivery preparation

- Prepared the THN repository's TEST workflow for the closed server-only Journal v2 binding and exact dedicated admin-origin contract. No binding file or public page payload was added or activated.
- Added credential-free pull-request validation and a read-only immutable-version rollback selector. The publish workflow records rollback evidence only after checking the returned published version.
- The existing draft validates without the optional binding. No other draft repository or public design was changed, and no workflow was dispatched.

## 2026-09-08 CT - The Hair Narrative Journal candidate

- Connected private authoring pages and public Home/Journal cards to the isolated
  blog. Added localized series/article payloads and authored empty-series taxonomy.
- Kept shared header/footer, approved Home sections outside Journal, Journal
  preparation state and other public pages unchanged. Styles remain Booksaw-based.
- Added owner instructions in the independent draft repository. All changes are
  local; publishing the draft, private origin and writer activation are still gated.
