# Drafts Changelog - 2026-06

Chronological draft-authoring and draft-QA history.

<!-- Keep this file chronological. Keep durable decisions in Codex.md or ai-notes, not here. -->

- 2026-06-30 19:20 CT Zoosite blog component catalog guard: updated `zoositioweb.com.mx` article editor copy so the disabled component catalog is clearly labeled as pending instead of implying that component/JSON editing is already available. Added an app-side draft contract guard that keeps the catalog button disabled and without an action until a validated catalog is implemented.

- 2026-06-30 18:54 CT Zoosite blog QA smoke alignment: updated `zoositioweb.com.mx` product-readiness QA checklist so the redacted live smoke expected checks exactly match the content-hub smoke CLI success payload, including validate/review/approve, public article HTML/body, CTA/reaction/share interactions, moderated comment enqueue, and post-interaction analytics. Validation passed: app content-hub/admin/SSR Node gates returned `99` pass, and `npm audit --omit=dev` returned `found 0 vulnerabilities`. Draft public-safety audit still blocks until the nested draft repo is committed; it reported no blocked paths or secret findings.

- 2026-06-30 12:25 CT Zoosite blog admin visible-copy pass: updated `zoositioweb.com.mx` blog admin draft copy so editor-facing screens avoid unexplained implementation terms such as secure content service, raw identifiers, hub IDs, and analytics event names. Analytics cards now use human labels like `Visitas`, `Avance de lectura`, `Clics en llamados`, and `Descargas`, and help text points editors to blog concepts instead of internal runtime boundaries. Validation passed: content-hub admin/binding/product Node tests returned 45 pass, a direct visible-copy extraction returned `NO_VISIBLE_INTERNAL_COPY_MATCHES`, and a read-only subagent review found no remaining user-facing matches.

- 2026-06-30 10:29 CT Zoosite blog overview mutation cleanup: updated `zoositioweb.com.mx` so `/admin/blog` no longer carries dead global validate/publish article components. The overview now keeps its table row actions as the only article-specific entry points, while publication and validation remain scoped to detail pages or row links with concrete article IDs. Validation passed: focused content-hub smoke/admin/binding Node tests returned 44 pass, and `git diff --check` returned only LF/CRLF warnings in the hub and nested draft repos.

- 2026-06-30 07:08 CT Zoosite blog analytics source alignment: updated `zoositioweb.com.mx` so `/admin/blog/analiticas` reads `remote.contentHub.analytics.items` from the protected `analyticsSummary` data source instead of reusing `articleList` with empty metric columns. The analytics table now tracks loading/error state through `remoteStatus.contentHub.analytics`. Validation passed: Zoosite content-hub admin bindings returned 7 pass, expanded app-side content-hub/preflight/admin tests returned 57 pass, and `npm run build` passed with the existing `quill-delta` warning. Draft public-safety audit reported no blocked paths or secret findings but still blocks while the nested draft repo is dirty until this change is committed.

- 2026-06-30 03:00 CT Zoosite blog product copy and tag SEO hardening: updated `zoositioweb.com.mx` blog/admin draft copy so public and admin routes describe user-facing blog concepts instead of implementation terms, changed visible "Tags" labels to "Etiquetas" where appropriate, cleaned public blog/article/category fallback text, and switched the public article category link label to generic `joinText` spacing. Validation passed: focused content-hub admin/SSR/product Node tests returned `82` pass, focused Angular metadata/value-handler tests returned `TOTAL: 16 SUCCESS`, local desktop/mobile browser QA passed for `/blog`, `/blog/tag/seo`, and `/blog/web/blog-builder-seo`, and the draft public-safety audit reported no current/history secret findings but still blocked while the nested draft repo is dirty.

- 2026-06-29 23:57 CT Zoosite blog editor/preview ID guard follow-up: updated `zoositioweb.com.mx` so the hidden editor `articleId` field is required and `Guardar cambios` stays disabled when an editor route lacks the article ID. Preview now links directly to the same article's SEO and Versiones routes instead of falling back to the article list. Validation passed: focused content-hub admin and Zoosite binding Node tests returned 25 pass, expanded content-hub product/admin/schema tests returned 58 pass, and draft public-safety audit after the nested draft commit returned `ok:true`, `blockingRepoCount:0`, with no blocked paths or secret findings.

- 2026-06-29 23:18 CT Zoosite blog schedule/taxonomy status follow-up: updated `zoositioweb.com.mx` Programados so cancel-schedule row actions show safe loading, success, and user-facing failure copy instead of leaving the status area silent. Category and tag admin forms now include hidden `taxonomyKind` fields (`category` and `tag`) in their interaction scopes so upsert actions send the correct taxonomy type while keeping the visible authoring form simple. Validation passed: focused content-hub admin/schema/binding Node tests returned 47 pass, focused content-hub/proxy Karma returned `TOTAL: 14 SUCCESS`, `npm run build` passed with the existing `quill-delta` CommonJS warning, `config:pack --domain=zoositioweb.com.mx` completed, and draft public-safety audit returned `ok:true` with no blocked paths or secret findings.

- 2026-06-29 22:26 CT Zoosite blog role discovery: updated `zoositioweb.com.mx` so `/mi-cuenta` shows the `Administrar blog` entry to users with dedicated blog roles (`zoosite-blog-editor`, `zoosite-blog-publisher`, `zoosite-blog-media`, `zoosite-blog-moderator`, or `zoosite-blog-analyst`) while keeping `/admin/usuarios` restricted to `zoosite-admin`. The blog entry now lands on `/admin/blog`, whose route guard accepts the same blog role set before subpages enforce their narrower route groups. Validation passed: focused draft auth and Zoosite content-hub admin contract tests returned 31 pass; public-safety audit will be rerun after committing the nested draft so the repo is clean.

- 2026-06-29 21:13 CT Zoosite public article dynamic analytics: updated `zoositioweb.com.mx` `/blog/:categorySlug/:articleSlug` so the public article CTA resolves analytics instructions from `contentHub.currentArticle.articleId` and `contentHub.currentArticle.path` instead of hardcoded seed article IDs. Removed the stale component-level `blog_view` seed event; article views remain owned by the runtime content-hub analytics path. Validation passed: focused content-hub draft tests returned 33 pass and focused generic link/wrapper Karma returned `TOTAL: 27 SUCCESS`.

- 2026-06-29 20:59 CT Zoosite blog schedule table source: updated `zoositioweb.com.mx` Programados so the table shows real pending schedule rows from `remote.contentHub.schedules.items`, identifies rows by `scheduleId`, and exposes only the supported cancel-schedule action instead of article edit/preview/SEO/version actions on schedule rows. Validation passed: focused content-hub admin draft tests returned 32 pass.

- 2026-06-29 18:38 CT Zoosite blog lifecycle guard hardening: updated `zoositioweb.com.mx` preview and SEO admin pages so `articleId` and `revisionId` are required in the interaction scope and lifecycle buttons stay disabled until those protected detail values are ready. This prevents publish/approval/archive actions from submitting empty IDs after direct navigation or a slow detail read. Validation passed: content-hub admin/schema/contract Node tests returned 54 pass and focused `proxy-action.handlers.spec.ts` Karma returned `TOTAL: 9 SUCCESS`. Testing deploy/browser QA is still pending for this nested draft change.

- 2026-06-29 15:53 CT Zoosite blog role groups: updated `zoositioweb.com.mx` so protected blog routes can use dedicated product groups beyond `zoosite-admin`. The `staff` auth profile now allowlists/manages `zoosite-blog-moderator` and `zoosite-blog-analyst`; `/admin/blog/moderacion` allows moderator access and `/admin/blog/analiticas` allows analyst access. Real Cognito groups were created in `us-east-1_Pq5OCadbK` for editor, publisher, media, moderator, and analyst. Validation passed: focused content-hub contract and Zoosite admin binding Node tests returned 17 pass.

- 2026-06-29 14:39 CT Zoosite blog taxonomy-driven controls: updated `zoositioweb.com.mx` so `/admin/blog/articulos/nuevo` and `/admin/blog/articulos/:id/editor` hydrate category dropdowns and tag autocomplete from protected content-hub taxonomy reads (`category` and `tag`) with static fallbacks only while the read is loading or empty. Validation passed: Zoosite content-hub admin binding tests returned 6 pass; content-hub/admin/schema/SSR Node tests returned 71 pass; focused Karma returned `TOTAL: 155 SUCCESS`; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed with the existing `quill-delta` CommonJS warning. Public-safety audit still blocks until the nested draft repo is committed, with no current/history blocked paths or secret findings in the summary run.

- 2026-06-29 02:38 CT Zoosite blog Programados/Versiones hardening: updated `zoositioweb.com.mx` blog admin scheduling links so Programados receives both `articleId` and `revisionId`, made the selected version required/read-only for scheduled publish, limited schedule actions to current backend-supported `publish` and `unpublish`, and made Versiones require explicit article/revision selection before restore with safe status copy. Validation passed: focused content-hub admin page and Zoosite admin binding Node tests returned 17 pass.

- 2026-06-29 02:06 CT Zoosite blog editorial lifecycle controls: updated `zoositioweb.com.mx` protected blog SEO/admin surface so selected articles can be sent to review, approved, published, unpublished, or archived through explicit content-hub actions. The draft runtime now declares the matching proxy actions and the UI shows safe, actionable Spanish status copy instead of raw backend errors. Validation passed: focused content-hub contract/admin/schema Node tests returned 46 pass.

- 2026-06-28 05:36 CT Zoosite blog create-package error copy: updated `zoositioweb.com.mx` article creation page so create-package failures no longer expose raw backend messages such as `Invalid id` to authors. The page now shows actionable Spanish copy that points to reloading the page or a stale testing content service. Validation passed: changed draft JSON parsed, focused Angular proxy-action Karma returned `TOTAL: 9 SUCCESS`, content-hub admin/schema Node tests returned 26 pass, content-hub BFF unit tests returned `Ran 23 tests ... OK`, and `npm audit --omit=dev` returned `found 0 vulnerabilities`. Public-safety audit still blocks while this nested draft change is uncommitted, with no current/history blocked paths or secret findings.

- 2026-06-27 02:14 CT Zoosite blog editor/schedule annotation fix: updated `zoositioweb.com.mx` Programados so its read-only article ID field resolves from `?articleId=` and schedule/publish buttons stay disabled when the article ID is missing. This prevents the protected content-hub action from receiving an empty ID and returning `Invalid id`. Validation passed: `admin-blog-programados/components.json` parsed and focused app Karma for `generic-rich-text` returned `TOTAL: 7 SUCCESS`; browser/deploy QA is pending for the committed test rollout.

- 2026-06-27 01:15 CT Zoosite blog editor integrated-browser fix: audited `https://test.zoolandingpage.com.mx/admin/blog/articulos/:id/editor` in the in-app browser and found that title/tags loaded but `articleId` remained empty in value-instruction outputs, causing Vista previa, SEO, and Versiones links to render with `articleId=` empty. Updated `zoositioweb.com.mx` article editor so the article ID is shown as read-only text and kept as a hidden technical interaction field for protected actions. Validation passed: editor JSON parse, content-hub admin Node tests returned 11 pass, focused app Karma returned `TOTAL: 32 SUCCESS`, `npm run build` passed with the existing `quill-delta` warning, `npm audit --omit=dev` returned `found 0 vulnerabilities`, and draft auth audit returned OK. Public-safety audit will be rerun after the nested draft commit clears dirty state.

- 2026-06-27 00:31 CT Zoosite blog admin annotation pass: updated `zoositioweb.com.mx` blog/admin draft pages so article action headers resolve to localized `Acciones`/`Actions`, row action links target the selected article for editor, preview, SEO, versions, and scheduling, article IDs render as read-only display values where appropriate, category controls use dropdowns, tags trim comma-separated values, schedule fields use `datetime-local`, and visible admin copy avoids unexplained acronyms such as BFF/CSRF. Category, tag, hub, config, analytics, moderation, and media screens now explain what each field is for, while public `/blog` uses visitor-facing copy. Validation passed: 280 draft JSON files parsed, `config:pack` completed, Zoosite content-hub admin contract tests passed, draft auth audit returned OK, focused Angular Karma returned `TOTAL: 104 SUCCESS`, and `npm run build` passed with the existing Quill `quill-delta` CommonJS warning. Public-safety audit will be rerun after the nested draft commit clears dirty state.

- 2026-06-26 20:28 CT Zoosite blog action touch-target follow-up: added explicit `actionButtonStyles` to `/admin/blog` and `/admin/blog/articulos` generic-table configs so row action links render as 52px icon-only touch targets even when dynamic Angora classes are not enough in the deployed runtime. Validation: focused app Karma returned `TOTAL: 31 SUCCESS`, Zoosite content-hub admin bindings returned 5 pass, and `npm run build` passed with the existing `quill-delta` warning.

- 2026-06-26 20:02 CT Zoosite blog admin UX follow-up: enlarged `/admin/blog` and `/admin/blog/articulos` row action touch targets to 52px icon-only buttons with tooltip labels, removed duplicate `?articleId=` query strings from editor/preview/SEO/versiones action links so detail pages hydrate from route param `id`, and aligned the draft memory note with the route-param contract. Validation: focused app Karma returned `TOTAL: 107 SUCCESS`, Node auth/schema/Zoosite binding tests returned 33 pass.

- 2026-06-26 19:26 CT Zoosite blog editor route-param hydration: changed `zoositioweb.com.mx` `content_hub_article_detail` input so article metadata hydrates from `/admin/blog/articulos/:id/editor` route param `id` instead of a query-string `articleId`. This keeps edit/preview/SEO/version links shareable and avoids losing title/metadata when the table action route only carries the path ID. Validation: focused app Karma returned `TOTAL: 121 SUCCESS`, Node site-config/content-hub binding tests returned 9 pass, `npm run build` passed with the existing `quill-delta` warning, and `npm audit --omit=dev` returned `found 0 vulnerabilities`. Public-safety audit will be rerun after the nested draft commit clears dirty state.

# 2026-06-30 14:44 CT Zoosite public article rich text rendering

- Updated `zoositioweb.com.mx` public blog article body to bind `articleContent` through `richTextHtmlOr` into `generic-text.config.html` instead of binding the raw Quill Delta through `varOr` into `config.text`. This prevents published article pages from rendering `[object Object]` when content-hub sends Delta-shaped article bodies.
- Validation in the app repo passed through focused value-handler tests, content-hub product/SSR tests, and `npm run build`. Remote testing publish/browser QA remains required for the nested draft change.

- 2026-06-26 18:40 CT Zoosite blog editor detail and mobile action follow-up: added protected `content_hub_article_detail` runtime data source to `zoositioweb.com.mx` so editor/preview/SEO/versiones pages can hydrate public-safe metadata by `articleId`. The article editor now preloads title, language, category, tags text, slug, and summary from that detail read. `/admin/blog` and `/admin/blog/articulos` table action buttons were enlarged to 48px touch targets with larger icons, and `/admin/blog/articulos/nuevo` summary rich text now includes heading controls. Validation: changed draft JSON parsed, content-hub/admin binding Node tests returned 20 pass, focused Angular tests returned `TOTAL: 114 SUCCESS`, `config:pack` completed, Zoosite draft auth audit returned OK, and draft public-safety audit returned `ok:true` with no current/history blocked paths or secret findings.

- 2026-06-26 16:52 CT Zoosite blog QA annotation fixes: updated `zoositioweb.com.mx` blog/admin draft config so `/admin/blog` and `/admin/blog/articulos` action tables use compact equal-size icon buttons with tooltip labels and localized `Acciones` headers, `/admin/blog/articulos/nuevo` uses `generic-rich-text` for the editorial summary, long policy dropdown labels wrap cleanly, idle status strips explain their purpose, category/tag form rows align text inputs and select buttons to the same height, and public `/blog` search uses styled generic search-box classes. The shared site menu button is icon-only with a generic tooltip, and hub/config empty states now explain that records appear only after content-hub has saved advanced hub records or authorized hub relations. Validation passed: all draft component JSON parsed, content-hub/admin binding Node tests returned 16 pass, runtime data-source condition guard returned `ok:true`, `config:pack` completed, draft auth audit returned OK, focused app Karma suites passed, and `npm run build` passed with the existing `quill-delta` warning. Browser testing deployment/QA is still pending for this change.

- 2026-06-26 17:35 CT Zoosite blog action-link follow-up: updated `zoositioweb.com.mx` blog admin tables to use `generic-table` row action `hrefTemplate` links for Edit, Preview, SEO, Versiones, and Programar so clicks behave as visible links instead of relying only on action event handlers. Added missing `/admin/blog` action i18n keys and tightened category/tag visible-select height to match neighboring text inputs. Validation passed: changed draft JSON parsed, `node --test tools/tests/components-schema.spec.mjs`, and focused Karma for the related app contract returned `TOTAL: 82 SUCCESS`.

- 2026-06-25 02:59 CT Zoosite shared chrome and article admin follow-up: unified auth, account, public blog, and blog/admin routes behind the shared `siteHeader`/`siteFooter` root components and added a draft-composed hamburger modal menu with public, account, admin, and legal sections. Protected account/admin links are conditionally visible from the mapped `auth-account` data source, so that account data source now runs globally while remaining limited to the account mapper and same-origin auth-admin BFF session. `/admin/blog/articulos` now has an aligned joined search control with smaller search icon, localized `Acciones`/`Actions` action header, button-styled vertical row actions, and the liked dropdown styling applied to all draft select controls. Cognito app client `16jb6ml9q5jdh6blj7f668fajp` was updated in AWS so access/id tokens last 12 hours, refresh token remains 30 days, and the obsolete `test.zoositioweb.com.mx` callback/logout URLs were removed; browser sessions still use server-cookie auth and do not expose tokens. Validation passed: focused Karma `TOTAL: 97 SUCCESS`, content-hub/admin/schema Node tests `11 pass`, `config:pack`, runtime data-source condition guard `ok:true`, draft auth audit OK, and `npm run build` with the existing `quill-delta` warning. Browser credential/session QA is still required after deployment.

- 2026-06-25 02:03 CT Zoosite site chrome pass: added draft-composed generic header and footer components to `zoositioweb.com.mx` routes that did not already have their own `siteHeader`/`siteFooter`, including auth, account, admin, and blog routes. Public marketing/legal routes with existing chrome kept their original header/footer to avoid duplicates. `/mi-cuenta` now shows an admin-only `Administrar blog` link to `/admin/blog/articulos` when the account data maps `isAdminText` to `Administrador`. This stays draft-config driven and does not add app TypeScript, backend code, tokens, secrets, or Cognito changes. Validation passed: 280 draft JSON files parsed, content-hub admin Node tests returned 9 pass, runtime data-source condition guard returned `ok:true`, draft auth audit returned OK, `config:pack` completed, `npm run build` passed with the existing `quill-delta` CommonJS warning, local browser QA verified `/blog` desktop/mobile header/footer with no horizontal overflow, and unauthenticated `/mi-cuenta` plus `/admin/blog/articulos` redirected to `/acceso` while preserving header/footer.

- 2026-06-25 01:19 CT Zoosite article admin UI polish: updated `/admin/blog/articulos` so search uses a joined input/button control with an authored i18n tooltip, dropdowns use right-aligned rotating triangle carets, page-size choices are `3`, `5`, `10`, `20`, and `50` with default `10`, row actions render as readable labeled controls, and the validation status area shows an explicit idle message instead of an empty strip. Public `/blog` article listing routes already existed for general visitors. Validation passed: draft JSON parse, content-hub admin Node tests returned 9 pass, runtime data-source condition guard returned `ok:true`, draft public-safety audit returned `ok:true`, and `git diff --check` had only LF/CRLF warnings.

- 2026-06-23 02:32 CT Zoosite blog article admin UX pass: updated `/admin/blog/articulos` so the search/filter toolbar is a real generic `interaction-scope` form with query-param navigation for `q`, `status`, `pageSize`, and page reset. Restyled the select dropdowns, search field, action buttons, table shell, loading copy, and external pagination config; disabled the internal `generic-table` Material paginator to avoid duplicate pagination UI. Added a guard in `tools/tests/content-hub-admin-pages.spec.mjs` to keep the article index from regressing to a standalone `search-box`, unstylized select indicator, or internal table paginator. Validation passed locally: content-hub admin Node tests returned 13 pass, and `runtime-data-source-condition-guard` returned `ok:true` / `issueCount:0`.
- 2026-06-23 02:44 CT Zoosite blog article admin query-param follow-up: fixed `/admin/blog/articulos` search/status/page-size controls to use component-level `valueInstructions` with `queryParamOr` instead of literal `{source:"queryParam"}` objects in `generic-input.config.value`. Testing browser QA had exposed the search field rendering `[object Object]`. The focused Node tests returned 13 pass, `runtime-data-source-condition-guard` returned `ok:true` / `issueCount:0`, `git diff --check` had only LF/CRLF warnings, and `npm run build` passed with the existing `quill-delta` CommonJS warning.

- 2026-06-22 22:29 CT Zoosite content-hub BFF path fix: fixed `runtime.contentHubs[].publicApiBasePath` from `/content-hub` to `/features/content-hub`, promoted `draft-zoositioweb-com-mx` dev to test through PR #82, and verified the test runtime version `20260623T042825-test-a7e97ee8073f` exposes the corrected path. Browser QA on `https://test.zoolandingpage.com.mx/admin/blog*?draftDomain=zoositioweb.com.mx&debugWorkspace=false&lang=es` rendered the blog admin routes after login, and CloudWatch for `zoolanding-content-hub-test-Function` showed authenticated BFF invocations including one cold/read at `6558.06 ms` followed by reads between `52.09 ms` and `91.03 ms`.
- 2026-06-22 22:48 CT Zoosite content-hub admin UI polish started: redesigned `/admin/blog/articulos/nuevo` as a generic-component admin workflow with grouped article metadata, SEO, policies, a sticky action bar, required-field validation, and `disabledWhenInvalidScope` on package creation. Also normalized `/admin/blog*` navigation to compact flex-wrap generic links because the previous draft grid token rendered each nav item as a full-width row in the browser. Local Angular build passed; draft public-safety audit still blocks while the nested draft repo is dirty and continues to report existing public contact/auth placeholder review findings, with no current blocked paths or secret findings in this pass.

- 2026-06-21 06:08 CT Zoosite blog admin BFF activation: updated `zoositioweb.com.mx` admin blog draft pages so validated content-hub actions are no longer visually blocked, article tables use `articleId` plus declared safe row event fields, and row actions navigate through generic `navigateWithEventData` templates instead of hardcoded seed article IDs. Removed demo fallback rows from the article admin tables so backend/read failures are visible through empty states. The media page now distinguishes local file selection from full binary upload serialization, while wiring the protected metadata/action entry point to `content_hub_upload_asset`. Static draft audits passed for auth and data-source conditions; public-safety audit still reports existing public contact/review findings and blocks while the nested draft repo is dirty until this change is committed.
- 2026-06-18 20:16 CT Zoosite admin users v1 polish: updated `zoositioweb.com.mx` `/admin/usuarios` draft config with a visible pending/client/admin/suspended guide, clearer loading/empty/error/action status copy, larger 48px admin action targets, explicit confirmations, action icons, per-status card help, and mapped disabled states for actions that should not be visually available on pending/approved/suspended/rejected rows. The change stayed generic/config-driven in draft JSON and did not add tokens, secrets, real user data, app TypeScript, backend code, deploys, or AWS changes. Validation passed: 164 draft JSON files parsed with `failed:0`, `runtime-data-source-condition-guard` returned `ok:true` / `issueCount:0` on repeated runs, `config:pack` completed, `deploy-draft.mjs --validate-only=true` returned `ok:true` with `fileCount:164`, focused Node guard/schema tests passed `7/7`, and `draft-auth-audit` returned `Draft auth audit: OK` with manual browser credential/session QA still required.
- 2026-06-18 19:31 CT Zoosite auth loading feedback follow-up: updated `zoositioweb.com.mx` auth/account/MFA draft pages so slow custom-auth operations show visible generic `loading-spinner` status blocks in addition to button loading labels. Covered registration, confirmation, resend-code, password recovery/reset, MFA challenge, MFA setup, voluntary account MFA enrollment, and self-service MFA disablement. The draft still stores no tokens, Cognito sessions, client secrets, full `otpauth://` values, or raw TOTP secrets.
- 2026-06-18 17:43 CT Zoosite MFA challenge error messages: updated `zoositioweb.com.mx` `/verificar-acceso` so custom-auth MFA failures distinguish environment mismatch, expired challenge, and generic wrong-code cases through `authForm.respondMfaChallenge.errorCode`. Root cause from testing: a production-scoped Cognito user correctly fails on `test.zoolandingpage.com.mx`; the page should not present that as an invalid authenticator code.
- 2026-06-18 15:55 CT Zoosite auth audit UX and admin MFA reset: updated `zoositioweb.com.mx` auth/account/admin draft config so sign-in and MFA challenge flows show explicit loading states, `/mi-cuenta` shows clearer protected-session loading copy, `/configurar-mfa` prioritizes the QR code with the manual key as fallback, and `/admin/usuarios` exposes a generic admin action to reset another user's TOTP MFA when they lose access to their authenticator. The server-only `staff` registry now publishes the safe same-origin `resetUserMfaPathTemplate`, while the draft still stores no tokens, Cognito sessions, TOTP secrets, client secrets, or raw `otpauth://` values. Validation for this pass includes JSON parse checks and the new runtime data-source condition guard against hidden mapper fields.
- 2026-06-18 12:51 CT Zoosite account MFA visibility: updated `zoositioweb.com.mx` `/mi-cuenta` so the generic-component security section shows the TOTP enrollment action only when `account.mfa.softwareTokenEnabled` is false, shows the self-service MFA disable action only when it is true, and shows a fallback message when the BFF reports `mfa.status: "unknown"`. This draft change depends on the auth-admin BFF exposing public `account.mfa` metadata from Cognito `AdminGetUser`; no tokens, Cognito sessions, TOTP secrets, or client secrets are stored in draft config.
- 2026-06-18 03:42 CT Zoosite configurable TOTP and MFA disable UI: updated `zoositioweb.com.mx` `staff` server-only auth profile so authenticator apps show `zoositioweb` with the user's email and the friendly device name `zoositioweb acceso`. Added a generic-component `/mi-cuenta` section for signed-in users to disable two-step verification by entering their current password and current 6-digit authenticator code, plus a display-only success banner for `authStatus=mfa-disabled`. No token, refresh token, Cognito session, client secret, full `otpauth://` URI, or raw TOTP secret was stored in draft config.
- 2026-06-18 02:26 CT Zoosite MFA QR setup: updated `zoositioweb.com.mx` `/mi-cuenta` voluntary TOTP setup to show a generic `qr-code` component bound to the BFF-provided `setup.otpauthUri`, with the manual shared secret kept as a wrapped fallback only. The draft does not store or render the full `otpauth://` URI as ordinary visible text.
- 2026-06-18 01:20 CT Zoosite voluntary MFA account UI: added a generic-component security section to `zoositioweb.com.mx` `/mi-cuenta` so signed-in users can start voluntary TOTP enrollment with their current password, view the temporary manual setup key, and verify the 6-digit authenticator code. The server-only `staff` registry now publishes safe same-origin enrollment paths and the enrollment CSRF cookie name through runtime auth metadata. No token, refresh token, Cognito session, client secret, or full `otpauth://` URI is stored in draft config.
- 2026-06-17 04:48 CT Zoosite account/admin auth foundation: updated `zoositioweb.com.mx` so `/mi-cuenta` loads account data through the generic auth-admin BFF for any authenticated user, and added `/admin/usuarios` as an admin-only route for approval, group elevation, suspension, and reactivation actions. The server-only auth registry now includes active `staff` auth-admin session/admin path metadata, `adminGroups`, `manageableGroups`, default pending user status, admin auto-approval policy, max session age, and server-only `allowedTokenUses` without raw secrets. The public draft config still uses `runtime.authRemote`, excludes private auth routes from the sitemap, and does not publish tokens or Cognito client secrets. Local validation passed: draft JSON parse passed, app focused auth/admin Karma returned `TOTAL: 108 SUCCESS`, Node auth/schema contracts returned `29 pass`, API proxy unittest returned `Ran 112 tests ... OK`, auth-admin unittest returned `Ran 20 tests ... OK`, and `npm run build` passed. Public-safety audit after committing the nested draft returned `ok:true`, `blockingRepoCount:0`, no current/history blocked paths, and no current/history secret findings; existing public email/phone review findings remain intentional contact/auth placeholder content.
- 2026-06-17 03:31 CT Zoosite auth flow production promotion: promoted `zoositioweb.com.mx` auth page polish through draft PR #40 (`codex/zoosite-auth-flow-polish` -> `dev`), PR #41 (`dev` -> `test`), and PR #42 (`test` -> `main`). Production deploy run `27679171657` completed successfully on merge commit `e3a2b7dcdebf15080594fc5e3dc765a4258e4dc1`. Public safety audit before promotion returned `ok:true`, `blockingRepoCount:0`, no current/history blocked paths, and no current/history secret findings. Production browser QA on `https://zoositioweb.com.mx` covered `/acceso`, `/registro`, `/confirmar-cuenta`, `/recuperar-contrasena`, and `/cambiar-contrasena` in desktop/mobile with `main-5YHFVBVA.js`, no console errors, no horizontal overflow, Spanish validation copy, visible disabled submit states, success banners, and account confirmation/reset instructions.
- 2026-06-17 03:05 CT Zoosite auth flow polish: updated `zoositioweb.com.mx` custom auth pages so signup moves users to confirmation, password recovery moves users to password-code entry, and login shows success banners after account confirmation or password reset through display-only `authStatus` query conditions. Auth submits across access, registration, confirmation, resend-code, recovery, and reset pages now use visible disabled styles and `disabledWhenInvalidScope` where relevant. Copy now explains that new accounts start as `zoosite-client` and require administrator approval or elevation for panel/admin permissions. JSON parse validation passed for the six affected auth component payloads, and the focused app Karma suite returned `TOTAL: 26 SUCCESS`.
- 2026-06-03 20:34 CT agent-learning multipage expansion: `agent-learning-claude-codex.zoolandingpage.com.mx` was expanded from a single dashboard-style draft into a local-only multipage study site. Added real draft routes for `/fundamentos`, `/prompting`, `/evaluaciones`, `/tool-use`, `/rag`, `/mcp`, `/workflows-agentes`, `/claude-code-codex`, `/laboratorios`, and `/flashcards`; each topic page includes deeper study content, examples, checklists, flashcards, related navigation, previous/next links, and local source references. Fixed missing page-local `i18n/es.json` resources that caused 500 responses on new pages. Validation passed: recursive draft JSON parse over 60 files, strict link/console/overflow audit over all 11 routes and discovered internal targets, targeted resource-error check, `npm run build`, draft pack, and browser QA for dashboard-to-`/mcp` navigation plus checklist interaction. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-03 21:02 CT agent-learning guide expansion: additional local material was reviewed from `C:\Users\lince\Documents\Codex\2026-06-01\revisa-lo-que-tenemos-ac-c`, `C:\Users\lince\Downloads\anthropic_training_plan - proposal`, and `C:\Users\lince\Documents\GitHub\Codex`. The draft now has 28 configured routes: home plus 27 study pages. Added an index, source library, and advanced pages for 4D fluency, context/memory, tool schemas, contextual RAG, advanced MCP, subagents, recommended skills, browser/computer-use QA, handoffs, dogfood/QA, security/publication, playbooks, agentic evals, repo workflow, and decision routing. Content was synthesized from markdowns/guides and local path citations; certificate PDFs and private Skilljar screenshots were not used. Validation passed: recursive draft JSON parse over 145 files, strict route/link/console/overflow audit over all configured routes and discovered internal targets with `failures: []`, draft pack, `npm run build`, and browser QA from dashboard to index to skills with checklist interaction and mobile `390x844` no-overflow evidence.
- 2026-06-03 21:03 CT agent-learning public-source cleanup and apps page: the public-facing draft UI was cleaned to remove local source paths, local-source labels, Skilljar/certificate references, and the old `58%` dashboard metric. The dashboard now shows `100%` coverage for the current web guide. Added `/apps-complementos` and navigation entries for active Codex plugins/apps/connectors, recommended usage, and Claude equivalents. The draft now has 29 configured routes. Validation passed: recursive draft JSON parse over 150 files, generated-draft grep and pack grep for local/source leak patterns with no matches, strict route/link/console/overflow audit with `failures: []`, `npm run build`, draft pack, and in-app browser QA for home and `/apps-complementos` with no overflow or public-facing local leaks. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-home-final.png` and `C:\Users\lince\.codex\memories\Output\agent-learning-apps-final.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-03 21:39 CT agent-learning Lynx Pardelle mascot integration: generated and integrated six friendly/kawaii Lynx Pardelle mascot scenes for the guide: welcome, prompting/context, evaluations, tool use/MCP, safety/publication, and workflows. The selected character identity is a lynx coder with round glasses, charcoal hoodie, subtle goth/punk details, work boots, laptop, and lavender. PNG originals were retained in draft assets and optimized WebP copies were created for runtime use, reducing each image to roughly 74-104 KB. The home dashboard now uses the mascot in coach/lab/infographic areas and topic pages use dynamic mascot overlays plus practice notes. The route metric was updated to `29 rutas navegables`. Validation passed: recursive draft JSON parse over 150 files, generated-draft/pack grep for local-source leak patterns with no matches, `npm run build`, draft pack, strict route/link/console/overflow audit with `failures: []`, and Playwright screenshots for home, `/prompting`, and mobile `/seguridad-publicacion` with no overflow and all Lynx images loaded.
- 2026-06-03 23:47 CT agent-learning Lynx sticker/layout pass: revised the mascot system so large Lynx illustrations have dedicated image panels instead of floating over dense content, and removed the card-inside-card visual wrapper around topic diagrams. Added 16 transparent `lynx-sticker-*.png` assets for floating/study accents and referenced all 16 across the draft; removed unused heavy large PNG/source sheet assets so runtime keeps the optimized `lynx-guide-*.webp` scenes plus sticker PNGs. Validation passed: recursive topic JSON parse `json ok 29`, asset check `{"count":0,"missing":[]}`, sticker reference count `16`, generated-draft grep for local/source leak and old overlay patterns with no matches, `npm run build`, draft pack, strict route/link QA with `failures: []`, and Playwright screenshots for `/prompting` and `/seguridad-publicacion` desktop/mobile with no horizontal overflow. New screenshot evidence was saved in `Output` as `agent-learning-no-nested-prompting.png`, `agent-learning-no-nested-safety-desktop.png`, and `agent-learning-no-nested-safety-mobile.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 02:16 CT agent-learning browser review pass: applied browser comments across home and topic pages. Increased spacing in the home hero, module grid, scenario cards, headings, and checklist; changed section labels from bordered pills to free uppercase text; fixed checkbox structure to remove duplicated visible labels while keeping accessible labels; removed repeated text from Lynx mascot cards; restructured topic hero sections so copy is on the left and the Lynx image is on the right, with the operational diagram below; differentiated `/indice` from `/fundamentos` by adding an index-only route map section; and replaced the evaluations SVG with a more legible diagram. Validation passed: `json ok 29`, asset check `{"count":0,"missing":[]}`, pack/public grep for local-source and old visual patterns with no matches, `npm run build`, draft pack, strict route/link QA with `failures: []`, and Playwright screenshots for home checklist, `/indice`, `/evaluaciones`, and mobile `/flashcards` with no horizontal overflow. Screenshot evidence was saved in `Output` as `agent-learning-review-home-checklist-v2.png`, `agent-learning-review-indice-v2.png`, `agent-learning-review-evaluaciones-v2.png`, and `agent-learning-review-flashcards-mobile-v2.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 13:22 CT agent-learning navigation/index QA pass: after relaunching the local Angular dev server at `http://localhost:4200/`, normalized the header across home and topic pages to the same 11 core links and changed the root route label from `Dashboard` to `Inicio`. `/indice` now exposes a complete `Todas las páginas` link table with all 29 configured routes, and the complete index appears near the top on desktop and within the first mobile viewport. Updated the local QA script to allow `AGENT_LEARNING_BASE` and default to localhost. Validation passed: route/index count check `{"routes":29,"indexLinks":29,"missing":[]}`, JSON/assets checks, `npm run build`, draft pack, strict link QA with `failures: []`, public/local grep with no matches, and Playwright desktop/mobile captures for home, index, fundamentals, and evaluations with no horizontal overflow. Screenshot evidence was saved in `Output` as `agent-learning-nav-indice-final.png` and `agent-learning-nav-indice-mobile-final.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 14:03 CT agent-learning compact header pass: reduced the Agent Lab header to the brand button plus `Índice`, because the brand button already links home and `/indice` owns full-site navigation. The header was kept on one compact row in mobile with no horizontal overflow; the home sidebar and `/indice` still keep the fuller route list for local study navigation. Validation passed: `npm run build`, draft pack, strict link QA with `failures: []`, public/local grep with no matches, and Playwright mobile screenshot for `/indice`. Screenshot evidence was saved in `Output` as `agent-learning-indice-header-brand-index-mobile.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 14:16 CT agent-learning sidebar cleanup: removed the `100%` coverage card from the home sidebar while keeping sidebar navigation and the main dashboard metrics. Validation passed: JSON parse `{"json":"ok","pages":29}`, asset check `{"missing":[]}`, Playwright home screenshot with `hasProgress:false` and no horizontal overflow, `npm run build`, draft pack, strict link QA with `failures: []`, and grep over draft/pack for the removed card and public-safety patterns with no matches. Angular build emitted the existing `allowedHosts: "*"` SSRF warning; it was not changed because it is app-level configuration outside this draft pass. Screenshot evidence was saved in `Output` as `agent-learning-home-sidebar-no-progress.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 13:58 CT Eros Barajas production-domain routing: prepared and deployed `erosbarajas.com` as the public production host for the `erosbarajas.com` draft while retiring the public Zoolanding subdomain paths. Updated the draft config so production aliases and test-environment aliases are empty, keeping testing access through shared query preview on `test.zoolandingpage.com.mx`; updated the shared app `allowedHosts` and public health-check default host to the definitive domain. Route 53 apex DNS, Traefik/Dokploy production routing, and TLS were configured without recording IPs, instance IDs, tokens, or raw secret values in repo notes. The old `erosbarajas.zoolandingpage.com.mx` and `test.erosbarajas.zoolandingpage.com.mx` hosts returned HTTP 404 after the routing change. Draft promotion used PR #5 (`dev -> test`) and PR #6 (`test -> main`); both deploy workflows completed successfully. Validation passed: runtime bundle for `erosbarajas.com` returned `ok=true` with no production or test aliases, `https://erosbarajas.com/` returned HTTP 200 with `<main>` and no old-host string, invented route returned HTTP 404, shared preview URL returned HTTP 200 with `<main>`, `npm run build`, `ssr-server.spec.mjs` 9/9, alias/front-door focused tests 13/13, and Edge Playwright smoke passed production desktop/mobile, production 404, and shared preview desktop/mobile with no horizontal overflow or unexpected console/request failures.
- 2026-06-04 15:27 CT Eros Barajas production refresh: enabled the draft's internal analytics and quick-stats path for production, kept GA4/Google Ads/GTM/Search Console disabled because no real Eros-specific values were provided or verified, polished footer/CTA spacing and first-viewport media, and removed the tracked `*_just_for_example*` Instagram reference screenshot from the draft. Uploaded optimized public assets through the presigned public asset flow: `hero-portrait-lcp.jpg` for first-viewport portrait use and `eros-barajas-logo-256.png` for logo/icon/loading use; the original portrait remains the Open Graph/Twitter image. Browser QA passed on local SSR for home desktop, home mobile, 404 mobile, and section viewports with no real console errors, failed requests, broken images, example-source assets, or horizontal overflow. Final Lighthouse evidence was saved under `reports/lighthouse/2026-06-04-erosbarajas-local/`: mobile `mobile-final-clean-clsfix` scored 100/100/100/100 and desktop `desktop-final-clean-clsfix-rerun` scored 99/100/100/100. Draft package was regenerated at `logs/drafts/erosbarajas-20260604-production-refresh-package.json`. Public safety audit found no current/history secrets and no blocked paths, but still returned `ok:false` because the draft repo is dirty and the public WhatsApp phone number triggers two `phone-or-whatsapp` review findings in `default/variables.json` and `not-found/variables.json`.
- 2026-06-04 15:44 CT agent-learning annotation/lightbox pass: resolved the latest browser annotations for `agent-learning-claude-codex.zoolandingpage.com.mx`. Moved the home active lab into the right column, added draft-local image lightboxes using `modalRootIds` and `openModal`, increased spacing in the safety panel, diversified Lynx/topic visuals, removed old single-image wrapper card IDs from topic hero visuals, changed `/skills-recomendados` title to `Skills`, and added `/skills-recomendados-casos` for recommended skills with descriptions and use cases. Added four optimized Lynx WebP scenes plus two readable SVG diagrams. Validation passed: route structure audit `{"routeCount":30,"issues":[]}`, Playwright render QA across 30 routes in desktop and mobile `{"checked":60,"issues":[]}`, centered lightbox QA, `npm run build`, draft pack, strict link QA summary `{"routes":30,"targets":30,"failures":0,"overflows":0,"badLinks":0,"missingHashTargets":0}`, and grep over generated draft/pack for local/private patterns and old IDs with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 17:12 CT agent-learning home guide-start pass: resolved the home-focused browser annotations for `agent-learning-claude-codex.zoolandingpage.com.mx`. Expanded the home modules grid to 26 topic cards, added a mobile-friendly start note before the sidebar route list, changed the next challenge to start with `Fundamentos`, made the flashcard preview dynamic per visit through a draft-local `embed-frame`, restricted the home infographic section to six SVG diagrams, removed Lynx scene cards from that section, rewrote safety/resource/footer copy for students, and added the public Anthropic Academy Skilljar link. Validation passed: Playwright home QA on desktop and mobile with no console errors or horizontal overflow, dynamic flashcards produced 6 unique titles across 8 entries, `npm run build`, draft pack, strict link QA summary `{"routes":30,"targets":30,"failures":0,"badLinks":0,"routeOverflows":0,"missingHashTargets":0}`, and grep over generated home/pack for local/private/old-copy patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-home-desktop-top-final-viewport.png` and `agent-learning-home-mobile-top-final-viewport.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 18:02 CT agent-learning fundamentos expansion: resolved the `/fundamentos` browser annotations. Outcome cards now have semantic titles and fuller descriptions, practice-sequence steps have titles, a new `Contrato mínimo` deep-dive section explains task/context/limits/output/validation/escalation, examples expanded to three weak-vs-operative cases, and guided practice expanded to three exercises before the checklist. Validation passed: Playwright `/fundamentos` QA on desktop and mobile with no console errors or horizontal overflow, no number-only outcome/step titles, three examples, three practices, six deep-dive cards, `npm run build`, draft pack, strict link QA summary `{"routes":30,"targets":30,"failures":0,"badLinks":0,"routeOverflows":0,"missingHashTargets":0}`, and grep over `/fundamentos` plus pack for local/private/source-leak patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-fundamentos-*.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 18:43 CT agent-learning fundamentos practice context: expanded `/fundamentos` again so students have enough context before attempting guided practice. Added an “Antes de practicar” block with four preparation cards, a six-field quick delegation template, and clearer prerequisites/expected results in the three guided practices. Validation passed: Playwright `/fundamentos` prep QA on desktop and mobile with no console errors or horizontal overflow, `npm run build`, draft pack, strict link QA summary `{"routes":30,"targets":30,"failures":0,"badLinks":0,"routeOverflows":0,"missingHashTargets":0}`, and grep over `/fundamentos` plus pack for local/private/source-leak patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-fundamentos-desktop-prep.png` and `agent-learning-fundamentos-mobile-prep.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 19:23 CT agent-learning prompting expansion: expanded `/prompting` to match the new topic-page standard. The page now treats prompting as a contract of output, with semantic outcome cards, eight core concepts, an anatomy deep-dive, a prompt preparation block, a reusable prompt template, three weak-vs-operative examples with explanations, and three detailed guided practices. Added draft-local `prompt-contract-map.svg` so the topic has a specific visual diagram instead of reusing a tool-choice matrix or duplicating Lynx imagery. Validation passed: Playwright `/prompting` QA on desktop `1440x1100` and mobile `390x900` with no console errors, no horizontal overflow, no number-only headings, no local/private text leaks, 8 concepts, 8 anatomy cards, 3 examples, 3 practices, and 6 preparation cards; `npm run build`; draft pack; route/link QA summary `{"routes":30,"anchors":616,"routeFailures":0,"failures":[]}`; and grep over `/prompting` plus pack for local/private/source-leak patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-prompting-*.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 23:52 CT agent-learning evaluaciones expansion: expanded `/evaluaciones` to the current topic-page standard and resolved the latest image annotations. The page now explains evaluations as cases, expected output, rubric, grader, threshold, regression, and publication gate; outcome and sequence cards have semantic titles; and the page includes 8 concepts, an 8-card deep-dive, a prep block, a reusable evaluation template, 3 weak-vs-operative examples with explanations, and 3 guided practices. `prompt-contract-map.svg` was simplified so its bottom rule fits, and `eval-pipeline.svg` was replaced with a cleaner readable diagram. Validation passed: Playwright `/evaluaciones` and `/prompting` desktop/mobile QA with no console errors, no horizontal overflow, no number-only headings, and no local/private text leaks; `npm run build`; draft pack; final route/link QA summary `{"routes":30,"anchors":616,"failures":0,"badLinks":0,"missingHashTargets":0}` after rerunning a transient `ERR_NETWORK_CHANGED` failure caused by dev-server rebuild; and grep over `/evaluaciones`, `/prompting`, and pack for local/private/source-leak/internal-instruction patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-evaluaciones-*.png` and `agent-learning-prompting-desktop-top-v2.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 16:29 CT agent-learning tool-use expansion: expanded `/tool-use` to the current topic-page standard. The page now explains tool use as a contract for controlled action, with semantic outcome cards, titled practice sequence steps, 8 concepts, an 8-card contract deep dive, a preparation block, a reusable tool-contract template, 3 weak-vs-operative examples with explanations, and 3 guided practices. Added draft-local `tool-contract-flow.svg` as a readable desktop/mobile diagram. Validation passed: Playwright `/tool-use` desktop/mobile QA with no console errors, no horizontal overflow, no number-only headings, no local/private text leaks, and SVG loaded at natural size `1200x720`; `npm run build`; draft pack; route/link QA summary `{"routes":30,"anchors":616,"failures":0,"badLinks":0,"missingHashTargets":0}`; and grep over `/tool-use` plus pack for local/private/source-leak/internal-instruction patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-tool-use-*.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 17:13 CT agent-learning rag expansion: expanded `/rag` to the current topic-page standard. The page now explains RAG as evidence discipline: corpus, chunks, retrieval, ranking, grounding, citations, fallback, missing evidence, and evaluation. Added semantic outcome cards, titled practice sequence steps, 8 concepts, an 8-card evidence deep dive, a preparation block, a reusable RAG answer template, 3 weak-vs-operative examples with explanations, and 3 guided practices. Added draft-local `rag-evidence-pipeline.svg` and adjusted it after visual QA so text no longer clips in desktop/mobile. Validation passed: Playwright `/rag` desktop/mobile QA with no console errors, no horizontal overflow, no number-only headings, no local/private text leaks, and SVG loaded at natural size `1200x720`; `npm run build`; draft pack; final route/link QA summary `{"routes":30,"anchors":616,"failures":0,"badLinks":0,"missingHashTargets":0}` after rerunning a transient `ERR_NETWORK_CHANGED` failure caused by dev-server settling; and grep over `/rag` plus pack for local/private/source-leak/internal-instruction patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-rag-*.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 18:01 CT agent-learning five-page package expansion: expanded `/mcp`, `/workflows-agentes`, `/claude-code-codex`, `/laboratorios`, and `/flashcards` to the current topic-page standard in one package. Each route now has semantic outcome cards, titled practice sequence steps, 8 concepts, an 8-card deep dive, preparation/template content, 3 examples or drills, and 3 guided practices. Added draft-local SVGs `mcp-primitives-map.svg`, `workflow-agent-router.svg`, `repo-agent-closeout.svg`, `lab-scenario-board.svg`, and `flashcard-study-system.svg`, replacing older generic visuals for these routes. Three read-only subagents were used: two to identify content/visual risks and one to evaluate the integrated result; flagged issues in `/flashcards` public wording and possible SVG title clipping were corrected before final QA. Validation passed: Playwright desktop/mobile QA for all five routes with no console errors, no horizontal overflow, no number-only headings, assets loaded, 3 examples, 3 practices, 8 concepts and prep blocks present; `npm run build`; draft pack; final route/link QA summary `{"routes":30,"anchors":616,"failures":0,"badLinks":0,"missingHashTargets":0}` after rerunning a transient `ERR_NETWORK_CHANGED` failure; and grep over runtime route files plus pack for local/private/source-leak/internal-instruction patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-*-pkg5-final.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 19:04 CT agent-learning flashcards plus five-page package expansion: added a dedicated `Todas las flashcards` section to `/flashcards` with the full configured 91-card guide bank. Expanded `/biblioteca`, `/fluidez-4d`, `/contexto-memoria`, `/tool-schemas`, and `/rag-contextual` to the current topic-page standard: semantic outcome cards, titled practice sequence steps, 8 concepts, an 8-card deep dive, preparation/template content, 3 examples, and 3 guided practices. Added draft-local SVGs `library-source-map.svg`, `fluency-4d-matrix.svg`, `context-memory-stack.svg`, `schema-validation-contract.svg`, and `contextual-rag-loop.svg`, replacing repeated/generic visuals for the package. Three read-only subagents were used for content, visual, and integrated source QA; public-facing internal wording found by the evaluator was rewritten before final validation. Validation passed: `node --check` for both generators, Playwright desktop/mobile QA for the six affected routes with no console errors, no horizontal overflow, no number-only headings, all images loaded, lightboxes opening, 3 examples, 3 practices, 8 concepts, and prep blocks present; `/flashcards` mobile rerun passed after a transient `ERR_NETWORK_CHANGED`; `npm run build`; draft pack; strict link QA with `failures: []`; and grep over generated runtime route files plus pack for local/private/source-leak/internal-instruction patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-*-pkg6-final-v3.png` and `agent-learning-flashcards-mobile-pkg6-final-v4.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 20:17 CT agent-learning 4D visual fix: corrected the `/fluidez-4d` topic SVG after browser annotation. `fluency-4d-matrix.svg` was simplified with shorter labels, wider spacing, and a two-line practical rule so text no longer clips in the full-size topic visual. Validation passed: Playwright `/fluidez-4d` desktop and mobile QA with no console errors, no horizontal overflow, image loaded, lightbox opening, and the old long clipped rule absent; `npm run build`; and draft pack. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-fluidez-4d-*-visual-fix.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 21:03 CT agent-learning MCP/subagents/skills/apps package expansion: expanded `/mcp-avanzado`, `/subagentes`, `/skills-recomendados`, `/skills-recomendados-casos`, and `/apps-complementos` to the current topic-page standard. Each route now has semantic outcome cards, titled practice sequence steps, 8+ concepts, an 8-card deep dive, preparation/template content, 3 examples, and 3 guided practices. Added draft-local SVGs `mcp-capability-gate.svg`, `subagent-orchestration-board.svg`, `skills-decision-stack.svg`, `skills-cases-router.svg`, and `apps-capability-map.svg`, replacing repeated/generic visuals. Rewrote skills/apps copy to use stable public learning categories instead of local-environment state. Three read-only subagents were used for content, visual/source QA, and integrated source evaluation; the final evaluator returned PASS with no corrections. Validation passed: `node --check` for both generators, Playwright desktop/mobile QA for the five affected routes with no console errors, no horizontal overflow, no number-only headings, images loaded, lightboxes opening, 3 examples, 3 practices, 8+ concepts, and prep blocks present; `npm run build`; draft pack; strict link QA with `failures: []`; and visible-text scan for local/private/source-leak/internal wording with no findings. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-*-pkg7-final.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 22:17 CT agent-learning final eight-page package expansion: expanded `/browser-computer-use`, `/handoffs-coordinacion`, `/qa-dogfood`, `/seguridad-publicacion`, `/playbooks`, `/evaluaciones-agenticas`, `/workflows-repo`, and `/decision-router`. The pages now use differentiated structures where appropriate: Browser QA observatory, handoff ledger, audit loop, publication safety gate, playbook library, agentic eval harness, repo workflow closeout, and decision router matrix. Each route includes semantic outcome cards, titled practice sequence steps, 8+ concepts, deep-dive cards, preparation/template content, 3 examples, and 3 guided practices. Added draft-local SVGs `browser-qa-observatory.svg`, `handoff-context-ledger.svg`, `qa-dogfood-audit-loop.svg`, `publication-safety-gate.svg`, `agent-playbook-library.svg`, `agentic-eval-harness.svg`, `repo-workflow-closeout.svg`, and `decision-router-map.svg`. Validation passed: `node --check` for both generators, static structure audit for the eight routes, Playwright desktop/mobile QA for all eight routes with no console errors, no failed visible images, no horizontal overflow and lightboxes opening, `npm run build`, draft pack, strict link QA rerun summary `{"routes":30,"targets":30,"anchors":616,"failures":0,"overflows":0,"badLinks":0,"missingHashTargets":0}`, and global visible-text scan for local/private/source-leak/internal wording with no findings. One link QA run hit transient `ERR_NETWORK_CHANGED` while the dev server settled after build; rerun passed cleanly. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-*-pkg8-final.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 22:49 CT agent-learning domain rename pre-test gate: renamed the local draft from `agent-learning-claude-codex.zoolandingpage.com.mx` to `agent-guide-claude-codex.com`, updated `site-config.json`, generator domain constants, QA script domain constants, shared domain metadata, and regenerated all draft routes. Local validation passed: `node --check` for generator/QA scripts, `npm run build`, draft pack for `agent-guide-claude-codex.com`, strict local link QA after one transient `ERR_NETWORK_CHANGED` retry with 30 routes, 30 targets, no bad links, no missing hash targets, no console findings, and no mobile overflow, plus a final grep showing no old-domain matches in the draft, generators, QA script, or pack. Required public safety audit was run with `node tools\draft-public-safety-audit.mjs --repo=drafts\agent-guide-claude-codex.com --history=true` and returned `ok:false`; the current draft had no blocked paths, secret findings, or review findings, but the tool blocked on dirty hub worktree status and historical hub paths under `logs/`, `reports/`, and `findings/`. Testing publication was not attempted, so remote QA on `test.zoolandingpage.com.mx` remains pending.
- 2026-06-06 00:12 CT agent-guide public-safety blocker resolution: resolved the prior audit blocker by creating a clean isolated public snapshot repo at `C:\Users\lince\Documents\GitHub\draft-agent-guide-claude-codex-com-audit-20260606-0008`, excluding local-only coordination/notes/investigation folders, initializing Git, and committing the public draft snapshot. `node tools\draft-public-safety-audit.mjs --repo=C:\Users\lince\Documents\GitHub\draft-agent-guide-claude-codex-com-audit-20260606-0008 --history=true` returned `ok:true`, `status:"clean"`, `blockingRepoCount:0`, and no blocked paths or secret findings. Non-blocking review findings were educational safety terms such as credentials, identity, and tokens, not exposed private data. Local testing deploy was not run because AWS signing environment variables are not present in this session.
- 2026-06-06 01:20 CT agent-guide testing publication: created and configured the standalone public draft repo `LynxPardelle/draft-agent-guide-claude-codex-com` for `agent-guide-claude-codex.com`, pushed `main`, `test`, and `dev`, configured GitHub Environments and deploy variables, applied branch protection on `test` and `main`, created AWS OIDC deployment roles for test and production, and registered those roles in the authoring Lambda deploy authorization config. The real draft repo public-safety audit passed with history enabled: `node ..\..\tools\draft-public-safety-audit.mjs --repo=. --history=true` returned `ok:true` with no blocked paths, no secret findings, and only non-blocking educational safety-word review findings. PR #1 (`dev` to `test`) deployed after resolving Lambda deploy authorization; PR #2 added test aliases `test.agent-guide-claude-codex.com` and `test.agent-guide-claude-codex.zoolandingpage.com.mx`; PR #3 uploaded 62 draft assets to `https://assets.zoolandingpage.com.mx/agent-guide-claude-codex.com/shared/draft-assets/`, replaced local draft asset paths with public CDN URLs, and deployed to test successfully. Runtime-bundle checks for the test alias returned `ok:true`, `environment:"test"`, and page IDs `default` and `indice`. Remote QA passed for `https://test.zoolandingpage.com.mx/?draftDomain=test.agent-guide-claude-codex.zoolandingpage.com.mx&debugWorkspace=false` on desktop `1440x1200` and mobile `390x1200`: HTTP 200, title `Claude/Codex Agent Lab`, H1 `Aprende agentes como sistema, no como lista de prompts`, no failed requests, no console errors, and no horizontal overflow. Known preview limitation: deep shared-host URLs such as `/indice?draftDomain=test.agent-guide-claude-codex.zoolandingpage.com.mx` and `/flashcards?...` render the correct client-side content but return SSR HTTP 404 from the shared test host; use the root preview plus in-site navigation until that app-level preview behavior is corrected.
- 2026-06-07 02:28 CT Pamela Betancourt copy refresh: updated `pamelabetancourt.com` local draft copy from `copy-web-pamela-betancourt.md` so Home, Servicios, Acerca de mí, Contacto, footer, route labels, and SEO now focus on free Instagram/Facebook diagnosis, monthly content planning, basic metrics, and strategic social-media support. Removed the old active-offer framing around web analytics, site journey, funnels, landing-page review, and generic digital diagnosis from active draft JSON. WhatsApp prefill copy was not implemented because no public WhatsApp target is configured in the draft; CTAs route to `/contact`. Validation passed: recursive JSON parse, `node tools/config-draft-sync.mjs pack --domain=pamelabetancourt.com`, `npm run build`, three config audit cycles, and local draft smoke with `node tools/draft-smoke-check.mjs --local-base-url=http://127.0.0.1:4200 --domain=pamelabetancourt.com --include-live=false --timeout-ms=10000`, which returned 7 routes across desktop/mobile with 0 local failures. In-app browser QA on clean local URLs with `debugWorkspace=false` covered Home, Servicios, Acerca de mí, and Contacto in desktop/mobile: expected copy was present, old-copy checks returned no matches, no unresolved-draft or framework overlay appeared, no horizontal overflow was found, and the Home hero CTA navigated to `/contact?draftDomain=pamelabetancourt.com&debugWorkspace=false` with `scrollY:0`. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\pamela-copy-qa-*.png`. Existing local browser console warning `NG0913` remains for the large Pamela profile image asset; no testing deploy was attempted in this pass.
- 2026-06-07 02:47 CT Pamela Betancourt WhatsApp target update: configured the provided Pamela WhatsApp target in `pamelabetancourt.com` draft defaults, added the shared WhatsApp prefill message from `copy-web-pamela-betancourt.md`, and changed the primary diagnosis CTAs on Home, Servicios, Acerca de mí, and the default page payload to open the WhatsApp flow with `whatsapp_click` tracking. The contact route and Google Form remain available through navigation. Validation passed: recursive JSON parse, `node tools/config-draft-sync.mjs pack --domain=pamelabetancourt.com`, `npm run build`, local smoke over 7 routes x desktop/mobile with 0 failures, in-app browser DOM checks for Home/Servicios/Acerca de mí showing all visible diagnosis CTAs use `wa.me`, `_blank`, `noopener noreferrer`, and `whatsapp_click`, and three corrected config audit cycles. Image follow-up evidence: the `acerca-de-mi` profile image renders near 298x373 while the asset is 1440x1800, HTTP 200, WebP, and 85422 bytes; two separate worktree chats were queued to investigate upload-lambda compression and safe S3 asset optimization.
- 2026-06-07 03:08 CT Pamela Betancourt CTA contrast fix: resolved the browser annotation on `acerca-de-mi` by applying draft-local inline `color: var(--ank-bgColor)` to the primary WhatsApp diagnosis CTAs on Home, Servicios, Acerca de mí, and the default payload. Browser QA on desktop and mobile showed all six visible WhatsApp CTAs now render text as `rgb(244, 231, 225)` over `rgb(115, 67, 50)` with contrast `6.74`, no horizontal overflow, and no framework overlay. Validation passed: recursive JSON parse, draft pack, local smoke over 7 routes x desktop/mobile with 0 failures, and three static CTA audit cycles.
- 2026-06-07 03:27 CT Pamela Betancourt testing publication: draft repo commit `42723bb` was pushed to `dev`, PR #5 (`dev` -> `test`) was created and merged after the `guard` check passed, and GitHub Actions run `27088389416` (`Deploy test draft`) completed with `success` on merge commit `29c6f63`. The runtime bundle for `test.pamelabetancourt.zoolandingpage.com.mx` returned `ok:true`, `environment:"test"`, and version `20260607T091615-test-29c6f63d9470`; the production-domain runtime bundle still returned production version `20260519T003354-production-a4873e2b0c3f`, confirming testing was isolated. Remote Playwright QA ran three cycles across shared preview and managed test alias, desktop and mobile, and routes `/`, `/servicios`, `/acerca-de-mi`, and `/contact`: 16 checks per cycle, 0 navigation errors, 0 console warnings/errors, 0 failed requests, 0 bad responses, 0 horizontal overflow, 24 WhatsApp CTAs per cycle, 0 href mismatches, and 0 low-contrast CTAs. CTA contrast remained `6.74` with `_blank` plus `noopener noreferrer`. Screenshot/report evidence was saved under `C:\Users\lince\.codex\memories\Output\pamela-testing-qa\`. `test.pamelabetancourt.com` returned local DNS error `Host desconocido. (test.pamelabetancourt.com:443)`, so QA relied on `https://test.pamelabetancourt.zoolandingpage.com.mx` and `https://test.zoolandingpage.com.mx/?draftDomain=pamelabetancourt.com&debugWorkspace=false`.
- 2026-06-16 13:11 CT Zoosite custom auth form prep: `zoositioweb.com.mx` `/acceso` was updated to use draft-auth forms assembled only from generic components for sign-in, sign-up, sign-up confirmation/resend, password recovery, and password reset. The server-only auth registry now enables optional `customAuth` for the `staff` profile with server-side tenant claim assignment and the default `zoosite-client` group. Validation passed: draft JSON parse for `/acceso` and `server/auth-profile-registry.json`, app auth contract tests, Angular focused auth/form tests, `npm run build`, API proxy unit tests, `sam validate --lint`, `pip-audit -r requirements.txt`, and `node tools/draft-public-safety-audit.mjs --repo=drafts\zoositioweb.com.mx --history=true --summary=true` returned `ok:true` with no current/history blocked paths or secret findings.
- 2026-06-16 16:32 CT Zoosite custom logout fix: `zoositioweb.com.mx` `/mi-cuenta` now uses `authFormAction:logout` for the account-page logout button so custom-auth sessions clear locally and return to `/acceso` without opening Cognito Hosted UI. This prevents Cognito `/error` after signing out from the shared Spanish testing preview.
- 2026-06-16 17:09 CT Zoosite auth page split: `zoositioweb.com.mx` split custom auth into generic-component pages so `/acceso` shows only sign-in, while `/registro`, `/confirmar-cuenta`, `/recuperar-contrasena`, and `/cambiar-contrasena` own account creation, confirmation, recovery, and reset flows. Password visibility toggles now declare explicit icons and stronger contrast/size classes, and draft field validation relies on authored Spanish messages instead of duplicate browser-required text.
- 2026-06-16 17:31 CT Zoosite auth page split testing: PR #32 (`dev` -> `test`) published the separated custom-auth pages, and PR #33 corrected the password visibility toggle classes to use Angora tokens present on the deployed shared testing host. Deploy runs completed successfully for merge commits `cd34a2e0b4fd9a17f00e258849e01fbc745e4475` and `b509a70f65c1dab181d69ac387caa09f351d9d2b`. Validation passed: draft JSON parse, `npm run build`, `git diff --check` with only LF/CRLF warnings, three public-safety audit reruns with `ok:true`, `blockingRepoCount:0`, no current/history blocked paths, and no current/history secret findings, plus local and remote browser QA for `/acceso`, `/registro`, `/confirmar-cuenta`, `/recuperar-contrasena`, and `/cambiar-contrasena` on desktop and mobile. Final remote QA on `https://test.zoolandingpage.com.mx` returned `allOk:true`; `/acceso` shows one sign-in form, `/registro` shows one sign-up form, confirmation shows the expected confirmation/resend forms, password toggles render at `44x44` with `24x24` icons, and toggle clicks switch `password -> text -> password`.
- 2026-06-16 18:00 CT Zoosite auth secondary action polish: resolved browser feedback that `/acceso` secondary auth links were too small for mobile. PR #34 (`dev` -> `test`) enlarged auth secondary actions across `/acceso`, `/registro`, `/confirmar-cuenta`, `/recuperar-contrasena`, and `/cambiar-contrasena` into full-width 48px touch targets and clarified ambiguous code labels; PR #35 made the secondary actions visually read as buttons instead of plain text. Deploy test runs `27655912776` and `27656081495` completed successfully. Validation passed: auth JSON parse, `npm run build`, focused `generic-input` Karma test returned `TOTAL: 12 SUCCESS`, public safety audit passed three times after each draft commit with `ok:true`, no blocked paths, and no secret findings. Remote Browser QA on `https://test.zoolandingpage.com.mx` returned no UI failures across the five auth routes on desktop and mobile: secondary links measured 327-720px wide and 47-49px tall, password icons measured `44x44` with `24x24` SVGs, there was no horizontal overflow, and no duplicate English required text appeared.
- 2026-06-16 18:24 CT Zoosite auth password validation polish: updated `/registro` and `/cambiar-contrasena` to show always-visible password rule checklists, require at least one symbol, compare repeated passwords through generic scope validation, and disable submit until scoped fields are valid. Added shared footer links for sign-in and account creation with Spanish/English i18n labels.
- 2026-06-17 02:16 CT Zoosite auth environment claim prep: `zoositioweb.com.mx` server-only auth registry now declares `environmentClaim: "custom:zoolanding_env"` and `customAuth.signup.setEnvironmentClaim: true` so testing and production users can share the same Cognito pool while remaining scoped by backend-assigned environment. App-side auth calls on `test.zoolandingpage.com.mx` now route to the testing API proxy stack before reaching Cognito.
# 2026-06-30 17:51 CT Zoosite blog analytics columns

- Updated `zoositioweb.com.mx` `/admin/blog/analiticas` so the analytics table shows all blog metrics already declared by the content-hub contract: views, read progress, CTA clicks, reactions, comments, shares, and asset downloads.
- Added app-side draft contract coverage so the analytics table cannot regress to only views and CTA clicks while the runtime declares broader summary metrics.

# 2026-06-30 16:50 CT Zoosite blog media upload guard

- Updated `zoositioweb.com.mx` `/admin/blog/medios` so media registration and the protected asset list require a selected article context, and media registration requires a selected file before the `Registrar asset` action is enabled. The page now explains that editors should open media from an article row and that the file dropzone only prepares local selection; the protected content-hub upload action still validates authorization and storage server-side.
- Scoped `/admin/blog/programados` schedule reads to a selected `articleId` as well, so the scheduling surface no longer asks the protected content service for article-specific data without article context.
- Added app-side draft contract coverage that fails if the media page stops requiring `articleId`, stops requiring a file selection, or re-enables the upload action while the interaction scope is invalid.

# 2026-06-30 17:08 CT Zoosite public blog share action

- Updated the public article share button so it now runs the generic `shareCurrentPage` browser action before recording the existing content-hub `share` interaction. This keeps analytics meaningful: a share click attempts native sharing or clipboard copy instead of only measuring intent.

# 2026-06-30 16:18 CT Zoosite blog moderation row actions

- Updated `zoositioweb.com.mx` `/admin/blog/moderacion` so moderation is driven by row actions in the queue table instead of visible manual comment-id and decision controls. The table now uses `commentId` as `rowIdPath`, emits only explicit moderation payload fields, and provides per-row approve, reject, and archive actions with 54px touch targets and tooltip labels. Shared reason/audit fields remain visible as context; the current backend action still accepts only `commentId`, `decision`, and `reason`.
- Added app-side draft contract coverage that fails if moderation regresses to global action buttons, visible manual ID/decision fields, missing `commentId` payloads, or a row table without approve/reject/archive actions.

# 2026-06-30 16:42 CT Zoosite public blog interaction wiring

- Updated the public article page so the main article CTA and a useful-reaction button call the existing `content_hub_record_interaction` action with current article id/path only, while authenticated comments use a separate `interaction-scope` and `content_hub_queue_comment`. The comment form tells visitors login is required and comments are reviewed before publication.
- Added draft contract coverage to keep article CTA/reaction/comment actions bound to `contentHub.currentArticle` and to keep simple interactions separate from the comment validation scope.

# 2026-06-30 07:27 CT Zoosite blog overview selection copy

- Replaced the `/admin/blog` validation/publish fallback copy that said `Artículo seleccionado pendiente` / `Versión seleccionada pendiente` with explicit instructions telling editors to select an article from the table before continuing. Validation passed: `node --test tools/tests/content-hub-admin-pages.spec.mjs` returned 19 pass. Public-safety audit still blocks only while the nested draft repo is dirty; it reported no current/history blocked paths or secret findings.

# 2026-06-30 07:31 CT Zoosite blog article-list action cleanup

- Removed the orphan `/admin/blog/articulos` `Validar selección` button because it was not mounted in a row/detail context and could not send the required article/revision identifiers. Updated the list copy so editors open article row actions for edit, SEO, versions, and scheduling instead of expecting a global validation action.

# 2026-06-30 04:03 CT Zoosite blog product copy guard

- Tightened `zoositioweb.com.mx` blog/admin visible copy so the overview and hub pages no longer describe internal hub IDs or server-only implementation boundaries to editors. Expanded the app-side content-hub admin page test guard to reject additional internal/product-placeholder terms such as `package`, `MVP`, `noindex`, `server-only`, `defaultCommentPolicy`, `reschedule`, `cancelSchedule`, and `Choose files` from user-facing strings while preserving backend IDs and action contracts. Validation passed: content-hub admin/binding Node tests returned `26 pass`, content-hub smoke/contract/schema tests returned `34 pass`, SSR server tests returned `23 pass`, focused rich-text/proxy-action Karma returned `TOTAL: 16 SUCCESS`, content-hub BFF unit tests returned `Ran 59 tests ... OK`, `npm audit --omit=dev` returned `found 0 vulnerabilities`, and `npm run build` passed with the existing Quill `quill-delta` CommonJS warning. Draft public-safety audit still blocks only while the nested draft repo is dirty; it reported no current or history blocked paths and no current or history secret findings.
