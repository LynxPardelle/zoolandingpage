# Drafts Changelog - 2026-06

Chronological draft-authoring and draft-QA history.

<!-- Keep this file chronological. Keep durable decisions in Codex.md or ai-notes, not here. -->

- 2026-06-03 20:34 CT agent-learning multipage expansion: `agent-learning-claude-codex.zoolandingpage.com.mx` was expanded from a single dashboard-style draft into a local-only multipage study site. Added real draft routes for `/fundamentos`, `/prompting`, `/evaluaciones`, `/tool-use`, `/rag`, `/mcp`, `/workflows-agentes`, `/claude-code-codex`, `/laboratorios`, and `/flashcards`; each topic page includes deeper study content, examples, checklists, flashcards, related navigation, previous/next links, and local source references. Fixed missing page-local `i18n/es.json` resources that caused 500 responses on new pages. Validation passed: recursive draft JSON parse over 60 files, strict link/console/overflow audit over all 11 routes and discovered internal targets, targeted resource-error check, `npm run build`, draft pack, and browser QA for dashboard-to-`/mcp` navigation plus checklist interaction. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-03 21:02 CT agent-learning guide expansion: additional local material was reviewed from `C:\Users\lince\Documents\Codex\2026-06-01\revisa-lo-que-tenemos-ac-c`, `C:\Users\lince\Downloads\anthropic_training_plan - proposal`, and `C:\Users\lince\Documents\GitHub\Codex`. The draft now has 28 configured routes: home plus 27 study pages. Added an index, source library, and advanced pages for 4D fluency, context/memory, tool schemas, contextual RAG, advanced MCP, subagents, recommended skills, browser/computer-use QA, handoffs, dogfood/QA, security/publication, playbooks, agentic evals, repo workflow, and decision routing. Content was synthesized from markdowns/guides and local path citations; certificate PDFs and private Skilljar screenshots were not used. Validation passed: recursive draft JSON parse over 145 files, strict route/link/console/overflow audit over all configured routes and discovered internal targets with `failures: []`, draft pack, `npm run build`, and browser QA from dashboard to index to skills with checklist interaction and mobile `390x844` no-overflow evidence.
- 2026-06-03 21:03 CT agent-learning public-source cleanup and apps page: the public-facing draft UI was cleaned to remove local source paths, local-source labels, Skilljar/certificate references, and the old `58%` dashboard metric. The dashboard now shows `100%` coverage for the current web guide. Added `/apps-complementos` and navigation entries for active Codex plugins/apps/connectors, recommended usage, and Claude equivalents. The draft now has 29 configured routes. Validation passed: recursive draft JSON parse over 150 files, generated-draft grep and pack grep for local/source leak patterns with no matches, strict route/link/console/overflow audit with `failures: []`, `npm run build`, draft pack, and in-app browser QA for home and `/apps-complementos` with no overflow or public-facing local leaks. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-home-final.png` and `C:\Users\lince\.codex\memories\Output\agent-learning-apps-final.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-03 21:39 CT agent-learning Lynx Pardelle mascot integration: generated and integrated six friendly/kawaii Lynx Pardelle mascot scenes for the guide: welcome, prompting/context, evaluations, tool use/MCP, safety/publication, and workflows. The selected character identity is a lynx coder with round glasses, charcoal hoodie, subtle goth/punk details, work boots, laptop, and lavender. PNG originals were retained in draft assets and optimized WebP copies were created for runtime use, reducing each image to roughly 74-104 KB. The home dashboard now uses the mascot in coach/lab/infographic areas and topic pages use dynamic mascot overlays plus practice notes. The route metric was updated to `29 rutas navegables`. Validation passed: recursive draft JSON parse over 150 files, generated-draft/pack grep for local-source leak patterns with no matches, `npm run build`, draft pack, strict route/link/console/overflow audit with `failures: []`, and Playwright screenshots for home, `/prompting`, and mobile `/seguridad-publicacion` with no overflow and all Lynx images loaded.
- 2026-06-03 23:47 CT agent-learning Lynx sticker/layout pass: revised the mascot system so large Lynx illustrations have dedicated image panels instead of floating over dense content, and removed the card-inside-card visual wrapper around topic diagrams. Added 16 transparent `lynx-sticker-*.png` assets for floating/study accents and referenced all 16 across the draft; removed unused heavy large PNG/source sheet assets so runtime keeps the optimized `lynx-guide-*.webp` scenes plus sticker PNGs. Validation passed: recursive topic JSON parse `json ok 29`, asset check `{"count":0,"missing":[]}`, sticker reference count `16`, generated-draft grep for local/source leak and old overlay patterns with no matches, `npm run build`, draft pack, strict route/link QA with `failures: []`, and Playwright screenshots for `/prompting` and `/seguridad-publicacion` desktop/mobile with no horizontal overflow. New screenshot evidence was saved in `Output` as `agent-learning-no-nested-prompting.png`, `agent-learning-no-nested-safety-desktop.png`, and `agent-learning-no-nested-safety-mobile.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 02:16 CT agent-learning browser review pass: applied browser comments across home and topic pages. Increased spacing in the home hero, module grid, scenario cards, headings, and checklist; changed section labels from bordered pills to free uppercase text; fixed checkbox structure to remove duplicated visible labels while keeping accessible labels; removed repeated text from Lynx mascot cards; restructured topic hero sections so copy is on the left and the Lynx image is on the right, with the operational diagram below; differentiated `/indice` from `/fundamentos` by adding an index-only route map section; and replaced the evaluations SVG with a more legible diagram. Validation passed: `json ok 29`, asset check `{"count":0,"missing":[]}`, pack/public grep for local-source and old visual patterns with no matches, `npm run build`, draft pack, strict route/link QA with `failures: []`, and Playwright screenshots for home checklist, `/indice`, `/evaluaciones`, and mobile `/flashcards` with no horizontal overflow. Screenshot evidence was saved in `Output` as `agent-learning-review-home-checklist-v2.png`, `agent-learning-review-indice-v2.png`, `agent-learning-review-evaluaciones-v2.png`, and `agent-learning-review-flashcards-mobile-v2.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 13:22 CT agent-learning navigation/index QA pass: after relaunching the local Angular dev server at `http://localhost:4200/`, normalized the header across home and topic pages to the same 11 core links and changed the root route label from `Dashboard` to `Inicio`. `/indice` now exposes a complete `Todas las páginas` link table with all 29 configured routes, and the complete index appears near the top on desktop and within the first mobile viewport. Updated the local QA script to allow `AGENT_LEARNING_BASE` and default to localhost. Validation passed: route/index count check `{"routes":29,"indexLinks":29,"missing":[]}`, JSON/assets checks, `npm run build`, draft pack, strict link QA with `failures: []`, public/local grep with no matches, and Playwright desktop/mobile captures for home, index, fundamentals, and evaluations with no horizontal overflow. Screenshot evidence was saved in `Output` as `agent-learning-nav-indice-final.png` and `agent-learning-nav-indice-mobile-final.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 14:03 CT agent-learning compact header pass: reduced the Agent Lab header to the brand button plus `Índice`, because the brand button already links home and `/indice` owns full-site navigation. The header was kept on one compact row in mobile with no horizontal overflow; the home sidebar and `/indice` still keep the fuller route list for local study navigation. Validation passed: `npm run build`, draft pack, strict link QA with `failures: []`, public/local grep with no matches, and Playwright mobile screenshot for `/indice`. Screenshot evidence was saved in `Output` as `agent-learning-indice-header-brand-index-mobile.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 14:16 CT agent-learning sidebar cleanup: removed the `100%` coverage card from the home sidebar while keeping sidebar navigation and the main dashboard metrics. Validation passed: JSON parse `{"json":"ok","pages":29}`, asset check `{"missing":[]}`, Playwright home screenshot with `hasProgress:false` and no horizontal overflow, `npm run build`, draft pack, strict link QA with `failures: []`, and grep over draft/pack for the removed card and public-safety patterns with no matches. Angular build emitted the existing `allowedHosts: "*"` SSRF warning; it was not changed because it is app-level configuration outside this draft pass. Screenshot evidence was saved in `Output` as `agent-learning-home-sidebar-no-progress.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 13:58 CT Eros Barajas production-domain routing: prepared and deployed `erosbarajas.com` as the public production host for the `erosbarajas.com` draft while retiring the public Zoolanding subdomain paths. Updated the draft config so production aliases and test-environment aliases are empty, keeping testing access through shared query preview on `test.zoolandingpage.com.mx`; updated the shared app `allowedHosts` and public health-check default host to the definitive domain. Route 53 apex DNS, Traefik/Dokploy production routing, and TLS were configured without recording IPs, instance IDs, tokens, or raw secret values in repo notes. The old `erosbarajas.zoolandingpage.com.mx` and `test.erosbarajas.zoolandingpage.com.mx` hosts returned HTTP 404 after the routing change. Draft promotion used PR #5 (`dev -> test`) and PR #6 (`test -> main`); both deploy workflows completed successfully. Validation passed: runtime bundle for `erosbarajas.com` returned `ok=true` with no production or test aliases, `https://erosbarajas.com/` returned HTTP 200 with `<main>` and no old-host string, invented route returned HTTP 404, shared preview URL returned HTTP 200 with `<main>`, `npm run build`, `ssr-server.spec.mjs` 9/9, alias/front-door focused tests 13/13, and Edge Playwright smoke passed production desktop/mobile, production 404, and shared preview desktop/mobile with no horizontal overflow or unexpected console/request failures.
- 2026-06-04 15:27 CT Eros Barajas production refresh: enabled the draft's internal analytics and quick-stats path for production, kept GA4/Google Ads/GTM/Search Console disabled because no real Eros-specific values were provided or verified, polished footer/CTA spacing and first-viewport media, and removed the tracked `*_just_for_example*` Instagram reference screenshot from the draft. Uploaded optimized public assets through the presigned public asset flow: `hero-portrait-lcp.jpg` for first-viewport portrait use and `eros-barajas-logo-256.png` for logo/icon/loading use; the original portrait remains the Open Graph/Twitter image. Browser QA passed on local SSR for home desktop, home mobile, 404 mobile, and section viewports with no real console errors, failed requests, broken images, example-source assets, or horizontal overflow. Final Lighthouse evidence was saved under `reports/lighthouse/2026-06-04-erosbarajas-local/`: mobile `mobile-final-clean-clsfix` scored 100/100/100/100 and desktop `desktop-final-clean-clsfix-rerun` scored 99/100/100/100. Draft package was regenerated at `logs/drafts/erosbarajas-20260604-production-refresh-package.json`. Public safety audit found no current/history secrets and no blocked paths, but still returned `ok:false` because the draft repo is dirty and the public WhatsApp phone number triggers two `phone-or-whatsapp` review findings in `default/variables.json` and `not-found/variables.json`.
- 2026-06-04 15:44 CT agent-learning annotation/lightbox pass: resolved the latest browser annotations for `agent-learning-claude-codex.zoolandingpage.com.mx`. Moved the home active lab into the right column, added draft-local image lightboxes using `modalRootIds` and `openModal`, increased spacing in the safety panel, diversified Lynx/topic visuals, removed old single-image wrapper card IDs from topic hero visuals, changed `/skills-recomendados` title to `Skills`, and added `/skills-recomendados-casos` for recommended skills with descriptions and use cases. Added four optimized Lynx WebP scenes plus two readable SVG diagrams. Validation passed: route structure audit `{"routeCount":30,"issues":[]}`, Playwright render QA across 30 routes in desktop and mobile `{"checked":60,"issues":[]}`, centered lightbox QA, `npm run build`, draft pack, strict link QA summary `{"routes":30,"targets":30,"failures":0,"overflows":0,"badLinks":0,"missingHashTargets":0}`, and grep over generated draft/pack for local/private patterns and old IDs with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 17:12 CT agent-learning home guide-start pass: resolved the home-focused browser annotations for `agent-learning-claude-codex.zoolandingpage.com.mx`. Expanded the home modules grid to 26 topic cards, added a mobile-friendly start note before the sidebar route list, changed the next challenge to start with `Fundamentos`, made the flashcard preview dynamic per visit through a draft-local `embed-frame`, restricted the home infographic section to six SVG diagrams, removed Lynx scene cards from that section, rewrote safety/resource/footer copy for students, and added the public Anthropic Academy Skilljar link. Validation passed: Playwright home QA on desktop and mobile with no console errors or horizontal overflow, dynamic flashcards produced 6 unique titles across 8 entries, `npm run build`, draft pack, strict link QA summary `{"routes":30,"targets":30,"failures":0,"badLinks":0,"routeOverflows":0,"missingHashTargets":0}`, and grep over generated home/pack for local/private/old-copy patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-home-desktop-top-final-viewport.png` and `agent-learning-home-mobile-top-final-viewport.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 18:02 CT agent-learning fundamentos expansion: resolved the `/fundamentos` browser annotations. Outcome cards now have semantic titles and fuller descriptions, practice-sequence steps have titles, a new `Contrato mínimo` deep-dive section explains task/context/limits/output/validation/escalation, examples expanded to three weak-vs-operative cases, and guided practice expanded to three exercises before the checklist. Validation passed: Playwright `/fundamentos` QA on desktop and mobile with no console errors or horizontal overflow, no number-only outcome/step titles, three examples, three practices, six deep-dive cards, `npm run build`, draft pack, strict link QA summary `{"routes":30,"targets":30,"failures":0,"badLinks":0,"routeOverflows":0,"missingHashTargets":0}`, and grep over `/fundamentos` plus pack for local/private/source-leak patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-fundamentos-*.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 18:43 CT agent-learning fundamentos practice context: expanded `/fundamentos` again so students have enough context before attempting guided practice. Added an “Antes de practicar” block with four preparation cards, a six-field quick delegation template, and clearer prerequisites/expected results in the three guided practices. Validation passed: Playwright `/fundamentos` prep QA on desktop and mobile with no console errors or horizontal overflow, `npm run build`, draft pack, strict link QA summary `{"routes":30,"targets":30,"failures":0,"badLinks":0,"routeOverflows":0,"missingHashTargets":0}`, and grep over `/fundamentos` plus pack for local/private/source-leak patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-fundamentos-desktop-prep.png` and `agent-learning-fundamentos-mobile-prep.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 19:23 CT agent-learning prompting expansion: expanded `/prompting` to match the new topic-page standard. The page now treats prompting as a contract of output, with semantic outcome cards, eight core concepts, an anatomy deep-dive, a prompt preparation block, a reusable prompt template, three weak-vs-operative examples with explanations, and three detailed guided practices. Added draft-local `prompt-contract-map.svg` so the topic has a specific visual diagram instead of reusing a tool-choice matrix or duplicating Lynx imagery. Validation passed: Playwright `/prompting` QA on desktop `1440x1100` and mobile `390x900` with no console errors, no horizontal overflow, no number-only headings, no local/private text leaks, 8 concepts, 8 anatomy cards, 3 examples, 3 practices, and 6 preparation cards; `npm run build`; draft pack; route/link QA summary `{"routes":30,"anchors":616,"routeFailures":0,"failures":[]}`; and grep over `/prompting` plus pack for local/private/source-leak patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-prompting-*.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-04 23:52 CT agent-learning evaluaciones expansion: expanded `/evaluaciones` to the current topic-page standard and resolved the latest image annotations. The page now explains evaluations as cases, expected output, rubric, grader, threshold, regression, and publication gate; outcome and sequence cards have semantic titles; and the page includes 8 concepts, an 8-card deep-dive, a prep block, a reusable evaluation template, 3 weak-vs-operative examples with explanations, and 3 guided practices. `prompt-contract-map.svg` was simplified so its bottom rule fits, and `eval-pipeline.svg` was replaced with a cleaner readable diagram. Validation passed: Playwright `/evaluaciones` and `/prompting` desktop/mobile QA with no console errors, no horizontal overflow, no number-only headings, and no local/private text leaks; `npm run build`; draft pack; final route/link QA summary `{"routes":30,"anchors":616,"failures":0,"badLinks":0,"missingHashTargets":0}` after rerunning a transient `ERR_NETWORK_CHANGED` failure caused by dev-server rebuild; and grep over `/evaluaciones`, `/prompting`, and pack for local/private/source-leak/internal-instruction patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-evaluaciones-*.png` and `agent-learning-prompting-desktop-top-v2.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 16:29 CT agent-learning tool-use expansion: expanded `/tool-use` to the current topic-page standard. The page now explains tool use as a contract for controlled action, with semantic outcome cards, titled practice sequence steps, 8 concepts, an 8-card contract deep dive, a preparation block, a reusable tool-contract template, 3 weak-vs-operative examples with explanations, and 3 guided practices. Added draft-local `tool-contract-flow.svg` as a readable desktop/mobile diagram. Validation passed: Playwright `/tool-use` desktop/mobile QA with no console errors, no horizontal overflow, no number-only headings, no local/private text leaks, and SVG loaded at natural size `1200x720`; `npm run build`; draft pack; route/link QA summary `{"routes":30,"anchors":616,"failures":0,"badLinks":0,"missingHashTargets":0}`; and grep over `/tool-use` plus pack for local/private/source-leak/internal-instruction patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-tool-use-*.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 17:13 CT agent-learning rag expansion: expanded `/rag` to the current topic-page standard. The page now explains RAG as evidence discipline: corpus, chunks, retrieval, ranking, grounding, citations, fallback, missing evidence, and evaluation. Added semantic outcome cards, titled practice sequence steps, 8 concepts, an 8-card evidence deep dive, a preparation block, a reusable RAG answer template, 3 weak-vs-operative examples with explanations, and 3 guided practices. Added draft-local `rag-evidence-pipeline.svg` and adjusted it after visual QA so text no longer clips in desktop/mobile. Validation passed: Playwright `/rag` desktop/mobile QA with no console errors, no horizontal overflow, no number-only headings, no local/private text leaks, and SVG loaded at natural size `1200x720`; `npm run build`; draft pack; final route/link QA summary `{"routes":30,"anchors":616,"failures":0,"badLinks":0,"missingHashTargets":0}` after rerunning a transient `ERR_NETWORK_CHANGED` failure caused by dev-server settling; and grep over `/rag` plus pack for local/private/source-leak/internal-instruction patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-rag-*.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 18:01 CT agent-learning five-page package expansion: expanded `/mcp`, `/workflows-agentes`, `/claude-code-codex`, `/laboratorios`, and `/flashcards` to the current topic-page standard in one package. Each route now has semantic outcome cards, titled practice sequence steps, 8 concepts, an 8-card deep dive, preparation/template content, 3 examples or drills, and 3 guided practices. Added draft-local SVGs `mcp-primitives-map.svg`, `workflow-agent-router.svg`, `repo-agent-closeout.svg`, `lab-scenario-board.svg`, and `flashcard-study-system.svg`, replacing older generic visuals for these routes. Three read-only subagents were used: two to identify content/visual risks and one to evaluate the integrated result; flagged issues in `/flashcards` public wording and possible SVG title clipping were corrected before final QA. Validation passed: Playwright desktop/mobile QA for all five routes with no console errors, no horizontal overflow, no number-only headings, assets loaded, 3 examples, 3 practices, 8 concepts and prep blocks present; `npm run build`; draft pack; final route/link QA summary `{"routes":30,"anchors":616,"failures":0,"badLinks":0,"missingHashTargets":0}` after rerunning a transient `ERR_NETWORK_CHANGED` failure; and grep over runtime route files plus pack for local/private/source-leak/internal-instruction patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-*-pkg5-final.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 19:04 CT agent-learning flashcards plus five-page package expansion: added a dedicated `Todas las flashcards` section to `/flashcards` with the full configured 91-card guide bank. Expanded `/biblioteca`, `/fluidez-4d`, `/contexto-memoria`, `/tool-schemas`, and `/rag-contextual` to the current topic-page standard: semantic outcome cards, titled practice sequence steps, 8 concepts, an 8-card deep dive, preparation/template content, 3 examples, and 3 guided practices. Added draft-local SVGs `library-source-map.svg`, `fluency-4d-matrix.svg`, `context-memory-stack.svg`, `schema-validation-contract.svg`, and `contextual-rag-loop.svg`, replacing repeated/generic visuals for the package. Three read-only subagents were used for content, visual, and integrated source QA; public-facing internal wording found by the evaluator was rewritten before final validation. Validation passed: `node --check` for both generators, Playwright desktop/mobile QA for the six affected routes with no console errors, no horizontal overflow, no number-only headings, all images loaded, lightboxes opening, 3 examples, 3 practices, 8 concepts, and prep blocks present; `/flashcards` mobile rerun passed after a transient `ERR_NETWORK_CHANGED`; `npm run build`; draft pack; strict link QA with `failures: []`; and grep over generated runtime route files plus pack for local/private/source-leak/internal-instruction patterns with no matches. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-*-pkg6-final-v3.png` and `agent-learning-flashcards-mobile-pkg6-final-v4.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 20:17 CT agent-learning 4D visual fix: corrected the `/fluidez-4d` topic SVG after browser annotation. `fluency-4d-matrix.svg` was simplified with shorter labels, wider spacing, and a two-line practical rule so text no longer clips in the full-size topic visual. Validation passed: Playwright `/fluidez-4d` desktop and mobile QA with no console errors, no horizontal overflow, image loaded, lightbox opening, and the old long clipped rule absent; `npm run build`; and draft pack. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-fluidez-4d-*-visual-fix.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 21:03 CT agent-learning MCP/subagents/skills/apps package expansion: expanded `/mcp-avanzado`, `/subagentes`, `/skills-recomendados`, `/skills-recomendados-casos`, and `/apps-complementos` to the current topic-page standard. Each route now has semantic outcome cards, titled practice sequence steps, 8+ concepts, an 8-card deep dive, preparation/template content, 3 examples, and 3 guided practices. Added draft-local SVGs `mcp-capability-gate.svg`, `subagent-orchestration-board.svg`, `skills-decision-stack.svg`, `skills-cases-router.svg`, and `apps-capability-map.svg`, replacing repeated/generic visuals. Rewrote skills/apps copy to use stable public learning categories instead of local-environment state. Three read-only subagents were used for content, visual/source QA, and integrated source evaluation; the final evaluator returned PASS with no corrections. Validation passed: `node --check` for both generators, Playwright desktop/mobile QA for the five affected routes with no console errors, no horizontal overflow, no number-only headings, images loaded, lightboxes opening, 3 examples, 3 practices, 8+ concepts, and prep blocks present; `npm run build`; draft pack; strict link QA with `failures: []`; and visible-text scan for local/private/source-leak/internal wording with no findings. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-*-pkg7-final.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 22:17 CT agent-learning final eight-page package expansion: expanded `/browser-computer-use`, `/handoffs-coordinacion`, `/qa-dogfood`, `/seguridad-publicacion`, `/playbooks`, `/evaluaciones-agenticas`, `/workflows-repo`, and `/decision-router`. The pages now use differentiated structures where appropriate: Browser QA observatory, handoff ledger, audit loop, publication safety gate, playbook library, agentic eval harness, repo workflow closeout, and decision router matrix. Each route includes semantic outcome cards, titled practice sequence steps, 8+ concepts, deep-dive cards, preparation/template content, 3 examples, and 3 guided practices. Added draft-local SVGs `browser-qa-observatory.svg`, `handoff-context-ledger.svg`, `qa-dogfood-audit-loop.svg`, `publication-safety-gate.svg`, `agent-playbook-library.svg`, `agentic-eval-harness.svg`, `repo-workflow-closeout.svg`, and `decision-router-map.svg`. Validation passed: `node --check` for both generators, static structure audit for the eight routes, Playwright desktop/mobile QA for all eight routes with no console errors, no failed visible images, no horizontal overflow and lightboxes opening, `npm run build`, draft pack, strict link QA rerun summary `{"routes":30,"targets":30,"anchors":616,"failures":0,"overflows":0,"badLinks":0,"missingHashTargets":0}`, and global visible-text scan for local/private/source-leak/internal wording with no findings. One link QA run hit transient `ERR_NETWORK_CHANGED` while the dev server settled after build; rerun passed cleanly. Screenshot evidence was saved under `C:\Users\lince\.codex\memories\Output\agent-learning-*-pkg8-final.png`. The draft remains local-only; public safety audit is still required before PR, merge, or publication.
- 2026-06-05 22:49 CT agent-learning domain rename pre-test gate: renamed the local draft from `agent-learning-claude-codex.zoolandingpage.com.mx` to `agent-guide-claude-codex.com`, updated `site-config.json`, generator domain constants, QA script domain constants, shared domain metadata, and regenerated all draft routes. Local validation passed: `node --check` for generator/QA scripts, `npm run build`, draft pack for `agent-guide-claude-codex.com`, strict local link QA after one transient `ERR_NETWORK_CHANGED` retry with 30 routes, 30 targets, no bad links, no missing hash targets, no console findings, and no mobile overflow, plus a final grep showing no old-domain matches in the draft, generators, QA script, or pack. Required public safety audit was run with `node tools\draft-public-safety-audit.mjs --repo=drafts\agent-guide-claude-codex.com --history=true` and returned `ok:false`; the current draft had no blocked paths, secret findings, or review findings, but the tool blocked on dirty hub worktree status and historical hub paths under `logs/`, `reports/`, and `findings/`. Testing publication was not attempted, so remote QA on `test.zoolandingpage.com.mx` remains pending.
- 2026-06-06 00:12 CT agent-guide public-safety blocker resolution: resolved the prior audit blocker by creating a clean isolated public snapshot repo at `C:\Users\lince\Documents\GitHub\draft-agent-guide-claude-codex-com-audit-20260606-0008`, excluding local-only coordination/notes/investigation folders, initializing Git, and committing the public draft snapshot. `node tools\draft-public-safety-audit.mjs --repo=C:\Users\lince\Documents\GitHub\draft-agent-guide-claude-codex-com-audit-20260606-0008 --history=true` returned `ok:true`, `status:"clean"`, `blockingRepoCount:0`, and no blocked paths or secret findings. Non-blocking review findings were educational safety terms such as credentials, identity, and tokens, not exposed private data. Local testing deploy was not run because AWS signing environment variables are not present in this session.
- 2026-06-06 01:20 CT agent-guide testing publication: created and configured the standalone public draft repo `LynxPardelle/draft-agent-guide-claude-codex-com` for `agent-guide-claude-codex.com`, pushed `main`, `test`, and `dev`, configured GitHub Environments and deploy variables, applied branch protection on `test` and `main`, created AWS OIDC deployment roles for test and production, and registered those roles in the authoring Lambda deploy authorization config. The real draft repo public-safety audit passed with history enabled: `node ..\..\tools\draft-public-safety-audit.mjs --repo=. --history=true` returned `ok:true` with no blocked paths, no secret findings, and only non-blocking educational safety-word review findings. PR #1 (`dev` to `test`) deployed after resolving Lambda deploy authorization; PR #2 added test aliases `test.agent-guide-claude-codex.com` and `test.agent-guide-claude-codex.zoolandingpage.com.mx`; PR #3 uploaded 62 draft assets to `https://assets.zoolandingpage.com.mx/agent-guide-claude-codex.com/shared/draft-assets/`, replaced local draft asset paths with public CDN URLs, and deployed to test successfully. Runtime-bundle checks for the test alias returned `ok:true`, `environment:"test"`, and page IDs `default` and `indice`. Remote QA passed for `https://test.zoolandingpage.com.mx/?draftDomain=test.agent-guide-claude-codex.zoolandingpage.com.mx&debugWorkspace=false` on desktop `1440x1200` and mobile `390x1200`: HTTP 200, title `Claude/Codex Agent Lab`, H1 `Aprende agentes como sistema, no como lista de prompts`, no failed requests, no console errors, and no horizontal overflow. Known preview limitation: deep shared-host URLs such as `/indice?draftDomain=test.agent-guide-claude-codex.zoolandingpage.com.mx` and `/flashcards?...` render the correct client-side content but return SSR HTTP 404 from the shared test host; use the root preview plus in-site navigation until that app-level preview behavior is corrected.
