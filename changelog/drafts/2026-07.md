# Drafts Changelog - 2026-07

Chronological draft-authoring and draft-QA history.

<!-- Keep this file chronological. Keep durable decisions in Codex.md or ai-notes, not here. -->

- 2026-07-03 11:09 CT Zoosite public blog article localization/layout repair: split the welcome and blog-builder guide articles into distinct public content, added ES/EN localized public article metadata and bodies, removed the literal `book-open` card icon label, centered the public article body/meta/comment sections, and kept article interaction/comment controls aligned with the theme.

- 2026-07-03 08:55 CT Zoosite public blog article interaction/i18n repair: centered public article interaction/comment sections, switched `Planear mi blog` to the same theme success tokens used by the header CTA, added ES/EN copy keys for article controls/statuses, formatted the published date instead of rendering raw ISO, declared `shares`, `readProgress`, and `assetDownloads` interaction policies, and removed the anonymous comment textarea/button from the public article template.

- 2026-07-03 10:01 CT Zoosite public interaction error copy: changed the public article interaction error message to render the safe localized runtime error, with a localized fallback only when the runtime has no detail. This keeps rate-limit and validation states understandable without exposing backend internals.

- 2026-07-03 07:56 CT Zoosite blog builder guide responsive cleanup: enriched the public `blog-builder-seo` article with reusable article-level combos, removed duplicated hero content from the rich HTML body, replaced the generic "Z" style visual with a content-planning image, and constrained article body/cover widths so the page remains responsive for any blog article using this route template. Browser QA passed locally on desktop and mobile with no duplicate images/text and no unintended horizontal overflow.

- 2026-07-03 02:46 CT Zoosite editor modal bootstrap repair: declared the `article-component-inspector` modal in the draft root `variables.ui.modals` config and expanded the content-hub admin contract test so the advanced editor cannot reference that modal without an accessible modal config. This targets the blank `/admin/blog/articulos/:id/editor` page in shared testing after the runtime component map fixes.

- 2026-07-02 11:55 CT Zoosite protected article detail route repair: aligned the editor, preview, SEO, and versions `page-config.route` values with the dynamic `/admin/blog/articulos/:id/...` routes already declared in `site-config.json`, and added a contract test so those page configs cannot drift back to static detail URLs. This targets the testing blank/404 page seen when opening an article detail route from the browser after login.

- 2026-07-02 05:52 CT Zoosite blog QA blocker follow-up: aligned `qa/admin-blog-qa-checklist.md` with the current content-hub product smoke by adding `taxonomyCleanup` to the expected successful checks. Verification passed through the Zoosite admin/schema Node contracts and the draft auth audit. The public safety audit still requires a clean draft repo before publication; no current secret or blocked-path findings were returned.

- 2026-07-02 00:05 CT Zoosite blog P4 controls: public article pages now consume safe runtime `commentPolicy`, `contentSafety`, and interaction policies to decide whether to show reactions, CTA tracking, forms, and comments. `/admin/blog/analiticas` now includes generic-component filters for article, category, tag, and date range, and visible helper copy avoids backend terminology.

- 2026-07-01 22:08 CT Zoosite blog P2 editor preview and lifecycle affordances: `/admin/blog/articulos/:id/editor` now uses `generic-component-preview` to render the current advanced TGeneric JSON instead of a static preview card. SEO and scheduling action buttons now disable based on protected article status from content-hub detail reads while backend authorization and status transitions remain server-side.

- 2026-07-01 21:23 CT Zoosite blog P2 editor save path: `/admin/blog/articulos/:id/editor` now collects `componentTreeJson` in the article editor scope and `site-config.json` includes it in `content_hub_update_package.inputFields`. Angular maps valid advanced JSON to content-hub `components` when `advancedMode` is active, and the editor hydrates the textarea from the selected article's saved visual structure.

- 2026-07-01 20:26 CT Zoosite combo runtime repair: renamed combo catalog form/action input `groups` to `comboGroups` in `/admin/combos` and `site-config.json`. Angular maps `comboGroups` back to backend `groups`, while the public runtime validator keeps `groups` blocked as an auth-like input. Validation passed through local site-config validation, combo/site-config contracts, content-hub admin contracts, and focused Karma.

- 2026-07-01 18:41 CT Zoosite P1 advanced-editor first cut: changed the article editor's disabled component-catalog button into a real link to `/admin/combos`, added an isolated JSON validation textarea for component-tree drafts, and kept that JSON field out of the article save payload until the TGeneric contract and visual preview are complete. Validation passed through the Zoosite content-hub/combo Node contracts and focused Angular component specs.

- 2026-07-01 17:53 CT Zoosite combo admin C8 wiring: updated `/admin/combos` from placeholder admin surface to a generic-component CRUD workspace for combo catalog reads/actions. The draft now enables `runtime.comboCatalog`, wires combo catalog list/detail/group/policy data sources, and exposes protected user-gesture actions for create, update, batch upsert, soft delete, group create/update, draft policy, and preview-oriented admin flows.

- 2026-07-01 06:10 CT Zoosite combo admin foundation: added a protected, noindexed `/admin/combos` route composed from generic draft components and wired `runtime.comboCatalog` as disabled until the generic combo catalog Lambda is deployed. Also corrected two Zoosite command fallbacks so value instructions reference valid command arguments instead of semicolon-split invalid commands. Validation passed through the Zoosite combo catalog contract test and draft command-instruction gate.

- 2026-07-01 00:10 CT Zoosite preview status guard: updated `zoositioweb.com.mx` article preview so the protected read error state no longer references `remoteStatus.contentHub.preview.requestId`, which is not exposed by that read status target. Action support IDs remain on mutation status targets that expose safe request IDs. Validation passed: `node tools/runtime-data-source-condition-guard.mjs --draft-root=. --domain=zoositioweb.com.mx` returned `ok: true`, `issueCount: 0`, and `admin-blog-articulo-preview/components.json` parsed successfully.

- 2026-07-01 00:17 CT Zoosite public article touch targets: updated `zoositioweb.com.mx` shared header nav/utility buttons and public article controls with draft-local inline sizing so category/back/CTA/reaction/share/login/comment actions render at 44px+ touch height even if Angora utility classes are stale in the deployed stylesheet. Validation passed: shared and article component JSON parsed successfully.
