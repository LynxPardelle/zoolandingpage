# Draft Changelog - 2026-08

## 2026-08-24 20:35 CT - grupoastralegal.com WeChat QR publication blocker closed technically

- Issued a one-use, 15-minute, no-overwrite direct-upload grant scoped only to the campaign JPEG. The token remained under the ignored `.zlp/` boundary, was never printed or committed, and was removed after its single use.
- Uploaded the approved source QR unchanged to `https://assets.zoolandingpage.com.mx/grupoastralegal.com/soft-landing-china/images/wechat-qr.jpeg`. A fresh public read returned HTTP 200, `image/jpeg`, 84,896 bytes, 820 x 1214 pixels, and the exact source SHA-256.
- Updated the campaign asset URL and status only after a failing contract captured the blocked state. The final draft contract passed 27/27; test and production validate-only checks each accepted 147 files; the runtime data-source guard reported zero issues; and feature readiness reported zero blockers or warnings.
- Focused EN/ZH desktop/mobile browser QA passed 4/4 with successful image decoding, localized labels and actions, clean navigation, no unexpected network or console errors, no overlap or overflow, and no accessibility, contrast, or layout findings. The focused QR audit scored 20/20 with zero findings at every severity.
- At 21:02 CT, Alec confirmed by scanning the public asset in WeChat that it resolves to the intended Luis Ortega contact. No draft deployment, publication, branch promotion, or configuration rollout was performed; the authorized asset upload was the only durable cloud write.

## 2026-08-24 17:16 CT - grupoastralegal.com QA-018 through QA-021

- Rebuilt the real-estate tax recovery calculator as a three-question, submit-driven interaction with localized validation, accessible radio groups, deterministic whole-peso estimates, the approved investment tiers, focus management, reset behavior, and the existing approved contact destinations. The calculation remains uncapped and page-specific structured data remains omitted because the current shared runtime cannot localize that payload safely.
- Added isolated English and Chinese campaign routes at `/soft-landing-china/eng` and `/soft-landing-china/zh`. Both routes use one native draft page package with route-bound languages, direct sibling switches, localized SEO, scoped components and styles, and no links from existing site surfaces.
- Adapted the supplied reference HTML into native draft JSON. The downloaded HTML references and original WeChat QR evidence remain ignored local artifacts and were not added to either repository.
- Preserved the pre-existing `/soft-landing-empresas-chinas` payload bytes and checked other site routes for unexpected rendered changes during the browser matrices.
- The original QR was recovered and reviewed locally, but the public asset URL remains intentionally empty until an authorized upload grant is available. The QR card and dependent action stay hidden, and this remains a publication blocker.
- Replaced the campaign FAQ's unavailable icon-font ligature with the supported `expand_more` GenericIcon SVG path. The focused browser retest passed all four EN/ZH desktop/mobile cases and all 32 closed/open toggle states with no visible implementation words, overflow, contrast issue, console error, or failed request.
- Contract, schema, readiness, public-safety, desktop/mobile, localization, interaction, contrast, and regression checks were run locally across the implementation and audit passes. The final draft contract passed 27/27, the automated feature matrix passed 128/128, and the feature verdict is GO; publication remains NO-GO only for the blocked QR upload. No deployment, publication, branch promotion, or cloud write was performed.
