# Draft Changelog - 2026-08

## 2026-08-27 CT - The Hair Narrative approved Booksaw draft prepared for testing

- Prepared the approved Booksaw composition across all eleven routes using generic Zoolandingpage components and draft-owned `thnBooksaw*` Angora combos. English and Spanish share the page trees, with Bridal body copy remaining English. Home and navigation retain Narrative, Journal, Letter, Practice and Bridal Narrative in the approved order; Letter remains a Home section for email interest, not an automated subscription form or an additional route.
- Kept the existing `Toydrum/draft-thehairnarrative-com` ownership and test-only deployment controls unchanged. Comparing the isolated release draft with the approved local source found 27 image/font URL substitutions and 30 explicit link-color utilities across 72 payload JSON files; routes, component order, translations and copy were preserved. Runtime references now use the versioned same-origin asset paths prepared by the [shared-runtime work](../app/2026-08.md).
- All 72 payloads passed runtime guards, with eleven routes and four font descriptors; feature readiness reported zero blockers and zero warnings. Public-safety review found no secrets or blocked paths. Review-only contact matches were the approved public site contacts, while detector definitions and a font Unicode-range string accounted for false positives; no private source document was added.
- Interactive review exposed dark-on-dark CTA hover text caused by GenericLink's inherited-color fallback overriding a combo-only color. Explicit matching color utilities on 25 CTAs and five desktop navigation links corrected it without changing shared Angular styles or the approved composition. The draft regression failed before the fix and passed three times afterward; source-preservation checks admit only these utilities and the public URL substitutions.
- After that correction, three complete 64-case English/Spanish responsive matrices passed: 192 completed route/language/viewport checks with no overflow, undersized touch targets, broken images, font issues, missing Angora combos, exact duplicate rules, unresolved text or browser-log errors. All 504 hover-state checks passed, as did the sampled visible-text contrast checks. H1 counts and heading-level progression passed. These are bounded snapshots, not continuous-session stability, CLS measurement, accessibility certification or live testing-host acceptance. Menu navigation keys, language selection, and skip-link focus were exercised; the automation did not establish the browser-native Tab/Shift+Tab default and a physical-keyboard check remains pending.
- The approved image crops retain their native-resolution limits. Font licenses accompany the source and public subsets; the client's reported commercial template license does not independently establish every photo right. Preliminary legal copy and applicable source rights remain gates for any separately authorized production publication.
- This preparation did not deploy or publish the revision. The [test-only lifecycle](../../docs/11-draft-lifecycle.md) still requires compatible runtime activation and exact public asset verification before payload promotion, followed by a live response retaining all four font descriptors and final test-host acceptance. Backend API changes, DNS, production, real article publication, newsletter automation, reservations and payments remain outside this release.

## 2026-08-27 12:05 CT - Greenstock published to testing with recorded limitations

- The earlier private-repository hold is superseded: the draft was already public when this release pass began, and its existing test branch, Environment and OIDC controls were verified and reused without weakening protections. Future production declarations remain unchanged and do not authorize production setup or publication.
- The isolated authoring operator reached `dev` through normal [PR39](https://github.com/LynxPardelle/zoolanding-config-authoring/pull/39), merge `1575543e10f2af2acc3995bc81c9b5327ad90f62`, with reviewed changes and successful unit/SAM CI, without an authoring-service deployment. Exactly one Greenstock/test grant addition produced 13 scopes/rules while preserving all 12 previous entries' canonical bytes and order; exact readbacks, a zero-change plan and three read-only audits passed by 11:50:41 CT. No retry, repair, IAM change or other draft grant modification occurred.
- Promoted approved draft `8a64a1d2a1e295581ae31bf0ddb3f9982b0e2aa0` through normal [PR9](https://github.com/LynxPardelle/draft-somosgreenstock-mx/pull/9) at 11:52:04 CT to test merge `ac436dd9aafa38478a75065d5056f901e1e51977`. The [test workflow](https://github.com/LynxPardelle/draft-somosgreenstock-mx/actions/runs/33100598973), attempt 1, succeeded at 11:52:30 CT and published version `test-ac436dd9aafa38478a75065d5056f901e1e51977-33100598973-1`.
- At 12:05:14 CT, the published pointer and draft version agreed at revision 2; all 35 JSON payloads matched their manifest's raw SHA-256 values and the approved source's content, with a closed, stable 36-object version set. The manifest SHA-256 is `b08aaae55fc64d63a2003aa773de65a5cfc65ecbc6006d2f137f5f6b1320d0af`.
- Live browser checks completed at 12:05:16 CT: all six routes plus unknown-route recovery at 1280px and 375px widths, 14 route/viewport checks, with no console errors, broken images or horizontal overflow. Navigation and Home recovery retained preview parameters; seven FAQs passed keyboard interaction at both widths. Three HTTP audits repeated route/metadata checks; 32 asset HEAD requests returned 200 and both available MP4 range requests returned 206. The [shared testing preview](https://test.zoolandingpage.com.mx/?draftDomain=somosgreenstock.mx&debugWorkspace=false) is available for client review.
- Recorded limitations remain: inherited P3 SSR canonicals omit `draftDomain` while client canonicals retain it, with `noindex` present in both; follow up before production. Native video playback remains unconfirmed because of native-control automation limits, not a demonstrated video error; two larger videos still use static fallbacks. The prior P3 focus-contrast finding remains. This is bounded testing QA, not an all-green audit or accessibility certification; no domain registration or production publication occurred.

## 2026-08-26 21:38 CT - Greenstock local review accepted with limitations

- Final manual QA accepted all six routes at 375x812, 768x1024, 1280x800 and 1920x1080, plus one unknown URL at each width: 28 views. The Home impact title recheck used rendered draft `70d2d73c78269ecb376088c6988b572163142a58`; no within-word fracture, clipping or overflow remained.
- The companion shared-runtime repair returned HTTP 404 with one visible main/H1, readable label/CTA contrast of 6.9179:1 and 9.1205:1, and a working Home recovery preserving validated preview context. Source review has zero open findings. The separately measured P3 focus-outline contrast of 2.23:1 remains; local acceptance is not WCAG AA certification.
- Documentation-only [PR8](https://github.com/LynxPardelle/draft-somosgreenstock-mx/pull/8) merged normally as `8a64a1d2a1e295581ae31bf0ddb3f9982b0e2aa0`. Canonical and isolated draft checkouts are clean on `dev`; all 35 payload JSON byte hashes are unchanged from the rendered checkpoint. Managed README/AGENTS routing now passes the scoped read-only fleet check with zero issues.
- Current draft checks remain 29/29 contracts, 35 validated payloads, readiness 0 blockers/0 warnings and 12/12 official smoke views at exactly 1440x900 and 390x844. The latest post-label hub build and focused SSR tests pass (30/30); the full SSR run is 54/58 with the same four baseline failures and zero new failures. Earlier Angular 846/846 and draft-context 11/11 results are separate checkpoints.
- Three documentation audits checked links, workflow boundaries and public safety. Local pack contains 35 payloads; public-safety history has no secret or blocked-path findings. The two remaining large videos retain static fallbacks.
- Testing remains **not deployed**. The private-repository protection/Environment prerequisites and explicit owner choice remain unresolved; no visibility, live scope, testing promotion, domain registration or production change occurred. Hub integration and actual testing-host QA remain separate release gates.

## 2026-08-26 21:20 CT - Greenstock measured local corrections

- Integrated bounded media/navigation, shared title scale, pending-media copy and Home impact-title corrections through normal draft feature PRs [#4](https://github.com/LynxPardelle/draft-somosgreenstock-mx/pull/4), [#5](https://github.com/LynxPardelle/draft-somosgreenstock-mx/pull/5), [#6](https://github.com/LynxPardelle/draft-somosgreenstock-mx/pull/6) and [#7](https://github.com/LynxPardelle/draft-somosgreenstock-mx/pull/7). The rendered draft checkpoint is `70d2d73c78269ecb376088c6988b572163142a58`; canonical and isolated runtime copies are clean on `dev`.
- Home and wholesale media figures now give their generic-media hosts a full-width track; primary navigation preserves readable labels, minimum 44px targets and wrapping. Pending FAQ/impact descriptions use source-backed preparation/process copy rather than claiming unseen footage was inspected. Asset status, URLs, static fallbacks and commercial facts were preserved.
- Final tablet QA found that Home's inline inverse title had not inherited the shared title correction. A second RED/GREEN regression required an explicit medium-breakpoint override, not merely removal of the oversized token. Independent pre-merge browser measurement at 768px confirmed the affected Home title at 32px in a 336px column, with no within-word fracture, clipping or overflow. Other headings are not claimed to render at that size.
- The latest draft contract passes 29/29, validate-only accepts 35 payload JSON files, and readiness reports zero blockers/warnings. Current/history public-safety checks report no secrets or blocked paths; two review-only matches belong to scanner definitions. All seven feature PRs used the normal check/merge path without a release-control bypass.
- The latest official local smoke passes 12/12 views at exactly 1440x900 and 390x844, with zero local failures in 29.12 seconds. Six live comparisons were skipped. Local packing also produced exactly 35 payloads with no repository-control or private-note paths; no authoring request was made.
- Final manual browser sign-off remains pending the companion shared-runtime 404 label-contrast correction and rechecks. Earlier 404 recovery/CTA repairs have local regression coverage, but no hub integration or testing promotion is implied. A separately recorded P3 visible-focus contrast measurement of 2.23:1 remains a limitation; this checkpoint is not WCAG AA certification.
- The read-only fleet-knowledge check found only missing managed routing markers in the draft README and AGENTS; their documentation-only correction is pending. It did not report missing branches, workflows, origin or immutable C1 routing.
- Testing remains **not deployed**. A fresh read-only GitHub check still finds the draft private, rulesets/classic test-branch protection unavailable with HTTP 403, no Environments and no test Environment. The owner has not authorized public visibility or selected a private setup that demonstrably supports every required control. Domain registration/ownership remain unconfirmed, two larger source videos remain pending, and no live authoring-scope change, `dev -> test` promotion or production publication occurred.

## 2026-08-26 CT - Greenstock draft and local QA checkpoint

- Prepared the Spanish Greenstock consumer catalogue and B2B draft under the approved proposed identity `somosgreenstock.mx`, with `/`, `/menu`, `/impacto`, `/mayoreo`, `/preguntas` and `/404`. Registration and ownership have not been confirmed; this workflow has not registered the domain or published production. Domain purchase and production publication remain outside scope.
- Integrated the initial draft, testing-safe canonicals and visual-polish changes through normal feature PRs [#1](https://github.com/LynxPardelle/draft-somosgreenstock-mx/pull/1), [#2](https://github.com/LynxPardelle/draft-somosgreenstock-mx/pull/2) and [#3](https://github.com/LynxPardelle/draft-somosgreenstock-mx/pull/3) into `dev`. The visual-polish checkpoint is merge `fc2b5491fd2c729e131df763fd672949a8e164f8`; no `dev -> test` promotion occurred.
- Kept Instagram as the only customer contact/order channel, with no invented phone number, form, checkout or payment integration. Client-supplied content and requested media usage are not represented as independently verified licences, health claims or certification.
- Published 28 source-image derivatives, two video posters and two available MP4 videos to the public CDN. Two larger client videos remain pending retrieval, with static fallback content and no playable URLs. Asset publication does not mean the website was deployed.
- At the visual-polish checkpoint, contract tests passed 23/23, validate-only accepted 35 payload JSON files and readiness reported zero blockers/warnings. The public-safety history audit had no blocked paths or secret findings; review-only matches were the bundled scanner's detection definitions.
- The official local smoke passed all six routes at desktop 1440×900 and mobile 390×844: 12/12 views, zero local failures, in 22.72 seconds. Live comparisons were skipped because no managed alias was available; there is no testing-site validation result.
- Independent manual rechecks at 375×812, 768×1024, 1280×800 and 1920×1080 remain separate from that smoke coverage. They have no final sign-off yet and reported open P2 layout findings: both home/wholesale players were only 96px wide (home measured 96×480px, with incomplete visible controls), and mobile navigation clipped the Preguntas label by 15.7px. Corrective changes and browser retests are still required.
- Testing remains **not deployed**: the current private GitHub Free setup cannot satisfy the required protected-release contract. The draft remains non-indexable with shared-test canonicals and no branded aliases. No visibility change, live authoring-scope change or control bypass was authorized by this documentation checkpoint; use the [canonical lifecycle](../../docs/11-draft-lifecycle.md) once the prerequisite is resolved.

## 2026-08-26 18:22 CT - Fleet browser-icon audit and Music favicon

- Audited all 12 canonical registered drafts. `grupoastralegal.com`, `erosbarajas.com`, and `zoositioweb.com.mx` already declare usable draft-specific browser icons; their configuration was retained. `music.lynxpardelle.com` now declares its published Lynx Pardelle mark as `site.icons.favicon` and has a draft-local CI contract for that URL.
- Confirmed that `agent-guide-claude-codex.com`, `alecfest-voliii.com`, `pamelabetancourt.com`, `pokeapi-demo.zoolandingpage.com.mx`, `robertorodriguezrodriguez.com.mx`, `sulandingpage.com.mx`, `thehairnarrative.com`, and `zoolandingpage.com.mx` do not currently contain a favicon-ready configured asset, so they intentionally use the shared Zoolandingpage Z fallback. The unregistered legacy `despacholegalastralex.com` checkout was not changed.
- Desktop/mobile browser inspection confirmed exactly one expected favicon for every registered draft. Full local fleet smoke then passed all 152 declared routes in both viewports (304 renderings) with zero failures. The Music contract passed from both its repository root and the hub root, and its 24 public files remained valid for test and production packaging. No draft was deployed, published, promoted, pushed, or otherwise changed remotely.

## 2026-08-25 22:50 CT - Agent Guide semantic media and release QA

- Removed the Agent Guide draft's default-page not-found override so unknown paths use the canonical 404 contract rather than rendering the home payload.
- Preserved its authored image captions as native `figcaption` elements. The final browser matrix passed 30 routes in desktop and mobile, 120 lightbox openings, 120 non-empty centered captions at the authored 0.85rem size, all images, and overflow and console/page-error checks.
- Restored the two orphaned `/indice` image triggers by mounting the mascot trigger in the hero grid and the visual trigger in the hero wrapper, matching the verified structure used by the other topic routes.
- Regression QA for the other in-scope drafts remained isolated from The Hair Narrative, whose production scope and repository state were not changed. Staged deployment and production publication are recorded separately when complete.

## 2026-08-25 17:37 CT - The Hair Narrative classified as test-only

- Added The Hair Narrative's explicit `['test']` deployment scope to the canonical registry while retaining it in fleet knowledge and public-safety audits.
- New production GitHub Environment setup, IAM role planning, bootstrap variables, active workflow requirements, and managed-alias plans are excluded for this draft by fail-closed tooling. Existing repository and cloud state is not presumed decommissioned.
- GitHub setup now reports stale production config/workflow bytes in the checkout or canonical `test`/`main`, incomplete GitHub Environment access, and a present or unverifiable production IAM role instead of returning a misleading success. A separate realpath-bounded local-only reconciliation can prepare the config and reviewed workflow diff without commit, push, `main` writes, or remote deletion; remote branch promotion plus any Environment, variables, or role remains decommission-pending until reviewed under separate authority and read-only absence checks pass.
- No GitHub setting, AWS resource, DNS record, branch, draft deployment, or production publication was changed.

## 2026-08-24 20:35 CT - grupoastralegal.com WeChat QR publication blocker closed technically

- Issued a one-use, 15-minute, no-overwrite direct-upload grant scoped only to the campaign JPEG. The token remained under the ignored `.zlp/` boundary, was never printed or committed, and was removed after its single use.
- Uploaded the approved source QR unchanged to `https://assets.zoolandingpage.com.mx/grupoastralegal.com/soft-landing-china/images/wechat-qr.jpeg`. A fresh public read returned HTTP 200, `image/jpeg`, 84,896 bytes, 820 x 1214 pixels, and the exact source SHA-256.
- Updated the campaign asset URL and status only after a failing contract captured the blocked state. The final draft contract passed 27/27; test and production validate-only checks each accepted 147 files; the runtime data-source guard reported zero issues; and feature readiness reported zero blockers or warnings.
- Focused EN/ZH desktop/mobile browser QA passed 4/4 with successful image decoding, localized labels and actions, clean navigation, no unexpected network or console errors, no overlap or overflow, and no accessibility, contrast, or layout findings. The focused QR audit scored 20/20 with zero findings at every severity.
- At 21:02 CT, Alec confirmed by scanning the public asset in WeChat that it resolves to the intended Luis Ortega contact. No draft deployment, publication, branch promotion, or configuration rollout was performed; the authorized asset upload was the only durable cloud write.

## 2026-08-24 17:16 CT - grupoastralegal.com QA-018 through QA-021

- Rebuilt the real-estate tax recovery calculator as a three-question, submit-driven interaction with localized validation, accessible radio groups, deterministic whole-peso estimates, the approved investment tiers, focus management, reset behavior, and the existing approved contact destinations. The calculation remains uncapped and page-specific structured data remains omitted because the current shared runtime cannot localize that payload safely.
- Added isolated English and Chinese campaign routes at `/soft-landing-china/eng` and `/soft-landing-china/zh`. Both routes use one native draft page package with route-bound languages, direct sibling switches, localized SEO, scoped components and styles, and no links from existing site surfaces.
- Adapted the supplied reference HTML into native draft JSON. The downloaded HTML references and original WeChat QR evidence remain ignored local artifacts and were not added to either repository.
- Preserved the pre-existing `/soft-landing-empresas-chinas` payload bytes and checked other site routes for unexpected rendered changes during the browser matrices.
- The original QR was recovered and reviewed locally, but the public asset URL remains intentionally empty until an authorized upload grant is available. The QR card and dependent action stay hidden, and this remains a publication blocker.
- Replaced the campaign FAQ's unavailable icon-font ligature with the supported `expand_more` GenericIcon SVG path. The focused browser retest passed all four EN/ZH desktop/mobile cases and all 32 closed/open toggle states with no visible implementation words, overflow, contrast issue, console error, or failed request.
- Contract, schema, readiness, public-safety, desktop/mobile, localization, interaction, contrast, and regression checks were run locally across the implementation and audit passes. The final draft contract passed 27/27, the automated feature matrix passed 128/128, and the feature verdict is GO; publication remains NO-GO only for the blocked QR upload. No deployment, publication, branch promotion, or cloud write was performed.

## 2026-08-05 13:59 CT - The Hair Narrative canonical remote

- Registered `thehairnarrative.com` as `Toydrum/draft-thehairnarrative-com` while retaining the existing fleet owner as the default for other drafts.
- Created the canonical remote as a private repository under `Toydrum` and pushed the clean initial draft commit `4c2452b` to `main`.
- Re-ran the current and historical public-safety audit: no blocking paths, secret findings, or history findings were found. Review-only email findings are the intentional public contact address.
- Branch protection, GitHub Environments, AWS OIDC, asset upload, and the shared test deployment remained pending at this point in the chronology. Production publication and DNS were not changed.
