# App Changelog - 2026-06

## 2026-06-16 16:32 CT - Custom auth logout routing

- Added `authFormAction:logout` support so custom draft-auth forms clear local public session metadata and navigate same-origin without calling Cognito Hosted UI.
- Kept Cognito Managed Login logout safer for shared testing by omitting the active draft `lang` query parameter from `logout_uri`; Cognito app clients require exact allowlisted logout URLs.
- Updated Zoosite's account-page logout button to use the custom form logout path, preventing the user-visible Cognito `/error` page after signing out from `/mi-cuenta` in the Spanish test preview.

## 2026-06-16 11:49 CT - Optional custom auth forms foundation

- Added `AuthCustomFormService` and the generic `authFormAction` handler for draft-auth forms assembled from generic components instead of draft-specific Angular code.
- Supported custom auth actions: `signin`, `signup`, `confirmSignup`, `resendConfirmation`, `forgotPassword`, and `confirmForgotPassword`, mapped to serverless `/auth/*` endpoints and carrying only public fields plus the current language.
- Added a generic password visibility toggle to `generic-input` plus schema support so draft auth forms can offer show/hide password without draft-specific Angular code.
- Kept custom signup/reset UX local and safe: `confirmPassword` is validated in the browser but never sent, and browser-supplied tenant/group/policy-looking fields are dropped before the request.
- Custom signin installs only sanitized public session metadata returned by the backend, then redirects to `postLoginPath` or the account route while preserving safe testing preview params. It does not store tokens or replace backend JWT authorization for protected APIs.
- Extended the server-only auth registry schema and validator with optional `customAuth.signin`, `customAuth.signup`, and `customAuth.passwordRecovery` policy, including server-side default group validation against `allowedGroups`.
- Validation passed: focused custom auth Karma specs (`TOTAL: 9 SUCCESS`), `node --test tools/tests/auth-profile-registry.spec.mjs`, and `node --test tools/tests/server-config-schema.spec.mjs`.

## 2026-06-16 11:07 CT - Cognito Managed Login language propagation

- Added the active draft language to Cognito Managed Login, sign-up, and password-recovery URL generation through the safe `lang` query parameter so Spanish-first drafts can request Cognito's Spanish-hosted UI where supported.
- Kept the change optional and browser-safe: invalid locale values are omitted, drafts that do not use auth are unaffected, and token/session handling remains unchanged.
- Documented the updated auth architecture direction: separate Cognito user pools per paying client or strong draft boundary by default, `custom:tenant_id` as a secondary backend authorization/dashboards scope, and per-profile choice between Cognito Managed Login and future custom generic auth forms.
- Validation passed: focused auth Karma specs (`TOTAL: 19 SUCCESS`), `git diff --check` with only LF/CRLF warnings, and `npm run build`.

## 2026-06-15 19:17 CT - Generic Cognito auth actions and Zoosite login surface

- Added reusable browser auth flow support for Cognito Managed Login: generic `authAction` event handling, PKCE login/signup/forgot-password URL generation, Cognito logout URL generation, authorization-code exchange, and `/auth/callback` processing into minimal public session metadata.
- Kept token material out of Angular auth state and browser session persistence. Callback handling validates public ID-token issuer/audience/expiry claims for client-side UX only; backend-protected APIs must still verify JWT signatures and tenant/group policy server-side.
- Updated Zoosite draft auth pages with generic components: `/acceso` now exposes login, create-account, and password-recovery actions; `/auth/callback` provides a retry action; `/mi-cuenta` contains a minimal protected account shell and logout action.
- Aligned the local Zoosite server-only auth registry with the real active Cognito profile public metadata while keeping it secret-free. Google/Facebook remain optional IdPs and were not required for this Cognito-only test path.
- Updated Angular packages from `22.0.0` to `22.0.1` after `npm audit --omit=dev` reported Angular 22.0.0 advisories; the production audit now reports zero vulnerabilities.
- Fixed local SSR protected-route redirects so `127.0.0.1:{port}` keeps the port when redirecting unauthenticated preview requests to the login path.
- Preserved shared-test preview context through Cognito by storing the exact same-origin `redirect_uri` in the PKCE transaction, reusing it in the token exchange, and carrying safe preview params into callback/logout URLs when auth starts from `test.zoolandingpage.com.mx`; logout URL language handling was later narrowed because Cognito logout allowlists are exact-match.
- Promoted the Zoosite Cognito auth surface to testing: the Zoosite draft `test` deployment completed successfully, the shared app `test` service was rebuilt from `a8db235`, and `https://test.zoolandingpage.com.mx/acceso?draftDomain=zoositioweb.com.mx&debugWorkspace=false` served `main-OUYUADGP.js` with the generic auth actions visible.
- Added the shared-test Cognito callback/logout URLs to the Zoosite public app client and verified the client still has Hosted UI OAuth enabled, code flow, `openid email profile` scopes, `COGNITO` as the only IdP, and no client secret. The first update omitted unchanged OAuth fields and temporarily reset Hosted UI settings; the client was immediately restored and rechecked.
- Recovered a front-door availability issue before testing: the Dokploy/Traefik EC2 instance had failed instance reachability and SSM was disconnected, then returned healthy after reboot. While deploying, tokenized Git remotes in Dokploy checkouts were replaced with non-secret GitHub URLs; any old token previously embedded there should be rotated or revoked outside the repo.
- Hardened Cognito callback handling after the integrated-browser test stayed on `Validando acceso`: PKCE transactions now verify persistence before leaving the app, fall back to a short-lived same-site cookie when `sessionStorage` is blocked, catch token-exchange exceptions, and redirect failed callbacks back to the login path so authorization codes are removed from the URL.

## 2026-06-09 04:06 CT - SSR protected auth redirect gate

- Added an Express SSR front-door auth redirect gate so `GET`/`HEAD` requests for published routes with `routes[].auth.required = true` return `302` to `routes[].auth.redirectTo` or `runtime.auth.loginPath` before Angular SSR renders a blank protected shell.
- The redirect target must remain a safe same-origin path and preserves only preview-critical query params (`draftDomain`, `debugWorkspace`, `lang`) so shared test previews such as Zoosite keep their draft context without carrying ad or sensitive query params into login routes.
- Added SSR smoke coverage for `test.zoolandingpage.com.mx/mi-cuenta?draftDomain=...` proving the server redirects to same-origin `/acceso` with `Cache-Control: no-store`.

## 2026-06-09 03:39 CT - Draft deploy server-only auth packaging fix

- Fixed the hub pack/status tooling and reusable draft repo deploy template so `server/auth-profile-registry.json` deploys as `server-auth-profile-registry` and `server/integrations.json` deploys as `server-integrations` without a `pageId`.
- Added regression coverage in `tools/tests/config-draft-sync.spec.mjs`, `tools/tests/draft-upload-status.spec.mjs`, and `tools/tests/draft-repo-preflight.spec.mjs` so manual packaging, upload status checks, and future generated draft repos do not classify server-only auth/integration policy as page payloads.
- Applied the same deploy-script fix to the local Zoosite nested draft repo before promoting its plan-only auth pilot, because its workflow uses the repo-local `tools/deploy-draft.mjs`.

## 2026-06-09 01:36 CT - Zoosite nested draft source separation

- Resolved the dirty nested `drafts/zoositioweb.com.mx` repo boundary by removing the sanitized Zoosite pilot files from the app repo index instead of vendoring the real draft inside the hub.
- Moved app-level Zoosite auth pilot assertions, including the plan-only server registry fixture, to `tools/tests/fixtures/zoosite-auth-pilot/` so schema/contract tests no longer depend on a locally cloned draft repo.
- Restored the app repo draft ignore rules so registered domain drafts remain local nested repos under `drafts/` rather than hub-tracked source.
- The actual Zoosite auth pilot content belongs in the `draft-zoositioweb-com-mx` repo, preserving the full commercial draft content instead of committing the temporary minimal overlay.

## 2026-06-09 00:43 CT - Local remote auth QA harness

- Added an Angular local-auth dev script and proxy config so `runtime.authRemote.endpoint = "/auth/runtime-config"` can be exercised against a real local `zoolanding-api-proxy` HTTP harness instead of falling through to Angular dev-server 404s.
- Documented the local browser QA flow: Angular proxies `/auth/runtime-config` to the API proxy, and the API proxy reads a server-only local auth registry from explicit dry-run env configuration.
- Added focused Angular coverage proving valid disabled public auth metadata from a non-active remote profile is installed in memory, leaves auth disabled, and removes `authRemote` after hydration.

## 2026-06-09 00:26 CT - Zoosite remote auth pilot

- Added a sanitized Zoosite pilot draft under `drafts/zoositioweb.com.mx` in the optional auth worktree without copying the source draft repo, `.git`, local agent state, CV/photos, findings, logs, reports, PDFs, or private docs.
- Configured public `site-config.json.runtime.authRemote` with `enabled: true`, `authProfileId: "staff"`, and `endpoint: "/auth/runtime-config"`; no static `runtime.auth`, `credentialRef`, `access`, `auth`, client secrets, tokens, or secret-store URI prefixes were added to browser config.
- Added placeholder-only auth routes `/acceso`, `/auth/callback`, and protected `/mi-cuenta` with `routes[].auth.required = true`, `redirectTo = "/acceso"`, and groups `zoosite-client` / `zoosite-admin`; pilot pages use `noindex,nofollow` and contain no private data or protected actions.
- Added Zoosite `server/auth-profile-registry.json` as a server-only plan-only contract with tenant `zoosite`, `status: "planned"`, callback/logout allowlists for `zoositioweb.com.mx` and `zoositioweb.com`, and Google/Facebook secret references under `/zoolanding/auth/zoosite/staff/*`.
- Narrowed the draft ignore allowlist so only the reviewed sanitized Zoosite pilot files are trackable; future local-only folders, `.env` files, and extra `server/*` files remain ignored unless explicitly reviewed.
- Added Node regression coverage for the Zoosite public authRemote payload, protected route contract, registry validation, and plan-only provisioning output. No deploy, AWS call, Cognito creation, secret generation, or commit was performed from this worktree.

## 2026-06-08 22:57 CT - Remote auth runtime resolution

- Added optional remote auth runtime resolution for draft configs: public `runtime.authRemote` references can resolve safe `runtime.auth` from `/auth/runtime-config` by sending only `{ domain, authProfileId }`.
- The Angular runtime validates the backend `auth` response with the same public auth validator before installing it into in-memory site runtime config, removes `authRemote` after hydration to avoid ambiguity, and keeps auth disabled on invalid responses or request failures.
- Audited and hardened remote auth hydration so the request cache de-duplicates only in-flight requests. Freshly reloaded `siteConfig` instances are rehydrated again, profile mismatches are rejected, and delayed responses cannot install auth into a different domain/profile/endpoint context.
- Validation now rejects public configs that include both static `runtime.auth` and `runtime.authRemote`, and rejects `access`, upstream `auth`, `credentialRef`, tokens, and client secrets in public auth config surfaces.

## 2026-06-08 22:30 CT - Server-only auth registry and integration access packaging

- Aligned the optional auth contract so drafts can package `server/auth-profile-registry.json` and `server/integrations.json` as official authoring file kinds without exposing those files through local static `/drafts` serving or browser runtime payloads.
- Updated the server-only Auth Profile Registry shape around `authProfileId`, `tenantId`, `status`, `issuer`, `hostedUiDomain`, `clientId`, `audiences`, callback/logout URL allowlists, same-origin login/logout paths, tenant/group claims, allowed groups, and `socialIdpSecretRefs` secret-store references.
- Added `server/integrations.json.access` validation for protected sources/actions and kept it separate from upstream credential `auth`, which remains reserved for `bearer`, `api-key-header`, and `oauth2-client-credentials` policies with secret-store `credentialRef`.
- Hardened public runtime validators so `site-config.json.runtime.dataSources` and `runtime.apiActions` reject server-only `access` and credential `auth` blocks.
- Added schemas and focused tests for server-only auth registry, server integrations, authoring package kinds, package/status tooling, and auth handler compatibility.
- No deploy, AWS call, Cognito creation, or secret generation was performed from this worktree.

## 2026-06-08 21:05 CT - Generic auth service handler scaffold

- Added `tools/auth-service-handlers.mjs`, an offline Lambda-style scaffold for a future generic Auth service that resolves safe public `runtime.auth`, gates provisioning plans behind a trusted server-side caller check, and evaluates API Gateway authorizer decisions through an injected JWT verifier.
- Added `tools/tests/auth-service-handlers.spec.mjs` covering public runtime metadata shaping, rejection of browser-supplied secret/policy fields, default-denied provisioning plans, trusted plan-only provisioning output, and JWT authorizer allow/deny behavior without echoing bearer tokens.
- Kept the implementation dependency-injected and side-effect free: no AWS calls, no Cognito creation, no CDK deploy, no secrets, and no Zoosite-specific config.

## 2026-06-08 20:21 CT - Server-only Auth Profile Registry foundation

- Added an offline, server-only Auth Profile Registry contract in `tools/auth-profile-registry.mjs` for resolving `domain + authProfileId` to tenant-scoped Cognito operational policy without calling AWS or creating Cognito resources.
- Added plan-only Cognito provisioning output for user pool, user pool client, Hosted UI domain, and social provider setup using secret references instead of raw credentials.
- Added backend authorization preparation helpers for JWT issuer/audience/group enforcement. The policy evaluator expects already-verified claims and does not replace cryptographic JWT verification.
- Documented the boundary in `docs/api-driven-config/17-auth-profile-registry.md`: public draft payloads may carry only safe auth metadata, while tenant ownership, provisioning state, callback allowlists, group policy, social IdP references, and backend authorization policy stay server-only.
- Added focused Node tests in `tools/tests/auth-profile-registry.spec.mjs` covering tenant isolation, public payload shaping, active-only runtime enablement, unsafe registry rejection, provisioning plan output, Cognito JWKS URI derivation, and claim policy decisions.
- No deploy, AWS call, Cognito creation, or Zoosite-specific draft config was performed from this worktree.

## 2026-06-08 18:59 CT - Optional draft auth runtime recovery

- Ported the valid optional draft auth foundation from snapshot `C:\Users\lince\.codex\worktrees\368b\zoolandingpage` into a real Angular 22 workspace without copying `node_modules`, `dist`, logs, reports, package-lock, or build artifacts.
- Added config-driven `runtime.auth` and per-route `routes[].auth` validation, Angular signal auth runtime evaluation, Cognito Hosted UI URL construction, and browser session metadata storage that strips profile display/email and token/secret-like fields.
- Kept auth disabled by omission for drafts with no `runtime.auth`; protected route redirects now prefer `routes[].auth.redirectTo`, then `runtime.auth.loginPath`, then no redirect, and never use `logoutPath` as a login fallback.
- Hardened public auth config so HTTPS issuer/Hosted UI URLs reject userinfo, backslashes, whitespace/control characters, and non-HTTPS schemes; same-origin path fields reject external URLs, protocol-relative URLs, `javascript:`, `data:`, backslashes, whitespace/control characters, and paths missing the leading `/`.
- Documented the current SSR limitation: without a server-side redirect/status hook, protected routes clear protected content rather than rendering private data, and pilot protected pages should stay free of private actions/data and use `noindex,nofollow` until server handling exists.

## 2026-06-08 04:52 CT - Karma/Chrome intermittent test isolation

- Fixed an intermittent Karma/Chrome failure where component specs could open or close Angora-backed controls, schedule a real `AngoraCombosService.scheduleCssCreate(...)` timer, and let the real `NgxAngoraService.cssCreate()` emit library `console.error` output during a later spec.
- Added a global no-op `NgxAngoraService` provider in `src/test-providers.ts` so ordinary Angular specs do not load the runtime Angora CSS generator unless they intentionally override the provider.
- Added a `test:zoneless-runner` regression check that fails if the global Karma provider stops isolating the real Angora CSS generator.
- Added an `AngoraCombosService` mock to `generic-dropdown.component.spec.ts` and asserted the dropdown still requests a CSS refresh when opening the overlay.
- Updated the AnalyticsService spec URL helper to call the captured native `History.prototype.replaceState` directly, avoiding cross-spec contamination from history spies when the full suite changes `window.history` methods.
- Validation passed: focused `generic-dropdown.component.spec.ts` (`TOTAL: 6 SUCCESS`), focused `analytics.service.spec.ts` (`TOTAL: 19 SUCCESS`), focused `generic-input.component.spec.ts` (`TOTAL: 11 SUCCESS`), `npm run test:zoneless-runner` (`pass 5`), three consecutive full Karma runs (`TOTAL: 468 SUCCESS` each), `npm run build`, and `npm run ssr:smoke` (`pass 16`).
- Promoted through PR #28 (`codex/karma-chrome-test-isolation` -> `dev`, merge `37ec3332dd777caa6c7947f20e5e88b0c002da39`), PR #29 (`dev` -> `test`, merge `f4d0894332c2031014c7d4f5a50524c1600fa44c`), and PR #30 (`test` -> `main`, merge `9be5ac405798c0a8ff4775a0d83e6f864e02ab16`).
- Testing audit passed for the shared test host across the 11 registry drafts in desktop/mobile (`checked: 22`, `failures: []`), and `tools/ops/public-site-health-check.mjs --hosts=test.zoolandingpage.com.mx` returned `"ok": true` at `2026-06-08 05:18:15` CT.
- Production public health passed for the live production hosts and Zoosite campaign aliases (`"ok": true`, `failed: []` at `2026-06-08 05:22:08` CT), including direct runtime alias assertions for `sitiosweb.zoolandingpage.com.mx`, `quierounsitioweb.zoolandingpage.com.mx`, and `crearpaginaweb.zoolandingpage.com.mx` resolving to `zoositioweb.com.mx`.
- Production browser QA passed for the 9 currently resolvable public hosts in desktop/mobile (`checked: 18`, `failures: []`). The canonical registry domains `alecfest-voliii.com`, `despacholegalastralex.com`, `agent-guide-claude-codex.com`, `pamelabetancourt.com`, and `robertorodriguezrodriguez.com.mx` still returned DNS `ERR_NAME_NOT_RESOLVED` from the local machine and were not counted as app regressions.

## 2026-06-08 04:09 CT - Runtime alias health assertions

- Added strict runtime alias assertions to `tools/ops/public-site-health-check.mjs` through the `--expected-runtime-domains=<host>=<domain>` option so public health checks can fail when a campaign alias returns `runtime.ok=true` but resolves to the wrong canonical draft or omits `metadata.resolvedAlias`.
- Included `tools/tests/ops-public-site-health-check.spec.mjs` in `npm run test:ops-front-door` and added coverage for the missing-`resolvedAlias` case.
- Rechecked the three Zoosite campaign aliases after runtime metadata/cache convergence. The direct runtime API returned `domain: "zoositioweb.com.mx"` with matching `resolvedAlias` values for `sitiosweb.zoolandingpage.com.mx`, `quierounsitioweb.zoolandingpage.com.mx`, and `crearpaginaweb.zoolandingpage.com.mx`.

## 2026-06-07 22:25 CT - Angular 22 and signal state foundation

- Updated the app dependency baseline to Angular 22.0.x, TypeScript 6.0.x, and Node 24.15.0 for local migration commands.
- Removed the temporary NgRx 21 foundation after verifying its packages declared Angular 21 peer dependencies and no Angular 22-compatible NgRx release was available during migration.
- Added an app-level Angular signal state foundation for future authentication, draft tenant/profile state, public blog summaries, and transient blog editor state without adding Cognito resources, AWS resources, secrets, draft-specific content, or deploy changes.
- Kept config-driven rendering in the existing `ConfigStoreService` and `VariableStoreService` signal stores so `DraftRuntimeService`, `ConfigBootstrapService`, `RuntimeService`, and runtime data source contracts continue to own draft payload flow.
- Added SSR-safe auth browser storage that persists only minimal non-secret session metadata.
- Adopted Angular 22-era signal APIs in the state foundation through Angular signal facades and `@angular/forms/signals` for blog editor form state.
- Updated `zoolandingpage.com.mx` and `sulandingpage.com.mx` draft alias overrides so GA4-enabled public aliases render without canonical redirects while still emitting canonical links to the primary `.com.mx` origins.
- Validation remained local-only at this point: `npm ci`, `npm audit`, focused Karma suites, `npm run build`, `npm run ssr:smoke`, draft tooling tests, `npm run test:draft-context`, and local draft smoke for 90 routes across 11 drafts and 2 viewports passed. No deploy was run from this worktree yet.
- Promoted the Angular 22 app branch to the shared `test` branch through PR #19. Promoted the draft alias changes to draft `test` through Sulanding PRs #11 and #12 and Zoolanding PR #9; the related draft test deploy workflows completed successfully.
- Test verification after deploy: `https://test.zoolandingpage.com.mx` served `main-MWH3SAC2.js`; remote shared-test draft smoke passed 90 routes across 11 drafts and 2 viewports with zero local failures.
- Production was not promoted. Public alias health failed at 2026-06-07 23:46 CT with 96 checks, 38 failures: several configured test aliases returned DNS `ENOTFOUND`, and several reachable aliases had AAAA records while `--fail-on-aaaa` was enabled.

## 2026-06-06 20:33 CT - Shared testing draft preview redirect fix

- Updated Express SSR front-door redirects so `https://test.zoolandingpage.com.mx/?draftDomain=...` stays on the shared testing host instead of redirecting canonical-host drafts such as `erosbarajas.com`, `sulandingpage.com.mx`, and `zoositioweb.com.mx` to their public domains.
- Tightened the SSR smoke test for the shared testing preview path by making redirects manual and asserting there is no `Location` header.
- Published commit `1327e62` to `test`.
- Validation: `npm run build` passed, `node --test tools/tests/ssr-server.spec.mjs` passed 14/14, remote curl checks kept all 11 published draft domains on `test.zoolandingpage.com.mx`, and remote Chrome headless checks confirmed each draft rendered a `<main>` with no boot curtain, no 404 text, no horizontal overflow, and no console errors. The only observed failed requests were non-render-blocking Google Analytics `g/collect` beacons returning `net::ERR_ABORTED`.

## 2026-06-04 18:36 CT - Static SSR boot curtain contrast gate

- Found the remaining Eros first-open production flash in `src/main.ts`: `releaseBootCurtainForStaticSsrContent()` could add `zlp-boot-curtain--leaving` after static selector coverage passed, before Angular runtime and `LoadingCurtainService` could validate final Angora theme colors.
- Added `hasReadableCriticalTextContrast(...)` so the static SSR release path keeps the boot curtain active when critical visible text hooks (`sectionTitle`, `sectionSubtitle`, `heroCaption`) still have low contrast against their effective background.
- Validation passed: focused Karma coverage for static style coverage, critical text contrast, Angora combos, runtime readiness, and loading curtain services (`52 SUCCESS`), plus `npm run build` generating local bundle `main-2ODLGFJC.js`.
- Production deploy verified at 2026-06-04 18:40:53 CT when `https://erosbarajas.com/` began serving `main-2ODLGFJC.js`. Three production desktop/mobile QA passes reported zero exposed stale-color samples, final `sectionTitle` color `rgb(32, 23, 18)`, and zero relevant console, failed-request, or bad-response findings. Evidence was saved under `C:\Users\lince\.codex\memories\Output\eros-prod-deploy-2026-06-04-18-41-54-ct` and `C:\Users\lince\.codex\memories\Output\eros-prod-deploy-repeat-2026-06-04-18-43-03-ct`.

## 2026-06-04 16:10 CT - Angora combo CSS first paint

- Fixed a first-paint race where `RuntimeService` could ask Angora to generate authored component classes before `AngoraCombosService` had applied the latest `ConfigStoreService` combo payload, so combo-only classes such as `sectionTitle` were filtered and the Eros draft could reveal with stale title color.
- `AngoraCombosService` now synchronizes draft combos from the store before explicit class updates, splits authored class strings into individual tokens, and checks rendered combo `__COM_*` rules before reporting CSS-ready.
- `RuntimeService` now re-samples rendered DOM classes inside loading-curtain readiness, requires stable ready passes before starting the hide path, and keeps timeout behavior resilient with a timer fallback around frame waits. Combo readiness checks critical color/text markers such as `__COM_sectionTitle-titleColor` and the matching elements' computed token color, because partial combo rules can exist before the final rendered color is active.
- Production readiness timeout for Angora combo CSS increased to 20 seconds after Eros production showed the final `sectionTitle` color could arrive after the old 4-second budget.
- Added final rendered-text color gates for critical draft text combos (`sectionTitle`, `sectionSubtitle`, `heroCaption`) in both `RuntimeService` and `LoadingCurtainService` so no early-exit path can start the boot curtain fade while visible text still computes stale theme colors.
- `LoadingCurtainService` resolves expected critical text colors from the draft theme payload before falling back to CSS variables, covering production cases where `--ank-titleColor` itself is briefly stale.
- Validation passed: focused `AngoraCombosService` + `RuntimeService` Karma suites 36/36 with Edge as `ChromeHeadless`, `npm run build`, local SSR first-paint QA for `erosbarajas.com` on desktop/mobile, and an extended local desktop probe that kept the curtain up until the final title color was present.

## 2026-06-04 15:27 CT - In-tree draft registry paths

- Updated `docs/drafts-registry.json`, draft preflight tooling, managed-alias sync fallback behavior, tests, and workflow docs so registered local draft repos resolve under `zoolandingpage/drafts/{domain}` instead of sibling `../draft-*` directories.
- Kept remote GitHub repo names using the existing `draft-*` naming convention; only local clone paths changed. Existing sibling `C:\Users\lince\Documents\GitHub\draft-*` folders are now legacy duplicates unless explicitly re-registered.
- Validation passed: registry JSON parse, preflight path resolution showing all registered repos under `C:\Users\lince\Documents\GitHub\zoolandingpage\drafts`, and focused registry/front-door tests 18/18.

## 2026-06-04 15:07 CT - Dependency audit remediation

- Updated the lockfile-only transitive dependency resolution for Karma/Express tooling so `body-parser` resolves to `1.20.5`, `qs` resolves to `6.15.2`, and `tmp` resolves to `0.2.7`.
- `npm audit` now reports zero vulnerabilities after the lockfile update.
- Validation: `npm ci`, `npm audit`, `npm run build`, and `npm run ssr:smoke` passed. `npm run test:logged` still has one unrelated AnalyticsService spec failure, and `npm run test:draft-context` still requires local ignored draft fixtures for `drafts/zoolandingpage.com.mx/site-config.json`.

## 2026-06-04 14:17 CT - SSR allowed-host gate

- Added an Express SSR host gate that validates `Host` and `X-Forwarded-Host` against approved static host families or the resolved local/runtime `site-config` host set before Angular SSR handles the request.
- Angular SSR now receives the exact validated host set for each request, allowing newly published runtime-backed domains without per-domain `angular.json` hotfixes and without using a wildcard Angular host allowlist.
- Added SSR smoke coverage for `erosbarajas.com`, runtime aliases, unknown custom hosts, test-host `draftDomain` preview, and production hosts ignoring cross-draft `draftDomain` query params.
