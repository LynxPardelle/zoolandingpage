# App Changelog - 2026-06

## 2026-06-30 11:41 CT - Content hub smoke schedule timestamp compatibility

- Updated the content-hub product-readiness smoke to send schedule timestamps without milliseconds, matching the BFF schedule contract accepted by testing and avoiding false `Invalid unpublish time` failures.
- Verification passed: `node --test tools/tests/content-hub-product-readiness-smoke.spec.mjs` returned 18 pass; authenticated testing smoke for `zoositioweb.com.mx` returned `ok:true` with all 18 checks passing, including `publicBundlePreview`, `publish`, `runtimeBundle`, `publicSearch`, `sitemap`, `feed`, `scheduleList`, and `cancelSchedule`.

## 2026-06-30 11:05 CT - Blog product completion execution plan and smoke diagnostics

- Added repo-local design context for the Zoosite blog/admin work so future UI passes keep the same public/admin audience, tone, generic-component boundary, and SEO/product-readiness constraints.
- Added a `.superpowers` execution plan for the 12 blog product-completion areas and recorded the current BFF conflict blocker without committing local Superpowers scratch files.
- Tagged each content-hub product-readiness smoke network/action step in thrown errors while preserving the existing redacted, browser-safe message handling, so failures identify the failed step without printing cookies, CSRF, tokens, or raw backend payloads.
- Verification passed: `node --test tools/tests/content-hub-product-readiness-smoke.spec.mjs` returned 18 pass; content-hub admin/binding/harness Node tests returned 38 pass; `node --test tools/tests/ssr-server.spec.mjs` returned 23 pass.

## 2026-06-30 10:29 CT - Content hub product smoke setup errors

- Refined the content-hub product smoke error classifier so local setup failures for missing `--runtime-base-url`, missing session cookie, or missing CSRF cookie remain distinguishable from backend authentication/service failures without printing sensitive values.
- Added a `/admin/blog` contract guard that prevents ambiguous global article mutations from returning to the overview page; validate/publish flows must stay row-scoped or detail-page-scoped with concrete article identifiers.
- Verification passed: focused content-hub smoke/admin/binding Node tests returned 44 pass, and `git diff --check` returned only LF/CRLF warnings.

## 2026-06-30 07:08 CT - Content hub analytics summary read

- Added the browser-safe `analyticsSummary` content-hub read contract to Angular runtime types, payload validation, JSON schema, docs, and the Zoosite product smoke.
- Updated product smoke coverage so the authenticated content-hub path verifies analytics-summary reads in addition to revision preview, media, moderation, scheduling, public runtime/search, sitemap, and feed checks.
- Verification passed: `node --test tools/tests/site-config-schema.spec.mjs tools/tests/zoosite-content-hub-admin-bindings.spec.mjs tools/tests/content-hub-product-readiness-smoke.spec.mjs` returned 29 pass; expanded app-side content-hub/preflight/admin tests returned 57 pass; focused config-validation Karma returned `TOTAL: 77 SUCCESS`; `npm run build` passed with the existing Quill `quill-delta` CommonJS warning; `npm audit --omit=dev` returned `found 0 vulnerabilities`.

## 2026-06-30 06:05 CT - Content hub preview/version smoke coverage

- Extended the authenticated content-hub product smoke with protected `revisionList` and `publicBundlePreview` reads after `updatePackage`, before validation and publish.
- Hardened the preview check so it must include the updated revision, preventing a stale preview bundle from passing the smoke.
- Added `restoreRevision` coverage for the smoke-owned article so the versions workflow is covered without touching pre-existing content.
- Added sitemap and feed checks for the published article using exact canonical XML entries instead of substring-only path checks.
- Added read-only `assetList` and `moderationQueue` checks to the authenticated product smoke so media and moderation surfaces are verified without creating extra records.
- Removed free-form interaction metadata from the Zoosite draft action contract so public interaction payloads stay limited to article/event/target/value/path fields.
- Hardened production smoke configuration so production runs require an explicit public host and `--shared-preview=false`.
- Added a Zoosite binding assertion for `cancelSchedule` so schedule cancellation cannot disappear from draft config without a local contract failure.
- Verification passed: `node --test tools/tests/content-hub-product-readiness-smoke.spec.mjs` returned 16 pass; content-hub smoke/admin/binding/preflight Node tests returned 51 pass; `npm run build` passed with the existing `quill-delta` CommonJS warning; testing preflight returned PASS with runtime `versionId=20260630T101718-test-a20076ee3c7b`.

## 2026-06-30 05:45 CT - Content hub preflight and smoke hardening

- Extended the public-site health check with Zoosite shared-preview options for `draftDomain`, runtime environment, expected runtime version, expected content-hub route exposure, and unauthenticated protected content-hub route verification.
- Sanitized content-hub product smoke CLI failures so raw backend messages such as `Invalid id`, private service URLs, cookies, and CSRF values are not printed to stderr.
- Verification passed: `node --test tools/tests/ops-public-site-health-check.spec.mjs` returned 9 pass; `node --test tools/tests/content-hub-product-readiness-smoke.spec.mjs` returned 13 pass; read-only testing preflight for `test.zoolandingpage.com.mx` + `zoositioweb.com.mx` returned PASS with runtime `versionId=20260630T101718-test-a20076ee3c7b` and content-hub unauthenticated status `401`.

## 2026-06-30 05:21 CT - Blog product hardening batch

- Added `@error` fallbacks to deferred app-shell, wrapper-orchestrator, and generic tab content surfaces so lazy generic component failures do not leave Angular `NG0750` blank states without a fallback.
- Hardened Zoosite blog/admin copy and draft guard tests so product-facing surfaces avoid internal implementation terms, raw backend error passthroughs, and ambiguous protected table empty states.
- Verification passed: content-hub admin/binding Node tests returned 26 pass; focused wrapper-orchestrator Karma returned `TOTAL: 13 SUCCESS`; `npm run build` passed with the existing `quill-delta` CommonJS warning; Zoosite draft auth audit returned OK; Zoosite public safety audit returned `ok: true` and `blockingRepoCount: 0`.

## 2026-06-30 04:37 CT - Test runtime fallback alignment

- Aligned Angular and Express SSR `test` runtime-read fallbacks to the active `zoolanding-config-runtime-read` endpoint already documented for `/runtime-bundle`.
- Verification passed: `rg -n "jaay9p8gv5" src tools docs README.md changelog -S` returned no matches; `node --test tools/tests/content-hub-product-readiness-smoke.spec.mjs tools/tests/ssr-server.spec.mjs` returned 34 pass; focused `ConfigApiService` Karma returned `TOTAL: 11 SUCCESS`; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-06-30 03:31 CT - Content hub smoke schedule alignment

- Aligned the content-hub product-readiness smoke with the current BFF contract by scheduling `unpublish` after the QA article is published instead of trying to schedule a second `publish`.
- Verification passed: `node --test tools/tests/content-hub-product-readiness-smoke.spec.mjs` returned `11` pass; content-hub BFF `python -m unittest tests.test_content_hub_handler` returned `Ran 59 tests ... OK`.

## 2026-06-30 03:17 CT - Content hub product lifecycle smoke

- Expanded the content-hub product-readiness smoke from create/approve/publish/search into the full editorial path: create, update package content, validate, submit for review, approve, publish, runtime bundle verification, public search verification, public article HTML fetch, schedule list, and schedule cancellation.
- Added focused smoke coverage for shared-preview public article URLs so test-host article checks preserve `draftDomain` and language context.
- Strengthened `generic-rich-text` coverage for underline and blockquote toolbar configuration without changing the component contract.
- Verification passed: focused rich-text Karma returned `TOTAL: 7 SUCCESS`; content-hub smoke/admin/binding/schema/SSR Node tests returned `83` pass.

## 2026-06-30 03:00 CT - Content hub product SEO and value labels

- Hardened content-hub tag filter SEO so `/blog/tag/*` routes stay `noindex,nofollow` and self-canonical on both SSR and hydrated Angular metadata instead of inheriting category canonical values.
- Added the generic `joinText` value handler for draft-composed labels that need safe spacing after `valueInstructions` argument trimming.
- Expanded content-hub product-readiness tests to catch public/admin visible copy that exposes internal implementation terms and to cover the tag-filter SSR/client SEO regression.
- Verification passed: focused Karma returned `TOTAL: 16 SUCCESS`, content-hub product/admin/schema/SSR Node tests returned `82` pass, `npm run build` passed with the existing `quill-delta` CommonJS warning, and `npm audit --omit=dev` returned `found 0 vulnerabilities`.

## 2026-06-30 00:35 CT - Content hub smoke approval gate

- Updated the content-hub product-readiness smoke to approve the newly created QA article before publishing, matching the BFF editorial lifecycle gate.
- Verification: content-hub smoke/admin/binding Node tests returned 35 pass, `npm audit --omit=dev` returned `found 0 vulnerabilities`, and `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-06-29 23:10 CT - Content hub smoke search coverage

- Extended the redacted content-hub product-readiness smoke so the public search gate verifies the newly published QA article by visible title, generated slug, public path, category, and tag.
- Verification: focused content-hub smoke/admin/schema/binding Node tests returned 47 pass, focused content-hub/proxy Karma returned `TOTAL: 14 SUCCESS`, `npm audit --omit=dev` returned `found 0 vulnerabilities`, and `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-06-29 22:56 CT - Content hub safe client errors

- Mapped protected content-hub read/action failures to localized user-facing messages in `ContentHubClientService` instead of exposing raw backend strings such as `Invalid id`, `forbidden`, CSRF, upstream, or validation terms in draft UI status surfaces.
- Kept same-origin credentials, CSRF, public hub headers, and bounded browser upload JSON behavior unchanged.
- Verification: focused content-hub client/proxy action Karma returned `TOTAL: 14 SUCCESS`; Zoosite content-hub admin/binding Node tests returned 25 pass; `npm run build` passed with the existing `quill-delta` CommonJS warning; `npm audit --omit=dev` returned `found 0 vulnerabilities`.

## 2026-06-29 21:13 CT - Dynamic generic event instructions

- Updated `generic-link` and `wrapper-orchestrator` so value-orchestrated `eventInstructions` resolve to a string before DOM attributes or component-event dispatch.
- Added focused coverage for dynamic link analytics instructions and wrapper dispatch normalization.
- Verification: focused Karma for `generic-link` and `wrapper-orchestrator` returned `TOTAL: 27 SUCCESS`.

## 2026-06-29 20:59 CT - Content hub schedule table contract

- Added draft-contract coverage that keeps the Zoosite Programados table bound to real `remote.contentHub.schedules.items` rows with `scheduleId` row identity and the supported cancel-schedule proxy action.

## 2026-06-29 20:25 CT - Content hub public search slug matching

- Expanded `/content-hub-search.json` query matching so public article search includes article IDs, slugs, paths, canonical paths, categories, and tags in addition to title and summary.
- Added SSR regression coverage for searching a published Zoosite article by full slug (`q=blog-builder-seo`).
- Verification: `npm run build` passed with the existing `quill-delta` CommonJS warning; `node --test tools/tests/ssr-server.spec.mjs` returned 23 pass.

## 2026-06-29 18:38 CT - Content hub schedule gate hardening

- Updated the shared content-hub action status normalizer so responses that expose only `article.latestRevisionId` or `createdArticle.latestRevisionId` also populate the safe `revisionId` shortcut used by draft next-step links.
- Tightened Zoosite preview/SEO admin draft contracts so article and revision identifiers are required before lifecycle buttons can execute, preventing protected actions from firing with empty IDs.
- Verification: content-hub admin/schema/contract Node tests returned 54 pass; focused `proxy-action.handlers.spec.ts` Karma returned `TOTAL: 9 SUCCESS`. Backend content-hub unit tests in the sibling BFF repo returned `Ran 45 tests ... OK`.

## 2026-06-29 16:59 CT - Content hub article detail hydration

- Aligned Zoosite article editor hydration with the mapped content-hub `articleDetail` result so rich text reads from `remote.contentHub.articleDetail.items.0.articleContent`.
- Added a site-config mapper field for sanitized editable article content returned by the protected content-hub BFF.
- Split category/tag admin tables to use dedicated `remote.contentHub.categories.items` and `remote.contentHub.tags.items` sources instead of a mixed taxonomy list.
- Verification: content-hub focused/unit contract tests passed 50 tests; Zoosite content-hub admin draft/binding tests passed 20 tests.

## 2026-06-29 16:18 CT - Content hub product-readiness smoke

- Added a redacted live content-hub smoke command that requires caller-supplied authenticated session input, creates a QA article, publishes it, verifies runtime-read and public search visibility, then creates/lists/cancels a schedule without printing cookies or CSRF values.
- Updated content-hub docs that still described the runtime-read/public-index bridge as missing; the bridge now exists, while per-draft live product-readiness evidence remains required after deploy.
- Added durable project memory that local/unit tests alone are not enough to declare the blog product-ready.

## 2026-06-29 15:53 CT - Zoosite blog role group alignment

- Aligned Zoosite blog route bindings and the content-hub role-policy harness with dedicated moderator and analyst groups instead of leaving those surfaces admin-only.
- Updated the Zoosite server-only auth profile to allow/manage `zoosite-blog-moderator` and `zoosite-blog-analyst` in addition to the existing admin/client/editor/publisher/media groups.
- Created the missing real Cognito groups in pool `us-east-1_Pq5OCadbK`: `zoosite-blog-editor`, `zoosite-blog-publisher`, `zoosite-blog-media`, `zoosite-blog-moderator`, and `zoosite-blog-analyst`.
- Verification: focused content-hub contract and Zoosite admin binding Node tests returned 17 pass.

## 2026-06-29 14:39 CT - Content hub taxonomy-driven article controls

- Added regression coverage so `generic-input` autocomplete options can hydrate from runtime variable sources, matching the existing dynamic select option contract.
- Tightened the Zoosite blog admin binding contract so article create/editor category and tag controls must use protected content-hub taxonomy reads instead of only static draft options.
- Verification: content-hub/admin/schema/SSR Node tests returned 71 pass; focused Karma for generic input, config services, draft runtime, and config validation returned `TOTAL: 155 SUCCESS`; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-06-29 02:38 CT - Content hub scheduling and revision contracts

- Tightened Zoosite admin page contracts so Programados requires both article and revision identity, Versiones requires explicit article/revision selection before restore, and protected table errors can use stable user-facing copy instead of raw backend error passthrough.
- Updated the content-hub package docs to define current scheduling as explicit `publish`/`unpublish` intents; schedule cancellation is reserved for a separate future action with a stable `scheduleId`.
- Verification: focused content-hub admin page and Zoosite admin binding Node tests returned 17 pass.

## 2026-06-29 02:06 CT - Content hub editorial lifecycle actions

- Extended the content-hub action schema and Zoosite draft bindings with `submitReview`, `approveArticle`, `unpublishArticle`, and `archiveArticle` so the blog lifecycle is explicit beyond publish-only flows.
- Added generic Zoosite SEO/editorial controls and safe status text for review, approval, publish, unpublish, and archive actions.
- Verification: focused site-config schema, content-hub contract/schema, admin page, and Zoosite admin binding Node tests returned 46 pass.

## 2026-06-29 01:44 CT - Content hub role-policy read alignment

- Aligned the product-readiness role-policy contract with the generic content-hub BFF by making protected read permissions explicit for taxonomy, media, and moderation reads.
- Kept protected bundle preview under `blog:article:read` instead of adding a separate preview-only permission that the current draft role contract did not grant.

## 2026-06-29 01:10 CT - Content hub product-readiness contract

- Added a product-readiness QA matrix check for Zoosite blog work so the admin/public blog checklist stays aligned with the 12 areas needed to move beyond MVP.
- Added content-hub role policy contracts for `hub-admin`, `blog-admin`, `blog-editor`, `blog-publisher`, `blog-reviewer`, `blog-moderator`, `blog-media-manager`, and `blog-analyst`, with auth-profile group validation, explicit action-scoped permissions, and wildcard rejection.
- Verification: content-hub contract/admin Node tests returned 40 pass; focused component/admin Node tests returned 14 pass.

## 2026-06-29 00:27 CT - Content hub route-bundle article SEO

- Fixed content-hub article SSR head decoration so article canonical links and `BlogPosting` JSON-LD prefer the route-specific runtime bundle `siteConfig` when a newly published article is not yet present in the root public index.
- Kept the public-safety boundary defensive: SEO is enriched only from matching content-hub entries that pass the existing published/public filters, and 404 responses still skip article SEO enrichment.
- Added SSR regression coverage for a route-only published article whose generic article page config still contains the seed article canonical.
- Verification: `npm run build` passed with the existing `quill-delta` CommonJS warning; focused route-bundle SSR test passed; full `node --test tools/tests/ssr-server.spec.mjs` returned 23 pass.

## 2026-06-29 00:09 CT - Testing runtime fallback for content hub

- Fixed shared testing runtime fallback selection so SSR and Angular runtime bundle reads can use the `test` runtime-read endpoint for `test.zoolandingpage.com.mx?draftDomain=...` instead of reading the production fallback.
- Added environment-specific runtime fallback maps for dev, test, and production while keeping legacy single fallback settings available.
- Made `generic-card` accept `linkHref` as a backward-compatible alias for `href`, restoring real SSR anchors for draft-composed public article previews that were rendering only CTA text.
- Verification: focused `generic-card` + `ConfigApiService` Karma returned `TOTAL: 18 SUCCESS`; content-hub contract/admin Node tests returned 36 pass; `node --test tools/tests/ssr-server.spec.mjs` returned 22 pass after build; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed with the existing `quill-delta` CommonJS warning; Zoosite public safety audit returned `ok: true` and `blockingRepoCount: 0`.

## 2026-06-28 20:02 CT - Dynamic content-hub SEO index

- Fixed public content-hub SSR helpers so sitemap, RSS feed, search JSON, and article SEO head decoration prefer the published runtime bundle when it includes public content-hub articles or taxonomy, even when the app image still has a local Zoosite draft config.
- Added SSR regression coverage for an article that exists only in runtime, proving it appears in sitemap/feed/search, gets article JSON-LD/canonical SEO, and still excludes private article-like entries.
- Verification: `npm run build` passed with the existing `quill-delta` CommonJS warning; focused SSR test passed; full `node --test tools/tests/ssr-server.spec.mjs` returned 21 pass; `npm audit --omit=dev` returned `found 0 vulnerabilities`; Zoosite public safety audit returned `ok: true`.

## 2026-06-28 05:59 CT - Public content-hub visibility hardening

- Added defense-in-depth filters so Angular SSR, content-hub route helpers, and runtime variable bootstrap ignore article entries with `visibility` present and not equal to `public`, even if an upstream runtime payload accidentally mixes such entries into `publicArticles`.
- Extended SSR coverage to prove private article-like entries do not appear in sitemap, feed, search JSON, shared-host `/blog` links, article SEO, JSON-LD, or public article routes.
- Verification: focused content-hub/bootstrap Karma returned `TOTAL: 12 SUCCESS`; expanded focused Karma returned `TOTAL: 42 SUCCESS`; content-hub/SSR Node tests returned 57 pass; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed with the existing `quill-delta` CommonJS warning; Zoosite public safety audit returned `ok: true`.

## 2026-06-28 03:34 CT - Public content-hub runtime variable bridge

- Added a bootstrap bridge that passes matched draft route path/params into `ConfigBootstrapService` and derives public blog variables from `runtime.contentHubs`.
- Public draft pages can now read live runtime-backed `contentHub.publicArticles.items`, `contentHub.categories.items`, `contentHub.tags.items`, and `contentHub.currentArticle` without adding blog-specific Angular page managers.
- Verification: focused `config-bootstrap.service.spec.ts` returned `TOTAL: 7 SUCCESS`; focused runtime/content-hub Karma returned `TOTAL: 49 SUCCESS`; `npm run build` passed with the existing `quill-delta` CommonJS warning; local browser QA covered `/blog`, `/blog/web`, `/blog/tag/seo`, and `/blog/web/blog-builder-seo` for Zoosite with no console errors observed.

## 2026-06-27 02:14 CT - Dirty rich text edit preservation

- Fixed `generic-rich-text` so dirty interaction-scope values stay authoritative when a parent wrapper replays stale `config.value`, preventing active Quill edits from resetting during draft re-renders.
- Verification: focused Karma for `generic-rich-text` returned `TOTAL: 7 SUCCESS`.

## 2026-06-27 01:15 CT - Draft routeParam value fallback and rich text model sync

- Fixed `routeParam` and `routeParamOr` value handlers so draft-composed dynamic pages can read the matched route params stored by `DraftRuntimeService`, not only Angular `RouterState` params.
- This restores dynamic href/value instructions on routes such as `/admin/blog/articulos/:id/editor`, where Angular itself is serving a generic draft shell while the draft runtime owns the `:id` match.
- Updated `generic-rich-text` to keep the Quill model synchronized with the latest editor content, preserving toolbar formatting/list state while still emitting the configured plain-text or delta value.
- Verification: focused Karma for route-param handlers, draft runtime, and generic-rich-text returned `TOTAL: 32 SUCCESS`; `node --test tools/tests/content-hub-admin-pages.spec.mjs tools/tests/components-schema.spec.mjs` returned 11 pass; `npm run build` passed with the existing `quill-delta` CommonJS warning; `npm audit --omit=dev` returned `found 0 vulnerabilities`.

## 2026-06-26 23:21 CT - Generic table action style contract

- Fixed the public config validator so `generic-table.config.actionButtonStyles` is accepted and validated as a style record, matching the existing `GenericTableComponent` runtime API.
- This prevents admin blog runtime bundles with styled action buttons from being rejected client-side and falling back to the legacy `/site-config` endpoint, which is blocked by CORS on the shared test host and left `/admin/blog` blank.
- Verification: the real testing `/admin/blog` runtime bundle shape validates end-to-end; focused `config-payload.validators.spec.ts` returned `TOTAL: 76 SUCCESS`; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-06-26 21:50 CT - Protected SSR no-store headers

- Fixed protected draft SSR responses so auth-required routes that rely on browser/BFF server-cookie revalidation still return non-cacheable HTML.
- Added `Cache-Control: no-store`, `Pragma: no-cache`, `Expires: 0`, and `Vary: Cookie` for protected SSR HTML to prevent stale or cross-route documents from being reused on `/mi-cuenta` and `/admin/*`.
- Verification: `npm run build` passed with the existing `quill-delta` CommonJS warning; `node --test tools/tests/ssr-server.spec.mjs` returned 21 pass.

## 2026-06-26 20:28 CT - Generic table action touch styles

- Added `generic-table.config.actionButtonStyles` so draft-composed row actions can receive explicit inline touch-target styles when dynamic Angora classes are not enough for deployed runtime surfaces.
- Kept the generic default conservative at 44px minimum action targets, while allowing drafts such as Zoosite to opt into 52px icon-only action buttons with tooltip labels.
- Verification: focused Karma for `generic-table`, `generic-rich-text`, and auth state returned `TOTAL: 31 SUCCESS`; Zoosite content-hub admin binding tests returned 5 pass; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-06-26 20:02 CT - Blog admin route checks and rich text follow-up

- Added a short, configurable server-cookie route access cache through `runtime.auth.session.routeAccessCacheMs` with a hard `0..60000` ms clamp and a 15s app default so protected admin page navigation does not repeatedly block on `/auth/session/me`.
- Kept backend authorization boundaries unchanged: the cache is only sanitized account metadata for Angular route rendering, while protected content-hub/auth-admin BFF reads and mutations still validate HttpOnly session, CSRF, tenant/profile context, and current roles server-side.
- Fixed `generic-rich-text` so parent echo updates from the interaction scope no longer replace the active Quill model during user typing, and corrected list toolbar generation so ordered and bullet list controls stay distinct.
- Updated auth/session schema, validators, registry tooling, docs, and focused tests for the new route-access cache metadata.
- Verification: focused Karma for rich text, generic table, auth runtime, and config validators returned `TOTAL: 107 SUCCESS`; Node registry/schema/Zoosite binding tests returned 33 pass.

## 2026-06-26 19:26 CT - Runtime routeParam inputs for content hub detail pages

- Added generic `routeParam` runtime data-source inputs so draft routes such as `/admin/blog/articulos/:id/editor` can hydrate protected BFF reads from the matched route parameter instead of duplicating identifiers in query strings.
- Extended `DraftRuntimeService` resolved context with matched route params and passed them into `RuntimeDataSourceService` for browser/SSR-safe input resolution.
- Updated Zoosite content-hub admin contract tests to require `articleDetail` to read `articleId` from route param `id`.
- Verification: focused Karma for draft runtime, runtime data sources, and config validators returned `TOTAL: 121 SUCCESS`; Node site-config plus Zoosite content-hub binding tests returned 9 pass; `npm run build` passed with the existing `quill-delta` CommonJS warning; `npm audit --omit=dev` returned `found 0 vulnerabilities`.

## 2026-06-26 18:40 CT - Content hub article detail and rich text stability

- Added `articleDetail` to the generic content-hub read contract, schema, validators, and Zoosite admin binding tests so draft-composed editors can hydrate public-safe article metadata by `articleId`.
- Changed runtime data-source startup to load independent initial reads concurrently instead of serializing page reads, while keeping each read's BFF session/role checks and per-source error state intact.
- Hardened `generic-rich-text` so Quill keeps a separate editor model from emitted/scope values; plain-text outputs can use formatting controls without cursor resets or formatting loss during user input.
- Added mapper transform `joinList` so generic inputs can hydrate editable comma-separated text from arrays of primitive or taxonomy-like objects.
- Verification: content-hub full unittest passed 19 tests; focused Karma for rich text, runtime data sources, mapper, and config validators returned `TOTAL: 114 SUCCESS`; site-config/content-hub admin/component Node tests returned 20 pass; `npm run build` passed with the existing `quill-delta` CommonJS warning; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `config:pack` completed; Zoosite draft auth audit returned OK; `git diff --check` had only LF/CRLF warnings.

## 2026-06-26 18:28 CT - Upload grant repo skill

- Added repo-local skill `.github/skills/zoolanding-upload-grant` so future agents can ask for the missing grant inputs, issue scoped `.token`/`.json` upload grant files with `tools/issue-upload-grant.mjs`, and report the generated paths without exposing token values.
- Registered the skill in `ai-notes/notes/copilot-skill-routing.md` and `Codex.md`.

## 2026-06-26 16:52 CT - Generic table compact actions and scope sync

- Added `generic-table.config.actionLabelMode: "tooltip"` and `actionIconClasses` so draft tables can render equal-size icon-only action buttons with accessible labels/native tooltips instead of oversized visible text.
- Fixed `wrapper-orchestrator` generic-table row actions to forward each row action's own `eventInstructions`, allowing draft-authored table actions such as edit, preview, SEO, versions, and schedule to execute from row buttons.
- Updated `InteractionScopeService.registerField(...)` so query/config-driven initial values resync while the field is still pristine, but preserve user-edited values once dirty. This prevents select controls from showing one value in the trigger and a different selected option in the dropdown.
- Updated component schema, validators, and focused tests for the new generic-table action display fields and pristine scope resync behavior.
- Verification: component schema tests passed 2/2, focused Karma passed for `generic-table` 5/5, `wrapper-orchestrator` 11/11, `interaction-scope` 6/6, and config validators 76/76; content-hub/admin binding Node tests passed 16/16; runtime data-source condition guard returned `ok:true`; `config:pack` completed; draft auth audit returned OK; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-06-25 02:59 CT - Generic table action labels and select value parity

- Added `generic-table.config.actionColumnLabel` so draft-composed action columns can expose localized headers such as `Acciones` / `Actions` instead of rendering an unlabeled header cell.
- Hardened `generic-input` select matching so query-string numeric fallbacks such as `pageSize=10` match string-authored option values like `"10"` in both the trigger label and dropdown selected state.
- Updated `components.schema.json`, validators, and focused tests for the new generic-table field and select parity behavior.
- Verification: focused Karma returned `TOTAL: 97 SUCCESS`, `node --test tools/tests/components-schema.spec.mjs tools/tests/content-hub-admin-pages.spec.mjs` returned 11 pass after updating the admin-page test to include shared draft components, and `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-06-25 01:31 CT - Generic tooltip anchor hardening

- Hardened `generic-tooltip` so draft-authored tooltips can bind to anchors by id after the view is ready, expose an explicit `anchorFor` input for wrapper usage, clean up anchor listeners correctly, and use a real `ViewChild` template for overlay rendering.
- Verification: focused Karma returned `TOTAL: 92 SUCCESS`, content-hub admin Node tests returned 9 pass, and `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-06-25 01:19 CT - Generic article admin control polish

- Exposed the existing `generic-tooltip` through `wrapper-orchestrator` with safe draft payload validation for `for` and `content`, so draft-composed controls can attach hover/focus tooltips without app-specific page code.
- Added reusable control polish for joined search inputs, dropdown caret rotation, and generic-table row action icons rendered through `generic-icon` instead of leaking raw Material icon names.
- Verification: focused Karma returned `TOTAL: 91 SUCCESS`, `npm run build` passed with the existing `quill-delta` CommonJS warning, content-hub admin Node tests returned 9 pass, runtime data-source condition guard returned `ok:true`, and `git diff --check` had only LF/CRLF warnings.

## 2026-06-24 23:44 CT - Grant-protected public image uploads

- Removed the unauthenticated public upload path from `zoolanding-image-upload`; every upload now requires a temporary grant stored server-side as a DynamoDB hash with TTL, scoped domain/page/kind/content-type/size/usage limits, and no default overwrite permission.
- Added hub tools for authorized grant issuance and client/agent uploads: `tools/issue-upload-grant.mjs` writes local gitignored token files without printing the token, and `tools/upload-draft-asset.mjs` uploads assets with a grant and returns the final `publicUrl`.
- Updated public asset docs, deployment docs, draft repo templates, Pamela draft docs, and gitignore rules so grants, `.env*`, `.zlp/`, token files, and signed URLs stay out of commits and draft payloads.
- Deployed the hardened `zoolanding-image-upload` stack in `us-east-1`: stack `UPDATE_COMPLETE`, grants table `zoolanding-image-upload-grants` is `ACTIVE` with pay-per-request billing, SSE enabled, and TTL enabled on `expiresAtEpoch`.
- Added upload-abuse monitoring: CloudWatch alarm `zoolanding-image-upload-grant-denied` watches `Zoolanding/ImageUpload UploadGrantDenied` and points to the upload-abuse SNS topic. The two operator email subscriptions were created and are pending email confirmation.
- Verification: backend `python -m unittest discover -s tests -p "test_*.py"` passed 7 tests, `sam validate --lint` passed, hub upload-tool tests passed 5 tests, content-hub/draft-preflight Node tests passed 18 tests, unauthenticated live upload smoke returned `401 missing_grant`, grant issuance for Pamela produced a gitignored token file, live direct upload smoke returned `ok: true`, and the temporary smoke object was deleted from S3.

## 2026-06-24 23:36 CT - Generic pagination disabled state

- Hardened `generic-pagination` so disabled boundary links always receive a component-owned `zlp-pagination-disabled` class, remove pointer cursor utilities, and leave the tab order with `tabindex="-1"`.
- Added focused coverage for disabled previous/next pagination links so draft-level pagination controls can stay visible without looking clickable when only one page is available.

## 2026-06-23 00:46 CT - Content hub atomic action wiring

- Extended generic content-hub action support so `runtime.apiActions[].contentHub.action` accepts `upsertTaxonomy`, `queueComment`, and `recordInteraction`.
- Updated `proxyAction` to build allowlisted inputs from the nearest `interaction-scope`, generic-table `rowData`, and direct event data, allowing draft-composed forms and tables to call protected content-hub mutations without page-specific Angular managers.
- Made `generic-rich-text` and `generic-file-dropzone` write values into the nearest interaction scope, so article bodies and selected upload files can be submitted by generic buttons.
- Added safe response refs for `fileName`, `taxonomyId`, `commentId`, and `interactionId`.
- Verification: focused Karma `TOTAL: 18 SUCCESS`, backend content-hub unit tests `Ran 18 tests ... OK`, Node schema/SSR/admin binding tests `30 pass`, `npm run build` passed with the existing `quill-delta` CommonJS warning, `npm audit --omit=dev` reported `found 0 vulnerabilities`.

## 2026-06-22 21:52 CT - Content hub BFF live front-door routing

- Applied the live Traefik dynamic route for the deployed `zoolanding-content-hub` BFF through `tools/ops/sync-content-hub-front-door.mjs` and SSM commands `2ffbf1c8-653e-49fb-bf5d-f753fa3db213` then `506382a8-4d2c-4a22-a6b1-f5a9a30b5d04`.
- The shared test host `test.zoolandingpage.com.mx` now routes exact paths `/features/content-hub/read` and `/features/content-hub/action` to the content-hub test API Gateway stage. The broken dedicated alias `test.zoositioweb.com.mx` was removed from this front-door config after smoke audit timed out against its CloudFront/DNS target.
- Production hosts `zoositioweb.com.mx` and `zoositioweb.com` now route those same exact paths to the content-hub production API Gateway stage.
- Smoke checks without auth returned `200 {"ok":true}` for `OPTIONS` and `401 {"ok":false,"error":"Authentication required"}` for protected `POST` reads/actions on both test and production front doors. An earlier `500 Content hub request failed` probe was traced to malformed PowerShell-escaped JSON in the manual curl command, not to the deployed Lambda.

## 2026-06-21 06:08 CT - Content hub BFF and admin activation

- Added the generic `zoolanding-content-hub` Lambda/SAM repo as the protected content-hub BFF for same-origin browser reads/actions under `/features/content-hub/read` and `/features/content-hub/action`.
- Added exact-path Traefik front-door sync tooling for the content-hub BFF without stealing future `/features/*` paths.
- Added `navigateWithEventData` as a generic internal-only event handler so draft tables can navigate from safe `eventPayloadFields` such as `articleId` without hardcoded seed article IDs.
- Updated protected feature docs to record that content-hub protected reads/actions are session-cookie, CSRF, tenant, environment, and role checked server-side; public SEO bundle publication remains a separate runtime-read bridge.
- Verification in this pass included Angular build, focused UI handler Karma (`TOTAL: 22 SUCCESS`), content-hub/admin/front-door Node contracts (`18 pass`), isolated SSR server suite (`21 pass`), SAM build/validate for `zoolanding-content-hub`, Python unit tests (`Ran 8 tests ... OK`), and `pip-audit` with no known vulnerabilities.

## 2026-06-21 04:04 CT - Content hub final audit hardening

- Completed the final content-hub audit pass for the current nine-phase slice.
- Hardened runtime-read public bundles so response metadata no longer exposes storage `bucket` or `prefix`, and content-hub metadata no longer exposes server-side article indexes or draft sharing allowlists.
- Aligned the standalone public content-hub schema with the public SEO index shape already supported by `site-config.schema.json`.
- Added SSR leak-resistance coverage for sitemap, feed, search JSON, and article `BlogPosting` JSON-LD outputs.
- Corrected Zoosite test traceability by merging `draft-zoositioweb-com-mx` PR #79 (`dev` -> `test`) after guard passed, so `origin/test` contains the blog page and SEO index commits already present in the test config environment.

## 2026-06-21 03:44 CT - Content hub dev/test seed and SEO runtime

- Published Zoosite's draft package into the new `dev` and `test` config stores through direct authoring Lambda invokes with authorized draft deploy identity context.
- Fixed draft packaging so `draft-repo.config.json` stays local-only and added `--environment=production|test|dev` support for config push/create/publish tooling.
- Updated `zoolanding-config-runtime-read` route matching so parameterized routes such as `/blog/:categorySlug` resolve in backend runtime bundles, then redeployed runtime-read dev/test.
- Added public content-hub article/taxonomy SEO indexes, SSR sitemap/feed/search support, and article `BlogPosting` metadata injection from published public metadata.
- Verification covered build, focused validators, Node schema/content-hub tests, SSR server tests, runtime-read unit tests, Zoosite auth audit, dev/test authoring publish responses, and runtime-read dev/test smoke for `/blog`, `/blog/web`, `/blog/web/blog-builder-seo`, and `/admin/blog`.

## 2026-06-20 17:54 CT - Content hub backend dev/test deploy

- Deployed `zoolanding-config-authoring-dev`, `zoolanding-config-authoring-test`, `zoolanding-config-runtime-read-dev`, `zoolanding-config-runtime-read-test`, `zoolanding-data-dropper-dev`, and `zoolanding-data-dropper-test` in `us-east-1`.
- Added retained non-production storage resources to the authoring and data-dropper IaC before deploy because dev/test tables and buckets did not exist.
- Copied the server-only deploy authorization config to test storage and wrote a dev variant that adds `dev` to rules that already allowed `test`.
- Smoke checks passed: stacks reached `CREATE_COMPLETE`/`UPDATE_COMPLETE`, DynamoDB tables are `ACTIVE` with SSE enabled, S3 buckets have public access blocks and AES256 encryption, data-dropper accepted safe blog events and rejected blog events with `email`, authoring rejected unsigned API calls and accepted direct signed-context smoke authorization.
- Runtime-read endpoints are reachable but return `404` for missing metadata until Zoosite or another draft is published into the new dev/test config stores.

## 2026-06-20 17:16 CT - Content hub backend phase foundation

- Started the backend phase for the content hub/blog platform by extending existing generic Lambda repos instead of creating blog-specific services.
- `zoolanding-config-authoring` gained optional content hub package indexing and validation for draft-like files under `{domain}/content-hubs/{hubId}/...`.
- `zoolanding-config-runtime-read` gained `dev` environment resolution and safe public `metadata.contentHubs` projection.
- `zoolanding-data-dropper-lambda` gained blog analytics validation requiring safe `contentHubId` and `articleId` values while rejecting obvious PII/credential fields.
- Added dev/test/prod SAM config and branch workflow scaffolding in the touched Lambda repos. No AWS deploy was run from this phase.

## 2026-06-20 16:25 CT - Zoosite blog admin surface

- Completed blog/content hub plan phase 6 with the full Zoosite blog admin surface composed from draft JSON packages and generic components.
- Rebuilt `admin-blog-articulos`, `admin-blog-articulos-nuevo`, `admin-blog-articulo-editor`, `admin-blog-articulo-preview`, `admin-blog-articulo-seo`, `admin-blog-articulo-versiones`, `admin-blog-programados`, `admin-blog-moderacion`, `admin-blog-medios`, `admin-blog-analiticas`, `admin-blog-categorias`, `admin-blog-tags`, `admin-blog-hub`, and `admin-blog-configuracion` with `page-config.json`, `components.json`, `variables.json`, `i18n/es.json`, `i18n/en.json`, and `angora-combos.json`.
- Added draft-composed admin controls for article search/list/create/edit, rich text, media upload selection, advanced component inspector, SEO, version restore, scheduling, moderation, analytics, taxonomy, hub visibility, and blog defaults.
- Added Zoosite `runtime.dataSources` and `runtime.apiActions` bindings using the public `kind: "content-hub"` contract for article lists, taxonomy, revisions, assets, moderation queue, public bundle preview, create/update/upload/validate/publish/schedule/moderate/restore actions.
- Added focused Node contract tests for the Zoosite admin page packages and content-hub admin bindings.
- Verification during implementation: new admin Node tests (`9 pass`), content-hub/site/component schema and harness tests (`29 pass`), focused Karma (`TOTAL: 54 SUCCESS`), `npm run build`, and `npm audit --omit=dev`.
- No deploy, AWS call, Cognito change, new Lambda repo, or secret material was added in this phase.

## 2026-06-20 03:04 CT - Zoosite content hub vertical slice

- Completed blog/content hub plan phase 5 with one Zoosite seed article and draft-composed public/admin page shells.
- Added generic draft route-pattern matching for `:param` routes and reused it in browser draft resolution plus SSR local route resolution.
- Added Zoosite public blog routes (`/blog`, `/blog/:categorySlug`, `/blog/:categorySlug/:articleSlug`) and admin blog routes under `/admin/blog*`, with admin routes protected by the existing Zoosite admin auth group.
- Added draft page packages for the public blog index/category/article pages and Zoosite admin blog surfaces using generic components only.
- Added a no-AWS seed article fixture under `tools/tests/fixtures/content-hub/` with article manifest, revision package files, taxonomy, metadata/index records, sanitized published bundle, and analytics event samples.
- Extended the local content-hub harness to validate and publish/read the seed article fixture, fail closed on missing SEO/sanitizer/asset/analytics gates, and reject server-only leakage in public bundles.
- Runtime now emits a generic content-hub view event (`eventPrefix + "_view"`, such as `blog_view`) for configured article routes; CTA tracking remains component-driven through stable generic component IDs.
- No deploy, AWS call, Cognito change, new Lambda repo, or secret material was added in this phase.

## 2026-06-20 02:18 CT - Content hub Angular service contracts

- Completed blog/content hub plan phase 4 with generic Angular runtime contracts for content hub reads and mutations.
- Added `ContentHubClientService` as a same-origin BFF client for `kind: "content-hub"` data sources and API actions, using `credentials: include`, public draft/hub context headers, and CSRF for mutations.
- Extended content hub runtime types with read bindings (`articleList`, `taxonomyList`, `moderationQueue`, `assetList`, `revisionList`, `publicBundlePreview`) and action bindings (`createArticle`, `updatePackage`, `uploadAsset`, `validate`, `submitReview`, `publish`, `schedule`, `moderateComment`, `restoreRevision`).
- Kept draft wiring generic through `runtime.dataSources` and `runtime.apiActions`; no blog-specific Angular manager UI was added.
- Hardened validators/schema so `runtime.contentHubs`, `dataSources[].contentHub`, `apiActions[].contentHub`, content hub source inputs, and action input fields reject server-only policy, upstream URL, credential, table/bucket, token, tenant, signed URL, authorizer, and group-role fields.
- Added request sanitization helpers that strip forbidden keys recursively before browser requests leave Angular.
- Post-audit hardening: normalized forbidden content-hub input keys across camelCase/snake_case/kebab-case/spaced variants, rejected nested secret-store refs and signed URL markers in string values, and restricted content-hub `proxySourceId`/`proxyActionId` to safe IDs.
- Verification during implementation: focused Karma suite for content hub client/runtime request/data source/action/validator specs (`TOTAL: 102 SUCCESS`) and Node content-hub/site-config contract tests (`23 pass`).

## 2026-06-20 02:00 CT - Generic content-builder frontend primitives

- Added generic content-builder primitives for the blog/page-builder foundation: `generic-table`, `generic-cell`, `generic-rich-text`, and `generic-file-dropzone`.
- Integrated the primitives into `wrapper-orchestrator`, public component validation, and `components.schema.json` without adding blog-specific Angular manager components.
- Added `ngx-quill@31.0.0` plus pinned `quill@2.0.2` for optional browser rich editing. The rich-text primitive keeps textarea fallback support and treats Quill Delta JSON/object or plain text as the controlled source; HTML export is not authoritative.
- Hardened `generic-table` events so full rows are not emitted by default; drafts must declare `eventPayloadFields` for row data needed by actions.
- Documented primitive usage in `docs/api-driven-config/20-generic-content-builder-primitives.md` and recorded the rich-text library decision under `.superpowers/blog-content-hub/evidence/rich-text-library-decision.md`.
- No draft route, Lambda repo, deploy, AWS call, Cognito change, or secret material was added.

## 2026-06-20 01:31 CT - Content hub backend boundary harness

- Added a no-AWS local content-hub contract harness for the first blog vertical slice, covering repository ownership decisions, required DynamoDB item families, deterministic S3 key layout, least-privilege IAM boundaries, and sanitized `blog_*` analytics events.
- Documented the phase 2 repo-boundary decision matrix: use `zoolanding-config-authoring` and `zoolanding-config-runtime-read` as first extension targets only if content namespaces remain isolated, split to generic `zoolanding-content-authoring` / `zoolanding-content-runtime-read` if content lifecycle would pollute site-package services, keep `zoolanding-image-upload` simple, and create future generic `zoolanding-feature-media` / `zoolanding-feature-interactions` only after their no-AWS contracts are proven.
- Aligned the content-hub S3 layout so package, component, variable, i18n, snapshot, delta, validation, and published bundle objects are revision-scoped by environment, hub, article, language, and revision.
- Added focused Node coverage proving the harness rejects unsafe IDs/paths, catches missing item families, catches wildcard/secret IAM actions, keeps analytics compatible with the existing `appName`/`timestamp` data-dropper envelope, and keeps analytics free of raw PII/comment/form fields.
- No new Lambda repo, deploy, AWS call, Cognito change, draft publish, or secret material was added.

## 2026-06-20 01:12 CT - Content hub article package contracts

- Added the first content-hub/blog article contract layer for draft-like article packages, public runtime hub references, published SSR bundles, server-only hub policy, shared taxonomy records, and publish validation reports.
- Added shared TypeScript content-hub types and a browser-safe `site-config.json.runtime.contentHubs` reference shape without adding blog-specific metadata to generic components.
- Added focused Node schema tests proving public contracts reject server-only fields, credential refs, signed URLs, external script fields, event-handler attributes, invalid public hub paths, and unsafe public bundle leakage.
- Documented S3 package layout, DynamoDB item families, taxonomy overrides, multilingual slug/SEO behavior, scheduling, deltas, media, interactions, sanitizer policy, blog roles, and the current route-pattern gap.
- No runtime fetching, editor UI, Lambda repo, deploy, AWS call, Cognito change, draft publish, or secret material was added.

## 2026-06-18 23:05 CT - Protected auth route loading and BFF revalidation hardening

- Added an app-level protected-route loading state for auth/private routes so the shell can show a visible, accessible loading panel while server-cookie BFF validation and initial protected data sources are in flight.
- Updated the boot curtain status during protected-route validation so direct reloads also show auth-specific loading text before the Angular shell is available.
- Hardened `AuthRuntimeService.evaluateRouteAccessAsync()` so server-cookie protected routes always revalidate with the auth-admin BFF, even when editable browser metadata already appears authenticated and has the required group.
- Preserved the requested URL language before draft language configuration so bootstrap/auth loading copy can respect `?lang=...` during early protected-route startup.
- Added focused regression coverage for the loading panel, runtime loading phases, requested-language bootstrap behavior, and BFF revalidation against editable browser metadata.

## 2026-06-18 21:11 CT - Protected feature contract v1

- Added `docs/api-driven-config/19-protected-feature-contract.md` for future per-draft protected features such as blogs, dashboards, analytics, uploads, and draft settings.
- Added a plan-only `protected-features.schema.json` plus focused schema contract tests covering ownership, auth-profile binding, default-deny role policy, DynamoDB and object-storage isolation, BFF/JWT authorizer objects, runtime route/data-source/action binding, draft configurability allowlists, structured audit event policy, protected error response shape, explicit server-only boundaries, and `dev -> test -> prod` rollout policy.
- Added validator-driven schema tests that prove the core protected blog/dashboard/upload contract example validates and public/server-only leakage through runtime bindings or a public `serverOnly.exposure` value is rejected.
- Aligned the Auth Profile Registry schema with the existing auth-admin reset MFA path contract by adding `resetUserMfaPathTemplate`.
- No runtime code, draft JSON, deploy, AWS call, or secret material was changed.

## 2026-06-18 20:15 CT - Draft auth static audit tooling

- Added `tools/draft-auth-audit.mjs` as a no-credential reusable static auth contract audit for Zoosite and future custom-auth drafts.
- The audit checks auth route structure, sitemap/noindex treatment, public `runtime.authRemote`, server-cookie session metadata, client/admin/MFA state wiring, admin lost-device reset wiring, mapper condition safety, and public-payload secret leakage.
- Added focused Node test coverage and package scripts `drafts:auth-audit` and `test:draft-auth-audit`.
- Updated the draft auth audit matrix and Codex memory so the static auth audit runs before package upload; browser credential/session QA remains manual.
- No runtime app code, draft page JSON, deploy, AWS call, or secret material was changed.

## 2026-06-18 19:31 CT - Auth loading duplicate-submit guard

- Added in-flight status-target guards to generic `authFormAction` and `authAdminAction` so repeated clicks while the same `*.state` is `loading` do not duplicate auth/session/admin requests before the UI settles.
- Added focused regression coverage that first reproduced duplicate submit behavior for auth forms and admin actions, then passed after the handler-level guard was added.
- Extended Zoosite draft auth/MFA pages with visible generic loading-spinner status blocks for long auth operations, while keeping authorization and mutation checks server-side.

## 2026-06-18 18:01 CT - Optional multi-environment auth claim mode

- Added server-only Auth Profile Registry support for `environmentClaimMode`, defaulting to strict `single` and allowing opt-in `list` mode for verified claims such as `prod,test`.
- Updated the registry schema, validation docs, JWT policy helper, and Zoosite server-only registry fixture so one Cognito user can be authorized in both testing and production only when the draft explicitly opts in.
- Focused validation passed locally: auth profile registry tests returned `22 pass`; server config schema tests returned `4 pass`.

## 2026-06-18 17:43 CT - Auth form error-code contract

- Added `AuthCustomFormRequestError` so generic draft auth form actions preserve sanitized backend `errorCode` values in `authForm.<action>.errorCode`.
- This lets drafts localize specific auth failures such as environment mismatch or expired MFA challenge without hardcoding long backend messages.
- Focused validation passed locally: auth custom form and auth form action Karma specs returned `TOTAL: 28 SUCCESS`.

## 2026-06-18 15:52 CT - Auth audit guard and admin MFA reset contract

- Added `tools/runtime-data-source-condition-guard.mjs` plus focused Node tests to catch draft component `condition` and `valueInstructions` references to raw data-source fields hidden by `mapper.fields`.
- Wired the guard into `config:pack` and standalone draft `deploy-draft.mjs --validate-only=true`, and added the standalone guard file to the draft repo template.
- Extended the generic auth-admin Angular contract with `resetUserMfaPathTemplate`, `AuthAdminClientService.resetUserMfa(...)`, and `authAdminAction:resetUserMfa`.
- Extended public config validation and JSON Schema to accept the new admin reset path/action while keeping same-origin path validation.
- Updated Auth Profile Registry validation to recognize current TOTP display/session metadata and the safe admin reset path.
- Added `docs/api-driven-config/18-draft-auth-audit-matrix.md` as the reusable draft-auth audit matrix for client, admin+MFA, pending, suspended, expired, admin lost-device reset, and error states.
- Focused validation passed locally: runtime data-source guard tests returned `3 pass`; auth profile registry tests returned `19 pass`; focused Angular auth-admin/validator tests returned `TOTAL: 80 SUCCESS`.

## 2026-06-18 03:42 CT - Configurable TOTP label and MFA disable contract

- Added generic Angular support for `authFormAction:disableMfa`, including runtime auth session metadata `mfaDisablePath`, same-origin validation, JSON Schema coverage, session CSRF headers, and safe navigation back to `/mi-cuenta?authStatus=mfa-disabled`.
- Extended the server-only auth profile schema with configurable TOTP display metadata: `mfa.totp.issuer`, `accountLabelTemplate`, and `friendlyDeviceName`.
- Updated the Auth Profile Registry docs so future drafts can choose their authenticator-app issuer/account label per profile while keeping those trusted values out of browser-authored config.
- Focused validation passed locally: auth custom form and handler Karma specs plus config validators returned `TOTAL: 96 SUCCESS`; Node auth/schema contracts returned `8 pass`; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed.

## 2026-06-18 02:26 CT - Generic QR code component

- Added a reusable `qr-code` generic component for draft-auth and future config-driven flows that need scannable values without rendering those values as visible text, `alt`, `title`, or ordinary DOM attributes. The component uses `uqr` to generate a module grid instead of a canvas or `innerHTML`.
- Integrated `qr-code` into `wrapper-orchestrator`, typed payloads, config validation, and API-driven component docs. Config supports value binding, size, margin, error correction level, colors, grid/module classes, fallback text, and safe styles.
- Added focused TDD coverage for direct component rendering, wrapper rendering, and payload validation. The RED run failed on the missing component; the GREEN focused run returned `TOTAL: 80 SUCCESS`.
- `npm audit --omit=dev` returned `found 0 vulnerabilities` after adding `uqr`; the full dev audit still reports existing toolchain advisories in Angular/Vite/Lighthouse-related dev dependencies.

## 2026-06-18 01:55 CT - Voluntary MFA auth-form handler fix

- Fixed the generic `authFormAction` handler allowlist so draft buttons configured with `authFormAction:startMfaEnrollment` and `authFormAction:verifyMfaEnrollment` actually call `AuthCustomFormService`.
- Root cause: the voluntary MFA actions existed in the auth service and draft config, but were missing from the event-handler allowlist, so clicks were ignored before loading/error state could be shown.
- Added focused regression coverage for both voluntary MFA enrollment actions. Red run reproduced `AuthCustomFormService.submit` never being called; green run returned `TOTAL: 8 SUCCESS`.

## 2026-06-18 01:20 CT - Voluntary TOTP enrollment contract

- Added generic Angular auth form actions for voluntary MFA enrollment from an authenticated auth-admin BFF session: `startMfaEnrollment` and `verifyMfaEnrollment`.
- Extended public runtime auth session metadata, TypeScript types, validators, and JSON Schema with safe same-origin `mfaEnrollStartPath`, `mfaEnrollVerifyPath`, and `mfaEnrollCsrfCookieName`.
- Documented the server-only contract: Cognito access tokens needed by Cognito software-token APIs stay behind a short-lived BFF enrollment record and are never returned to Angular.
- Focused validation passed locally: `AuthCustomFormService` Karma returned `TOTAL: 16 SUCCESS`; config validators returned `TOTAL: 68 SUCCESS`; Node site-config schema tests returned `pass 4`.

## 2026-06-18 00:21 CT - Config-driven Cognito MFA challenge flow

- Added Angular custom-auth support for Cognito `SOFTWARE_TOKEN_MFA` and `MFA_SETUP` challenge responses through generic `authFormAction` handlers, without returning raw Cognito challenge session data to browser state.
- Extended public runtime auth session metadata, validators, schemas, and registry tooling with same-origin challenge paths plus a challenge CSRF cookie name.
- Added generic Zoosite draft pages for `/verificar-acceso` and `/configurar-mfa`, excluded them from the sitemap, and kept the setup page from rendering the full `otpauth://` URI as visible text.
- Extended the server-only Auth Profile Registry contract to validate optional TOTP MFA policy and emit a deterministic provisioning operation for Cognito MFA configuration.
- Verification in this worktree before final release validation: focused Karma auth/custom-form, validators, runtime-config, and auth-admin client specs returned `TOTAL: 97 SUCCESS`; Node auth/schema/front-door contracts returned `38 pass`; API proxy unittest returned `Ran 116 tests ... OK`; auth-admin unittest returned `Ran 31 tests ... OK`.

## 2026-06-17 23:17 CT - Auth private-route hardening follow-up

- Fixed protected auth-admin BFF hard reload performance by bootstrapping Angular immediately when SSR produced an intentionally empty protected shell. The static SSR bootstrap delay remains only for routes with renderable SSR content.
- Added in-flight dedupe and a 10-second timeout to `AuthAdminClientService.me()` so the route guard and account data source do not duplicate `/auth/session/me` during first-load protected route validation.
- Added focused coverage for empty-shell detection, auth-admin client headers/CSRF across account and admin methods, sensitive-data-free success URLs, and protected SSR HTML that must not expose tokens, session cookies, CSRF cookies, tenant policy, or admin group policy.
- Repaired the `RuntimeService` spec `DomainResolverService` mock so broader app-shell/runtime suites include the storage-key dependency used by language/theme services.
- Validation passed locally: `npm run build`; focused Karma auth/custom/bootstrap specs (`TOTAL: 23 SUCCESS`); broader runtime/app-shell/auth/data-source specs (`TOTAL: 68 SUCCESS`); Node contract/SSR/front-door tests (`30 pass`); `npm audit --omit=dev` returned `found 0 vulnerabilities`; `git diff --check` had only LF/CRLF warnings.
- Security note: Cognito MFA was verified as `MfaConfiguration: OFF`, but required MFA was not enabled because the current custom BFF login returns `challenge-required` without a generic draft challenge UI. MFA enforcement remains a follow-up after auth-admin challenge endpoints and draft screens exist.

## 2026-06-17 21:21 CT - Auth-admin SSR robots guard

- Fixed Express SSR head decoration so protected auth-admin BFF routes that intentionally skip server-side redirects still emit `<meta name="robots" content="noindex,nofollow">`.
- Kept the rule generic for future drafts: SSR forces noindex for `routes[].auth.required`, explicit `sitemap.excludePaths`, and technical robots-disallow prefixes such as `/admin/`.
- Aligned server-side page SEO typing with existing page config usage by supporting `seo.robots` and `seo.canonical` through the shared local SEO type.
- Validation passed locally: `npm run build` and `node --test tools/tests/ssr-server.spec.mjs` returned `20 pass`.

## 2026-06-17 19:53 CT - Custom auth loading feedback

- Extended `generic-button` with `loadingLabel` and `loadingClasses`, visible default spinner styling, native disabled state, and `aria-busy` while `config.loading` is true.
- Kept custom-auth loading state generic: `authFormAction` continues to write `authForm.<action>.state = "loading"` before awaiting the auth Lambda, and drafts can bind button loading through `valueInstructions`.
- Added component/schema/validator coverage for button loading config and auth-form pending status before network resolution.
- Updated the Zoosite draft auth buttons for sign-in, sign-up, confirmation, resend-code, password recovery, password reset, and logout to show Spanish loading labels and prevent repeat submits while requests are pending.

## 2026-06-17 19:08 CT - Auth-admin remote profile direct route reload

- Fixed direct reloads of protected `runtime.authRemote` routes so the Angular guard can revalidate the HttpOnly BFF session before redirecting, even when the public remote auth profile has not been installed in memory yet.
- Auth-admin BFF requests now can derive `X-ZLP-Auth-Profile-Id` from `runtime.authRemote.authProfileId` during that first protected-route pass, while still deriving the draft domain from site config or `DomainResolverService`.
- Fixed the Express SSR protected-route redirect guard so auth-admin BFF drafts using `runtime.authRemote` or `runtime.auth.session.mode: "server-cookie"` are not redirected before Angular can call `/auth/session/me` with the HttpOnly session context.
- Added focused regression coverage for the auth-admin client headers and route guard authorization with only `runtime.authRemote` present.
- Verification passed before the SSR correction: focused Karma coverage for `auth-admin-client` and auth runtime state specs returned `TOTAL: 20 SUCCESS`; focused auth/runtime Karma block returned `TOTAL: 103 SUCCESS`; Node auth/schema contracts returned `21 pass`; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed and generated `main-FD5FDGCG.js`. Browser QA then showed production hard reload of `/admin/usuarios` still redirecting to `/acceso`, which localized the remaining bug to the SSR redirect guard.
- Verification after the SSR correction passed: `npm run build`; `node --test tools/tests/ssr-server.spec.mjs` returned `20 pass`; Node auth/schema contracts returned `21 pass`; `npm audit --omit=dev` returned `found 0 vulnerabilities`; focused auth/runtime Karma block returned `TOTAL: 103 SUCCESS`.

## 2026-06-17 18:49 CT - Auth-admin direct protected route reload

- Fixed the Angular auth-admin client so account/admin BFF requests resolve the draft domain from `DomainResolverService` when `siteConfig.domain` is not available yet during first-load protected-route checks.
- This targets direct reloads of server-cookie protected draft routes such as Zoosite `/admin/usuarios`, where navigation from `/mi-cuenta` worked but hard reload could redirect to `/acceso`.
- Cleaned up auth specs that temporarily override `document.cookie` so the full focused auth suite no longer depends on test execution order.
- Verification passed: focused `auth-admin-client` Karma spec returned `TOTAL: 3 SUCCESS`; focused auth/runtime Karma block returned `TOTAL: 101 SUCCESS`; Node auth/schema contracts returned `21 pass`; `npm run build` passed.

## 2026-06-17 CT - Auth-admin front-door sync tooling

- Added repeatable Traefik front-door tooling for the generic auth-admin BFF. The generated dynamic file routes only `/auth/session`, `/auth/session/*`, `/auth/admin`, and `/auth/admin/*` for the configured test and production hosts, adds the API Gateway stage prefix per environment, and preserves `/auth/callback` as an Angular-rendered route.
- Added Node contract coverage so future front-door changes do not regress into a broad `/auth/*` router.

## 2026-06-17 04:48 CT - Generic auth-admin account and user-management contract

- Added the Angular-side generic auth-admin client for same-origin server-cookie sessions, carrying `X-ZLP-Domain` and `X-ZLP-Auth-Profile-Id` on account/admin requests and using CSRF headers for mutating admin/logout actions.
- Added `auth-admin` runtime data sources and `authAdminAction` event handling so drafts can render `/mi-cuenta`, admin user lists, approval, group changes, suspend, and reactivate workflows through generic components.
- Extended `generic-card` with configurable actions and event instructions for loop-rendered admin cards.
- Extended the server-only Auth Profile Registry schema/tooling and public runtime schema to document safe `session`/`admin` path metadata while keeping admin groups, manageable groups, approval policy, tenants, and secrets server-only.
- Validation passed locally: focused Karma auth/admin suite returned `TOTAL: 108 SUCCESS`, Node auth/schema contracts returned `29 pass`, API proxy unittest returned `Ran 112 tests ... OK`, auth-admin unittest returned `Ran 20 tests ... OK`, `npm run build` passed, `npm audit --omit=dev` returned `found 0 vulnerabilities`, and both serverless templates passed `sam validate --lint`.

## 2026-06-17 03:31 CT - Custom auth flow production promotion

- Promoted the custom auth flow polish through app PR #45 (`codex/zoosite-auth-flow-polish` -> `dev`), PR #46 (`dev` -> `test`), and PR #47 (`test` -> `main`). Production app merge commit: `b72d2e240126a4947164e399d6585ce9cf75fb18`.
- Remote testing QA passed for Zoosite auth routes on desktop and mobile before production promotion. `https://test.zoolandingpage.com.mx` served `main-5YHFVBVA.js` and `/acceso`, `/registro`, `/confirmar-cuenta`, `/recuperar-contrasena`, and `/cambiar-contrasena` had Spanish copy, no horizontal overflow, no console errors, visible disabled submit states, success banners, confirmation/reset instructions, and the symbol password rule where relevant.
- Production verification passed after cache convergence: 25 HTTP samples across the five production auth routes all served `main-5YHFVBVA.js`, and browser QA on `https://zoositioweb.com.mx` passed the same five routes plus both login success states across desktop and mobile with no console errors, no horizontal overflow, no old English required-message copy, and disabled buttons computing `cursor: not-allowed` with opacity `0.45`.

## 2026-06-17 03:05 CT - Custom auth success-flow navigation and query conditions

- Added a generic `queryEq` condition handler so drafts can show display-only banners from safe query flags such as `authStatus=account-confirmed`.
- Updated custom auth form navigation so successful signup routes to account confirmation, password recovery routes to password-code entry, and successful confirmation/reset returns to login with an `authStatus` success flag while preserving safe preview params.
- Kept user email out of post-auth URLs; `authStatus` is UX-only and does not authorize account actions.
- Added configurable `disabledClasses` to `generic-button` so drafts can make disabled auth submits visibly disabled while preserving the default disabled styling for existing drafts.
- Validation passed: focused Karma suite for custom auth navigation, generic button disabled classes, and query conditions returned `TOTAL: 26 SUCCESS`.

## 2026-06-17 02:16 CT - Auth test stack routing and environment claim contract

- Added a host-aware auth endpoint resolver so `test.zoolandingpage.com.mx` sends custom auth form calls and remote auth resolution to the `zoolanding-api-proxy-test` stack.
- Added `apiProxyTestUrl` to Angular environment config with `https://11zpm6wug2.execute-api.us-east-1.amazonaws.com/Prod`, the stable stage path for the separate testing API proxy stack.
- Extended the server-only auth profile registry schema/tooling to accept optional `environmentClaim` values such as `custom:zoolanding_env`.
- Updated auth registry tests to cover `setEnvironmentClaim`, environment-scoped JWT policy checks, and Zoosite fixture validation.
- Verification passed: focused Karma auth/runtime tests returned `TOTAL: 17 SUCCESS`; Node auth registry/schema tests returned `19 pass`; `npm run build` passed.

## 2026-06-17 18:09 CT - Server-cookie route guard revalidation

- Updated protected draft route access so profiles with `runtime.auth.session.mode: "server-cookie"` revalidate browser sessions against the auth-admin BFF `/auth/session/me` before redirecting or rendering protected routes.
- This fixes custom-auth users who can load `/mi-cuenta` through the BFF session but were redirected from `/admin/*` because the older route guard only looked at editable public auth metadata in `sessionStorage`.
- Verification passed: focused Karma auth specs returned `TOTAL: 27 SUCCESS`; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed.

## 2026-06-16 21:58 CT - Shared app Docker context excludes local drafts

- Excluded local `drafts/` nested repos from the shared app Docker build context so SSR metadata, browser icons, and the boot curtain resolve from the published runtime API instead of stale local draft files.
- Fixed Express SSR auxiliary runtime reads so shared testing preview requests pass `environment=test` for site config, page config, route status, redirects, robots, sitemap, JSON-LD, icons, and the boot curtain.
- This followed Grupo Astra Legal testing QA where the visible draft content came from the current test deploy, but server-injected JSON-LD and the boot curtain still used old production Despacho config.
- Validation passed: `npm run build`, `node --test tools/tests/ssr-server.spec.mjs`, and `git diff --check` with only LF/CRLF warnings.

## 2026-06-16 21:34 CT - SSR status for dynamic draft auth routes

- Fixed Express SSR 404 detection for shared testing draft routes whose cached root site route map is incomplete but whose route-specific runtime bundle exists.
- The server now checks the requested route runtime bundle before returning a not-found status, while still preserving 404 status when the bundle resolves to the configured 404 route or not-found page ID.
- Shared testing preview status checks now use a published `environments.test.aliases` domain when present, so `test.zoolandingpage.com.mx?...draftDomain=...` does not calculate route status against the production published pointer.
- Added focused Node coverage for both cases and verified the built SSR server locally: `/registro`, `/cambiar-contrasena`, and `/acceso` return 200 for Zoosite preview context, while `/no-existe` still returns 404.

## 2026-06-16 18:24 CT - Generic auth form validation UX

- Added generic `matchesField` interaction-scope validation so drafts can compare fields such as password confirmation without draft-specific Angular.
- Added optional generic-input validation checklists that stay visible and update each rule as users type.
- Added `disabledWhenInvalidScope` to generic buttons so draft-auth submits can remain disabled until all scoped fields are valid.
- Added a lightweight app-shell route transition status bar for client-side draft page transitions.
- Added text-input `change`/deferred `beforeinput` fallback handling so custom auth validation updates reliably after remote SSR hydration without fighting the browser's live input value.
- Added blur-time text input synchronization so uncontrolled rendered auth fields update scoped validation before touched/submit state is evaluated.
- Added an SSR bootstrap gate for form-heavy pages so static auth forms hydrate immediately and keep the loading curtain until Angular event handlers are ready.
- Added a route-transition bridge for the app's custom same-origin navigation helper so draft links that preserve preview query params show the existing loading bar while runtime config refreshes.
- Updated component schema/validators and focused specs. Validation passed: focused Karma suite (`TOTAL: 124 SUCCESS`) and `node --test tools/tests/components-schema.spec.mjs`.

## 2026-06-16 16:32 CT - Custom auth logout routing

- Added `authFormAction:logout` support so custom draft-auth forms clear local public session metadata and navigate same-origin without calling Cognito Hosted UI.
- Kept Cognito Managed Login logout safer for shared testing by omitting the active draft `lang` query parameter from `logout_uri`; Cognito app clients require exact allowlisted logout URLs.
- Updated Zoosite's account-page logout button to use the custom form logout path, preventing the user-visible Cognito `/error` page after signing out from `/mi-cuenta` in the Spanish test preview.

## 2026-06-16 11:49 CT - Optional custom auth forms foundation

- Added `AuthCustomFormService` and the generic `authFormAction` handler for draft-auth forms assembled from generic components instead of draft-specific Angular code.
- Supported custom auth actions: `signin`, `signup`, `confirmSignup`, `resendConfirmation`, `forgotPassword`, and `confirmForgotPassword`, mapped to serverless `/auth/*` endpoints and carrying only public fields plus the current language.
- Added a generic password visibility toggle to `generic-input` plus schema support so draft auth forms can offer show/hide password without draft-specific Angular code.
- Kept custom signup/reset UX local and safe: `confirmPassword` is validated in the browser but never sent, and browser-supplied tenant/group/policy-looking fields are dropped before the request.
- Custom signin installs only sanitized public session metadata returned by the backend, then redirects to `postLoginPath` or the account route while preserving safe testing preview params. It does not store tokens or replace backend JWT authorization for protected APIs.
- Extended the server-only auth registry schema and validator with optional `customAuth.signin`, `customAuth.signup`, and `customAuth.passwordRecovery` policy, including server-side default group validation against `allowedGroups`.
- Validation passed: focused custom auth Karma specs (`TOTAL: 9 SUCCESS`), `node --test tools/tests/auth-profile-registry.spec.mjs`, and `node --test tools/tests/server-config-schema.spec.mjs`.

## 2026-06-16 11:07 CT - Cognito Managed Login language propagation

- Added the active draft language to Cognito Managed Login, sign-up, and password-recovery URL generation through the safe `lang` query parameter so Spanish-first drafts can request Cognito's Spanish-hosted UI where supported.
- Kept the change optional and browser-safe: invalid locale values are omitted, drafts that do not use auth are unaffected, and token/session handling remains unchanged.
- Documented the updated auth architecture direction: separate Cognito user pools per paying client or strong draft boundary by default, `custom:tenant_id` as a secondary backend authorization/dashboards scope, and per-profile choice between Cognito Managed Login and future custom generic auth forms.
- Validation passed: focused auth Karma specs (`TOTAL: 19 SUCCESS`), `git diff --check` with only LF/CRLF warnings, and `npm run build`.

## 2026-06-15 19:17 CT - Generic Cognito auth actions and Zoosite login surface

- Added reusable browser auth flow support for Cognito Managed Login: generic `authAction` event handling, PKCE login/signup/forgot-password URL generation, Cognito logout URL generation, authorization-code exchange, and `/auth/callback` processing into minimal public session metadata.
- Kept token material out of Angular auth state and browser session persistence. Callback handling validates public ID-token issuer/audience/expiry claims for client-side UX only; backend-protected APIs must still verify JWT signatures and tenant/group policy server-side.
- Updated Zoosite draft auth pages with generic components: `/acceso` now exposes login, create-account, and password-recovery actions; `/auth/callback` provides a retry action; `/mi-cuenta` contains a minimal protected account shell and logout action.
- Aligned the local Zoosite server-only auth registry with the real active Cognito profile public metadata while keeping it secret-free. Google/Facebook remain optional IdPs and were not required for this Cognito-only test path.
- Updated Angular packages from `22.0.0` to `22.0.1` after `npm audit --omit=dev` reported Angular 22.0.0 advisories; the production audit now reports zero vulnerabilities.
- Fixed local SSR protected-route redirects so `127.0.0.1:{port}` keeps the port when redirecting unauthenticated preview requests to the login path.
- Preserved shared-test preview context through Cognito by storing the exact same-origin `redirect_uri` in the PKCE transaction, reusing it in the token exchange, and carrying safe preview params into callback/logout URLs when auth starts from `test.zoolandingpage.com.mx`; logout URL language handling was later narrowed because Cognito logout allowlists are exact-match.
- Promoted the Zoosite Cognito auth surface to testing: the Zoosite draft `test` deployment completed successfully, the shared app `test` service was rebuilt from `a8db235`, and `https://test.zoolandingpage.com.mx/acceso?draftDomain=zoositioweb.com.mx&debugWorkspace=false` served `main-OUYUADGP.js` with the generic auth actions visible.
- Added the shared-test Cognito callback/logout URLs to the Zoosite public app client and verified the client still has Hosted UI OAuth enabled, code flow, `openid email profile` scopes, `COGNITO` as the only IdP, and no client secret. The first update omitted unchanged OAuth fields and temporarily reset Hosted UI settings; the client was immediately restored and rechecked.
- Recovered a front-door availability issue before testing: the Dokploy/Traefik EC2 instance had failed instance reachability and SSM was disconnected, then returned healthy after reboot. While deploying, tokenized Git remotes in Dokploy checkouts were replaced with non-secret GitHub URLs; any old token previously embedded there should be rotated or revoked outside the repo.
- Hardened Cognito callback handling after the integrated-browser test stayed on `Validando acceso`: PKCE transactions now verify persistence before leaving the app, fall back to a short-lived same-site cookie when `sessionStorage` is blocked, catch token-exchange exceptions, and redirect failed callbacks back to the login path so authorization codes are removed from the URL.

## 2026-06-09 04:06 CT - SSR protected auth redirect gate

- Added an Express SSR front-door auth redirect gate so `GET`/`HEAD` requests for published routes with `routes[].auth.required = true` return `302` to `routes[].auth.redirectTo` or `runtime.auth.loginPath` before Angular SSR renders a blank protected shell.
- The redirect target must remain a safe same-origin path and preserves only preview-critical query params (`draftDomain`, `debugWorkspace`, `lang`) so shared test previews such as Zoosite keep their draft context without carrying ad or sensitive query params into login routes.
- Added SSR smoke coverage for `test.zoolandingpage.com.mx/mi-cuenta?draftDomain=...` proving the server redirects to same-origin `/acceso` with `Cache-Control: no-store`.

## 2026-06-09 03:39 CT - Draft deploy server-only auth packaging fix

- Fixed the hub pack/status tooling and reusable draft repo deploy template so `server/auth-profile-registry.json` deploys as `server-auth-profile-registry` and `server/integrations.json` deploys as `server-integrations` without a `pageId`.
- Added regression coverage in `tools/tests/config-draft-sync.spec.mjs`, `tools/tests/draft-upload-status.spec.mjs`, and `tools/tests/draft-repo-preflight.spec.mjs` so manual packaging, upload status checks, and future generated draft repos do not classify server-only auth/integration policy as page payloads.
- Applied the same deploy-script fix to the local Zoosite nested draft repo before promoting its plan-only auth pilot, because its workflow uses the repo-local `tools/deploy-draft.mjs`.

## 2026-06-09 01:36 CT - Zoosite nested draft source separation

- Resolved the dirty nested `drafts/zoositioweb.com.mx` repo boundary by removing the sanitized Zoosite pilot files from the app repo index instead of vendoring the real draft inside the hub.
- Moved app-level Zoosite auth pilot assertions, including the plan-only server registry fixture, to `tools/tests/fixtures/zoosite-auth-pilot/` so schema/contract tests no longer depend on a locally cloned draft repo.
- Restored the app repo draft ignore rules so registered domain drafts remain local nested repos under `drafts/` rather than hub-tracked source.
- The actual Zoosite auth pilot content belongs in the `draft-zoositioweb-com-mx` repo, preserving the full commercial draft content instead of committing the temporary minimal overlay.

## 2026-06-09 00:43 CT - Local remote auth QA harness

- Added an Angular local-auth dev script and proxy config so `runtime.authRemote.endpoint = "/auth/runtime-config"` can be exercised against a real local `zoolanding-api-proxy` HTTP harness instead of falling through to Angular dev-server 404s.
- Documented the local browser QA flow: Angular proxies `/auth/runtime-config` to the API proxy, and the API proxy reads a server-only local auth registry from explicit dry-run env configuration.
- Added focused Angular coverage proving valid disabled public auth metadata from a non-active remote profile is installed in memory, leaves auth disabled, and removes `authRemote` after hydration.

## 2026-06-09 00:26 CT - Zoosite remote auth pilot

- Added a sanitized Zoosite pilot draft under `drafts/zoositioweb.com.mx` in the optional auth worktree without copying the source draft repo, `.git`, local agent state, CV/photos, findings, logs, reports, PDFs, or private docs.
- Configured public `site-config.json.runtime.authRemote` with `enabled: true`, `authProfileId: "staff"`, and `endpoint: "/auth/runtime-config"`; no static `runtime.auth`, `credentialRef`, `access`, `auth`, client secrets, tokens, or secret-store URI prefixes were added to browser config.
- Added placeholder-only auth routes `/acceso`, `/auth/callback`, and protected `/mi-cuenta` with `routes[].auth.required = true`, `redirectTo = "/acceso"`, and groups `zoosite-client` / `zoosite-admin`; pilot pages use `noindex,nofollow` and contain no private data or protected actions.
- Added Zoosite `server/auth-profile-registry.json` as a server-only plan-only contract with tenant `zoosite`, `status: "planned"`, callback/logout allowlists for `zoositioweb.com.mx` and `zoositioweb.com`, and Google/Facebook secret references under `/zoolanding/auth/zoosite/staff/*`.
- Narrowed the draft ignore allowlist so only the reviewed sanitized Zoosite pilot files are trackable; future local-only folders, `.env` files, and extra `server/*` files remain ignored unless explicitly reviewed.
- Added Node regression coverage for the Zoosite public authRemote payload, protected route contract, registry validation, and plan-only provisioning output. No deploy, AWS call, Cognito creation, secret generation, or commit was performed from this worktree.

## 2026-06-08 22:57 CT - Remote auth runtime resolution

- Added optional remote auth runtime resolution for draft configs: public `runtime.authRemote` references can resolve safe `runtime.auth` from `/auth/runtime-config` by sending only `{ domain, authProfileId }`.
- The Angular runtime validates the backend `auth` response with the same public auth validator before installing it into in-memory site runtime config, removes `authRemote` after hydration to avoid ambiguity, and keeps auth disabled on invalid responses or request failures.
- Audited and hardened remote auth hydration so the request cache de-duplicates only in-flight requests. Freshly reloaded `siteConfig` instances are rehydrated again, profile mismatches are rejected, and delayed responses cannot install auth into a different domain/profile/endpoint context.
- Validation now rejects public configs that include both static `runtime.auth` and `runtime.authRemote`, and rejects `access`, upstream `auth`, `credentialRef`, tokens, and client secrets in public auth config surfaces.

## 2026-06-08 22:30 CT - Server-only auth registry and integration access packaging

- Aligned the optional auth contract so drafts can package `server/auth-profile-registry.json` and `server/integrations.json` as official authoring file kinds without exposing those files through local static `/drafts` serving or browser runtime payloads.
- Updated the server-only Auth Profile Registry shape around `authProfileId`, `tenantId`, `status`, `issuer`, `hostedUiDomain`, `clientId`, `audiences`, callback/logout URL allowlists, same-origin login/logout paths, tenant/group claims, allowed groups, and `socialIdpSecretRefs` secret-store references.
- Added `server/integrations.json.access` validation for protected sources/actions and kept it separate from upstream credential `auth`, which remains reserved for `bearer`, `api-key-header`, and `oauth2-client-credentials` policies with secret-store `credentialRef`.
- Hardened public runtime validators so `site-config.json.runtime.dataSources` and `runtime.apiActions` reject server-only `access` and credential `auth` blocks.
- Added schemas and focused tests for server-only auth registry, server integrations, authoring package kinds, package/status tooling, and auth handler compatibility.
- No deploy, AWS call, Cognito creation, or secret generation was performed from this worktree.

## 2026-06-08 21:05 CT - Generic auth service handler scaffold

- Added `tools/auth-service-handlers.mjs`, an offline Lambda-style scaffold for a future generic Auth service that resolves safe public `runtime.auth`, gates provisioning plans behind a trusted server-side caller check, and evaluates API Gateway authorizer decisions through an injected JWT verifier.
- Added `tools/tests/auth-service-handlers.spec.mjs` covering public runtime metadata shaping, rejection of browser-supplied secret/policy fields, default-denied provisioning plans, trusted plan-only provisioning output, and JWT authorizer allow/deny behavior without echoing bearer tokens.
- Kept the implementation dependency-injected and side-effect free: no AWS calls, no Cognito creation, no CDK deploy, no secrets, and no Zoosite-specific config.

## 2026-06-08 20:21 CT - Server-only Auth Profile Registry foundation

- Added an offline, server-only Auth Profile Registry contract in `tools/auth-profile-registry.mjs` for resolving `domain + authProfileId` to tenant-scoped Cognito operational policy without calling AWS or creating Cognito resources.
- Added plan-only Cognito provisioning output for user pool, user pool client, Hosted UI domain, and social provider setup using secret references instead of raw credentials.
- Added backend authorization preparation helpers for JWT issuer/audience/group enforcement. The policy evaluator expects already-verified claims and does not replace cryptographic JWT verification.
- Documented the boundary in `docs/api-driven-config/17-auth-profile-registry.md`: public draft payloads may carry only safe auth metadata, while tenant ownership, provisioning state, callback allowlists, group policy, social IdP references, and backend authorization policy stay server-only.
- Added focused Node tests in `tools/tests/auth-profile-registry.spec.mjs` covering tenant isolation, public payload shaping, active-only runtime enablement, unsafe registry rejection, provisioning plan output, Cognito JWKS URI derivation, and claim policy decisions.
- No deploy, AWS call, Cognito creation, or Zoosite-specific draft config was performed from this worktree.

## 2026-06-08 18:59 CT - Optional draft auth runtime recovery

- Ported the valid optional draft auth foundation from snapshot `C:\Users\lince\.codex\worktrees\368b\zoolandingpage` into a real Angular 22 workspace without copying `node_modules`, `dist`, logs, reports, package-lock, or build artifacts.
- Added config-driven `runtime.auth` and per-route `routes[].auth` validation, Angular signal auth runtime evaluation, Cognito Hosted UI URL construction, and browser session metadata storage that strips profile display/email and token/secret-like fields.
- Kept auth disabled by omission for drafts with no `runtime.auth`; protected route redirects now prefer `routes[].auth.redirectTo`, then `runtime.auth.loginPath`, then no redirect, and never use `logoutPath` as a login fallback.
- Hardened public auth config so HTTPS issuer/Hosted UI URLs reject userinfo, backslashes, whitespace/control characters, and non-HTTPS schemes; same-origin path fields reject external URLs, protocol-relative URLs, `javascript:`, `data:`, backslashes, whitespace/control characters, and paths missing the leading `/`.
- Documented the current SSR limitation: without a server-side redirect/status hook, protected routes clear protected content rather than rendering private data, and pilot protected pages should stay free of private actions/data and use `noindex,nofollow` until server handling exists.

## 2026-06-26 17:35 CT - Generic table action links

- Added `generic-table` support for row action `hrefTemplate` values so draft-composed admin tables can render safe same-origin action links from row IDs while preserving the existing row-action event payload.
- Kept `hrefTemplate` restricted to internal paths rooted at `/` and rejected protocol-relative/external targets before rendering or navigating.
- Updated the component schema, config validator, docs, and focused tests for the new row-action contract.
- Validation passed: `node --test tools/tests/components-schema.spec.mjs` and focused Karma for `generic-table.component.spec.ts` plus `config-payload.validators.spec.ts` returned `TOTAL: 82 SUCCESS`.

## 2026-06-26 22:10 CT - Protected admin SSR bootstrap

- Fixed protected draft SSR shells so routes with `route.auth.required` add no-cache headers and mark `<app-root data-zlp-protected-shell="true">`.
- Updated the browser bootstrap path so protected shells skip the production static-content delay instead of releasing the boot curtain from shared chrome before Angular validates the server-cookie session through the BFF.
- This prevents `/admin/blog` and similar protected routes from appearing blank after navigating from public pages or hard reloading a route that intentionally SSRs without private admin content.
- Validation passed: `npm run build`, `node --test tools/tests/ssr-server.spec.mjs`, focused Karma for `static-interactive-controls.utility.spec.ts` and `app-shell.component.spec.ts`, and content-hub admin contract tests.

## 2026-06-28 02:25 CT - Content-hub public runtime and article metadata bridge

- Wired Zoosite admin article detail data sources and action links to use the `/admin/blog/articulos/:id/...` route parameter instead of duplicating `articleId` query strings for editor, preview, SEO, versions, revisions, and editor-scoped assets.
- Added contract coverage that keeps Zoosite content-hub admin links clean and verifies route-param detail bindings.
- Extended `zoolanding-config-runtime-read` locally so published content-hub article paths can load safe S3 bundle JSON into public runtime variables, SEO, i18n, and component payloads, and so unknown content-hub article paths render configured 404 instead of an empty article shell.
- Extended `zoolanding-content-hub` locally so `createArticle` persists normalized `slug`, `categorySlug`, deduplicated tags, taxonomy metadata, and default `/blog/{categorySlug}/{articleSlug}` paths, and `publish` reuses the stored path when no explicit path is provided.
- Validation passed: Zoosite Node contract tests `20/20`, Angular `npm run build`, runtime-read unit tests `17/17` plus `sam validate`, content-hub unit tests `11/11` plus `sam validate`, and `git diff --check` in all touched repos returned only LF/CRLF warnings. Zoosite public-safety audit still blocks while the nested draft is dirty; it reported no current or history secret findings, but existing review findings for public email/phone copy remain.

## 2026-06-28 04:12 CT - Content-hub public article 404 hardening

- Added shared content-hub public route helpers so Angular runtime, SSR, and analytics can distinguish article-looking paths from actually published public articles.
- Unknown or draft-only paths that match `runtime.contentHubs[].articlePathPattern` now resolve to the configured not-found page instead of rendering the article shell with empty `currentArticle` data.
- SSR skips content-hub article SEO enrichment on 404 responses so not-found pages do not receive article `BlogPosting` metadata or article canonical links by path pattern alone.
- Runtime analytics no longer emits `blog_view` for article-looking paths unless a matching published `publicArticles` entry exists.
- Validation passed: focused Karma (`TOTAL: 50 SUCCESS`), `npm run build` passed with the existing Quill `quill-delta` CommonJS warning, `node --test tools/tests/ssr-server.spec.mjs` passed (`21` pass, `0` fail), and the existing `zoolanding-config-runtime-read` unit test `RuntimeHandlerTest.test_missing_content_hub_article_path_renders_configured_404` passed.

## 2026-06-27 00:31 CT - Generic blog admin runtime primitives

- Added `routeParam` and `routeParamOr` value handlers so draft-composed detail pages can hydrate IDs from parameterized routes such as `/admin/blog/articulos/:id/editor` without duplicating the ID in query strings or editable inputs.
- Localized `generic-table` Material paginator labels through the active language service and added `hideWhenSinglePage` to hide misleading paginator controls when the current result set fits on one page.
- Extended `generic-input` and component schema support for `date`, `time`, and `datetime-local` input types, allowing draft-composed scheduling controls without a feature-specific Angular component.
- Validation passed: `node --test tools/tests/components-schema.spec.mjs tools/tests/content-hub-admin-pages.spec.mjs`, focused Karma returned `TOTAL: 104 SUCCESS`, `npm audit --omit=dev` returned `found 0 vulnerabilities`, and `npm run build` passed with the existing Quill `quill-delta` CommonJS warning.

## 2026-06-28 05:08 CT - Content hub SSR preview links and overlay guard

- Fixed shared-host SSR navigation for draft-composed cards, links, and tables so `/blog` links on `test.zoolandingpage.com.mx?draftDomain=...` preserve `draftDomain`, `debugWorkspace`, and `lang` before client hydration.
- Added SSR regression coverage for Zoosite content-hub public list/category/article links under the shared test host.
- Hardened generic tooltip, search-box, dropdown, and modal overlay usage so CDK Overlay/focus-trap services are instantiated only in the browser, avoiding `window is not defined` crashes during server-side rendering.
- Validation passed: focused Karma suites for overlay primitives returned `TOTAL: 16 SUCCESS`; focused card/link/table suites returned `TOTAL: 29 SUCCESS`; `npm run build` passed with the existing Quill `quill-delta` CommonJS warning; `node --test tools/tests/ssr-server.spec.mjs` returned `pass 21, fail 0`.

## 2026-06-30 07:27 CT - Zoosite blog selected-state copy guard

- Added `tools/tests/content-hub-admin-pages.spec.mjs` coverage so `/admin/blog` cannot regress to vague selected-state fallback copy such as `Artículo seleccionado pendiente` or `Versión seleccionada pendiente`. Focused validation passed: `node --test tools/tests/content-hub-admin-pages.spec.mjs` returned 19 pass.

## 2026-06-30 07:31 CT - Zoosite blog orphan action guard

- Updated the content-hub admin page contract test so `/admin/blog/articulos` must not expose the dead list-level `Validar selección` button. Article mutations that need IDs must stay bound to row/detail context instead of global buttons without payload.

## 2026-06-30 08:00 CT - Content hub public search route projection

- Fixed public content-hub SEO reads so `/content-hub-search.json`, sitemap, and feed load the runtime bundle for the blog route instead of the root route. Browser QA on testing found `/blog` rendering two public articles while search JSON exposed only one stale root article. The SSR regression now asserts shared testing content-hub reads request `path=/blog`.

## 2026-06-30 08:42 CT - Content hub public index collection shape

- Fixed content-hub public index handling so SSR search, sitemap, feed, Angular bootstrap, route matching, validators, and schema accept `publicArticles` and `publicTaxonomy` as either arrays or `{ "items": [...] }` collection objects. Testing showed runtime-read for the Zoosite `/blog` route returning two articles while `/content-hub-search.json` exposed only the seed article because SSR expected array-only indexes.
- Added coverage in SSR, site-config schema, runtime validation, config bootstrap, and public route matching tests. This is a release gate for public blog discovery because route-specific runtime projections can use the collection object shape.

## 2026-06-30 09:41 CT - Content hub public index language propagation

- Promoted `zoolanding-config-runtime-read` fixes through PR #11 (`codex/runtime-read-content-hub-pagination` -> `dev`) and PR #12 (`dev` -> `test`); both GitHub deploy workflows completed successfully. Direct runtime probes confirmed `siteConfig.runtime.contentHubs[0].publicArticles` for Zoosite testing includes `art_1782600317_qa-e2e-content-hub-20260627224423` plus the seed article when `lang=es`.
- Fixed Express SSR auxiliary content-hub reads so runtime bundle URLs include the effective language and runtime site-config cache keys include language. When no language is passed, SSR now refetches the public hub route with the runtime `site.i18n.defaultLanguage` before falling back to no-lang results.
- Local verification passed: `npm run build` completed with the existing `quill-delta` CommonJS warning, `node --test tools/tests/ssr-server.spec.mjs` returned `23` pass and `0` fail, `npx tsc -p tsconfig.app.json --noEmit` passed, and a local production-server probe returned search count `2` with the QA article present in search, sitemap, and feed.

## 2026-06-08 04:52 CT - Karma/Chrome intermittent test isolation

- Fixed an intermittent Karma/Chrome failure where component specs could open or close Angora-backed controls, schedule a real `AngoraCombosService.scheduleCssCreate(...)` timer, and let the real `NgxAngoraService.cssCreate()` emit library `console.error` output during a later spec.
- Added a global no-op `NgxAngoraService` provider in `src/test-providers.ts` so ordinary Angular specs do not load the runtime Angora CSS generator unless they intentionally override the provider.
- Added a `test:zoneless-runner` regression check that fails if the global Karma provider stops isolating the real Angora CSS generator.
- Added an `AngoraCombosService` mock to `generic-dropdown.component.spec.ts` and asserted the dropdown still requests a CSS refresh when opening the overlay.
- Updated the AnalyticsService spec URL helper to call the captured native `History.prototype.replaceState` directly, avoiding cross-spec contamination from history spies when the full suite changes `window.history` methods.
- Validation passed: focused `generic-dropdown.component.spec.ts` (`TOTAL: 6 SUCCESS`), focused `analytics.service.spec.ts` (`TOTAL: 19 SUCCESS`), focused `generic-input.component.spec.ts` (`TOTAL: 11 SUCCESS`), `npm run test:zoneless-runner` (`pass 5`), three consecutive full Karma runs (`TOTAL: 468 SUCCESS` each), `npm run build`, and `npm run ssr:smoke` (`pass 16`).
- Promoted through PR #28 (`codex/karma-chrome-test-isolation` -> `dev`, merge `37ec3332dd777caa6c7947f20e5e88b0c002da39`), PR #29 (`dev` -> `test`, merge `f4d0894332c2031014c7d4f5a50524c1600fa44c`), and PR #30 (`test` -> `main`, merge `9be5ac405798c0a8ff4775a0d83e6f864e02ab16`).
- Testing audit passed for the shared test host across the 11 registry drafts in desktop/mobile (`checked: 22`, `failures: []`), and `tools/ops/public-site-health-check.mjs --hosts=test.zoolandingpage.com.mx` returned `"ok": true` at `2026-06-08 05:18:15` CT.
- Production public health passed for the live production hosts and Zoosite campaign aliases (`"ok": true`, `failed: []` at `2026-06-08 05:22:08` CT), including direct runtime alias assertions for `sitiosweb.zoolandingpage.com.mx`, `quierounsitioweb.zoolandingpage.com.mx`, and `crearpaginaweb.zoolandingpage.com.mx` resolving to `zoositioweb.com.mx`.
- Production browser QA passed for the 9 currently resolvable public hosts in desktop/mobile (`checked: 18`, `failures: []`). The canonical registry domains `alecfest-voliii.com`, `despacholegalastralex.com`, `agent-guide-claude-codex.com`, `pamelabetancourt.com`, and `robertorodriguezrodriguez.com.mx` still returned DNS `ERR_NAME_NOT_RESOLVED` from the local machine and were not counted as app regressions.

## 2026-06-08 04:09 CT - Runtime alias health assertions

- Added strict runtime alias assertions to `tools/ops/public-site-health-check.mjs` through the `--expected-runtime-domains=<host>=<domain>` option so public health checks can fail when a campaign alias returns `runtime.ok=true` but resolves to the wrong canonical draft or omits `metadata.resolvedAlias`.
- Included `tools/tests/ops-public-site-health-check.spec.mjs` in `npm run test:ops-front-door` and added coverage for the missing-`resolvedAlias` case.
- Rechecked the three Zoosite campaign aliases after runtime metadata/cache convergence. The direct runtime API returned `domain: "zoositioweb.com.mx"` with matching `resolvedAlias` values for `sitiosweb.zoolandingpage.com.mx`, `quierounsitioweb.zoolandingpage.com.mx`, and `crearpaginaweb.zoolandingpage.com.mx`.

## 2026-06-07 22:25 CT - Angular 22 and signal state foundation

- Updated the app dependency baseline to Angular 22.0.x, TypeScript 6.0.x, and Node 24.15.0 for local migration commands.
- Removed the temporary NgRx 21 foundation after verifying its packages declared Angular 21 peer dependencies and no Angular 22-compatible NgRx release was available during migration.
- Added an app-level Angular signal state foundation for future authentication, draft tenant/profile state, public blog summaries, and transient blog editor state without adding Cognito resources, AWS resources, secrets, draft-specific content, or deploy changes.
- Kept config-driven rendering in the existing `ConfigStoreService` and `VariableStoreService` signal stores so `DraftRuntimeService`, `ConfigBootstrapService`, `RuntimeService`, and runtime data source contracts continue to own draft payload flow.
- Added SSR-safe auth browser storage that persists only minimal non-secret session metadata.
- Adopted Angular 22-era signal APIs in the state foundation through Angular signal facades and `@angular/forms/signals` for blog editor form state.
- Updated `zoolandingpage.com.mx` and `sulandingpage.com.mx` draft alias overrides so GA4-enabled public aliases render without canonical redirects while still emitting canonical links to the primary `.com.mx` origins.
- Validation remained local-only at this point: `npm ci`, `npm audit`, focused Karma suites, `npm run build`, `npm run ssr:smoke`, draft tooling tests, `npm run test:draft-context`, and local draft smoke for 90 routes across 11 drafts and 2 viewports passed. No deploy was run from this worktree yet.
- Promoted the Angular 22 app branch to the shared `test` branch through PR #19. Promoted the draft alias changes to draft `test` through Sulanding PRs #11 and #12 and Zoolanding PR #9; the related draft test deploy workflows completed successfully.
- Test verification after deploy: `https://test.zoolandingpage.com.mx` served `main-MWH3SAC2.js`; remote shared-test draft smoke passed 90 routes across 11 drafts and 2 viewports with zero local failures.
- Production was not promoted. Public alias health failed at 2026-06-07 23:46 CT with 96 checks, 38 failures: several configured test aliases returned DNS `ENOTFOUND`, and several reachable aliases had AAAA records while `--fail-on-aaaa` was enabled.

## 2026-06-06 20:33 CT - Shared testing draft preview redirect fix

- Updated Express SSR front-door redirects so `https://test.zoolandingpage.com.mx/?draftDomain=...` stays on the shared testing host instead of redirecting canonical-host drafts such as `erosbarajas.com`, `sulandingpage.com.mx`, and `zoositioweb.com.mx` to their public domains.
- Tightened the SSR smoke test for the shared testing preview path by making redirects manual and asserting there is no `Location` header.
- Published commit `1327e62` to `test`.
- Validation: `npm run build` passed, `node --test tools/tests/ssr-server.spec.mjs` passed 14/14, remote curl checks kept all 11 published draft domains on `test.zoolandingpage.com.mx`, and remote Chrome headless checks confirmed each draft rendered a `<main>` with no boot curtain, no 404 text, no horizontal overflow, and no console errors. The only observed failed requests were non-render-blocking Google Analytics `g/collect` beacons returning `net::ERR_ABORTED`.

## 2026-06-04 18:36 CT - Static SSR boot curtain contrast gate

- Found the remaining Eros first-open production flash in `src/main.ts`: `releaseBootCurtainForStaticSsrContent()` could add `zlp-boot-curtain--leaving` after static selector coverage passed, before Angular runtime and `LoadingCurtainService` could validate final Angora theme colors.
- Added `hasReadableCriticalTextContrast(...)` so the static SSR release path keeps the boot curtain active when critical visible text hooks (`sectionTitle`, `sectionSubtitle`, `heroCaption`) still have low contrast against their effective background.
- Validation passed: focused Karma coverage for static style coverage, critical text contrast, Angora combos, runtime readiness, and loading curtain services (`52 SUCCESS`), plus `npm run build` generating local bundle `main-2ODLGFJC.js`.
- Production deploy verified at 2026-06-04 18:40:53 CT when `https://erosbarajas.com/` began serving `main-2ODLGFJC.js`. Three production desktop/mobile QA passes reported zero exposed stale-color samples, final `sectionTitle` color `rgb(32, 23, 18)`, and zero relevant console, failed-request, or bad-response findings. Evidence was saved under `C:\Users\lince\.codex\memories\Output\eros-prod-deploy-2026-06-04-18-41-54-ct` and `C:\Users\lince\.codex\memories\Output\eros-prod-deploy-repeat-2026-06-04-18-43-03-ct`.

## 2026-06-04 16:10 CT - Angora combo CSS first paint

- Fixed a first-paint race where `RuntimeService` could ask Angora to generate authored component classes before `AngoraCombosService` had applied the latest `ConfigStoreService` combo payload, so combo-only classes such as `sectionTitle` were filtered and the Eros draft could reveal with stale title color.
- `AngoraCombosService` now synchronizes draft combos from the store before explicit class updates, splits authored class strings into individual tokens, and checks rendered combo `__COM_*` rules before reporting CSS-ready.
- `RuntimeService` now re-samples rendered DOM classes inside loading-curtain readiness, requires stable ready passes before starting the hide path, and keeps timeout behavior resilient with a timer fallback around frame waits. Combo readiness checks critical color/text markers such as `__COM_sectionTitle-titleColor` and the matching elements' computed token color, because partial combo rules can exist before the final rendered color is active.
- Production readiness timeout for Angora combo CSS increased to 20 seconds after Eros production showed the final `sectionTitle` color could arrive after the old 4-second budget.
- Added final rendered-text color gates for critical draft text combos (`sectionTitle`, `sectionSubtitle`, `heroCaption`) in both `RuntimeService` and `LoadingCurtainService` so no early-exit path can start the boot curtain fade while visible text still computes stale theme colors.
- `LoadingCurtainService` resolves expected critical text colors from the draft theme payload before falling back to CSS variables, covering production cases where `--ank-titleColor` itself is briefly stale.
- Validation passed: focused `AngoraCombosService` + `RuntimeService` Karma suites 36/36 with Edge as `ChromeHeadless`, `npm run build`, local SSR first-paint QA for `erosbarajas.com` on desktop/mobile, and an extended local desktop probe that kept the curtain up until the final title color was present.

## 2026-06-04 15:27 CT - In-tree draft registry paths

- Updated `docs/drafts-registry.json`, draft preflight tooling, managed-alias sync fallback behavior, tests, and workflow docs so registered local draft repos resolve under `zoolandingpage/drafts/{domain}` instead of sibling `../draft-*` directories.
- Kept remote GitHub repo names using the existing `draft-*` naming convention; only local clone paths changed. Existing sibling `C:\Users\lince\Documents\GitHub\draft-*` folders are now legacy duplicates unless explicitly re-registered.
- Validation passed: registry JSON parse, preflight path resolution showing all registered repos under `C:\Users\lince\Documents\GitHub\zoolandingpage\drafts`, and focused registry/front-door tests 18/18.

## 2026-06-04 15:07 CT - Dependency audit remediation

- Updated the lockfile-only transitive dependency resolution for Karma/Express tooling so `body-parser` resolves to `1.20.5`, `qs` resolves to `6.15.2`, and `tmp` resolves to `0.2.7`.
- `npm audit` now reports zero vulnerabilities after the lockfile update.
- Validation: `npm ci`, `npm audit`, `npm run build`, and `npm run ssr:smoke` passed. `npm run test:logged` still has one unrelated AnalyticsService spec failure, and `npm run test:draft-context` still requires local ignored draft fixtures for `drafts/zoolandingpage.com.mx/site-config.json`.

## 2026-06-04 14:17 CT - SSR allowed-host gate

- Added an Express SSR host gate that validates `Host` and `X-Forwarded-Host` against approved static host families or the resolved local/runtime `site-config` host set before Angular SSR handles the request.
- Angular SSR now receives the exact validated host set for each request, allowing newly published runtime-backed domains without per-domain `angular.json` hotfixes and without using a wildcard Angular host allowlist.
- Added SSR smoke coverage for `erosbarajas.com`, runtime aliases, unknown custom hosts, test-host `draftDomain` preview, and production hosts ignoring cross-draft `draftDomain` query params.
