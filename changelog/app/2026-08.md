# App Changelog - 2026-08

## 2026-08-27 11:25 CT - Greenstock testing runtime activation

- Activated immutable SSR release `206d6b28c2c1679920389ec70748bc78eb7da3d9` in testing through the existing infrastructure `test` source `973397942463718ab40a83c20d5740717c19a971`. The normal [infrastructure run](https://github.com/LynxPardelle/zoolandingpage-aws-infra/actions/runs/33098097741) completed successfully at 11:25:23 CT; no infrastructure source or production release changed.
- Three runtime/control-plane audits passed at 11:26:17, 11:28:01 and 11:29:15 CT: exact release/artifact digests, healthy Lambda and completed stack/distribution state, matching static prefix, HTTP 200 health, and public JavaScript/CSS bytes matching the immutable artifact. Six observed metric points reported 115 invocations with zero errors or throttles; ingestion delay limits that observation.
- The deployment-event closure contained only the SSR Lambda, test distribution and two release parameters, with no IAM or DNS resource events. The previously verified rollback artifact `f0be8f9b7dae95ca8b22d13e586c12138f1f8275` was rechecked; no rollback was executed.
- The existing HIGH transitive `brace-expansion` finding in the pinned infrastructure build tree remains documented; no dependency, TLS or security gate was changed. Runtime activation did not itself publish Greenstock's payload or establish live browser acceptance; the subsequent publication and bounded browser results are recorded in the [draft changelog](../drafts/2026-08.md).

## 2026-08-27 00:54 CT - Hermetic fixed-language SSR fixture

- Routed only the fixed-language test's production renderer through a loopback fixture API backed by the real local runtime bundle. Unrelated API misses return 404, and remote fallback overrides are disabled only for that fixture child process; production code and draft payloads are unchanged.
- Preserved every existing route, canonical, hreflang, and sitemap assertion. The exact hreflang failure reproduced with valid system trust before the repair; the focused case then passed with and without system trust, the full context suite passed 11/11, and the production build plus SSR smoke passed 59/59 with process-scoped system trust and TLS verification enabled. No deployment or repository visibility change was performed by this test-only repair.

## 2026-08-26 21:26 CT - Readable shared not-found status label

- Changed only the shared SSR status label from the accent token to the existing neutral text token, preserving the themed panel, route recovery, status code, and fallback shell.
- Extended the existing SSR theme-contrast case test-first: the observed orange/panel pair failed at 1.6944:1, then passed after the one-line style change. All 30 focused recovery checks and the production build passed. Full SSR smoke passed 54 of 58 with the same four previously documented failures; no unrelated assertion was relaxed.
- Local browser checks at 375×812 and 768×1024 measured 6.9179:1 for the label and 9.1205:1 for the recovery action. Both real clicks returned to populated home content with validated preview context. This is scoped draft QA, not a WCAG certification or production release; link-focus contrast remains a separately recorded follow-up.

## 2026-08-26 20:55 CT - Safe shared not-found recovery

- The Express-rendered not-found home link now uses the existing navigation serializer with validated, allowlisted preview context. It drops page overrides and untrusted query fields, keeps published-host boundaries unchanged, and preserves a relative, escaped destination that actually resolves home.
- Replaced the recovery CTA's mismatched accent/success colors with the existing text/background pair inverted, with readable neutral fallbacks. No new theme API or client-specific styling was introduced.
- Test-first verification passed 25 focused SSR cases, 11 local-context cases, 846 ChromeHeadlessStable cases, and the production build. Full SSR smoke passed 49 of 53, retaining the same four published-host/content expectations that failed before this change; those assertions were not weakened. Independent browser QA and release remain separate gates. No deployment, publication, or infrastructure change was performed.

## 2026-08-26 18:35 CT - Fleet-complete local SVG coverage

- Used the new unresolved-icon smoke gate against every registered testing draft. It found 25 additional Material icon names in Sulanding, `dashboard_customize` in ZooSite, and the authored `+` markers in The Hair Narrative that still depended on ligature text.
- Added bounded local paths for that exact remote inventory. Material Icons use Google's filled 24 px geometry, `monitoring` uses the equivalent official Material Symbols geometry normalized to the existing `0 0 24 24` view box, and `+` uses the standard local add path.
- Consolidated the component regression around all 32 fleet-reported names and now verifies that every path has non-zero rendered bounds as well as no leaked fallback text.
- Kept the change additive in the shared renderer: no draft payload, external font, schema, route, asset, or environment contract changed. Post-render CSSOM inspection confirmed the existing Angora 24 px icon sizing after its normal asynchronous generation pass.

## 2026-08-26 18:22 CT - Per-draft browser icon ownership

- Replaced the inherited Angular CLI binary with a six-resolution, PNG-backed Zoolandingpage Z `favicon.ico` and removed its competing `alternate icon` declaration. The shared shell, SSR head injection, and client metadata synchronization now retain exactly one effective browser favicon, honoring `site.icons.favicon` when configured and otherwise using the Zoolandingpage Z fallback.
- Added a no-store legacy `/favicon.ico` redirect for both Express SSR and Nginx. Nginx keeps the redirect relative behind the TLS-terminating proxy, avoiding scheme downgrade; CloudFront's static `*.ico` route receives the committed Z binary and requires explicit `/favicon.ico` invalidation when the release is activated.
- Added and connected CI regression coverage for the static shell, SVG fallback asset, multi-resolution static ICO, Nginx redirect contract, packaged SSR redirect target, configured-draft SSR output, and client removal of competing favicon links.
- Verification passed on a fresh production build: the browser-icon contract returned 2/2, the focused SSR redirect returned 1/1, and the focused metadata service returned 16/16. Desktop/mobile browser inspection confirmed one expected favicon for each of the 12 registered drafts, and the full local fleet smoke passed 152 routes across two viewports (304 renderings) with zero local failures. No deployment, publication, branch promotion, or cloud mutation was performed.

## 2026-08-26 17:50 CT - Local SVG coverage for authored icons

- Added local inline SVG paths for `flight_land`, `account_balance_wallet`, `lightbulb`, `work_off`, and `account_balance`. Astra Legal uses those names on the home, services, and property-tax recovery pages; the renderer no longer exposes their Material Icons ligature text when the external font is absent.
- Kept icon rendering independent from Google Fonts and preserved the existing generic-icon sizing, color, theming, and accessibility contract across every route and locale.
- Extended the draft smoke gate to report any remaining `span.material-icons` fallback as a runtime failure, closing the silent visual gap that the previous console, image, and overflow checks could not detect.
- Focused component and smoke-gate regressions passed. Local Astra Legal browser QA passed all 21 routes in desktop and mobile viewports with no unresolved icon text, browser errors, broken images, or horizontal overflow.

## 2026-08-25 22:50 CT - Angora 1.6.2 and production-release hardening

- Published `ngx-angora-css` 1.6.2 from the independently audited library artifact. Runtime CSS creation now batches stylesheet indexing and mutation, skips unchanged palette work, preserves compound, quoted, escaped, and bounded-media selectors, and reports recoverable per-rule failures without discarding valid rules.
- Updated the hub to consume the registry artifact instead of a local link. A clean `npm ci` installed `ngx-angora-css` 1.6.2 with the locked Angular 22.0.7 tree; the published tarball shasum matched the audited dry run.
- Added native `figure` and `figcaption` authoring support, deferred initializer-time authentication redirects until the browser shell connects, and avoided duplicate Angora palette application for the same CSS-readiness revision.
- Hardened fleet smoke evidence with isolated inspection failures, desktop/mobile console and page errors, broken-image and overflow checks, concrete samples for dynamic content-hub routes, generic-404 detection, and fail-closed redaction for browser findings and broken-image metadata.
- Remote testing exposed two additional release-gate cases: SSR page metadata for dynamic Content Hub taxonomy routes now loads the concrete request path instead of the route template, and smoke QA ignores only the expected same-origin unauthenticated `401` from `/auth/session/me` while continuing to fail every other console error. Ordinary routes that render a supported localized not-found title now fail the gate.
- Final registry-backed gates passed 846 Angular tests, 18 focused Node/schema tests, the production build, and a production-dependency audit with zero known vulnerabilities. The full desktop/mobile fleet validation and staged deployment are recorded separately when those stages complete.

## 2026-08-25 17:37 CT - Fail-closed per-draft deployment environments

- Upgraded the canonical draft registry to version 2 with one mandatory, ordered deployment scope per entry. Eleven existing drafts retain test and production; The Hair Narrative is explicitly test-only.
- Updated repository preflight, bootstrap, GitHub setup, OIDC role planning, managed-alias synchronization, and knowledge routing so new operational plans honor the declared environments while public-safety and knowledge audits continue to include every registered draft. The recoverable production workflow template now stops before promotion validation or plan creation when production is disabled in the generated draft config.
- Rejected missing, empty, duplicated, reversed, unknown, and production-only scopes. Version 1 remains transitional with both environments implicit, and version 1 entries cannot use the new field.
- Existing test-only repositories now receive a fail-closed production-drift audit before setup across the current checkout and canonical GitHub `test`/`main` refs. The full production workflow must equal the reviewed template after EOL normalization; Environment absence requires a complete paginated inventory rather than an ambiguous 404, and production-role absence requires an STS-account-verified read-only IAM lookup. An explicit reconciliation mode can prepare only realpath-contained tracked config/workflow files from a clean non-production branch, preserving unmanaged config and leaving commit, push, protected remote reconciliation, and every GitHub/AWS decommission action to separate review. Production/all alias work now rejects unknown registry scope and registered-path domain mismatches, and production apply cannot disable registry discovery.
- Added focused regression coverage for schema validation, environment-scoped preflight, test-only bootstrap/configuration, IAM inventory, knowledge routing, drift/reconciliation, and alias filtering. No repository setting, Environment, variable, IAM role, DNS record, branch, deployment, or production state was changed; any existing production surface remains decommission-pending rather than implicitly retired.

## 2026-08-24 17:16 CT - Fixed-language routes, accessible draft primitives, and CSS readiness

- Added the optional fixed-language route contract across shared types, schemas, runtime validation, local preview, client navigation, SSR, sitemap, canonical, and hreflang output. The hub accepts the same normalized restricted BCP 47 variant subtags as Authoring and Runtime Read. A route-bound language is transient and does not overwrite the saved preference used by ordinary routes.
- Added the generic authoring primitives needed by the Astra Legal calculator and campaign: radio semantics and keyboard behavior, nullable number inputs, localized validation associations, focus-by-id instructions, live result regions, exact currency formatting, safe hash navigation, and per-link language-query preservation control.
- Coalesced CSS-readiness work by normalized class, combo, and theme signature; obsolete cycles are canceled before additional CSSOM work and synchronous failures release the signature for a safe retry.
- Preserved the authored distinction between an explicitly empty decorative image `alt` and an omitted `alt` in `generic-media`; document-style media continues to fall back to its source URL when no non-empty label is available.
- Centralized server-rendered not-found copy so Chinese requests receive aligned Chinese title, metadata, message, and home action instead of the previous Spanish fallback. A fixed-language `/404` now carries the final runtime route language through the SSR document, head, and diagnostic shell while ordinary unbound not-found routes keep their request language.
- Synchronized the canonical and draft-template Commerce schemas, golden fixture, and canonical/template readiness validation with the Config Authoring contract: required plan-change and pause policies, optional tax and bounded migration policies, the code-owned migration capability, tracked-inventory enforcement, and the optional shipping-country allowlist. Both schema copies match the Config Authoring source after LF normalization (9,978 bytes; SHA-256 `1F1B2934FBBF025ABC3C1CB7D70FAF2EAB4D856DFC7CCA6461E53037A4E3B6CE`).
- Added focused Angular, schema, local-preview, navigation, CSS-readiness, SEO, sitemap, and production SSR regression coverage. Final local checks passed 841 Angular cases, 33 schema cases, two focused SSR parity cases, and 11 local-context cases; the broader SSR smoke passed 23 of 27, with the same four unrelated failures reproduced at the base revision. Local implementation and QA did not deploy, publish, promote a branch, or mutate cloud state.

## 2026-08-05 13:59 CT - Per-draft GitHub ownership and scoped setup

- Extended the canonical draft registry so an individual draft can use a verified GitHub owner that differs from the fleet default without changing the ownership of existing repositories.
- Propagated the resolved owner through repository setup and GitHub OIDC role planning, including exact trust evidence for the selected repository, Environment, and branch.
- Made `--domain` mandatory for applied GitHub and AWS setup so an incomplete command fails before any GitHub, Git, or AWS operation. Applied GitHub setup also requires the exact STS-verified 12-digit AWS account ID before writing role ARNs.
- Added regression coverage for mixed-owner registries, exact domain selection, fail-closed mutation guards, account binding, and the final cross-owner OIDC subject composition.
- No AWS resource, deployment, production publication, or DNS state was changed by this tooling update.
