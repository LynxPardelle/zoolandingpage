# Application changelog - September 2026

## 2026-09-07 CT - Repeatable local SSR archive

- Normalized ZIP dates so unchanged SSR inputs do not change the artifact digest merely because source timestamps differ. Windows sets metadata before opening each entry; the POSIX path supplies an ordered file inventory with UTC timestamps and strips extended attributes.
- Added a real-process regression that packages identical inputs with widely different timestamps and compares SHA-256 digests. Windows verification uses the installed Node 22 and PowerShell toolchain; Linux execution is not asserted by this local result.
- Kept deployment coordinates, source/run checks, default-off admin delivery and all runtime behavior unchanged. Build timestamp remains in the external manifest, not in ZIP entries.

## 2026-09-07 CT - Journal source consolidation (local validation)

- Combined the reviewed Journal bundle/schema, SSR/SEO and isolated delivery changes on the current frontend integration base, preserving the existing configurable font loader.
- Made four published-host SSR routing tests use unchanged repository-owned draft fixtures through a loopback Runtime Read stub. They no longer depend on mutable external publication state; production routing and the original assertions remain unchanged.
- Corrected Windows Lambda ZIP entries to use relative POSIX paths. A real packaging regression test covers nested files and workspace paths containing spaces and apostrophes; the Linux packaging branch is unchanged.
- Local validation passed 999 Angular tests, 152 tooling/schema contracts, 60 SSR smoke tests, 11 cross-draft context checks, and the production build. This is not deployment or private-editor activation.
- The local QA build contains the public regression fixtures used for cross-draft checks. It is not an approved release artifact: final source-commit provenance, payload inventory, rollback coordinates and the remaining Workstream A gates are still required.

## 2026-09-07 CT - Isolated Journal delivery preparation

- Added complete SSR browser/server inventory hashes and source/run/attempt binding to the immutable artifact handoff; conditional S3 writes prevent overwriting a prior release prefix.
- Added default-off, TEST-only validation of The Hair Narrative's exact admin route and explicitly supplied static-asset inventory. No origin, route or private UI is activated by this change.
- Added evidence-only TEST rollback validation and retained artifact coordinates; infrastructure continues to own activation and rollback execution.
- Focused negative tests cover tampering, missing/extra files, linked directories, wrong origins, source drift, and invalid rollback sources. No workflow was dispatched and no release was published or selected during preparation.
