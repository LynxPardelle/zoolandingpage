# Application changelog - September 2026

## 2026-09-09 CT - Query-string dependency security regression

- Pin the existing `qs` dependency to 6.16.0, fixing
  [GHSA-x5fp-wj9c-mxmx](https://github.com/advisories/GHSA-x5fp-wj9c-mxmx)
  and [GHSA-4mjr-xmp4-gh2g](https://github.com/advisories/GHSA-4mjr-xmp4-gh2g).
  Only `qs`, `side-channel` and `side-channel-list` package versions change;
  preserve unrelated lockfile metadata and the existing Vite override.
- Exercise the dependency resolved by Express with bounded regression inputs
  and ordinary draft, locale and repeated query parameters. Run those tests in
  Angular validation CI without weakening its production-dependency audit.
- Validation: production build and 1,035 Angular tests passed; three rounds of
  77 query, SSR, protected-origin and private-artifact tests passed with no skips.
  `npm audit --omit=dev` reports zero vulnerabilities. No routes, draft payloads,
  visual styles, access policies or deployed configuration change in this fix.

## 2026-09-07 CT - Private fixed-article media helpers (local candidate)

- Added opt-in browser image normalization, private cover/inline upload,
  acknowledged-revision binding, cancel/context-change protection and
  memory-only Blob previews. Existing editor text is retained on failures.
- Enforced separate private upload limits, including the exact 4 MiB boundary,
  without touching the v1 uploader. Replaced a large-base64 regular expression
  after its boundary regression exposed a browser stack overflow.
- Added native-browser normalization and inert-preview markup checks. Helpers
  remain unwired to the visible desk; no draft routes, components, public pages,
  auth bootstrap defaults or deployment settings changed.

## 2026-09-07 CT - Opt-in published locale contract (local candidate)

- Added optional `localePolicy: "published-only"` to the public Content Hub type,
  runtime validator and both public config schemas. Omission preserves legacy
  behavior; unknown values are rejected and no global default is injected.
- Documented matching reader behavior: omit unpublished translations, never
  borrow localized fields, and do not recover them from static article fallbacks.
- Added validator and schema parity regressions. No draft payload, rendered
  component, private-editor route, workflow, IAM or deployed configuration changed.
  A compatible reader release is required before publishing a draft opt-in.

## 2026-09-07 CT - Legacy TEST recovery compatibility

- Added a separate local-only verifier for the reviewed pre-delivery TEST release, preserving the exact original manifest, server ZIP and complete browser inventory. It does not synthesize modern CI provenance or weaken the modern rollback verifier.
- Captured 67 existing TEST objects (5,166,658 bytes) locally with two equal reads per object, stable release coordinates, matching deployed Lambda code and a successful historical source run. The ignored receipt is SHA-256 sealed; no source artifacts, credentials or cloud coordinates were committed.
- Added negative tests for wrong source/run, altered or missing bytes, extra files, unsafe paths/headers, linked files/directories, invalid dates and attempted activation. Validation-only workflows exercise the new tests.
- Kept legacy debug-workspace bytes only in the non-deployable local recovery copy. No AWS mutation, site/draft change, account provisioning, release selection or deployment occurred. The original infrastructure recovery assembly remains a separate gate.

## 2026-09-07 CT - Repeatable local SSR archive

- Normalized ZIP dates so unchanged SSR inputs do not change the artifact digest merely because source timestamps differ. Windows sets metadata before opening each entry; the POSIX path supplies an ordered file inventory with UTC timestamps and strips extended attributes.
- Added a real-process regression that packages identical inputs with widely different timestamps and compares SHA-256 digests. Windows verification uses the installed Node 22 and PowerShell toolchain; Linux execution is not asserted by this local result.
- Kept deployment coordinates, source/run checks, default-off admin delivery and all runtime behavior unchanged. Build timestamp remains in the external manifest, not in ZIP entries.

## 2026-09-07 CT - Journal source consolidation (local validation)

- Combined the reviewed Journal bundle/schema, SSR/SEO and isolated delivery changes on the current frontend integration base, preserving the existing configurable font loader.
- Made four published-host SSR routing tests use unchanged repository-owned draft fixtures through a loopback Runtime Read stub. They no longer depend on mutable external publication state; production routing and the original assertions remain unchanged.
- Corrected Windows Lambda ZIP entries to use relative POSIX paths. A real packaging regression test covers nested files and workspace paths containing spaces and apostrophes; the Linux packaging branch is unchanged.
- Local validation passed 999 Angular tests, 152 tooling/schema contracts, 60 SSR smoke tests, 11 cross-draft context checks, and the production build. This is not deployment or private-editor activation.
- The local QA build contains the public regression fixtures used for cross-draft checks. It is not an approved release artifact: final source-commit provenance, payload inventory, rollback coordinates and the remaining Workstream A gates are still required.

## 2026-09-07 CT - Isolated Journal delivery preparation

- Added complete SSR browser/server inventory hashes and source/run/attempt binding to the immutable artifact handoff; conditional S3 writes prevent overwriting a prior release prefix.
- Added default-off, TEST-only validation of The Hair Narrative's exact admin route and explicitly supplied static-asset inventory. No origin, route or private UI is activated by this change.
- Added evidence-only TEST rollback validation and retained artifact coordinates; infrastructure continues to own activation and rollback execution.
- Focused negative tests cover tampering, missing/extra files, linked directories, wrong origins, source drift, and invalid rollback sources. No workflow was dispatched and no release was published or selected during preparation.

## 2026-09-07 CT - Connected private Journal desk (local only)

- Connected the six THN-only access/MFA/list/new/edit/preview route packages to
  the real v2 auth contract, fixed article transport, autosave and private media.
  There is no client URL input; publication remains unavailable.
- Preserved unsaved text across blocking reauthentication and explicit conflict
  recovery. Added locale-aware document resets and translated publication states.
- Restricted the private Quill image registry to that editor's authorized Blob
  sources; the global editor and other drafts retain their defaults.
- Added server-owned origin inventory enforcement, a minimal safe SSR bootstrap
  tuple, and TEST-only configuration selection for the protected admin origin.
  No auth or article hydration payload is sent in the protected HTML shell.
- Final local verification: 1021 Angular tests, 75 SSR/route/context tests, 35
  transport/editor/media/origin tests and 36 real-SSR browser combinations pass.
  Browser authentication is synthetic; authoring/media handlers use memory stores.
- No deployment, private-origin activation, account/registry change or other
  draft content change. Dedicated public publisher/invalidation/orphan completion,
  public Journal wiring and environment acceptance remain outstanding.

## 2026-09-08 CT - Connected Journal publication and public reading (local only)

- Connected explicit publish/confirmed withdrawal, stable retry identity and
  longer publication timeout to the existing private fixed editor. No URL input
  or automatic sibling-language publication was added.
- Added the opt-in published-locale Journal projection for Home, lists, localized
  series and article detail. Existing hubs retain their route/fallback behavior.
- Preserved the actual reader's root article-content bundle, localized social
  cover metadata, canonical/alternate routes, sitemap and safe search output.
  Marked strict Journal SSR JSON-LD for reuse during hydration to avoid duplicates.
- Added strict production-browser 404 resolution before loading missing article
  payloads. Empty series receive a localized canonical URL in SSR and browser.
- Verification: 1031 Angular tests, 76 SSR/context/origin tests and targeted
  transport/schema/projection tests pass. Public SSR browser QA covers ES/EN at
  375, 768 and 1280 px without overflow or unexpected console/network errors.
- Local candidate only; no AWS or GitHub mutation, activation or gate signature.

## 2026-09-08 CT - Exact private Journal artifact delivery (local only)

- Added the TEST-only dependency-closure producer, immutable CSS/font/icon copies,
  server-only packaged origin binding and exact private-shell asset projection.
  Public HTML/CSS originals and the independent THN draft remain unchanged.
- Accepted the compiler's bounded uppercase base32 hashes alongside existing hex
  hashes. CI font selection now uses the tracked closed public asset manifest,
  not an ignored draft checkout. Packaging rejects stale/default-off bindings,
  cross-release manifests, route drift and changed asset bytes.
- Kept selection explicit and default-off in a TEST workflow dispatch. Before
  OIDC, input, sealed artifact and existing TEST Environment policy must agree;
  an enabled TEST policy rejects a default-off replacement. Production and
  ordinary no-THN job gates receive no new Environment association.
- Excluded only the reserved local `browser/drafts/_debug` directory from release
  staging, preserving original development output and all customer draft bytes.
  Other unexpected/private paths, including the server binding, remain rejected.
- Verified 1035 Angular tests, clean-checkout producer and negative delivery
  contracts, real packaged SSR, and 24 exact-gateway browser combinations across
  six private routes, ES/EN, mobile/desktop. All 40 selected assets were fetched
  through the closed HTTPS inventory and hash-verified; 14 negative requests
  were denied. Authentication and authoring storage used local synthetic data.
- The actual local ZIP and complete browser delivery pass sealing and both
  verifiers after the diagnostic exclusion. This remains uncommitted QA evidence,
  not CI provenance or an activated release. No AWS/GitHub writes, DNS changes,
  owner provisioning or production/draft publication occurred.
