# App Changelog - 2026-07

## 2026-07-22 00:51 CT - Phase 8 infrastructure readiness local closure

- Closed TASK-066 through TASK-072 locally across Data Spaces, Commerce, Integrations, Notifications, Config Authoring, Auth Admin, the frontend infrastructure worktree, and the hub. No AWS `dev` stack/profile/environment or deploy workflow was added; test and production workflows preserve `dev -> test -> main`, bind the exact candidate artifact, and grant OIDC only to the dependent deploy job.
- Added exactly 14 literal HTTPS frontend service routes with caching disabled; the direct Stripe webhook and every internal service route remain outside frontend CloudFront. Added environment-scoped non-secret SSM publication/consumption, exact descriptor/config/auth reads, least-privilege runtime roles, cross-service caller allowlists, same-partition/account/region deployment checks, queue source-account enforcement, and Config Authoring's final pre-S3 secret-lifecycle revalidation.
- Added bounded native admission controls and redacted alarms for the public surfaces, queues/DLQs, functions, provider/webhook failures, migration/reservation state, SMTP circuits, and test/live mismatch. No WAF was added. Detailed API Gateway metrics are limited to five public methods but remain a predeployment cost gate because their 4XX signal combines 400, 403, and 429.
- Hardened bootstrap and evidence: an initial empty Integrations caller list denies all internal routes and is allowed only for first creation; the follow-up requires all four Commerce roles plus the Notifications worker. Notifications verifies its Commerce topic/worker/queue share one account and reads the exact topic/API SSM values before deploy. Data Spaces keeps its internal snapshot caller disabled until an owning Commerce role exists.
- Added four service smokes and a hub aggregator that consumes only bounded JSON from their actual stdout, requires one environment and fresh observation epochs, preserves closed failure categories, rejects hand-written labels, mixed/stale/future/inconsistent evidence, and never reflects credentials or raw inputs. The operator runbook is documented in the canonical architecture contract.
- Three final service audit rounds passed 109 Data Spaces tests, 283 Commerce tests plus one expected skip, 306 Integrations tests, 65 Notifications tests, and seven hub smoke tests, with service compilation, actionlint, SAM lint/build and packaged-handler checks, dependency audits, diff checks, and redacted secret checks. Final hub revalidation passed 585 of 586 Node checks with one intentional local `jq` skip, all 836 Karma tests through the installed Brave binary, and the production build. A compatible lockfile-only update moved `fast-uri` to 3.1.4 and `immutable` to 5.1.9, leaving zero production dependency vulnerabilities; the full development audit still reports three moderate advisories through the current Angular CLI -> MCP SDK -> Hono toolchain, for which npm proposes only a forced incompatible Angular CLI downgrade.
- Deployment remains **NO-GO**. This closure performed no push, remote creation, GitHub Environment change, AWS/provider mutation, stack/route/secret/SSM/webhook creation, Stripe call, SMTP send, or draft publication. Remote prerequisites include exact identities/SSM/bootstrap, load and failure evidence, detailed-metric cost approval, Commerce stream-policy proof, the unresolved CDK bundled dependency advisory, provider/tax/fiscal/retention gates, and explicit TASK-087 authorization. The three moderate hub advisories are confined to the development toolchain and remain an upstream-tracking item rather than a runtime deployment blocker.

## 2026-07-21 22:44 CT - Phase 7 generic server feature clients local closure

- Closed TASK-059 through TASK-065 locally with exact `data-space`, `commerce`, and `integrations` runtime bindings, same-origin clients, browser-safe request projection, protected/public and SSR separation, CSRF, ambiguity-safe mutation idempotency, localized safe errors, and generic runtime/action dispatch.
- Reused the existing table/input/modal/card/button/text composition surface; no central administration UI, commerce-specific Angular page, pilot draft route, arbitrary endpoint, provider identifier, credential, or server scope was added. Synthetic authoring examples cover collections, catalog/price versions, inventory, bounded subscription review, migration, and integration status; unavailable order/subscription list/detail reads are documented rather than invented.
- Hardened Stripe browser handoffs so onboarding query values are removed before exchange and Connect, Checkout, and Billing URLs are validated and consumed only as transient navigation. Provider URLs, OAuth values, and fiscal access proofs are not persisted in VariableStore/status data.
- Closed callback ambiguity before bootstrap: runtime action IDs are globally unique, one route-load return maps to exactly one protected route, normalized and parameterized Cognito callback collisions fail validation, and the early OAuth exchange writes only a closed safe readiness/error projection that excludes code, state, account references, provider text, and redirect URLs before and after bootstrap.
- Closed mutation recovery ambiguity: browser fetches reject redirects, while transport failures, malformed/unreadable responses, 3xx, 5xx, 408, 425, and 429 preserve the exact idempotency key for an exact-input retry. Success and definitive client errors clear it; the unresolved-key set remains bounded and fails closed.
- Added the matching dependency-free pre-S3 runtime-config guard to Config Authoring sync and the draft deployment template. Invalid new bindings fail with a bounded stable code before any authoring request, credential-dependent signed POST, or upload; the guard is included in the controlled draft-tooling rollout closure and existing draft repositories remain unchanged until that closure is applied.
- Kept Karma randomized while adding an explicit reproducibility seed. Three complete rounds with seeds `72131`, `72132`, and `72133` each passed `836/836` Angular tests, `284` Node tooling/schema/harness tests with one intentional local `jq` skip, the production build, a zero-vulnerability production dependency audit, the repository knowledge check with zero errors/warnings, and `git diff --check`. The build retains only the pre-existing `quill-delta` CommonJS warning; file-based secret scanning remains unverified by the knowledge checker.
- Deployment remains **NO-GO**. This closure created no AWS resource, provider call, connected account, secret, customer record, deployed route, draft publication, or live/test activation. Phase 8 owns infrastructure/routing/CI/CD/observability and provider-backed test evidence; Phase 9 owns clean pilot configuration.

## 2026-07-21 17:41 CT - Phase 6 outbound notifications local closure

- Closed TASK-054 through TASK-058 locally across Commerce, Integrations, Config Authoring, the new local-only `zoolanding-notifications` repository, and the hub contract. Commerce emits only the exact policy-bound payment success/failure notification event; Integrations owns pending SMTP registration, operator-only activation, isolation claims, and private active-binding resolution; Notifications owns exact-version policy replay, current secret lifecycle checks, one-recipient plaintext templates, bounded rate/circuit state, and ambiguity-safe SMTP delivery/reconciliation.
- Independent audits ended with zero open P0/P1/P2 findings. Hardening closed duplicate-worker sends, stale/ambiguous retries, exhausted operator retry authorization, policy-cache order dependence, permissive mailbox syntax, broad IAM roles and deploy-parameter wildcards, mismatched Integrations URL/ARN destinations, the Python `secrets` module collision, non-canonical reconcile identifiers, inactive-binding claim reservation, and DynamoDB `Decimal` incompatibility that would otherwise have blocked every runtime claim.
- Three stable cycles passed 263 Commerce tests, 282 Integrations tests, 59 Notifications tests, and 159 Config Authoring tests. The hub passed 68 readiness/template/public-artifact tests (67 pass and one intentional local `jq` availability skip) plus knowledge routing with zero errors/warnings. Service verification also passed compilation, dependency audits, SAM lint/build for 11 Commerce, 24 Integrations, one Notifications, and one Config Authoring function, packaged imports, transformed IAM checks, cross-service event snapshots, and source secret scans.
- Deployment remains **NO-GO**. This closeout created no AWS resource, SMTP/recipient secret, SMTP2GO request, email, customer record, API route, provider activation, remote repository, or deployment workflow. Phase 8 still owns exact deployed identities/routes, stack parameters, secrets, alarms/quotas, DLQ-age telemetry, cross-service and slow-SMTP failure injection, and provider-backed acceptance evidence; the SMTP timeout is per operation, not a proven end-to-end deadline. Phase 9 owns clean pilot configuration and activation.

## 2026-07-21 16:15 CT - Phase 5 bulk subscription migration local closure

- Closed TASK-047 through TASK-053 locally across Commerce, Integrations, Config Authoring, and the hub contract. Commerce now owns fresh-authorized migration requests, exact dry-run approval, idempotent command receipts, protected paginated status, and event projection; Integrations owns provider-read-only preview, bounded durable work selection, canary/concurrency controls, exact next-renewal and immediate-proration adapters, pending-update reconciliation, and guarded next-renewal rollback.
- Hardened the migration path against cross-draft/account/environment use, changed-input idempotency reuse, command/event races, stale or jumped revisions, unbounded terminal scans, replayed `needs_review`, cancellation counter drift, post-mutation Schedule reread failure, rollback over structurally drifted or already elapsed phases, and public actor attribution. Provider snapshots exclude customer identity, credentials, provider payloads, signed URLs, and PaymentIntent client secrets.
- Verification passed 266/266 Integrations tests, 256 Commerce tests with one intentional local `botocore` availability skip, 159/159 Config Authoring tests, and 66 hub readiness/template/public-artifact tests with one intentional local `jq` availability skip. Integrations also passed Python compilation, dependency audit, SAM lint, and an uncached SAM build for 23 functions with the official Python 3.13 runtime. The earlier workstation `OPENSSL_Applink` failure was runtime selection, not an open product defect.
- Deployment remains **NO-GO**. No AWS resource, Stripe object, connected account, webhook, secret, provider call, customer record, browser activation, or live migration was created or claimed. Phase 8 still owns infrastructure/provider-backed validation and Phase 9 owns pilot activation.
- Before production, approve a retention/deletion period, access policy, and cleanup runbook for retained Commerce `MigrationRequest` records, including their pseudonymous actor hashes. Provider-backed TEST-013, cross-service TEST-014, browser TEST-015, and production live gates remain open.

## 2026-07-21 04:01 CT - Phase 4 local implementation closure

- Closed TASK-035 through TASK-046 for the approved local-only scope against Integrations `8814bdc600b256c259962c8e766f63cf245762ea`, Commerce `2b495fdcf68132729863496b7a483fb44d69cf13`, Config Authoring `badc2e35b99837364774a6043e79fd09bf7e5ec4`, and hub contract baseline `808f5b689a471e1f12b35f6b63d309322a13f47f`.
- Final Integrations hardening grants the browser action Lambda only the exact Registry transaction permission it needs; requires server-owned production tax approval before every subscription mutation; binds subscription mutation claims to one subscription revision; validates the restricted portal configuration revision/hash; requires an explicit Stripe account strategy; and rejects redirect-command success without a validated ephemeral redirect.
- Local verification passed 182 Integrations tests, 206 Commerce tests with one intentional local `botocore` runtime skip, 158 Config Authoring tests, 60 hub feature-readiness tests with one local `jq` availability skip, and five public-artifact boundary tests. Three audit/fix/retest rounds closed all confirmed Phase 4 code findings.
- This was documentation and local verification only. No AWS resource, Stripe/provider call, connected account, webhook endpoint, secret, credential, customer data, live tax approval, deployment, browser QA, or pilot activation was created or claimed. TEST-013, TEST-014, TEST-015, Phase 8 deployment, and Phase 9 pilot activation remain open.

## 2026-07-21 03:18 CT - Phase 4 additional contract hardening

- Required a bounded same-origin `customerPortalReturnPath` exactly for Stripe bindings that advertise `customer-portal`; bindings without that capability must omit it. The server composes the final portal return URL from the published canonical domain behind the approved front door, never from browser headers, provider identifiers, query strings, or fragments.
- Closed the production tax-mutation boundary: a draft may carry only an opaque `taxApprovalId`, while the server must find a current Registry approval record scoped to environment, tenant, draft, domain, connection, provider-account hash, mode, and revision. Missing, stale, or mismatched approval fails closed; only tests may inject a verifier.
- Reconciled OAuth completion to exactly one sanitized `code` or `error` outcome after consuming validated state, with no token reaching the browser, and limited reconnect to the same provider account unless the existing connection is deauthorized first.
- Documented shared Commerce/Integrations runtime limits for monetary values, counters, display names, recurrence, and redemptions. These are runtime mutation controls, not draft descriptor or catalog-seed fields.
- Kept Phase 4 local-only and explicitly open: Accounts v2, production live-gate evidence, AWS deployment, provider activation, credentials, and pilot rollout remain deferred to their existing gates.
- Three audit/fix/retest rounds passed 158 Config Authoring tests per round, 60 hub readiness tests with one local `jq` skip, 5 public-artifact boundary tests, Python/JavaScript syntax checks, deterministic seven-file packaging, SAM lint/build, the production application build, dependency audits with zero vulnerabilities, redacted added-line secret scans, and diff checks. All three integration schema copies share SHA-256 `2852AC8BDF03AEB3367F3ABEBC6B38159C39747526DAA8F30D785EECEFE92F4C`; the build retains only the existing `quill-delta` CommonJS warning.

## 2026-07-21 02:24 CT - Phase 4 Stripe Connect strategy contract

- Closed every Stripe binding to one required Accounts v1 `accountStrategy`: `oauth-standard-v1` for an externally owned Standard account or `controller-account-link-v1` for a platform-created controller account with code-owned controller properties. Accounts v2 remains unavailable pending the Mexico Sandbox proof.
- Kept ownership server-derived, added the closed `start|return|deauthorize` onboarding operations, and prohibited browser-supplied account IDs, return URLs, ownership, controller properties, credentials, tokens, and secret references. OAuth tokens are discarded immediately after server exchange.
- Replaced per-draft Stripe client credentials in the approved architecture with one test/production-separated environment secret at `/zoolanding/{environment}/integrations/stripe/connect-platform`, containing only `clientId` and `secretKey`; SMTP credential isolation remains per draft.
- Expanded the exact signed Stripe allowlist to 12 event types with `account.application.deauthorized`, which disables only the internal connection/routing claim and emits no Commerce event. Phase 4 remains local-only; infrastructure and pilot activation stay in Phases 8 and 9.

## 2026-07-20 23:40 CT - Phase 4 integration authoring contract sync

- Synchronized both hub copies of `integration-bindings.schema.json` byte-for-byte with the approved Config Authoring contract for generic integration administration, Auth Profile capabilities, and bounded same-origin Stripe Connect onboarding routes.
- Extended the canonical and generated-draft readiness validators with the same fail-closed authorization, Auth Profile scope/group, route-presence, and embedded provider-resource checks without changing the existing production live gate or public artifact boundary.
- Added synthetic local/test contract coverage and documented the complete descriptor shape. No credential, provider account identifier, secret path, customer data, AWS resource, live integration, browser administration route, or deployment was created.
- Verification passed the hub feature-readiness suite (`55` pass, `1` local `jq` skip), public artifact/safety/auth boundaries (`16` pass), knowledge checks (`7` tests plus zero routing errors/warnings), the complete Config Authoring suite (`155` pass), dependency audits with zero vulnerabilities, exact cross-repository schema hash parity, redacted Gitleaks worktree/history scans, and diff checks.

## 2026-07-20 22:06 CT - Commerce Phase 3 local closure

- Completed TASK-030 through TASK-034 locally with eight literal browser routes, fresh Auth Admin/CSRF boundaries, provider-neutral catalog/inventory/Checkout/subscription state, normalized event inbox/outbox processing, sparse reservation reconciliation, and isolated manual fiscal intake.
- Hardened public surfaces after independent adversarial review: catalog cursors now have identical canonical base64url validation in CloudFormation and runtime; public Checkout requires a distinct 256-bit recovery capability and rejects line quantities above the code-owned 1,000,000-unit storage/abuse ceiling before policy or storage reads.
- Defined 11 explicit least-privilege Lambda roles with no AWS-managed policies, wildcard resources, or X-Ray permissions. The reconciler processes at most 25 due markers with a 1,500 ms safety cutoff, never releases inventory from time alone, and remains disabled until TASK-040 supplies canonical provider status.
- Three clean audit/fix/retest rounds passed 158 Commerce tests, dependency audit, Python compilation, SAM source/build/transform verification for all 11 functions, workflow lint, and diff checks. Cross-repository validation passed 50 hub readiness tests with one local-only `jq` skip and all 151 Config Authoring tests; all three Commerce schema copies share SHA-256 `294E46645A7E1048C98E96DF30244218474C46306E00C71A78BD8C77A5E31251`.
- No AWS deployment, provider call, credential, real PII, fiscal record, or customer data was created. Commerce remains local-only with no remote; Phase 4 (generic Integrations control plane and the first Stripe adapter) is next.

## 2026-07-20 18:52 CT - Atomic Commerce inventory transaction contract

- Completed TASK-029 locally in `zoolanding-commerce` at commit `50390c4`: tracked stock can be initialized/adjusted with conditional revisions, and reserve/commit/release write immutable stock movements without backorders or refund-driven restock.
- Reservation now creates stock mutations, a Catalog reservation/due marker, the Operations order, a draft-scoped unique PaymentAttempt binding, and a 90-day idempotency receipt in one cross-table transaction. Twenty distinct tracked lines produce 45 unique actions, checked with the complete serialized plan before DynamoDB is called.
- Enforced one server timestamp with exact `checkoutExpiresAt = created + 35 minutes` and initial reconciliation at `expires + 5 minutes`. Ambiguous provider outcomes hold stock; only closed canonical completion reasons can commit/release. Still-uncertain work atomically moves its scoped due marker five minutes to avoid starvation, and TTL never acts as the stock-release timer.
- Hardened concurrency and isolation: the short DynamoDB token includes scope plus the exact plan, PaymentAttempt reuse conflicts within a draft but not across drafts, identical concurrent requests apply once, divergent idempotency payloads conflict, terminal races have one winner, and receipt/read failures remain sanitized unknown outcomes.
- Three audit/fix/retest passes completed. Final verification passed 55 tests, 92% total coverage and 86% storage coverage, Python compilation, dependency audit, SAM lint/build, workflow lint, staged-diff checks, and redacted secret scans. Three independent final reviews found no blocker.
- No handler, IAM policy, route, scheduler, provider call, dependency, AWS resource, GitHub remote, credential, PII, provider payload, fiscal field, or customer data was created. TASK-030 owns the executable handlers/scheduler/authorization boundary; TASK-040 owns exact provider-status evidence. Data Spaces and Commerce remain undeployed.

## 2026-07-20 18:08 CT - Immutable Commerce catalog, offer, and discount contract

- Completed TASK-028 locally in `zoolanding-commerce`: immutable catalog items now support the four approved sellable types, optional variants/SKUs, and identifier-only references to one exact Data Spaces record revision and field allowlist.
- Added immutable one-time/monthly/yearly `OfferVersion` and percentage/fixed-amount `DiscountVersion` models with closed provider semantics, canonical schema-versioned SHA-256 fingerprints, exact economic/restriction snapshots, and the approved `draft -> provisioning -> active -> existing_only -> retired` lifecycle.
- Lifecycle and presentation revisions advance independently and remain outside provider fingerprints. Display text rejects HTML markers, control/format characters, header injection, and URI tokens; customer-facing discount code casing remains part of the immutable fingerprint while case-insensitive active-code uniqueness remains assigned to TASK-041.
- Three audit/fix/retest passes completed. Final verification passed 32 tests, 99% domain coverage, Python compilation, dependency audit, SAM lint/build, workflow lint, staged-diff checks, and redacted worktree/staged secret scans. Three independent final reviews reported no blocker.
- No AWS resource, GitHub remote, Data Spaces/Stripe call, persistence write, credential, PII, provider payload, fiscal field, or customer data was created. TASK-029 is next; Data Spaces and Commerce remain undeployed.

## 2026-07-20 17:44 CT - Deterministic Karma lifecycle and browser-state isolation

- Stabilized the zoneless Karma suite with a test-only Chromium launcher that disables History API flood throttling, while preserving Angular's no-sandbox launcher and keeping production browser flags unchanged.
- Centralized native History restoration, URL cleanup, storage/cookie isolation, and asynchronous TestBed teardown; removed no-op navigation spies and made language-dependent component fixtures explicit.
- Hardened runtime teardown so destroyed AppShell hosts cannot reconnect, in-flight draft initialization is invalidated, and stopped runtime data-source loads cannot publish stale results, retry, or create refresh intervals.
- Updated the auth API URL assertion to follow the active environment contract and excluded the Karma harness from production TypeScript compilation.
- Refreshed the lockfile to patched `body-parser` releases after GitHub Actions surfaced the new low-severity invalid-limit denial-of-service advisory; no direct dependency or application API changed.
- Verification passed three consecutive randomized full runs at `789/789`, a post-audit full run at `789/789`, 12 zoneless runner contracts, the production SSR build and 25 SSR smoke tests, `git diff --check`, and both production-only and full `npm audit` with zero vulnerabilities. The build retained the existing `quill-delta` CommonJS warning.

## 2026-07-20 16:50 CT - Commerce Phase 3 pure domain foundation

- Completed TASK-027 locally in `zoolanding-commerce`: seven dependency-free modules now define the first provider-neutral catalog, money, inventory, order, subscription, shipping, and fiscal invariants without adding handlers or persistence.
- Money is immutable, non-negative, integer minor-unit only, and requires the immutable currency allowlist resolved by the owning server-side policy. Unsupported but syntactically valid codes fail closed; no Stripe-specific or stale global currency registry was introduced.
- Closed registries reject unknown sellable, shipping, fiscal-disclosure, and tax-behavior values; quantities and totals remain integer-only; physical recurring offers are rejected.
- Three audit/fix/retest passes completed. Final verification passed 21 tests, dependency audit, Python compilation, SAM lint/build, workflow lint, staged-diff checks, and redacted secret scanning. Independent review findings were fixed and rerun.
- No AWS resource, GitHub remote, provider call, persistence write, credential, PII, fiscal field, or customer data was created. TASK-028 is next; Data Spaces remains undeployed.

## 2026-07-20 16:34 CT - Repository cleanup and development dependency security

- Updated the Angular 22 toolchain to compatible patch releases, Lighthouse to 13.4.1, and the transitive Vite/brace-expansion resolutions required to remove the reported development-only Babel, esbuild, OpenTelemetry/Sentry, and brace-expansion advisories. A clean install now reports zero npm vulnerabilities, including development dependencies.
- Removed three tracked generated reports that had no reader, and ignored the reproducible root performance report. Retained `@angular/animations` after the Angular test bundle proved it is still required.
- Deleted two already-merged remote `codex/*` branches with zero commits exclusive from `dev`, pruned stale refs, and enabled GitHub's native automatic head-branch deletion after merge. Protected `dev`, `test`, and `main` were unchanged.
- Validation passed `npm ci`, full `npm audit`, the production SSR build/package workflow, knowledge check, and the focused knowledge/public-safety suites. The full Karma suite compiles with the new dependencies but remains baseline-red under local Brave with order/environment-sensitive failures also reproduced from the prior merge commit; no unrelated application test was changed or suppressed.

## 2026-07-20 16:14 CT - Commerce Phase 3 storage foundation

- Completed TASK-026 locally in `zoolanding-commerce`: the undeployed SAM template defines exactly `CommerceCatalogTable`, `CommerceOperationsTable`, and `FiscalTable` with on-demand billing, server-side encryption, point-in-time recovery, retained deletion/replacement policy, and server-owned `pk`/`sk` keys.
- Catalog and Operations enable `expiresAt` only for eligible cleanup records; Fiscal has no TTL pending an approved retention policy. Only Operations enables a `NEW_IMAGE` stream. No Lambda, API, event-source mapping, queue, role, index, or AWS `dev` surface was added.
- Corrected the task boundary: TASK-030 owns least-privilege handler IAM and fiscal isolation; TASK-032 owns the real outbox consumer, filter, `ReportBatchItemFailures`, and its dedicated SQS failure destination. This avoids a fake consumer or placeholder infrastructure.
- Three audit/fix/retest passes completed. Final verification passed 17 tests, dependency audit, Python compilation, SAM lint/build, workflow lint, exact-resource assertions, staged-diff checks, and redacted secret scanning. Independent review found no blocking or major issue.
- No AWS resource, GitHub remote, provider connection, credential, PII, fiscal record, or customer data was created. TASK-027 is next; Data Spaces remains undeployed.

## 2026-07-20 15:08 CT - Commerce Phase 3 local scaffold

- Completed TASK-025 in the new local-only `zoolanding-commerce` repository: Python 3.13/SAM entrypoints, no AWS `dev` profile or workflow, fail-closed test/production release gates, and PAT-007 resolution with standard-library contract validation for the exact immutable `server/commerce.json` plus optional protected Auth Admin policy.
- The resolver rereads the published pointer, caches only immutable version-addressed descriptors, enforces exact environment/tenant/draft/domain scope, validates the closed Commerce policy, rejects prefix confusion, duplicate JSON keys, oversized/deep input, non-canonical IDs, and overlong domains, and returns only stable sanitized failures.
- Three audit/fix/retest passes completed. Final verification passed 16 unit/contract tests, dependency audit, Python compilation, SAM lint/build, workflow lint, staged-diff checks, and a redacted Gitleaks scan. Independent review found no blocking or major issue.
- No catalog table, handler, route, GitHub remote, AWS resource, provider call, credential, PII, or customer record was created. TASK-026 is the next implementation step; Data Spaces remains undeployed until Commerce and the later infrastructure gates provide its exact internal caller identity and cross-service evidence.

## 2026-07-16 23:36 CT - Runtime Read security findings promoted to production

- Used the six reportable findings recovered from the failed Codex Security workspace without reopening the plugin: deny-by-default public site projection, bounded Content Hub index/article work, bounded config/content S3 reads, and an immutable exact-SHA deployment artifact. Added the separately confirmed anonymous-amplification control as an exact API Gateway throttle for only `GET /runtime-bundle` at 25 requests/second with burst 50; `OPTIONS` remains unchanged and no WAF was added.
- Promoted through protected PRs [#55](https://github.com/LynxPardelle/zoolanding-config-runtime-read/pull/55), [#56](https://github.com/LynxPardelle/zoolanding-config-runtime-read/pull/56), [#57](https://github.com/LynxPardelle/zoolanding-config-runtime-read/pull/57), and [#58](https://github.com/LynxPardelle/zoolanding-config-runtime-read/pull/58). Test run `29557492625` and production run `29557690806` both succeeded from their exact merge SHAs.
- Read-only AWS verification found both original stacks `UPDATE_COMPLETE`, unchanged API/Lambda identities, retained execution boundaries, reserved concurrency `100`, exactly one stage method setting, and no failed/replaced resource. Sanitized production smokes returned `200` for the home bundle and a controlled not-found bundle for `/server/integrations.json`, with no server route exposed.
- Codex Security itself was not reopened or marked as a successful scan because its UI/workflow failed; deterministic fixes, CI, deployment provenance, AWS readback, and live smoke evidence are the release basis.

## 2026-07-16 23:19 CT - Data Spaces Phase 2 local implementation

- Completed TASK-018 through TASK-024 in the new local-only `zoolanding-data-spaces` repository: PAT-007 policy resolution, draft/tenant-derived storage, four separately permissioned handlers, bounded generic schemas/records, fresh Auth Admin authorization, CSRF, idempotency, immutable revisions, safe publication, opaque pagination, and exact internal snapshots.
- Hardened the transport before closure: oversized Base64 input is rejected before decoding, failures use the platform-safe top-level error envelope, and internal snapshots include an exact field allowlist plus canonical SHA-256 content hash.
- Independent review found and closed a stale-publication path by permitting only additive schema evolution once records exist. It also added an exact internal Commerce caller-role check and a public-read method throttle; trusted-service cross-draft scope, PII governance, domain-reassignment session revocation, quotas/alarms, direct-endpoint routing, and runtime-SDK packaging remain explicit deployment gates.
- Verification passed: 83 unit/contract/security tests, dependency audit, Python compilation, SAM lint/build, built-handler imports, workflow lint, and a Git-index-based closed artifact inspection. The implementation created no AWS resource, GitHub remote/CI run, provider connection, secret, or customer record.

## 2026-07-16 22:55 CT - Phase 1 implementation closure and Runtime Read hardening

- Closed TASK-007 through TASK-017 as the implemented server-only foundation after reconciling the current live deployment evidence, while keeping the explicit historical-access residual-risk decision and all provider/fiscal/pilot gates open.
- Merged Runtime Read's deny-by-default public projection, bounded Content Hub hydration, exact article metadata lookup, bounded S3 reads, and exact-SHA packaging through its protected `dev` PR after 71 unit tests, SAM build/validation, canonical Zoosite compatibility proof, and independent security review.
- Coordinated the public contract and publication boundary: `site-config.json.runtime.contentHubs` now accepts at most four entries, and Config Authoring rejects a fifth before S3/DynamoDB writes or published-pointer movement. Both changes passed their repository checks and merged to `dev`.
- Began Phase 2 as a local-only Data Spaces repository. No AWS stack, provider connection, production secret, fiscal capture, or live integration was created by this transition.

## 2026-07-16 22:36 CT - Runtime content-hub schema request budget

- Capped the `site-config.json.runtime.contentHubs` JSON Schema at four entries so the public contract matches Runtime Read's bounded per-request hub budget.
- Documented the authoring limit in the public content-hub contract and added focused schema regression coverage.
- Verification passed: the focused guard returned `1/1`; the site, content-hub, and server-config schema suites returned `28/28`; public-safety and knowledge-routing suites returned `16/16`; the repository knowledge check returned zero errors and zero warnings, while reporting secret scanning as not verifiable from files.

## 2026-07-15 10:16 CT - Runtime Read least-privilege identity migration complete

- Migrated Runtime Read test and production deployments to exact repository/Environment/branch GitHub OIDC callers plus retained CloudFormation service roles. Both Lambda execution roles have the code-owned boundary, and the stack `RoleARN`, Lambda/API physical IDs, Lambda code hash/size, state, and reserved concurrency were read back after cutover.
- The first test deployment failed closed because the CloudFormation service role lacked access to the exact SAM transform ARN; the stack remained stable. Adding only that transform access allowed the same path to succeed. The temporary exact-role boundary-removal permission used for rollback bootstrap was then removed from test and production.
- Scoped-caller deployments `29429756155` (test) and `29430865648` (production) succeeded. Final infrastructure deployment `29431531986` succeeded, and live readback found zero `iam:DeleteRolePermissionsBoundary` statements on either CloudFormation service role.
- The previous test and production deploy roles now have deny-only web-identity trust with zero trust `Allow`. They remain present only for the explicit rollback review window through at least 2026-07-22 CT; they were not deleted.
- Removed the two stale variables from the Runtime Read `dev` GitHub Environment. It now has zero variables and zero secrets, no workflow consumes it, and its referenced legacy AWS role did not exist when checked.
- Three consecutive final live audits passed GitHub-variable/trust checks, IAM boundary checks, stack/service-role readback, unchanged resource identity checks, and test/production home, missing-language, and server-descriptor isolation smokes. Codex Security remained blank and supplied no evidence, so it was not used as a release gate.

## 2026-07-15 08:28 CT - Phase 1 release-policy repin and dev residual audit

- Promoted the corrected dynamic-reference public-safety detector, then immutably repinned the reusable policy and draft template through the protected chain. The active anchors are auditor `92445c8670e4ecae63c1c1dfde9de8925f4b88c8`, reusable workflow `c8b04670b5cca800ccf0f723815813897e596600`, and template main `ef21066041373349c856f95f5819a2bf1342eb79`; the template main push completed both Angular validation and SSR artifact publication successfully.
- Repinned only `.github/workflows/pr-safety.yml` in all 11 registered draft repositories and promoted each change through `dev -> test`. Every test push succeeded, emitted no deployment artifact, and skipped plan, upload, OIDC, and AWS work. `dev` and `test` are tree-identical for the repin, while all 11 `main` branches remain unchanged and no `test -> main` PR was opened.
- Repeated the live `dev` retirement audit with `ADMIN-AIM-CLI`. Across 25 relevant AWS service inventories in `us-east-1`, no Zoolanding/draft resource matched `dev` or `development`; 12 mapped remote repositories also contained zero current workflows combining `dev` with OIDC or AWS credentials.
- The initial inventory found eight unused GitHub Environments named `dev` with 27 variables and 3 secrets. Their eight role-ARN variables referenced roles that no longer existed, and no current workflow consumed the Environments. During closeout, Runtime Read's two variables were removed and that Environment reached zero variables and zero secrets; the remaining retired metadata stays a controlled cleanup backlog because three opaque secret values are unrecoverable and deletion would not rotate an independently active credential.
- Production dependency audit returned zero vulnerabilities. The full development dependency audit remains a separate toolchain backlog: 3 low and 17 moderate vulnerability nodes, with the available aggregate fixes requiring major-version changes; GitHub Dependabot currently exposes 2 open low-severity alerts on the default branch.

## 2026-07-15 07:23 CT - Draft promotion artifact and OIDC boundary hardening

- Hardened the hub's draft-repository templates without changing any live draft repository. The exact promotion verifier now binds both merge parents to the associated PR, rechecks the target tip around remote evidence, classifies the first-parent `name-status` diff, skips authoring only for added/modified files in the existing 15-path rollout closure, fails closed for unsafe allowlisted control changes, and always deploys on manual dispatch.
- Moved draft planning into the unprivileged job. It now emits a closed, deterministic-per-attempt plan whose immutable version ID binds environment, full target SHA, run ID, and run attempt; local plan mode requires official run metadata or an explicit bounded version ID. Unknown draft JSON paths, root package metadata, sensitive fiscal/provider payloads, and privileged runtime values cannot enter the plan.
- Restricted plan output to a new relative path under the draft root with `lstat`/`realpath` checks and exclusive creation. Absolute/traversal paths, linked or missing parents, existing targets, and symlink/junction targets fail closed; the workflow replaces `.draft-deploy` without following a root link or deleting outside the checkout.
- Bound the one-day artifact name and SHA-256 manifest to run ID, run attempt, and SHA, and exported its single numeric artifact ID plus the manifest's SHA-256 outside the artifact. The OIDC job first validates those outputs, downloads exactly one artifact by ID, requires the exact two-file set, compares the external manifest digest, and runs strict checksum and closed-schema/path validation. It runs no checked-out repository code, does not recalculate attempt-bound names or versions on a failed-job rerun, rechecks the current target tip immediately before OIDC, and sends ordered upsert/publish requests with runner-native `curl --aws-sigv4` while suppressing payload and response bodies.
- Set deploy concurrency to preserve an in-flight upsert/publish pair rather than cancel it. Config Authoring's immutable-version conflict remains fail closed; an upsert that succeeds before a later failure can leave an unpublished immutable version, and future backend cleanup or equality reconciliation is still required.
- No draft rollout, branch promotion, publish, deploy, push, GitHub configuration change, AWS mutation, or live endpoint request was performed.
- The changed-surface Node suite passed `80/80` with the official locally verified `jq` binary. A broader 47-spec tools command returned no captured result before its 184-second command timeout; the unchanged `ssr-server.spec.mjs` also returned no output when isolated for more than 120 seconds and was terminated without modification. Excluding that spec, the concise 46-spec run returned 493 markers: 411 passed and 82 failed. Its visible failures included missing generated `dist/zoolandingpage/stats.json` and missing files from the independent Zoosite draft repository, which is not present in this isolated worktree; those out-of-scope files and tests were not changed.

## 2026-07-15 04:13 CT - Server-only integrations Phase 1 remote foundation

- Fixed Config Authoring's post-commit `Decimal` response failure at the shared JSON boundary and added fail-closed pre-serialization before compare-and-set and S3 writes. The focused suite passed `130/130`, SAM lint/build and artifact inspection passed, and the same verified Lambda artifact was promoted through test and production.
- Completed a PokeAPI test canary through GitHub OIDC and preserved its redacted, hash-verified evidence outside the repositories. The private production scope and v2 authorization bundle were applied with S3 versioning while the legacy authorization object remained byte-for-byte unchanged.
- Rolled the exact generic 15-path secure deployment bundle to `dev` and `test` in all 11 registered draft repositories. One Pamela regression test was corrected to use the canonical local-only-directory rule; no draft content, `main` branch, or production draft publication was changed. All 11 current test tips have successful deployment evidence; the already-current AlecFest tip was safely redispatched after Config Authoring was fixed because its earlier deployment had failed at the then-broken authoring call.
- Applied and read back strict branch protection, the GitHub Actions app-pinned `guard`, exact selected-branch Environment policies, and per-repository/per-environment IAM trust for all 22 draft release paths. Production IAM changes were policy-only; no trust drift remained after apply.
- A late audit found the Config Authoring `test` and `main` branches unprotected. Protection was applied immediately after the known-good deployment with the expected required checks; both branch SHAs remained unchanged during the interval.
- Live verification confirmed test and production Config Authoring use the same artifact, IAM-only Function URLs, v2 authorization, and unsigned `403` responses. The first Windows `curl` probe failed locally on certificate revocation lookup; the explicit Schannel-compatible retry produced the valid `403` result.
- Codex Security rendered an empty view and supplied no finding evidence. Release assurance therefore used deterministic tests, Actionlint, secret/diff scans, live GitHub/AWS readback, and independent agent reviews; an empty plugin view was not treated as a pass.
- Active test and production frontend stacks reference commits containing the sanitized artifact-boundary fix, and eight bodyless private-path probes now return `404`. A read-only inventory found no forbidden draft keys across 32 test and 19 production release prefixes. Final Brave/Playwright QA passed 44 desktop/mobile views with zero blocking responses, runtime failures, console errors, or overflow. Historical S3 and CloudFront access logging was disabled, so prior access cannot be reconstructed from those sources; an explicit residual-risk/credential decision remains the incident-closure gate.
- Remaining live-provider gates include accountant/legal answers, real Stripe volumes and validated topology/tax/webhook evidence, audited owning services and infrastructure, clean pilots, and written HostGator authorization or an approved alternative. The exhaustive list remains under [Deferred Live Gates](../../docs/api-driven-config/22-server-only-integration-microservices.md#deferred-live-gates).

## 2026-07-14 19:11 CT - SAM validation and exact draft-promotion provenance

- Installed the official signed 64-bit AWS SAM CLI `1.163.0` MSI and verified its published SHA-256 and Amazon Authenticode signature. `sam validate --lint` reports `Z:\GitHub\zoolanding-config-authoring\template.yaml is a valid SAM Template`; no build, deploy, or AWS mutation was performed.
- Replaced the draft deploy template's permissive source-ancestor check with a read-only GitHub associated-PR proof. Test deploys now require the exact merged same-repository `dev -> test` PR and production requires `test -> main`; both require the exact merge commit and second parent, while push runs also require the exact first parent and reject direct, created, deleted, or forced pushes.
- Kept OIDC out of validation: it receives only `contents: read` and `pull-requests: read`; the dependent exact-commit deploy job remains the only job with `id-token: write`. GitHub API or evidence failure blocks deployment with redacted stable errors.
- Corrected draft GitHub setup aggregation so `--apply` reports failure when native protection could not be installed on either `test` or `main`. Setup now reads back the complete branch policy, rejects an explicit Environment tag policy, pins `guard` to the API-verified GitHub Actions app ID `15368`, clears and verifies PR bypass allowances, anchors owner to the canonical registry, and rejects colliding, invalid, or overlong role names before any AWS mutation. GitHub's full protection endpoint receives its required legacy context while the dedicated status-check subresource is then patched with the app-pinned check before final readback. The build with stats completed, the complete Node suite passed `460/460`, and three consecutive focused audit cycles each passed `44/44`, Actionlint, and `git diff --check`; the diff check emitted only existing LF-to-CRLF working-copy warnings. No draft repository, GitHub protection setting, release, or cloud resource was changed by the local verification pass.
- A read-only GitHub API audit found all 11 registered draft repos protected on both branches: 22/22 `test`/`main` endpoints require strict `guard`, a PR, and admin enforcement while blocking force-push/deletion and containing no named PR bypass. All 22 currently leave the `guard` provider unpinned, and their checked-out workflows still use the older general-ancestor proof, so the stronger local policy remains a controlled fleet rollout rather than an active remote control.
- A deeper read-only audit found a separate live OIDC gap: all 22 registered GitHub Environments were unrestricted with zero protection rules, and all 22 corresponding AWS roles had the exact repository/environment subject but no exact branch `ref`. Local controls now require selected `test`/`main` Environment rules, the same branch in IAM trust, current-tip revalidation immediately before credentials, and per-environment concurrency. No live Environment or IAM policy was changed.
- The AWS setup dry run initially discovered one local, unregistered draft that would have expanded the role inventory. Role planning now reads only `docs/drafts-registry.json`; the corrected dry run covers exactly 22 registered roles with zero mutations. One pre-existing legacy test role for the unregistered draft remains a separate live cleanup decision.
- Documented the remaining trust boundary: with zero required approvals, every writer/merger is a trusted deploy authority because the verifier lives in the draft repo. Also documented the narrow stale-publication race when a newer run fails before entering the serialized deploy job; closing that fully would require an independent immutable approval/verifier or a server-side latest-approved-SHA compare-and-set.

## 2026-07-14 17:58 CT - Server-only integrations Phase 1 local foundation

- Added closed, bounded server-only schemas for Data Spaces, Commerce, generic integration bindings, and notification policies; extended protected runtime bindings with generic feature kinds while preserving existing Auth Admin and API Proxy contracts.
- Added dependency-free schema/semantic readiness checks, exact six-file server descriptor kinds, code-owned provider/capability/template registries, Auth Profile scope checks, production live gates, strict JSON/secret/PII/signed-URL checks, valid opaque SSM/Secrets Manager reference handling, and redacted errors. Every remote authoring operation now requires an explicit environment; local `dev` maps reads to test and cannot mutate remotely, while pulls verify returned domain/environment/stage before local clean or write.
- Added OIDC-free draft-template validation followed by an exact-commit deploy job with scoped OIDC, exact public draft JSON/media projection, encoded/traversal-shaped artifact rejection, a global case/encoding/traversal-resistant SSR draft-namespace guard, generated browser/SSR artifact inspection, immutable Config Authoring manifests with SHA-256 verification, registry revision CAS, rollback validation, and metadata-only notification-secret preflight.
- Split SSR validation and publication into separate GitHub jobs. The validation job has no OIDC permission and transfers only the checked artifact through the official GitHub artifact service; the publication job receives `id-token: write`, downloads the validated artifact without checking out repository code, and rechecks manifest coordinates plus the server-bundle checksum before assuming AWS credentials. The added official actions are pinned to the full commits for `actions/upload-artifact@v7.0.1` and `actions/download-artifact@v8.0.1`.
- Config Authoring deploy jobs remain deliberately disabled until repository-root SAM `CodeUri` is replaced with a verified allowlisted build artifact. No commit, push, deploy, AWS mutation, provider activation, production secret, fiscal capture, or real SMTP delivery was performed.
- Three consecutive post-fix local audits each passed draft context `10/10`, feature readiness/template `37/37`, artifact boundary `5/5`, public safety `7/7`, combined Node contracts `156/156`, focused Angular validation `79/79` in Brave, Actionlint, and `git diff --check`. Config Authoring separately completed three clean `68/68` unit-test passes plus Python compilation, JSON/YAML parsing, schema parity, and diff checks.
- TASK-007 through TASK-017 pass locally. Phase 1 remains release-blocked by the open live draft-artifact incident, pending AWS integration evidence, accountant answers, real Stripe volumes, and HostGator authorization or an approved alternative provider.

## 2026-07-14 16:32 CT - Draft public artifact incident detected and locally contained

- Read-only bodyless probes confirmed that both test and production returned HTTP `200` for repository metadata and an encoded path to a server-only draft descriptor, while the canonical server-only path returned `404`. No live object body or response headers were downloaded or recorded.
- Root cause in the source build was recursive copying of `drafts/**`; the pre-fix generated browser artifact contained 7,270 files, including server-only descriptors, Git/GitHub metadata, tooling, draft-repository configs, and private source-material paths.
- Replaced recursive copying with an exact public JSON/media projection, added a redacted generated-artifact boundary check, hardened SSR against encoded/case-variant private paths, and required the artifact check before AWS credentials in the publish workflow.
- The live incident remains open. No deploy, cache invalidation, old-release quarantine, access-log review, object-body inspection, or credential rotation was performed; those actions require an explicitly authorized incident procedure followed by desktop/mobile browser QA and three clean audits.

## 2026-07-13 11:41 CT - Zoolanding repository branch cleanup

- Audited 24 GitHub repositories matching `zoolanding*` or `draft-*`, merged retained changes through protected pull requests, and deleted 99 remote branches after verifying they were integrated, superseded, or intentionally obsolete.
- Three consecutive fleet audits found zero branches outside `main`, `test`, and `dev`.
- Frontend, SuLanding, and frontend infrastructure reached `test` with identical `dev`/`test` trees and successful validation/deploy workflows. SuLanding browser smoke passed 13 routes in desktop and mobile viewports with zero failures or live mismatches.
- Config Authoring and Data Dropper retained successful changes in `dev`, but their `dev -> test` pull requests remain open because test deploys have no usable `AWS_ROLE_ARN` credentials. No IAM/OIDC role was invented or created during branch cleanup.

## 2026-07-13 18:18 CT - AWS dev retirement and local-to-test development

- Retired the eight Zoolanding AWS dev stacks and their retained data resources, deploy roles, and shared static `frontend/angular-ssr/dev/` prefix after creating a local untracked rollback copy. Test, production, Portfolio resources, and the shared public-files bucket were not removed.
- Removed cloud dev deployment workflows/profiles from the app, frontend CDK repo, and affected backend SAM repos. Runtime-read maps `dev`, `development`, and `local` requests to test.
- Local Angular development keeps local drafts first and uses test endpoints/data for remote reads. SSR packaging defaults to `test`; the app artifact workflow publishes only test or production.
- Verification passed: three AWS residual audits returned no Zoolanding dev resources; focused app specs, runtime-read, frontend CDK, Angular build, and YAML parsing passed. SAM CLI was unavailable.

## 2026-07-10 16:21 CT - Public health redirect handling

- Updated `tools/ops/public-site-health-check.mjs` so page checks follow canonical redirects before validating title and main content, while DNS, health, and runtime checks keep their stricter behavior.
- Verification passed: `node --test tools/tests/ops-public-site-health-check.spec.mjs`; `npm run test:ops-front-door` returned 53 pass; full production public health for the 9 serverless hostnames returned 36 checks, 0 failures.

## 2026-07-10 14:03 CT - Draft smoke browser detection repair

- Fixed `tools/draft-smoke-check.mjs` so absolute Edge/Chrome executable paths are used directly instead of being probed with `--version`, which could leave Microsoft Edge running and hang local Playwright smoke checks on Windows.
- Verification passed: `npm run test:draft-smoke`; `node tools/draft-smoke-check.mjs --domain=zoolandingpage.com.mx --domain=erosbarajas.com --local-base-url=https://test.zoolandingpage.com.mx --include-live=false --timeout-ms=25000` returned 4 routes, 2 viewports, 0 failures; `node tools/draft-smoke-check.mjs --domain=zoositioweb.com.mx --local-base-url=https://test.zoolandingpage.com.mx --include-live=false --timeout-ms=25000` returned 49 routes, 2 viewports, 0 failures.
- In-app browser audit confirmed Zoosite home and `/blog` on `test.zoolandingpage.com.mx` render main content without console warnings/errors; mobile menu opened and exposed the expected close control and links. The in-app browser screenshot capture path closed the target twice, so screenshot evidence was not retained from that surface.

## 2026-07-12 15:48 CT - Astra Legal production identity repair

- Completed the approved Astra Legal identity transition by making `grupoastralegal.com` the canonical draft registry, deployment, and active operational-documentation domain.
- Retired active instructions that still routed agents toward `despacholegalastralex.com`; historical changelog entries remain unchanged.
- Root cause of the production deployment failure was split external state: GitHub production variables, IAM trust, and authoring deploy authorization still referenced the retired Astralex repository/domain while test already used Grupo Astra Legal.
- Created the scoped `draft-grupoastralegal-com-production-deploy` role, added its authoring authorization, aligned GitHub production variables, and removed both obsolete Astralex roles and authoring rules after the replacement deploy passed. S3 backups preserve the pre-change and pre-prune authorization manifests.
- Verification passed twice after repair, including once after pruning the retired roles. The production runtime endpoint returned HTTP `200` with `domain: grupoastralegal.com`, `environment: production`, and a populated payload.

## 2026-07-12 15:33 CT - Fleet knowledge routing rollout completed

- Promoted the task-routed documentation entrypoints, immutable C1 caller, and injection-safe draft promotion guard through `dev -> test -> main` in all 11 registered draft repositories and the Runtime Read, Auth Admin, and Content Hub satellites.
- Verified three consecutive fleet audits with 14 repositories, zero routing issues, identical `dev`, `test`, and `main` trees, and zero open rollout pull requests.
- Verified successful production workflows for 10 drafts and all three satellites. Grupo Astra Legal remains an isolated infrastructure incident: its unchanged production workflow cannot assume the legacy `draft-despacholegalastralex-com-production-deploy` role through OIDC; one rerun reproduced the failure, and the available AWS identity is not authorized to inspect `iam:GetRole`.
- No domains, DNS, GitHub Pro features, or manual AWS configuration were added or changed.

## 2026-07-12 14:24 CT - Fleet knowledge routing foundation

- Added an idempotent hub command that audits or applies marked documentation-routing blocks across registered drafts and the Runtime Read, Auth Admin, and Content Hub satellite pilot without committing, pushing, merging, or changing cloud configuration.
- Added `--checkout-root` so the hub can manage independent sibling clones by registered repository name without turning tracked `drafts/{domain}` source folders into nested Git repositories.
- Normalized both local and `origin/*` branch refs during audits, and made draft apply mode refresh the canonical injection-safe promotion guard while leaving satellite workflows untouched.
- Scoped C1's draft-specific public-safety phase to callers with `draft-repo.config.json` while retaining full-history Gitleaks for every repo and supporting exact `.gitleaksignore` fingerprints for verified false positives.
- Extended new-draft templates with a task-routed `AGENTS.md`, human `README.md`, compact `Codex.md`, and immutable C1 caller while preserving local critical safety, release, and rollback rules.
- Moved pull-request branch names from direct shell interpolation into step-scoped environment variables in the shared promotion guard template, resolving the `actionlint` script-injection warning.
- Added the machine-readable satellite routing registry and updated lifecycle/setup guidance so agents open one task-specific contract instead of loading entire documentation trees or changelogs.
- Focused verification passed for managed-block preservation/idempotence, remote/path validation, bootstrap output, registries, C1 policy, and repository hygiene.

## 2026-07-11 CT - Routed knowledge and fail-closed draft fleet tooling

- Replaced the oversized default `Codex.md` read with a small `AGENTS.md` router, task-oriented documentation index, verified repository map, and minimal draft entrypoint template.
- Added a native knowledge check for tracked routers, context budgets, registry validity, broken entrypoints, local-evidence links, and retired draft paths.
- Made draft preflight, setup, and public-safety discovery resolve the strict registry path and expected GitHub origin; unregistered, mismatched, escaping, dirty, or transition-state repositories fail closed before pull or mutation.
- Removed the unverified dedicated Zoosite test host from generated auth-admin front-door defaults; it remains opt-in pending explicit DNS, TLS, HTTP, and browser verification.

## 2026-07-11 CT - Retired stale implementation plans

- Removed the tracked `plan/` tree after verifying that its 99 files described obsolete Angular 19/Jest/Cypress/Husky/SonarQube work, already-implemented features, or historical release steps rather than a trustworthy backlog.
- Repointed the only two external plan links to the current lifecycle, registry, preflight, public-safety, and GitHub setup contracts.
- Did not convert 959 stale checkbox markers into issues. Git history retains the original plans; current work belongs in verified repository docs or explicit backlog items.

## 2026-07-09 14:08 CT - Serverless pre-cutover CloudFront draft QA

- Allowed generated `*.cloudfront.net` frontend audit hosts to honor `draftDomain` query params so pre-cutover serverless distributions can render the intended published draft before custom-domain cutover.
- Kept branded production hosts restricted to their own hostnames, so cross-draft `draftDomain` query params remain limited to local, testing, and generated CloudFront audit hosts.
- Verification passed: focused `DomainResolverService` Karma returned `TOTAL: 15 SUCCESS`; `npm run package:ssr:lambda` passed with the existing `quill-delta` CommonJS warning.

## 2026-07-04 18:10 CT - Angora boot curtain readiness hardening

- Fixed static SSR startup so pages with critical Angora text hooks bootstrap Angular immediately and leave boot-curtain release to runtime CSS readiness instead of the old short static fallback.
- Raised the static fallback budget to `20_000ms`, removed the runtime short hide path when Angora reports CSS not ready, and made combo readiness accept generated `VAL` variant markers while limiting computed color blocking to critical text combos.
- Aligned loading-curtain theme checks with the alternate boot palette used when a saved browser theme overrides the draft default.
- Verification passed: `node --test tools\tests\client-boot-curtain.spec.mjs`; focused Karma for Angora combos, runtime, loading curtain, and bootstrap constants returned `TOTAL: 61 SUCCESS`; `npm run build` passed with the existing `quill-delta` CommonJS warning; local SSR Playwright QA on Astra Legal verified light/mobile and saved dark theme release after Angora CSS generation with no horizontal overflow.
- Follow-up remote QA on testing exposed avoidable browser console 404s from raw runtime fallback reads. Browser runtime now tries the stable config API first and only falls back to raw runtime when the stable endpoint fails, while SSR keeps raw-first behavior.
- Verification passed: focused Karma for config API, Angora combos, runtime, loading curtain, and bootstrap constants returned `TOTAL: 73 SUCCESS`.

## 2026-07-04 10:40 CT - Public content-hub cover image contract

- Allowed public content-hub article summaries and localizations to expose safe cover images through `imageSrc` and `imageAlt`, with `imageSrc` limited to same-origin paths or HTTPS absolute URLs.
- Aligned TypeScript runtime types, payload validators, JSON Schemas, and schema contract tests so draft-authored blog cards can render real cover images instead of empty icon placeholders.
- Verification passed: `node --test tools\tests\site-config-schema.spec.mjs tools\tests\content-hub-schema.spec.mjs tools\tests\content-hub-admin-pages.spec.mjs` returned `50` pass; focused SSR host tests returned `2` pass; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-07-04 08:50 CT - SSR runtime route validation before content-hub 404 fallback

- Fixed Angular SSR draft-context resolution so a stale or empty public content-hub index does not force a route-matched Zoosite article URL into the canonical 404 when the path-specific runtime bundle can render the expected article page.
- Kept true missing article slugs on the 404 path by requiring the route-specific runtime payload to match the requested page id before bypassing the content-hub missing-path fallback.
- Verification passed: focused `draft-runtime.service.spec.ts` Karma returned `TOTAL: 27 SUCCESS`; `npm run build` passed with the existing `quill-delta` CommonJS warning; `node --test tools/tests/ssr-server.spec.mjs` passed `25` tests after one local `ECONNREFUSED` retry from the test server.

## 2026-07-04 08:23 CT - SSR content-hub readers normalize single-hub runtime

- Fixed server-side content-hub readers used by sitemap, feed, search, route status, and public SSR decoration so they accept the published runtime `runtime.contentHubs` single-hub object shape as well as the app-authored array shape.
- Verification passed: `npm run build` passed with the existing `quill-delta` CommonJS warning; `node --test tools/tests/ssr-server.spec.mjs` passed `25` tests, including Zoosite public blog/article routes with a single-hub runtime payload.

## 2026-07-04 08:03 CT - Content-hub single-hub runtime shape support

- Hardened public content-hub route helpers to accept `runtime.contentHubs` as either an array or the single-hub object shape returned by the published runtime bundle, preserving article, taxonomy, and missing-route checks across both shapes.
- Verification passed: focused content-hub public route Karma returned `TOTAL: 9 SUCCESS`; full SSR server suite returned `25` pass; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-07-04 06:48 CT - Zoosite blog public runtime cleanup

- Hardened generic blog cards so published article cards prefer configured cover images over decorative icon placeholders, avoiding blank icon squares on public blog listings.
- Hardened generic search placeholders so serialized dynamic function text cannot leak as public copy when a draft value fails to resolve.
- Improved SSR host resolution behind the platform front door so forwarded custom hosts such as `zoositioweb.com.mx` keep the correct runtime bundle and canonical metadata when traffic enters through `zoolandingpage.com.mx`.
- Verification passed: `npm audit --omit=dev` returned `found 0 vulnerabilities`; focused generic-card/search-box/runtime host Karma returned `TOTAL: 49 SUCCESS`; selected Node content-hub/schema/runtime SSR contracts returned `66` pass; Zoosite public-safety audit returned `ok: true`.

## 2026-07-04 04:15 CT - SSR forwarded-host draft routing repair

- Centralized server-side request URL parsing so Angular SSR services use the forwarded branded host when local absolute proxy URLs such as `127.0.0.1` reach the app, preventing published Zoosite blog routes from being misclassified as missing routes.
- Added a visible localized SSR 404 shell for true missing routes instead of leaving an empty app root.
- Verification passed: `npm audit --omit=dev` returned `found 0 vulnerabilities`; Zoosite schema/draft route contracts returned `7` pass; focused Karma for SSR host services returned `TOTAL: 29 SUCCESS`; full `npm run ssr:smoke` returned `24` pass and `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-07-03 13:22 CT - Localized content-hub SSR status alignment

- Passed the requested `lang` through browser/runtime config loading and SSR route status checks so localized article paths resolve against the correct runtime bundle.
- Added public content-hub route helpers that distinguish existing public article/taxonomy paths from confirmed misses, allowing localized articles to return HTTP 200 while stale or mistyped article/category/tag URLs keep HTTP 404.
- Verification passed: `npm audit --omit=dev` returned `found 0 vulnerabilities`; Node content-hub/schema contracts returned `36` pass; focused Karma for content-hub public routes and generic icon returned `TOTAL: 12 SUCCESS`; full SSR server suite returned `24` pass; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-07-03 12:19 CT - Localized content-hub public route hardening

- Treated `localizations.{lang}.path` as valid published content-hub article paths for public route matching so translated article URLs do not return HTTP 404 while rendering content.
- Added SSR-side localized article merging for public articles so sitemap, search, canonical, and `BlogPosting` data can use localized metadata even when runtime-read returns the raw canonical article record.
- Added a `book-open` SVG alias to `generic-icon` so catalog/icon names used by drafts do not leak as visible text when a material icon font cannot resolve them.

## 2026-07-03 11:09 CT - Public content-hub localized article projection

- Added browser/runtime support for localized public content-hub article summaries and optional `articleContent` on `runtime.contentHubs.publicArticles`, so drafts can serve multi-language blog cards and article fallbacks without duplicating page templates.
- Added defensive mojibake repair for legacy public article metadata that was already published with broken UTF-8 decoding, while keeping UTF-8 source data as the expected canonical state.
- Extended content-hub runtime types, payload validation, JSON Schema, config bootstrap, and Zoosite draft contracts for localized article metadata and article body projection.
- Verification passed: `npm audit --omit=dev` returned `found 0 vulnerabilities`; focused Karma for `config-bootstrap.service.spec.ts` returned `TOTAL: 11 SUCCESS`; Node schema/admin contracts returned `36` pass; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-07-03 08:55 CT - Public content-hub interaction/date contract alignment

- Added a generic `formatDate` value handler so draft-authored pages can display ISO runtime dates through the active language instead of rendering raw timestamps.
- Aligned public content-hub interaction typing, runtime validation, and JSON Schema with the backend policy map by allowing `shares`, `readProgress`, and `assetDownloads` alongside `reactions`, `ctas`, and `forms`.
- Added focused coverage for formatted date rendering and expanded public interaction policy fixtures.

## 2026-07-03 07:56 CT - Generic media responsive style support

- Added `styles` support to `generic-media` so draft-authored image, video, audio, and document components can use the same dynamic inline style contract already used by other generic components.
- Hardened rich HTML rendered by `generic-text` so embedded images, media, tables, preformatted blocks, figures, and long copy stay within their container on mobile and desktop.
- Verification passed: `npm audit --omit=dev` returned `found 0 vulnerabilities`; focused Karma for `generic-media`, `generic-text`, and Angora combos returned `TOTAL: 40 SUCCESS`; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-07-03 05:22 CT - Content-hub missing taxonomy 404 alignment

- Tightened public content-hub route checks so stale category/tag URLs such as `/blog/bienvenido-al-blog-de-zoosite` or `/blog/tag/no-existe` resolve to the configured 404 instead of rendering a generic listing page.
- Aligned Express SSR and browser draft runtime helpers with the same public route contract used for missing article routes.
- Added regression coverage for missing category/tag paths and reserved `/blog/tag/*` routes so tag listings cannot masquerade as article routes.
- Verification passed: focused content-hub public route Karma returned `TOTAL: 7 SUCCESS`; `npm run build` passed with the existing `quill-delta` CommonJS warning; full SSR server suite returned `24` pass; `npm run test:draft-smoke` returned `5` pass; `npm audit --omit=dev` returned `found 0 vulnerabilities`.

## 2026-07-03 01:27 CT - Zoosite protected editor runtime validation repair

- Aligned config payload validation with the existing `generic-input` JSON validation rule used by advanced editor JSON textareas.
- Root cause: the live Zoosite editor runtime bundle validated `siteConfig`, `pageConfig`, variables, combos, and i18n, but rejected `componentTreeJsonInput` because `{ type: "json" }` was missing from the payload validator. The browser then discarded the valid Zoosite bundle and fell through to canonical fallback content.
- Added regression coverage for `json` validation rules in input component payloads and for shared testing dynamic-route domain resolution.
- Verification passed: live runtime-bundle validator probe returned `full: true`; focused config validation Karma returned `TOTAL: 79 SUCCESS`; focused domain resolver Karma returned `TOTAL: 8 SUCCESS`; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-07-02 12:33 CT - Protected route auth timeout fail-closed

- Added a bounded browser timeout around protected-route auth bootstrap work: remote auth resolution, auth callback completion, and server-cookie route access validation.
- If any protected-route auth step stalls, the runtime now clears protected roots and redirects to the route login path instead of leaving the SSR protected shell stuck on `Validando acceso`.
- Added focused runtime coverage for a stalled `/auth/session/me` validation on a protected dynamic admin route.
- Verification passed: focused runtime Karma returned `TOTAL: 25 SUCCESS`; combined runtime/config Karma returned `TOTAL: 43 SUCCESS`; Zoosite content-hub/schema/command Node contracts returned `44` pass; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed with the existing `quill-delta` CommonJS warning; Zoosite public-safety audit returned `ok: true`; `git diff --check` returned only CRLF warnings.

## 2026-07-02 11:25 CT - Shared testing alias fallback removal

- Removed browser-side synthesized runtime-bundle candidates such as `test.{domain}` and `test.{firstLabel}.zoolandingpage.com.mx` from shared testing preview. `test.zoolandingpage.com.mx` now requests only the explicit `draftDomain` provided by the URL.
- Normalized runtime-bundle component payloads when runtime-read returns `components.components` as an object keyed by component ID, preserving Angular's internal array shape before validation and rendering.
- Added regression coverage proving shared testing article detail routes request only `zoositioweb.com.mx` with `environment=test`, preventing later fallback requests from overwriting the valid Zoosite route with canonical 404 content.
- Verification passed: focused Karma for `config-source.service.spec.ts` returned `TOTAL: 17 SUCCESS`.

## 2026-07-02 10:06 CT - Protected SSR hidden 404 repair

- Hardened protected SSR shells so the hidden `<app-root>` body is replaced with an empty clean host instead of preserving stale SSR draft content.
- Stripped Angular SSR/hydration attributes from sanitized protected roots after browser QA found Angular attempted to reconcile the replaced shell and left the protected route stuck behind the validation overlay.
- Removed Angular document hydration markers, transfer-state scripts, and stale JSON-LD from protected SSR shells after testing still reproduced `Cannot set properties of null (setting '__ngContext__')` when the document kept `<!--nghm-->` / `ng-state` while the protected root children had been removed, and HTTP QA found fallback 404 structured data in the protected editor head.
- Added regression coverage that fails if Zoosite protected article detail routes hide `notFoundHero`, "Página no encontrada", or unpublished-route copy inside the protected `app-root`.
- Extended the protected detail-route regression so those shells fail if they ship `<!--nghm-->` or `<script id="ng-state">`.
- Clamped `generic-table` row action inline sizing to a minimum 44px touch target so draft-configured table actions remain usable on mobile even if a draft supplies smaller `actionButtonStyles`.
- Verification passed: focused Karma for `generic-table` and `app-shell` returned `TOTAL: 22 SUCCESS`; Zoosite content-hub/combo/schema/command Node contracts returned `49` pass; full SSR server suite returned `24` pass after build; `npm audit --omit=dev` returned `found 0 vulnerabilities`; Zoosite public-safety audit returned `ok: true`. Testing deploy and browser QA still need to pass before production signoff.

## 2026-07-02 07:55 CT - Protected SSR detail-route shell hardening

- Added a localized safe SSR overlay for protected auth-admin BFF routes while preserving the Angular-rendered `<app-root>` content for hydration, preventing direct Zoosite article detail reloads from visually showing not-found content before browser session revalidation.
- Updated protected route initialization so SSR keeps the private loading shell active instead of rendering draft components before browser-only authorization can run.
- Corrected testing-host SSR runtime environment injection to use the actual request host, keeping shared preview metadata aligned.
- Verification passed: focused Karma for runtime/config/app-shell returned `TOTAL: 78 SUCCESS`; full SSR server suite returned `24` pass; Zoosite content-hub/schema Node contracts returned `42` pass; `npm audit --omit=dev` returned `found 0 vulnerabilities`; Zoosite public-safety audit returned `ok: true`.

## 2026-07-02 06:51 CT - Browser runtime fallback hardening

- Stopped browser runtime config loading from falling through to legacy `site-config`, `page-config`, `components`, and `i18n` endpoints after runtime-bundle misses, preventing shared testing CORS failures from blanking protected detail routes.
- Kept variables and Angora combo legacy fallback tolerant because those payloads do not gate the initial route shell, but guarded thrown fallback errors.
- Changed runtime Combo Catalog reads to keep the configured same-origin endpoint instead of rewriting testing reads through the auth API proxy helper.
- Verification passed: focused Karma for config-source, combo catalog runtime, and draft runtime returned `TOTAL: 45 SUCCESS`; Zoosite content-hub/schema Node contracts returned `42` pass; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed with the existing `quill-delta` CommonJS warning; Zoosite public safety audit returned `ok: true`.

## 2026-07-02 07:10 CT - Protected blog detail route 404 guard

- Restricted content-hub missing-public-article checks to public routes so protected admin detail pages render even when the selected article is not in the public published index.
- Updated SSR and browser draft runtime route resolution for Zoosite `/admin/blog/articulos/:id/editor` and similar protected detail routes.
- Verification passed: focused draft/config/combo runtime Karma returned `46 SUCCESS`; Zoosite content-hub admin/bindings/schema Node contracts returned `42` pass.

## 2026-07-02 06:24 CT - Shared testing detail-route bootstrap fix

- Changed shared testing runtime-bundle domain selection so `draftDomain` canonical domains are requested before synthetic `test.*` aliases, while keeping aliases as fallback candidates if the canonical runtime request is unavailable.
- Added regression coverage for Zoosite protected article detail routes so `/admin/blog/articulos/:id/editor` requests `zoositioweb.com.mx` with `environment=test` and preserves the dynamic route path.
- Verification passed: focused Karma for `config-source.service.spec.ts` returned `TOTAL: 16 SUCCESS` after first reproducing the old failure where the first domain was `test.zoositioweb.com.mx`.

## 2026-07-02 05:52 CT - Blog QA blocker guardrails

- Hardened `generic-input` host layout so draft-composed form fields render as full-width block grid items with `min-width: 0`, preventing admin form controls from collapsing on narrow viewports when the custom element is placed directly in responsive grids.
- Added a Zoosite admin command gate that validates `/admin/blog/*` and `/admin/combos` `valueInstructions`, `eventInstructions`, row actions, and submit instructions against the registered runtime allowlists so invalid command IDs fail before draft publication.
- Verification passed: focused Karma for `generic-input`, `generic-table`, and runtime data-source route params returned `TOTAL: 55 SUCCESS`; Zoosite/schema Node contracts returned `42` pass; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-07-02 02:36 CT - P4 pre-production security and UI gates

- Hardened `zoolanding-content-hub` public `recordInteraction` so public writes require an existing published public article, a published bundle, an enabled per-event interaction policy, and a per-minute actor/article/event rate-limit key before storing the interaction.
- Added DynamoDB TTL configuration for public interaction rate-limit keys on the content-hub interactions table.
- Improved shared `generic-table` default wrapping for horizontal overflow and enlarged `generic-rich-text` toolbar touch targets without adding a new editor dependency.
- Tightened Zoosite `/admin/combos` validation so batch JSON, group mutation, and policy mutation buttons stay disabled until their interaction scopes are valid, and replaced infrastructure wording with editor-facing copy.
- Updated `/mi-cuenta` loading copy to state that session and permissions are being validated while preserving server-side authorization.

## 2026-07-02 01:18 CT - Testing runtime-read fallback alignment

- Corrected the Angular and Express SSR `test` runtime-read fallback from the production runtime-read stack to the dedicated test runtime-read stack (`jaay9p8gv5.../Prod`).
- This prevents shared testing routes from silently hydrating against the stale production registry test pointer when `api.zoolandingpage.com.mx/runtime-bundle` is still routed to the production runtime-read origin.

## 2026-07-02 03:34 CT - P4 smoke public propagation and fixture cleanup

- Hardened `content-hub:smoke` so public search, article HTML, sitemap, and feed checks wait with cache-busting for bounded post-publish propagation instead of failing on the first stale public read.
- Added smoke cleanup for QA category/tag taxonomy after archive and regression coverage proving the smoke turns those fixtures invisible.
- Cleaned leaked QA smoke articles from the testing metadata table by marking `qa-product-smoke-*` articles archived/private and hiding matching QA taxonomies.
- Verification passed: `content-hub:smoke` completed the authenticated P0-P4 flow against `https://test.zoolandingpage.com.mx` using the dedicated test runtime-read endpoint, and `content-hub:public-fixture-audit` returned no leaks across search, sitemap, feed, and `/blog`.

## 2026-07-02 08:38 CT - Protected SSR overlay hydration guard

- Tightened the app-shell protected SSR overlay release so browser hydration removes the safe protected shell only after `RuntimeService.privateRouteLoading` takes ownership of a protected route.
- Added regression coverage proving stale prerendered roots such as not-found content cannot remove the protected SSR overlay before auth/runtime validation starts.
- Verification passed: focused app-shell/draft-runtime/runtime Karma returned `TOTAL: 62 SUCCESS`; `npm run build` passed with the existing `quill-delta` CommonJS warning; SSR server tests returned `24` pass; Zoosite content-hub/schema/command contract tests returned `46` pass; `npm audit --omit=dev` returned `found 0 vulnerabilities`; Zoosite public safety audit returned `ok: true` with no blockers or secret findings.

## 2026-07-02 09:05 CT - Protected SSR root visual hiding guard

- Added explicit protected SSR CSS for `app-root[data-zlp-protected-shell="true"]` so stale SSR roots are visually hidden while the static protected overlay is active.
- Delayed browser overlay removal until the client private-route loading panel exists or a non-404 draft root is ready, preventing protected editor routes from revealing the hidden not-found root.
- Deferred SEO and structured-data head writes while the static protected overlay is active, preventing hidden not-found roots from changing the document title/metadata during auth-admin route validation.
- Verification passed: focused app-shell Karma returned `TOTAL: 12 SUCCESS`; `npm run build` passed with the existing `quill-delta` CommonJS warning; SSR server tests returned `24` pass; Zoosite content-hub/schema/command contract tests returned `46` pass; `npm audit --omit=dev` returned `found 0 vulnerabilities`.

## 2026-07-02 01:00 CT - P4 smoke comment redaction fix

- Adjusted the content-hub product-readiness smoke so moderated comment fixtures no longer include compact numeric timestamps that can legitimately trigger phone-number redaction in the backend.
- Added cache-busting and longer polling for public absence checks after unpublishing articles, plus regression coverage that keeps the smoke comment body human-readable while avoiding long digit runs.

## 2026-07-03 02:10 CT - Protected route render before data-source bootstrap

- Changed browser runtime initialization so validated draft roots are installed before runtime data sources start. Protected admin pages can now render their shell even if a content-hub/auth-admin data source fails or stalls during bootstrap.
- Hardened `RuntimeService.startRuntimeDataSources()` to catch synchronous data-source bootstrap failures in addition to rejected promises.
- Added runtime regression coverage for a protected article editor route whose content-hub data source throws synchronously.
- Verification passed: focused `runtime.service.spec.ts` returned `TOTAL: 26 SUCCESS`; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `npm run build` passed with the existing `quill-delta` CommonJS warning; Node schema/content-hub/auth contract tests returned `37` pass; Zoosite public safety audit returned `ok: true`; `git diff --check` returned exit 0 with LF/CRLF warnings only.

## 2026-07-03 02:24 CT - Runtime component map orchestration guard

- Updated `ConfigurationsOrchestratorService` so external draft component payloads can be installed from either the app-native component array or a runtime-read component map keyed by component ID.
- Root cause: the browser bootstrap validator already accepted the published runtime-read map shape, but the downstream orchestrator still reduced non-array `components` to an empty list. Protected Zoosite admin detail routes could therefore load valid runtime bundles and still render an empty wrapper shell.
- Added regression coverage proving an external component map hydrates parent and child components before wrapper rendering.
- Verification passed: focused runtime/orchestrator Karma returned `TOTAL: 48 SUCCESS`; `npm audit --omit=dev` returned `found 0 vulnerabilities`; Node schema/auth/content-hub/combo contracts returned `37` pass; `npm run build` passed with the existing `quill-delta` CommonJS warning; `git diff --check` returned exit 0 with LF/CRLF warnings only.

## 2026-07-02 00:05 CT - Blog P4 public interactions and analytics filters

- Added the browser-safe `/features/content-hub/public-action` contract for sanitized public blog interactions while keeping comments, moderation, publishing, media, versions, and analytics behind authenticated server-side authorization.
- Extended public content-hub runtime summaries with safe `commentPolicy`, `contentSafety`, and interaction policy fields so drafts can show comments, CTAs, reactions, and forms without exposing backend internals.
- Added content-hub analytics query filters for article, category, tag, and date range, plus contract coverage for public action routing, runtime schemas, frontend request routing, and Zoosite admin/public blog bindings.

## 2026-07-01 23:20 CT - P3 JSON-LD dedupe

- Prevented public article SSR from emitting duplicate `BlogPosting` JSON-LD when Angular SSR and the Express head decorator both render structured data.
- Added SSR coverage so content-hub article routes must contain exactly one `BlogPosting` structured-data block.

## 2026-07-01 23:00 CT - P3 public SEO hreflang dedupe

- Prevented duplicate SSR `hreflang` alternates when Angular-rendered SEO links and Express head decoration both exist in the final HTML.
- The server now skips fallback `hreflang` output when Angular SSR already rendered alternates and strips pre-rendered alternates before injecting server fallbacks when needed.
- Verification passed: `npm run build` passed with the existing `quill-delta` CommonJS warning; focused SSR draft context test returned `1` pass; focused Zoosite content-hub SSR SEO test returned `1` pass; content-hub product-readiness smoke unit tests returned `24` pass; `npm audit --omit=dev` returned `found 0 vulnerabilities`.

## 2026-07-01 23:13 CT - P3 public QA fixture cleanup gate

- Added `npm run content-hub:public-fixture-audit` to check public content-hub search, sitemap, feed, and `/blog` for old QA fixture article titles, slugs, routes, tags, and categories.
- Cleaned the live Zoosite testing content-hub public surface by archiving six old QA smoke articles, deleting their public slug rows, and hiding QA-only taxonomy records in the test metadata table. The real seed article `art_20260620_blog_builder` was left public.
- Verification passed: live test audit returned no fixture leaks for `/content-hub-search.json`, `/sitemap.xml`, `/feed.xml`, and `/blog`; the search surface returned only `art_20260620_blog_builder`.

## 2026-07-01 22:49 CT - P3 public SEO preview-query cleanup

- Hardened SSR SEO URL generation so canonical/hreflang-style URLs drop `draftDomain`, `draftPageId`, `debugWorkspace`, `cacheBust`, ad params, and sensitive contact-like query params before entering SEO tags.
- Added focused SSR regression coverage proving dirty preview/ad query params do not appear in rendered canonical or hreflang links while route language alternates still render correctly.
- Verification passed: `npm run build` passed with the existing `quill-delta` CommonJS warning; focused draft SSR context test returned `1` pass; content-hub product-readiness smoke unit tests returned `24` pass; runtime-read environment/public-index tests returned `2` pass.

## 2026-07-01 22:08 CT - Generic component preview primitive

- Added `generic-component-preview` as a reusable Angular primitive that renders draft-authored TGeneric JSON from scope, variable, or literal sources through auxiliary runtime components.
- Hardened preview inputs by enforcing an allowlist of renderable generic component types and stripping event instructions or sensitive runtime keys from preview payloads.
- Extended config validation, wrapper orchestration, and contract tests so drafts can use the primitive without blog-specific Angular managers.
- Verification passed: focused config validation Karma returned `TOTAL: 79 SUCCESS`; Zoosite content-hub admin/bindings Node contracts returned `34` pass; `npm run build` passed with the existing `quill-delta` CommonJS warning; `npm audit --omit=dev` returned `found 0 vulnerabilities`.

## 2026-07-02 12:08 CT - Runtime bundle domain ownership guard

- Rejected browser runtime bundles whose payload belongs to a different draft domain than the requested runtime domain, preventing shared-testing canonical 404 fallbacks from being cached or rendered for Zoosite protected article detail routes.
- Added focused `ConfigSourceService` coverage for a Zoosite protected detail route receiving a wrong-domain `zoolandingpage.com.mx` not-found payload.
- Verification passed: focused Karma for `config-source.service.spec.ts` returned `TOTAL: 18 SUCCESS`.

## 2026-07-04 02:12 CT - Public blog card and search-box rendering repair

- Updated `generic-search-box` to resolve dynamic placeholder and ARIA values before rendering so draft-composed search inputs do not expose serialized value-instruction code to users.
- Updated `generic-card` to skip the feature-icon wrapper when no icon is configured, preventing empty icon tiles in public article cards.
- Added focused regression coverage for dynamic search-box labels and iconless cards.

## 2026-07-01 21:23 CT - Blog P2 advanced visual structure save path

- Added a generic `jsonVarOr` value handler so draft-composed JSON editors can hydrate formatted JSON from runtime variables such as content-hub `articleDetail.components`.
- Changed content-hub `updatePackage` runtime input building to parse `componentTreeJson` only when `advancedMode` is active, recursively sanitize the parsed tree, strip the raw field, and submit the result as `components`.
- Added focused coverage for advanced JSON hydration, malformed JSON fail-closed behavior, and Zoosite editor contracts that now include the advanced visual structure in article saves.

## 2026-07-01 20:26 CT - Combo catalog runtime input validation repair

- Renamed the public combo catalog draft input field from `groups` to `comboGroups` and mapped it client-side back to the backend `groups` payload so browser runtime validation keeps rejecting auth-like `groups` inputs.
- Added combo catalog contract coverage to prevent Zoosite from exposing `groups` as a public runtime action input again.
- Verification passed: local Zoosite config validation returned `siteConfig: true`; combo/site-config Node contracts returned `12` pass; content-hub admin/bindings Node tests returned `34` pass; focused Karma for combo client and config validators returned `82 SUCCESS`.

## 2026-07-01 19:15 CT - Zoosite P1 editor primitives completion

- Completed the Zoosite article editor P1.5 primitive layer with a generic modal inspector, component allowlist preview, Combo Catalog-backed selector, controlled visual preview card, and continuously validated JSON textarea kept outside article save payloads.
- Aligned Zoosite taxonomy and media admin tables with their real row identifiers and payload fields: categories/tags use taxonomy fields and article-filter row actions, while media lists use `assetId` plus public metadata fields.
- Added contract coverage for the editor modal, combo option data source mapping, JSON validation isolation, taxonomy creation/table placement, taxonomy row actions, and media asset table payloads.
- Verification passed: Zoosite content-hub/combo/schema Node contract tests returned `46` pass, and focused Karma for `generic-rich-text`, `generic-input`, and runtime data-source mapping returned `47 SUCCESS`.

## 2026-07-04 07:22 CT - Browser host wins over stale SSR request metadata

- Fixed browser-side draft domain resolution so hydrated branded hosts such as `zoositioweb.com.mx` keep using `window.location.hostname` instead of a stale SSR `REQUEST` token from the platform front door.
- This prevents the client bootstrap from replacing a valid Zoosite `/blog` SSR page with the canonical ZoolandingPage 404 after hydration.
- Verification passed: focused `DomainResolverService` Karma spec returned `13 SUCCESS`; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-07-01 18:41 CT - Advanced editor JSON validation primitive

- Added a reusable `json` validation rule to `InteractionScopeService` so draft-composed editors can validate JSON textareas continuously through existing `generic-input` validation.
- Added focused component/scope coverage for valid and invalid JSON textarea state without introducing a blog-specific Angular editor component.
- Verification passed: focused Karma for `interaction-scope`, `generic-input`, and `generic-rich-text` returned `41 SUCCESS`; Zoosite content-hub/combo Node contract tests returned `39` pass.

## 2026-07-01 17:53 CT - Combo catalog admin runtime wiring

- Added `kind: "combo-catalog"` support to runtime data sources and runtime API actions so generic draft admin pages can call the combo catalog BFF without routing through the public API proxy or content-hub client.
- Added the combo catalog client and request sanitizer for public-safe read/action payloads, comma-list normalization, batch JSON normalization, auth profile headers, CSRF-protected writes, same-origin endpoint validation, and localized user-facing errors.
- Extended config validation and `site-config.schema.json` for combo catalog read/action bindings.
- Added focused coverage for combo catalog request sanitization, client request shape, localized errors, runtime data-source dispatch, action dispatch, schema, and Zoosite draft contract.

## 2026-07-01 16:55 CT - Combo catalog deploy and front-door routing

- Added `tools/ops/sync-combo-catalog-front-door.mjs`, `ops:sync-combo-catalog-front-door`, and front-door contract tests for exact-path routing of `/features/combo-catalog/read` and `/features/combo-catalog/action`.
- Deployed the new public `zoolanding-combo-catalog` repository through dev/test/main promotion workflows. GitHub Actions reported success for CI and deploy jobs on all three branches.
- Confirmed CloudFormation stacks `zoolanding-combo-catalog-dev`, `zoolanding-combo-catalog-test`, and `zoolanding-combo-catalog-prod` are `CREATE_COMPLETE`.
- Confirmed DynamoDB tables in all three environments use `PAY_PER_REQUEST`, SSE `ENABLED`, and PITR `ENABLED`.
- Applied the Traefik front-door route through SSM command `2a09d0db-ea3f-4aa7-8830-6b35335ff520`; the remote file `/etc/dokploy/traefik/dynamic/zoolanding-combo-catalog.yml` was created with `backup=none` and `updated=...zoolanding-combo-catalog.yml`.
- Verification passed: `node --test tools/tests/draft-command-instructions.spec.mjs tools/tests/zoosite-combo-catalog.spec.mjs tools/tests/site-config-schema.spec.mjs tools/tests/ops-combo-catalog-front-door.spec.mjs` returned `17` pass; `npm run test:ops-front-door` returned `52` pass; focused Karma returned `28 SUCCESS` for combo runtime and `19 SUCCESS` for auth route access cache; `npm audit --omit=dev` returned `found 0 vulnerabilities`; `python -m unittest discover -s tests -p "test_*.py"` in `zoolanding-content-hub` returned `67` tests OK; `python -m unittest discover -s tests -p "test_*.py"`, `sam validate`, and `pip-audit -r requirements.txt` passed in `zoolanding-combo-catalog`.
- Live smoke passed through execute-api and front-door: test/prod `runtimeCombos` reads returned `200` with empty safe combo data, unauthenticated action returned `401 Authentication is required`, and an unapproved origin returned `403 Combo catalog access denied`.
- Known gap: Zoosite `/admin/combos` remains a protected generic admin surface, but full CRUD wiring to combo-catalog read/action endpoints is not product-complete yet.

## 2026-07-01 16:40 CT - Combo catalog precedence hardening

- Changed `AngoraCombosService` merge order so the `combo-catalog` auxiliary scope loads below local draft `angora-combos.json` files, while temporary debug/preview auxiliary scopes still override local combos.
- Added regression coverage for catalog-vs-draft precedence and temporary preview overrides.
- Verification passed: focused Karma for `angora-combos.service.spec.ts` and `combo-catalog-runtime.service.spec.ts` returned `28 SUCCESS`; `node --test tools/tests/draft-command-instructions.spec.mjs tools/tests/zoosite-combo-catalog.spec.mjs tools/tests/site-config-schema.spec.mjs` returned `11` pass. ChromeHeadless failed its first local launch attempt because it could not create a temp dir, retried, connected, and completed successfully.

## 2026-07-01 06:10 CT - Generic combo catalog foundation

- Added the browser-safe `runtime.comboCatalog` contract to TypeScript types, runtime validators, and `site-config.schema.json`.
- Added `ComboCatalogRuntimeService` to resolve optional remote combos and install them into Angora as the `combo-catalog` auxiliary scope while preserving local combo fallbacks.
- Added a draft command-instruction gate that fails when Zoosite root runtime `eventInstructions` or `valueInstructions` reference command IDs not present in app allowlists.
- Added generic combo catalog documentation covering runtime references, read/write contracts, capabilities, and precedence.
- Verification passed: `node --test tools/tests/site-config-schema.spec.mjs tools/tests/draft-command-instructions.spec.mjs tools/tests/zoosite-combo-catalog.spec.mjs` returned `11` pass, focused Karma validators returned `79 SUCCESS`, focused combo runtime service specs returned `3 SUCCESS`, `npm run build` passed with the existing `quill-delta` CommonJS warning, and `npm audit --omit=dev` returned `found 0 vulnerabilities`.

## 2026-07-01 02:00 CT - Content-hub product-readiness hardening

- Filtered browser runtime content-hub projections by effective language so Angular hydration does not mix `publicArticles` or `publicTaxonomy` from other locales.
- Filtered content-hub sitemap entries by request language and made dynamic category pages self-canonical from runtime public taxonomy instead of inheriting fixed page canonical values.
- Hardened content-hub action ID filtering so targeted mutations fail closed when required IDs such as `articleId`, `revisionId`, `scheduleId`, or `commentId` are missing, while taxonomy creation remains valid without a pre-existing taxonomy ID.
- Stabilized `generic-rich-text` Quill module references so unrelated config replays do not reconfigure the editor and reset selection/cursor.
- Expanded product-readiness smoke coverage to reject internal/sensitive fields in analytics, taxonomy, media, and moderation responses, and to close the lifecycle with `archiveArticle` plus `articleDetailAfterArchive`.
- Synchronized content-hub contract docs and the Zoosite QA checklist with the complete read/action/check list.
- Verification passed: focused Karma returned `36 SUCCESS`; content-hub admin/bindings/smoke node tests returned `58` pass; SSR server tests returned `23` pass; `npm run build` passed with the existing `quill-delta` CommonJS warning.

## 2026-07-01 00:40 CT - Preview read support ID contract

- Aligned the Zoosite blog admin contract test so support-ID assertions apply only to content action errors that expose safe backend `requestId` values, not to the protected preview read status that intentionally omits `requestId`.
- Verification passed: `node --test tools/tests/content-hub-admin-pages.spec.mjs tools/tests/zoosite-content-hub-admin-bindings.spec.mjs` returned `34` pass, and `node tools/runtime-data-source-condition-guard.mjs --draft-root=. --domain=zoositioweb.com.mx` returned `ok: true`, `issueCount: 0`.

## 2026-07-01 01:16 CT - Runtime auth redirect during bootstrap

- Fixed browser-side runtime navigation so protected server-cookie routes that redirect during app initialization, such as Zoosite `/admin/blog`, render `/acceso` instead of leaving an empty shell.
- Added focused regression coverage for unauthenticated `/admin/blog` bootstrap redirecting to the login page with roots rendered.
- Verification passed: runtime service specs returned `23` success; app config + runtime + auth specs returned `45` success; content-hub/admin node tests returned `80` pass; `npm run build` passed with the existing `quill-delta` CommonJS warning; `npm audit --omit=dev` returned `found 0 vulnerabilities`; local SSR browser QA confirmed `/admin/blog?draftDomain=zoositioweb.com.mx&debugWorkspace=false&lang=es` redirects to rendered `/acceso`.
- Promotion PRs merged into `dev` and `test`, but the public testing host still served the older `main-242GV75R.js` bundle immediately after merge. Run or wait for the shared Dokploy app container deploy before treating the runtime fix as live on `https://test.zoolandingpage.com.mx`.
