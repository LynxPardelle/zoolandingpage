/**
 * AppContainer Barrel Export
 * 
 * Central export point for AppContainer component and related types.
 * Following MANDATORY atomic file structure requirements.
 */

export { AppContainerComponent } from './app-container.component';
export type { ContainerProps, ContainerSize, ContainerAlignment } from './app-container.types';
export { APP_CONTAINER_DEFAULTS } from './app-container.constants';
