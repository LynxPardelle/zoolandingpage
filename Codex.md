# Zoolandingpage Codex Memory

This file is durable project memory, not a changelog. Keep it short, stable, and useful before editing the repo.

## Purpose

- Store only important project decisions, boundaries, reusable rules, and active constraints.
- Do not add chronological closeouts, terminal transcripts, QA dumps, deployment logs, or one-off task summaries here.
- Put chronological history in `changelog/app/` or `changelog/drafts/`.
- Put reusable procedures and deeper guidance in `ai-notes/`.
- Put runtime, test, and development logs under `logs/`.

## Canonical Paths

- App source: `src/`
- App tooling and tests: `tools/`
- Local draft source tree and draft scratch workspace: `drafts/`
- Local draft-specific notes: `drafts/{domain}/ai_notes/`, `drafts/{domain}/findings/`, and `drafts/{domain}/errors-reports/`
- App changelog: `changelog/app/`
- Draft changelog: `changelog/drafts/`
- Runtime/dev/test logs: `logs/`
- Curated AI/developer guidance: [ai-notes/README.md](./ai-notes/README.md)
- Shared workspace prompts and community AI tooling hub: `.github/prompts/`
- Portable repo-local skills: `.github/skills/`
- Note templates: `ai-notes/templates/`
- Optional local-only scratch: `devonly/`
- Main implementation docs: `docs/`
- Draft repo registry: `docs/drafts-registry.json`
- Generated Microsoft documents, PDFs, and images: `Output/`

## Memory Boundaries

- `AGENTS.md` and this file are repo-level AI hooks and must stay as durable memory/rules only.
- `ai-notes/` is the curated, shared-first knowledge base for reusable workflow, authoring, QA, operations, and release guidance.
- `changelog/` is the only committed home for chronological app and draft history.
- `drafts/{domain}/ai_notes/`, `drafts/{domain}/findings/`, and `drafts/{domain}/errors-reports/` are local-only draft history and investigation areas, not committed source of truth.
- `.superpowers/`, `devonly/`, generated reports, and raw logs are local scratch unless a reusable lesson is distilled into `ai-notes/` or an event belongs in `changelog/`.
- Superpowers plans, specs, QA evidence, scripts, and companion artifacts must stay under ignored `.superpowers/`; do not commit or push `.superpowers/` or `docs/superpowers/` content.
- Keep note and changelog timestamps in Central Time.
- Notes should stay English-first to match repo documentation, while quoted source content may stay in its original language.

## Current Project Decisions

- `drafts/` is the local authored draft tree and local per-draft scratch area.
- 2026-05-15 10:44 CT: Domain drafts are mirrored to one GitHub repo per draft under `LynxPardelle/draft-*`. Per-draft repos should track sanitized runtime draft config and required public assets only. Keep `ai_notes/`, `findings/`, `errors-reports/`, `CVs_N_photos/`, PDFs, logs, `.env*`, private keys, certificates, credential JSON, local databases, local agent state, and other PII/secrets local-only through each draft repo `.gitignore`.
- 2026-05-17 CT: Draft repos may be public only after a public-safety audit passes against current files and git history. Run `node tools/draft-public-safety-audit.mjs --history=true` from the hub repo before changing visibility, before PR, and before merges that publish draft content. Blocking findings include tracked local-only folders/files, secret-looking values, credential material, local DBs/exports, and PII source documents. Review findings such as emails, phone/WhatsApp numbers, and identity keywords must be confirmed as intentional public draft content or removed.
- 2026-05-17 CT: Secure draft release workflow is active for the current public `LynxPardelle/draft-*` repos: `dev -> test -> main`, GitHub Actions deploys only from `test` and `main`, source-branch guards, GitHub OIDC to per-repo/per-environment AWS IAM roles, IAM-protected authoring Lambda Function URL for deploys, environment-aware test/production aliases, and native GitHub branch protection on `test` and `main` requiring the `guard` check with zero required approvals so the repo owner can merge their own PRs after checks pass. The `draft-zoolandingpage-com-mx` test workflow was verified through OIDC without changing the production pointer.
- 2026-05-20 CT: A successful `draft-*` deploy only proves the draft config payload was published. Before declaring a production draft ready, verify the shared app container is serving the current `main-*.js` bundle, the real browser is not stuck on the boot curtain, and the console has no config/CORS bootstrap errors. If the public domain still calls old endpoints such as `/site-config`, redeploy or wait for the app/Dokploy runtime instead of changing draft content.
- 2026-05-16 CT: Before Zoolanding or draft repo work, run `git pull --ff-only` in every clean target repo, including affected `draft-*` repos. If a target repo is dirty, report it instead of pulling over local changes. New draft repos must carry this same rule in their repo memory.
- 2026-05-17 CT: `docs/drafts-registry.json` is the canonical registry of draft domains, GitHub repos, clone URLs, and sibling local paths. Before work, `node tools/draft-repo-preflight.mjs --pull=true` must read that registry, clone any missing registered `draft-*` repo into the sibling workspace path, then pull every clean target repo. If a registered path exists but is not a git repo, or a repo is dirty, report it instead of overwriting it.
- 2026-05-18 21:14 CT: Managed `*.zoolandingpage.com.mx` aliases require two steps after draft deploy: runtime alias records from authoring, then front-door sync through `tools/ops/sync-managed-alias-front-door.mjs` for Route 53, Traefik file-provider routing, and TLS issuance. Keep front-door IP, SSM target, and remote Traefik file path out of committed notes and pass them by environment or CLI.
- 2026-05-18 21:30 CT: Runtime-read reset probes showed sequential custom-domain and raw API Gateway requests clean, but burst probes still fail: custom domain can return `ECONNRESET` when raw stays clean at moderate concurrency, while higher concurrency can also make raw API Gateway return HTTP `500`. Keep SSR raw fallback active and use `tools/ops/probe-runtime-front-door.mjs` for before/after evidence before claiming a front-door/API fix.
- 2026-05-20 CT: Runtime-read burst failures are capacity-related unless new evidence disagrees: the audit found Lambda account concurrency at `10`, API Gateway `5XXError` during burst windows, and CloudFront `/runtime-bundle*` previously on disabled caching. Keep `/runtime-bundle*` on a short CloudFront cache policy keyed by all query strings through `tools/ops/configure-runtime-front-door-cache.mjs`. Use `tools/ops/probe-runtime-front-door.mjs --cache-mode=default --target=custom-domain` to validate viewer cache, and `--cache-mode=no-store` only for origin stress. The Lambda regional quota later showed `ConcurrentExecutions: 1000`; `ConfigRuntimeRead` has reserved concurrency `100` as a no-fixed-cost cap/reservation, leaving `900` unreserved concurrency for other functions. Keep `tools/ops/configure-runtime-observability.mjs --apply` as the repeatable setup for runtime-read alarms, SNS, tags, and a notification-only monthly budget. Do not enable Provisioned Concurrency or CloudWatch Synthetics without explicit cost approval.
- 2026-05-18 21:57 CT: Express SSR helper reads for robots, sitemap, and 404 route decisions must follow the same raw-first runtime-read fallback rule as Angular bootstrap. `src/server.ts` should try `CONFIG_API_SERVER_FALLBACK_URL` / `CONFIG_API_RUNTIME_FALLBACK_URL` before `CONFIG_API_URL` and preserve stage path prefixes such as `/Prod`.
- 2026-06-28 20:02 CT: Public content-hub SSR surfaces (`/sitemap.xml`, `/feed.xml`, `/content-hub-search.json`, and article SEO head decoration) must prefer the published runtime bundle when it contains `runtime.contentHubs.publicArticles` or `publicTaxonomy`, even if a local draft config exists. Local environment still prefers local draft config. This avoids stale app-image draft config hiding newly published articles from SEO/search.
- 2026-06-29 16:18 CT: Do not declare the content-hub blog product-ready after local/unit tests alone. Run a redacted live smoke for each target environment that proves authenticated `createArticle` -> `publish` -> runtime-read `/runtime-bundle` -> public `/content-hub-search.json`, plus schedule list/cancel, using caller-supplied session cookies/CSRF without printing or committing those values.
- 2026-06-29 20:25 CT: Public content-hub search must match published article title, summary, article ID, slug, path, canonical path, category, and tags. SEO/product QA should include slug queries such as `q=blog-builder-seo`, not only visible title words, because users and internal links often search by URL terms.
- 2026-06-29 21:13 CT: Generic component `eventInstructions` may be value-orchestrated at runtime, but event dispatchers must resolve the dynamic value to a string before sending it to handlers. Use this for draft-composed blog/article analytics such as CTA events that need `contentHub.currentArticle.articleId` or path, instead of hardcoding seed article IDs in public page JSON.
- 2026-06-29 22:56 CT: Content-hub client errors shown through draft UI status surfaces must be user-facing and localized. Do not pass raw backend strings such as `Invalid id`, `Invalid identifier`, CSRF, upstream, endpoint, bucket, table, authorizer policy, or token-related messages into rendered draft content; keep raw details only in server logs or redacted support paths.
- 2026-06-30 10:13 CT: Blog module product readiness is not proven by draft/admin route coverage. The test public runtime/search/sitemap/feed path is green after localized runtime-read promotion, but the product remains incomplete until a redacted authenticated smoke passes the full create/edit/revision/preview/validate/review/approve/publish/runtime-read/search/article/sitemap/feed/schedule/cancel loop in each target environment. The smoke itself must classify missing runtime URL, missing cookie/CSRF, auth failure, payload validation, and backend rejection separately without exposing sensitive values.
- 2026-06-30 10:29 CT: Content-hub article mutations must stay scoped to row actions or detail pages that carry a concrete `articleId` and, when required, `revisionId`. Do not add global validate/publish/archive buttons to `/admin/blog` or other overview pages unless the draft first exposes an explicit, persisted selection model that the BFF can validate server-side.
- 2026-05-19 11:57 CT: Per-host alias overrides are supported through `site.hostOverrides`. Use them for alias-specific `seo.canonicalOrigin`, canonical host/HTTPS enforcement, Search Console verification, and Google tag/Ads/GTM config while keeping the base draft config as the default. Top-level `aliases`, `environments.<env>.aliases`, and `site.hostOverrides` keys all resolve to the owning draft in local SSR.
- 2026-05-19 12:05 CT: For current product aliases on Zoosite, Zoolandingpage, and Sulandingpage, aliases should canonicalize to the primary `.com.mx` domain instead of self-canonicalizing. Example: `zoositioweb.com` canonicalizes to `https://zoositioweb.com.mx`.
- 2026-05-16 CT: Approved OIDC direction is one AWS GitHub OIDC provider per account with deploy roles split by draft repo and GitHub Environment/branch. Keep it bootstrap-driven so agents can create/update roles, GitHub Environments, role ARN variables, and workflows consistently.
- 2026-05-17 CT: Draft deployment workflows should use Node 24-based GitHub Actions majors: `actions/checkout@v5`, `actions/setup-node@v5`, and `aws-actions/configure-aws-credentials@v6`. Do not generate new draft workflows with `@v4` for these actions because GitHub runners warn about Node 20 action runtime deprecation.
- `zoolandingpage` is the canonical home for shared workspace prompts and community AI tooling because it owns `zoolandingpage.code-workspace`.
- Shared prompts or AI tooling that apply across multiple Zoolanding repos should live in this repo first; service-specific deploy prompts and workflow skills stay in their service repos.
- 2026-06-26 18:28 CT: Use repo-local skill `.github/skills/zoolanding-upload-grant` for future temporary image upload grant files. It should ask for recipient, exact draft domain, asset kinds, expected uploads, max bytes, expiration, pages, overwrite, presigned PUT, and AWS override only when needed, then run `tools/issue-upload-grant.mjs`; never print or commit token values.
- `devonly/` may be used only as optional local scratch and should be cleaned when the work is done.
- Use `tools/draft-upload-status.mjs` to compare local draft JSON with the S3-backed authoring state before/after draft publish work. Treat `uploaded`, `needs-upload`, and `not-uploaded` as the standard release inventory statuses.
- Do not store secrets, credentials, tokens, raw environment values, signed URLs, private customer data, or PII in committed notes, changelogs, or generated docs.
- If a workflow depends on sensitive data, describe the dependency abstractly instead of copying the value.
- 2026-06-07 22:25 CT: Angular 22 is the active app baseline and local CLI work should use Node 24.15.0+ because Angular CLI 22 requires the newer Node line. Because NgRx 21 declared Angular 21 peers and no NgRx 22 package was available during the migration, platform auth, draft tenant/profile, public blog, and blog editor state now use Angular signals plus `@angular/forms/signals` for editor form state; revisit NgRx only after an Angular 22-compatible release exists. Config rendering payloads remain in the existing `ConfigStoreService` and `VariableStoreService` signal stores. Never put OAuth/JWT tokens, client secrets, upstream credentials, signed URLs, or server-only integration policy in signal state, browser storage, or public draft payloads. The runtime build stays zoneless; Karma tests still load `zone.js/testing` until the test suite is migrated off fakeAsync/tick helpers.
- 2026-06-08 CT: User confirmed Angular 22 plus Angular-native signals/state is the current platform direction until NgRx publishes Angular 22-compatible packages. Future Cognito work should target strong client isolation by default, but Auth design and implementation should wait until the local Node/npm/nvm verification environment can run Angular 22 build/test commands reliably.
- 2026-06-08 CT: Auth Profile Registry is a server-only contract for optional draft auth. Browser draft payloads may reference `authProfileId` and safe Cognito metadata only; tenant ownership, Cognito provisioning state, callback allowlists, group policy, social IdP secret references, JWT issuer/audience enforcement, and backend authorization policy must stay server-side. The current app repo foundation is offline and plan-only; it does not create AWS or Cognito resources.
- 2026-06-08 23:12 CT: Remote `runtime.authRemote` resolution must be treated as an in-flight de-duplication only, not a durable success cache. `DraftRuntimeService` reloads `siteConfig` on navigation, so `RuntimeConfigService` must rehydrate `runtime.auth` after a fresh config load and must refuse to install a remote response unless the current `siteConfig.domain`, `authRemote.authProfileId`, and `authRemote.endpoint` still match the request context. Reject profile mismatches and stale responses fail closed.
- 2026-06-25 02:59 CT: Generic select controls must keep trigger labels and dropdown selected state aligned when dynamic values cross string/number boundaries, especially `queryParamOr` fallbacks such as `pageSize=10`. Compare options by exact value first, then string form. `generic-table.config.actionColumnLabel` is the reusable way to label action columns for accessibility and i18n.
- 2026-06-26 18:40 CT: Runtime data sources load independent initial reads concurrently. Do not re-serialize admin page reads for perceived safety; BFFs still enforce session, tenant, CSRF for mutations, and roles server-side. Use this only for independent reads, not for authored flows where a later source truly depends on a previous source's output.
- 2026-06-26 18:40 CT: `generic-rich-text` keeps a separate Quill model from emitted/scope values so plain-text outputs can still use formatting UI without resetting cursor/selection. For text-input hydration from arrays, prefer mapper transform `joinList` over draft-specific string hacks.
- 2026-06-30 13:01 CT: `generic-rich-text` must not treat Quill `api` or `silent` content-change events as user edits. For Quill delta modes, hydrate legacy plain strings into a Delta-shaped value and pass ngx-quill `compareValues` so parent config replays or detail-refreshes do not reset cursor/selection while the user is editing.
- 2026-06-30 14:43 CT: Public draft-rendered rich-text values should use the dedicated `richTextHtmlOr` or `richTextTextOr` value handlers instead of plain `varOr`. `varOr` preserves raw objects for generic data binding, while rich-text handlers safely derive renderable text/HTML from Quill Delta, `{html}`, `{text}`, or string values and avoid `[object Object]` in public pages.
- 2026-06-29 16:59 CT: Content-hub admin article detail reads should map the protected BFF `item.articleContent` into the runtime data source result, then draft components should hydrate from `remote.contentHub.articleDetail.items.0.articleContent`. Do not bind draft components directly to the raw BFF `item` shape when the data source mapper is configured with `singleItem`.
- 2026-06-29 18:38 CT: Content-hub action status shortcuts should treat `article.latestRevisionId` and `createdArticle.latestRevisionId` as safe `revisionId` fallbacks for draft next-step links. Draft lifecycle buttons that depend on protected `articleId`/`revisionId` must use required interaction-scope fields plus `disabledWhenInvalidScope` so slow detail reads cannot submit empty IDs and surface generic `Invalid id` errors.
- 2026-06-26 20:02 CT: Server-cookie protected route checks may use a short browser-side `/auth/session/me` result cache for navigation UX through `runtime.auth.session.routeAccessCacheMs` with a hard `0..60000` ms clamp and 15s app default. This cache is only sanitized account metadata for route rendering; protected BFF/API reads and mutations must still validate HttpOnly session, CSRF, tenant/profile headers, and current roles server-side.
- 2026-06-07 23:46 CT: Angular 22 reached the shared test host and rendered all local/remote shared-test draft routes, but production promotion is blocked until configured test aliases pass public health. The failing test alias audit showed unresolved DNS aliases and AAAA records while `--fail-on-aaaa` was enabled; do not merge this Angular 22 rollout to production until those front-door/DNS findings are fixed or the alias set is intentionally narrowed and re-audited.
- 2026-06-08 CT, updated 2026-06-22 22:03 CT: New draft creation must ask the user for the intended canonical domain and every production/test alias before creating the draft repo or authoring config. The default test path is the shared host `https://test.zoolandingpage.com.mx/?draftDomain={domain}`; do not create dedicated `test.*` aliases unless the user explicitly approves that host. `test.zoositioweb.com.mx` is no longer an approved/supported test alias because its CloudFront/DNS target timed out during content-hub front-door audit; Zoosite testing should use the shared preview host until a dedicated alias is explicitly repaired and reapproved.
- 2026-06-08 CT: Managed draft aliases should support both HTTP and HTTPS at the front door, but HTTP must redirect to HTTPS before content is served. The managed alias Traefik generator owns the repeatable redirect middleware for full-file output; router-block updates must reference an existing file-provider redirect middleware or use full-file mode first.
- 2026-06-08 03:58 CT: Runtime alias repair must keep both registry layers consistent: the canonical `SITE#<domain>` metadata must list the aliases, and each public alias must also have an `ALIAS#<alias>, sk=SITE` lookup record pointing back to the canonical domain. Public health checks for campaign aliases should pass `--expected-runtime-domains=<alias>=<canonical-domain>` so a page served through Traefik cannot mask a runtime-read alias miss.
- 2026-06-08 CT, updated 2026-06-22 22:03 CT: Multi-tenant CloudFront distribution `E10Y59XAIPQY6A` uses origin request policy `3106d023-4842-41f8-9061-96474e9ca144` to forward all viewer request values plus only the CloudFront-generated `CloudFront-Forwarded-Proto` header. Keep this header available so Express SSR can distinguish HTTPS viewer requests from the distribution's `http-only` origin hop and avoid self-redirect loops on any explicitly approved dedicated test alias. Do not use legacy `test.zoositioweb.com.mx` as an example of a healthy alias until its CloudFront/DNS target is repaired and reapproved. Rollback target is the prior managed policy `216adef6-5c7f-47e4-b989-5492eafa07d3` only if post-change HTTPS checks prove a regression caused by the new policy.
- 2026-06-09 CT: Dokploy front-door recovery showed two reusable production rules. First, the EC2 public front door must use a stable Elastic IP or equivalent stable endpoint before Route 53 A records are updated; stop/start on an auto-assigned public IP breaks every managed draft alias. Second, Traefik `v3.1.2` is not compatible with Docker Engine 29's minimum API `1.44`; keep the host Traefik runtime on a newer compatible image such as `traefik:v3.6` while preserving the same mounted config, network, and published ports. Same-origin draft auth must route only the exact `/auth/runtime-config` path to `https://api.zoolandingpage.com.mx` with the API proxy host header, not `/auth/*`, because `/auth/callback` remains a draft-rendered page.

## App Guardrails

- Public SSR releases should enable text compression at the Node/Express layer, defer analytics/prefetch work until after first render, and keep accordion disclosure buttons free of `aria-selected`.
- Do not remove the `src/index.html` Angora CSS link targets (`/css/animations.css`, `/css/angora-styles.css`, `/css/angora-styles-responsive.css`) without desktop/mobile visual QA; runtime CSS generation depends on those stylesheet surfaces.
- Production `optimization.styles.inlineCritical` stays disabled while Angora runtime CSS link targets live in `src/index.html`; otherwise Angular can emit misleading `C:\css\...` warnings.
- Angora-owned classes should be generated through `ngx-angora-css` managed stylesheets, not hardcoded back into `src/styles.scss`.
- Browser-only behavior should be gated with `PLATFORM_ID`/`isPlatformBrowser(...)`; keep the optional `REQUEST` token for SSR request URL/header data only.
- 2026-06-08 00:51 CT: Karma tests run zoneless through `src/test.ts` and `src/test-providers.ts`; keep `angular.json` test polyfills empty and do not re-add `zone.js/testing`. The current `@angular/build:karma` target does not accept `providersFile`, so use the custom `main` setup until the runner changes. Angular specs should avoid Zone-only helpers from `@angular/core/testing` such as `fakeAsync`, `tick`, `flush`, `flushMicrotasks`, and `waitForAsync`; prefer async/await, direct `fixture.detectChanges()`, and Jasmine clock timers when timers are the behavior under test.
- 2026-06-08 04:52 CT: Karma specs use a global no-op `NgxAngoraService` provider from `src/test-providers.ts` unless a spec intentionally overrides it. Leaving the real Angora service active can schedule delayed `cssCreate()` timers that emit library `console.error` output in later specs and make Chrome Headless fail intermittently; specs that assert Angora scheduling should mock `AngoraCombosService` or override `NgxAngoraService` locally.
- Runtime API credentials belong in server-only policies and secret stores. Browser draft payloads should reference safe IDs, mappings, allowlists, and variable targets only.
- 2026-06-07 21:09 CT: Personalized client blogs require an auth/authorization foundation before blog mutation endpoints, client dashboards, or browser-origin image uploads are exposed. Draft browser payloads may carry only public auth profile references and safe Cognito runtime config such as issuer/client ID/callback URLs; Cognito client secrets, tokens, ownership policy, and domain-to-tenant authorization must stay server-side. Blog content that renders HTML/Markdown needs server-side validation/sanitization in addition to Angular client rendering safeguards.
- 2026-06-08 18:59 CT, updated 2026-06-09 04:06 CT: Optional draft auth is config-driven and disabled by omission. Protected route redirects resolve in this order: `routes[].auth.redirectTo`, `runtime.auth.loginPath`, then no redirect; never use `logoutPath` as a login fallback. Public auth payloads must reject secrets/tokens and unsafe URLs/paths. Express SSR now redirects protected `GET`/`HEAD` routes to the same-origin login path before Angular SSR, preserving only `draftDomain`, `debugWorkspace`, and `lang` preview params and using `Cache-Control: no-store`.
- 2026-06-08 20:21 CT: The Auth Profile Registry boundary is server-only. Only `active` profiles may generate enabled public `runtime.auth`; `planned`, `provisioning`, `suspended`, and `failed` profiles must remain disabled in browser payloads. JWT authorizer setup must validate signature separately, preserve the full Cognito issuer path for JWKS resolution, accept the expected public client through `aud` or Cognito access-token `client_id`, and enforce tenant/group policy from the server-only profile.
- 2026-06-16 11:02 CT: Draft/customer auth isolation should default to a separate Cognito user pool per paying client or strong draft boundary, not one shared multi-tenant user pool. This keeps user isolation, callback/IdP configuration, blast radius, and future per-client billing simpler. `custom:tenant_id` remains useful inside a pool for backend feature scoping such as specialized dashboards, account areas, and permissioned landing-page analytics, but it is not the primary isolation mechanism. Draft auth UX must be per-profile optional: Cognito Managed Login remains the default when enough, while drafts that need stronger branding, Spanish-first copy, show-password, confirm-password, custom registration, or server-side tenant assignment should use generic draft components backed by server-side auth Lambdas. Browsers may send only public context such as `domain` and `authProfileId`; trusted tenant/user-pool decisions stay server-side.
- 2026-06-16 23:13 CT: `LynxPardelle/zoolanding-cognito-user-lifecycle` is the separate Cognito trigger repo for user lifecycle tenant/group assignment outside custom auth forms. `zoolanding-api-proxy` remains responsible for browser-facing custom auth endpoints; the lifecycle Lambda covers Cognito-side `PostConfirmation` and optional `PostAuthentication` repair using server-side profile config only. The repo uses `dev`, `test`, and `main`, deploy environments `dev`, `test`, and `production`, and protected `test`/`main` branches with required `guard` and `test` checks.
- 2026-06-16 23:53 CT: Zoosite Cognito user pool `us-east-1_Pq5OCadbK` has lifecycle triggers attached to `zoolanding-cognito-user-lifecycle-prod-Function` for both `PostConfirmation` and `PostAuthentication`. The latter is enabled to repair console-created users on later sign-in, while custom signup remains the preferred first-token path. A one-time aggregate repair saw 2 users and verified all had `custom:tenant_id=zoosite` plus `zoosite-client` or `zoosite-admin`; no usernames/emails were printed.
- 2026-06-17 03:51 CT: Future auth-admin Lambdas must be generic and reusable across drafts, with per-draft/client isolation for Cognito user pools and DynamoDB data stores by default. `/mi-cuenta` is the authenticated account area for any approved user role, while `/admin/*` is a separate administrator-only surface. Drafts may configure these surfaces, but user approval, group elevation, suspension, audit history, tenant/environment checks, and cross-draft isolation must be enforced server-side, never by browser payloads.
- 2026-06-17 21:21 CT: Auth-admin BFF routes that skip SSR redirects for `runtime.authRemote` or `runtime.auth.session.mode: "server-cookie"` must still emit `robots noindex,nofollow` for protected routes. The Express SSR decorator now also treats `sitemap.excludePaths` and technical robots-disallow prefixes as noindex signals, so private account/admin pages are not indexable while Angular performs HttpOnly session revalidation.
- 2026-06-17 21:52 CT, updated 2026-06-18 00:52 CT: Zoosite auth audit passed the current no-token/no-secret and access-boundary checks after production promotion: protected account/admin pages emit `noindex,nofollow`, unauthenticated session/admin endpoints return `401`, runtime auth config rejects bad origins and server-only fields, unsigned provisioning endpoints return `403`, raw auth-admin API Gateway endpoints still require the Lambda-enforced session, and authenticated `/mi-cuenta` plus `/admin/usuarios` rendered without token-like DOM or console errors. Remaining hardening items: auth-admin DynamoDB PITR is `DISABLED` in test/prod, and private route bootstrap currently takes about 10-12 seconds before useful content appears.
- 2026-06-08 21:05 CT: The generic Auth service handler scaffold lives in `tools/auth-service-handlers.mjs` until it is moved into the final serverless repo. Its public runtime-config route may emit only safe `runtime.auth`; provisioning plans require an injected trusted-caller check and are denied by default; JWT authorizer handling requires an injected verifier and must never treat frontend/sessionStorage auth state as authorization.
- 2026-06-08 22:30 CT: Drafts may package server-only `server/auth-profile-registry.json` and `server/integrations.json` through authoring as `server-auth-profile-registry` and `server-integrations`, but static `/drafts` serving and browser runtime bundles must not expose them. `server/integrations.json.access` is user/JWT authorization and must stay separate from upstream credential `auth`; public `runtime.dataSources` and `runtime.apiActions` must not carry either block.
- 2026-06-09 03:39 CT: Draft packaging tools and repo deploy scripts must classify `server/auth-profile-registry.json` as `server-auth-profile-registry` and `server/integrations.json` as `server-integrations`, with no `pageId`. Keep `tools/config-draft-sync.mjs`, `tools/draft-upload-status.mjs`, and `tools/templates/draft-repo/tools/deploy-draft.mjs` aligned so manual and GitHub Actions deploys do not publish server-only files as page payloads.
- 2026-06-08 22:57 CT: Drafts may use `runtime.authRemote` as the public remote auth reference instead of full static `runtime.auth`. It may contain only `enabled`, `authProfileId`, and `endpoint`; Angular posts only `{ domain, authProfileId }` to the endpoint, validates the returned API proxy `auth` object with the public `runtime.auth` validator, installs it in memory, and fails closed without rendering protected content when the request or response is invalid. Do not author both `runtime.auth` and `runtime.authRemote`.
- 2026-06-15 19:17 CT: Draft auth UI should be assembled from generic draft components plus reusable auth event handlers, not from draft-specific Angular components. `authAction:login`, `authAction:signup`, and `authAction:forgotPassword` open Cognito Managed Login with PKCE; `authAction:logout` clears public session metadata and uses Cognito logout with allowlist-stable callback/logout URLs. Store only the temporary PKCE verifier/state and minimal public session metadata in `sessionStorage`; never persist OAuth/JWT token strings or client secrets. Callback handling may unlock client-side UX routes from validated ID-token claims, but backend authorization must still verify JWT signatures and tenant/group policy server-side.
- 2026-06-16 11:49 CT, updated 16:32 CT: Cognito Managed Login remains the default draft auth UX, but drafts may opt into custom generic auth forms per server-only `authProfileId` through `customAuth`. Use generic components plus `authFormAction` for `signin`, `signup`, `confirmSignup`, `resendConfirmation`, `forgotPassword`, `confirmForgotPassword`, and local `logout`; Angular may send only public fields, while tenant claims, default groups, user pool/client policy, and billing isolation stay server-side. Custom sign-up/reset can use local confirm-password and show-password UX, but `confirmPassword`, tenant IDs, groups, and policy fields must never be sent from browser payloads. Custom signin installs only sanitized public session metadata and still does not replace backend JWT authorization. Custom logout should clear local public session metadata and navigate same-origin, not round-trip through Cognito Hosted UI.
- 2026-06-16 18:24 CT: Custom draft auth forms may use generic input validation checklists, `matchesField` validation, and `disabledWhenInvalidScope` buttons so draft-auth UX can keep password requirements visible, require symbols, block submit until scope validity, and compare password confirmation without draft-specific Angular. These remain client-side UX gates only; backend auth Lambdas must still enforce Cognito/password policy and server-side tenant/group rules.
- 2026-06-15 21:46 CT: PKCE transaction persistence may fall back from `sessionStorage` to a short-lived same-site cookie when browser storage is blocked. This cookie is only for the temporary verifier/state, expires quickly, and must be cleared after callback; never use it for OAuth/JWT tokens, refresh tokens, client secrets, or authorization policy.
- 2026-06-09 00:26 CT: The optional auth foundation worktree includes a sanitized Zoosite pilot under `drafts/zoositioweb.com.mx` using public `runtime.authRemote` only, placeholder `/acceso` and `/auth/callback`, and protected `/mi-cuenta` for `zoosite-client`/`zoosite-admin`. Its `server/auth-profile-registry.json` is server-only, plan-only, `status: planned`, and uses non-secret refs such as `/zoolanding/auth/zoosite/staff/google`; no AWS, Cognito, deploy, raw secrets, or static browser auth payload were created.
- 2026-06-09 01:36 CT: `drafts/zoositioweb.com.mx` is a real nested `draft-zoositioweb-com-mx` repo and must not be vendored by the app repo. Keep app-level Zoosite auth contract regression data under `tools/tests/fixtures/zoosite-auth-pilot/`; apply actual Zoosite auth pilot content in the draft repo itself, preserving the full commercial draft routes, SEO, analytics, and alias configuration.
- 2026-06-09 00:43 CT: Local optional-auth QA uses the sibling `zoolanding-api-proxy` local auth harness plus `npm run start:local-auth` so `/auth/runtime-config` is proxied from Angular dev server to a real local API proxy. The browser still sends only `{ domain, authProfileId }`, while the proxy reads `server/auth-profile-registry.json` from an explicit env-configured local server-only source and returns Angular-valid public `runtime.auth` metadata with `enabled: false` for non-active profiles.
- 2026-06-08 23:55 CT, superseded 2026-06-09 01:36 CT: Do not allowlist real domain draft repos such as `drafts/zoositioweb.com.mx` in the app repo. Keep domain drafts ignored by the hub and keep sanitized auth pilot assertions in fixtures under `tools/tests/fixtures/` so local-only folders such as `findings/`, `logs/`, `CVs_N_photos/`, `.env`, or extra `server/*` files cannot be accidentally tracked by the app repo.
- Production draft domains must ignore `draftDomain` query params. Cross-draft preview by query param is allowed only on local hosts and `test.zoolandingpage.com.mx`.
- `generic-media` may use bounded image retries for transient asset CDN resets, but do not treat those retries as proof that the CDN/API/front-door reset source has been repaired.
- Express SSR helper reads should prefer the raw runtime-read fallback and use only bounded retries before falling back to the API custom domain.
- 2026-06-04 16:10 CT, updated 18:36 CT: Loading-curtain readiness for Angora drafts must account for rendered combo CSS, not just "some CSS rules exist." Before explicit `cssCreate` class updates, sync draft combos from `ConfigStoreService`, split authored class strings into individual tokens, and keep the curtain active until the current DOM has rendered combo classes such as `sectionTitle`, their critical color/text combo markers exist (for example `__COM_sectionTitle-titleColor`), and matching elements compute the expected token color. Re-sample the DOM after successful readiness passes before hiding, because dynamic roots or same-combo elements can appear after the first class list is checked. Before calling `hideWhenReady`, verify rendered critical text combos (`sectionTitle`, `sectionSubtitle`, `heroCaption`) compute their theme token colors, and keep the same guard in `LoadingCurtainService` because runtime early-exit paths funnel through that service. Resolve expected critical text colors from `VariableStoreService.theme.palettes` first, not from the current CSS var, because production can briefly have stale `--ank-titleColor`. The static SSR curtain release in `src/main.ts` must also require readable contrast for the same critical text hooks before adding `zlp-boot-curtain--leaving`, because selector coverage alone can still reveal light text on a light background before theme colors hydrate. Keep a finite production fallback, but use a long enough readiness budget for slow Angora combo generation; 4 seconds exposed Eros before `sectionTitle` computed its final color, while 20 seconds gives the runtime room to settle. Use a timer fallback around `requestAnimationFrame` in readiness code so automated/background browsers cannot leave the curtain permanently stuck.
- 2026-06-16 CT: SSR pages that include form controls (`form`, visible `input`, `textarea`, `select`, `generic-input`, or `interaction-scope`) must bootstrap Angular immediately and keep the boot curtain/loading state until client event handlers are ready. Do not expose draft auth forms as visually ready while the delayed production bootstrap is still pending, because users can type into SSR inputs before validation, password toggles, or submit handlers are active. Custom same-origin draft navigation that bypasses Angular Router must emit the shared client-navigation start/end events so the app-shell loading bar covers the runtime config refresh.
- 2026-06-17 CT: Custom draft-auth success flows should use same-origin navigation with short `authStatus` query flags for user-facing banners, and must not put the user's email in the URL. Treat `authStatus` and `queryEq` conditions as display-only UX hints, never as authorization or proof that an account action happened.
- 2026-06-18 17:43 CT: Generic custom-auth form failures may expose a sanitized `errorCode` in `authForm.<action>.errorCode` alongside the human-readable error. Drafts should use codes such as `auth_environment_mismatch` and `auth_challenge_expired` for localized, draft-specific copy instead of matching long backend messages. Codes are UX hints only and do not authorize user state.
- 2026-06-18 18:01 CT: Server-only auth profiles may set `environmentClaimMode: "list"` to let a verified Cognito custom claim such as `prod,test` satisfy either stack environment. The default remains strict `single`; use list mode only as an explicit draft/profile opt-in for admin/test or transitional accounts, and do not treat it as client isolation.

## Draft Memory

- 2026-06-07 02:47 CT: `pamelabetancourt.com` is now positioned around a clear first offer: free Instagram/Facebook strategic diagnosis, monthly personalized content planning, and strategic social-media support. Keep primary CTAs focused on requesting the free diagnosis. Pamela's WhatsApp target is now intentionally client-facing; primary diagnosis CTAs may route to the WhatsApp flow with the provided prefilled diagnosis message, while `/contact` remains available for the Google Form.
- 2026-06-07 03:27 CT: `pamelabetancourt.com` testing publication used draft PR #5 (`dev` -> `test`) and deployed version `20260607T091615-test-29c6f63d9470`. Remote QA passed on `https://test.zoolandingpage.com.mx/?draftDomain=pamelabetancourt.com&debugWorkspace=false` and `https://test.pamelabetancourt.zoolandingpage.com.mx`; `test.pamelabetancourt.com` did not resolve from the local machine, so use the managed Zoolanding test alias/shared preview until branded test DNS is configured.
- `zoositioweb.com.mx` is the current complete-website product draft. It should stay commercial, client-facing, bilingual-ready, and theme-ready, with WhatsApp as the first contact path until first-class forms/email/CRM integrations are productized.
- 2026-06-15 19:17 CT: Zoosite auth now has a real active Cognito profile for the `staff` auth profile. The local nested draft registry may contain public Cognito IDs and same-origin paths only; Google/Facebook IdPs remain optional per draft and are not required for the current Cognito-only test. Self-registered or console-created users must be assigned to `zoosite-client` or `zoosite-admin` before `/mi-cuenta` is usable.
- 2026-06-15 21:17 CT, updated 2026-06-16 16:32 CT: Shared-host auth testing for draft previews must preserve `draftDomain` through Cognito OAuth. When login starts from `https://test.zoolandingpage.com.mx/...?...draftDomain=...`, the PKCE transaction stores the exact same-origin `redirectUri`, token exchange reuses that exact URI, and Cognito app clients used for testing must allow the matching shared-test callback/logout URLs. Hosted UI logout URLs should preserve only allowlisted preview params such as `draftDomain` and `debugWorkspace`; omit open-ended language variants such as `lang` unless the exact URL is registered in Cognito. Do not use this to bypass per-draft auth isolation; it is only a testing transport for the draft preview host.
- 2026-06-16 17:31 CT: For draft-only Zoosite auth UI fixes, prefer Angora classes already present in the deployed app CSS bundle when publishing to testing. Local dev can generate newer draft class tokens that are not yet available on the remote shared test host, so remote QA must verify visible icon dimensions and contrast after the draft deploy. Browser QA against SSR/hydrated draft pages should wait for `#zlp-boot-curtain` removal before asserting link URLs or clicking form controls; otherwise static SSR anchors or pre-hydration controls can create false negatives.
- 2026-06-16 18:00 CT: Zoosite custom-auth secondary actions should render as real touch targets, not inline text links. Use generic `link` components with deployed Angora tokens that produce full-width visible buttons at least 44px tall on mobile, and keep labels explicit enough to distinguish account confirmation from password reset.
- 2026-06-16 CT: Shared app container builds must exclude local `drafts/` folders from Docker context. Real domain drafts are ignored nested repos and should be served through the published runtime API; vendoring them into the app image can make SSR head metadata and the boot curtain prefer stale local config over the current draft deploy.
- 2026-06-16 CT: Shared testing SSR preview must pass `environment=test` on auxiliary runtime-bundle reads used for site config, page config, 404 checks, redirects, robots, sitemap, JSON-LD, icons, and the boot curtain. Otherwise Angular SSR can render the test draft while Express head decoration still reads the production pointer.
- 2026-06-15 21:23 CT: Dokploy app checkout remotes must not embed GitHub tokens or other credentials in `origin` URLs. If one is discovered, replace it with a non-secret GitHub URL, avoid copying the value into notes/logs, and rotate or revoke the old token because process output or SSM command history may have exposed it.
- 2026-05-15 10:58 CT: Use `zoositioweb` as the public brand name for the complete-website product. Do not use the previous short name in active draft content, SEO metadata, WhatsApp messages, aliases, storage keys, or authoring scripts.
- zoositioweb Phase 1 active routes are `/`, `/servicios`, `/planes`, `/sitios-web-para-pymes`, `/plataforma`, `/nosotros`, `/preguntas-frecuentes`, `/contacto`, `/sectores/consultorios`, `/sectores/despachos`, `/sectores/inmobiliarias`, `/sectores/servicios-locales`, `/sectores/constructoras`, and `/sectores/agencias`.
- zoositioweb plan names are `Presencia`, `Clientes`, and `Crecimiento`; keep public copy outcome-led and avoid exposing internal cost structure.
- 2026-05-14 22:19 CT: zoositioweb keeps FAQs centralized in `/preguntas-frecuentes` instead of repeating FAQ accordions on every commercial page. Public copy should stay commercial, positive, and client-facing; avoid negative phrasing, promises, and internal terms such as `draft`.
- 2026-05-14 22:19 CT: zoositioweb dark theme uses `#25d366` as the live-data accent and `#128c7e` as the darker WhatsApp action color through `successColor`; WhatsApp CTAs should use `successColor`, not the general accent token.
- 2026-05-14 22:19 CT: For draft work, avoid changing app code unless the user explicitly asks for it or grants permission. Prefer draft configuration first; if the draft cannot express the needed behavior and app changes are not approved, record the gap as a possible future platform feature in AI notes.
- 2026-05-14 22:19 CT: zoositioweb card icon slots should be handled through draft classes with explicit SVG width/height classes, and local-only card hrefs may include the draft preview query params when raw anchor card links must preserve preview context.
- 2026-05-14 22:46 CT, superseded 2026-05-15 CT: `zoolandingpage.com.mx` served `sulandingpage.com.mx` and `sulandingpage.com` temporarily before Sulanding became its own draft. Current Zoolanding aliases should stay scoped to `zoolandingpage.com` and its test aliases unless a new cutover explicitly changes them.
- 2026-05-15 CT: zoositioweb route design should keep distinct page personalities through draft configuration: home as commercial storefront, servicios as process/workflow, planes as comparison/pricing board, plataforma as technical foundation, contacto as action-focused intake, FAQs as help center, and sector pages with industry-specific accents.
- 2026-05-15 09:44 CT: zoositioweb sector pages should not share one generic visual template. Keep consultorios calm and appointment-oriented, despachos formal and practice-area oriented, inmobiliarias search/listing oriented, servicios locales fast-contact and service-area oriented, constructoras portfolio/project oriented, and agencias case-study/portfolio oriented. Express these differences through draft configuration first.
- 2026-05-18 17:29 CT: zoositioweb sector pages may use page-local complete `variables.theme` overrides for sector-specific palettes. The runtime merges page variables over shared variables and `ThemeService` applies `variables.theme`, but the validator requires complete light/dark 18-token palettes. For sector accents, prefer `var(--ank-accentColor)` in component styles so theme changes control borders and gradients without app/global style edits.
- 2026-05-18 18:21 CT: zoositioweb CTAs that use `successColor` should pair it with `onSuccessColor` instead of hard-coded white text so sector palettes remain readable in light and dark themes. Expandable controls should expose an `expand_more` affordance and emit `accordion_toggle`; theme and language controls should keep their behavior action and also emit measurable events.
- 2026-05-18 18:47 CT: Keep `/nosotros` exposed in shared header, mobile navigation, footer, and home because team expertise is a sales asset for zoositioweb. Home should keep this as a compact expert-proof section that links to `/nosotros`; deeper team detail belongs on the `/nosotros` route.
- 2026-05-18 18:47 CT: Future zoositioweb SEO expansion remains blog/guides, comparison pages, location pages, case studies/examples, deeper platform/cloud-limit content after real cost validation, and unsupported integration pages only after those capabilities are productized.
- 2026-05-18 19:06 CT: zoositioweb home and `/nosotros` team cards should use borderless skill badges with SVG icons instead of the generic-card empty feature-icon square. Do not publish "photo pending" copy for Oswaldo.
- 2026-05-18 21:08 CT: User confirmed Oswaldo García should keep Google Ads in Zoosite team cards but not marketing; marketing belongs to Pamela Betancourt. Use `drafts/zoositioweb.com.mx/CVs_N_photos/oswaldo.jpeg` for Oswaldo and frame him around web development, responsive work, and Google Ads. Hector Coronado's public role should emphasize `desarrollo de sitios y aplicaciones web profesionales` instead of backend/fullstack wording.
- 2026-05-18 21:20 CT: Zoosite team positioning: Alec Montaño and Hector Coronado should use the public role `Desarrollo de software, Arquitectura de Nube, Ingeniería de Datos e IA`; Oswaldo García should use `Desarrollo web y Google Ads`; Pamela Betancourt is the only team member positioned with marketing. `/nosotros` should be SEO-oriented around software, cloud, data, and AI, with a post-contact Pamela Betancourt recommendation/backlink section to `https://pamelabetancourt.zoolandingpage.com.mx/`.
- 2026-05-18 21:46 CT: Zoosite should treat `datos propios`, `analítica propia`, and `recopilación/análisis de datos desde el día 1` as a core sales and SEO advantage across the product site. Public copy may mention the team's own system collecting interaction signals such as pages consulted, section attention, important clicks, language choice, and WhatsApp actions, while avoiding claims about sensitive personal-data collection or unsupported integrations.
- 2026-05-18 22:32 CT: Zoosite SEO hardening should keep route titles under roughly 65 characters, descriptions in the 100-170 character range, unique sector descriptions, canonical URLs on `https://zoositioweb.com.mx`, hydrated JSON-LD, `/404` and unknown routes as `noindex,follow`, and accessibility-safe draft markup. Avoid semantic `ul/li` in config patterns where `wrapper-orchestrator` becomes a direct child of the list; use visual `div` rows unless the app can render list children without wrappers.
- 2026-05-18 23:44 CT: Zoosite may publicly communicate first test round in 5 days when base scope and materials are complete, prices before IVA, 100% invoiceable service, human support from developers, and consulting guidance for technology, data, cloud, and AI. Keep `/privacidad` and `/terminos` as draft-local legal footer routes.
- 2026-05-19 11:57 CT: Zoosite, Zoolandingpage, and Sulandingpage have disabled Google tag scaffolds, ad-attribution storage, and `site.hostOverrides` for their configured aliases. Do not enable Google tags or Search Console verification until real GA4, Google Ads, conversion label, optional GTM, and Search Console values are provided.
- 2026-05-19 16:52 CT, updated 20:19 CT: GA4 measurement is configured for Zoolandingpage, Sulandingpage, and Zoosite primary domains plus the provided `.com` aliases. The current Google Ads direction is GA4-only and centralized on the `zoositioweb.com.mx` GA4 stream (`G-QRWR768FCM`) because the external owner configured GA4 cross-domain measurement; do not add physical `AW-...` tags or Ads conversion labels unless that strategy changes explicitly. Internal `whatsapp_click` maps to Google event `lead_conversion_whatsapp` with safe `pyme_id` values (`zoolandingpagecommx`, `sulandingpagecommx`, `zoositiowebcommx`). Optional GTM IDs and Search Console verification values are still pending.
- 2026-05-19 20:03 CT: GA4-forwarded events now wait for `gtag` `event_callback` with a 200 ms fallback timeout. WhatsApp handlers already wait for `AnalyticsService.track(...)` before opening `wa.me`, so GA4-only `lead_conversion_whatsapp` gets a delivery window without adding direct `AW-...` tags.
- 2026-05-19 23:12 CT: For zoositioweb WhatsApp analytics coverage, keep link tracking declarative at component/draft config level. Do not add a global document-level WhatsApp click fallback unless the user explicitly reopens that strategy.
- 2026-05-19 16:54 CT, updated 20:19 CT: Aliases with active GA4 measurement should render with `enforceCanonicalHost: false` and canonical links to the primary `.com.mx` domain; a 301 redirect would prevent the alias tag from firing, whether the alias uses the central GA4 stream or its own destination.
- 2026-05-18 10:18 CT: On zoositioweb, when a page-level closing section before the footer needs to avoid card-inside-card composition, prefer a full-width band and transparent inner layout container. Apply it to the specific page being reviewed; `/planes` currently uses this pattern without inner borders and with right-aligned CTAs, while home keeps its previous contained close.
- 2026-05-20 18:06 CT, updated 20:26 CT: Provisional Zoolandingpage/Zoosite identity uses a compact square Z logo with a teal rule, no decorative dot, plus a temporary `zoositioweb` wordmark for contexts where text branding is useful. Draft browser icons belong in `site-config.json.site.icons`; use HTTPS public asset URLs from `assets.zoolandingpage.com.mx` when assets are uploaded, with `/assets/brand/zoolandingpage-default-favicon.svg` as the app fallback. Default social share images should use the uploaded raster logo card at `https://assets.zoolandingpage.com.mx/zoolandingpage.com.mx/shared/seo-images/zoolandingpage-zoositioweb-default-logo-card.jpg`; drafts may override `site.seo.defaultImage` or page Open Graph/Twitter image fields when they have their own social artwork.
- `pokeapi-demo.zoolandingpage.com.mx` is the client-facing runtime API demo and should keep API-specific labels, routes, and per-type styling in draft mapper/config data rather than hardcoding them into generic app code.
- `music.lynxpardelle.com` keeps Spotify/TIDAL credentials out of draft config and browser payloads. Server-only runtime API proxy patterns and Secrets Manager references are required before reactivation.
- 2026-06-17 02:16 CT: Auth testing must use the separate `zoolanding-api-proxy-test` stack for `/auth/*` when the app runs on `test.zoolandingpage.com.mx`. Drafts that share one Cognito pool across test/prod may declare server-only `environmentClaim`, currently `custom:zoolanding_env` for Zoosite; custom signup writes the environment from the backend stack and JWT validation must reject mismatches.
- 2026-06-17 04:48 CT: Generic auth-admin features use a serverless BFF with HttpOnly `__Host-zlp_session` cookies, readable `zlp_csrf` plus `X-ZLP-CSRF`, and request context headers for `domain` and `authProfileId`. `/mi-cuenta` is the authenticated self-service surface for any draft user, including pending users, while `/admin/*` is admin-only and must re-check fresh server-side user state before every admin action. Future blogs, analytics dashboards, uploads, and draft configuration tools should reuse this pattern instead of returning browser OAuth/JWT tokens. Auth Profile Registry JWT policy requires a subject plus allowed Cognito `token_use`; `allowedTokenUses` stays server-only and defaults to `id` plus `access`.
- 2026-06-17 CT: Auth-admin front-door routing must be path-specific and same-origin: route only `/auth/session`, `/auth/session/*`, `/auth/admin`, and `/auth/admin/*` to the auth-admin BFF. Keep `/auth/runtime-config` owned by the API proxy and `/auth/callback` owned by the Angular draft app; never add a broad `/auth/*` router. Store auth-admin deployment config as a GitHub Environment secret, not a visible variable, even when it contains no raw credentials.
- 2026-06-17 16:55 CT: The `zoolanding-api-proxy-test` stack is still the required auth/API proxy target for `test.zoolandingpage.com.mx`, but its API Gateway stage path is `/Prod` because the shared SAM template keeps `StageName: Prod` literal to preserve production logical IDs. Angular `apiProxyTestUrl` must point to `https://11zpm6wug2.execute-api.us-east-1.amazonaws.com/Prod`; environment isolation comes from the separate stack/API ID plus `AUTH_RUNTIME_ENVIRONMENT=test`.
- 2026-06-17 18:09 CT, updated 2026-06-18 23:05 CT: Protected draft route guards must revalidate with the auth-admin BFF when `runtime.auth.session.mode` is `server-cookie`. Do not rely only on editable `sessionStorage` public auth metadata for custom-auth `/mi-cuenta` or `/admin/*` routes; call `/auth/session/me` with same-origin credentials before rendering protected draft content. A positive browser metadata match is still not authorization: server-cookie protected routes must revalidate with the BFF even when local metadata already appears authenticated and has the required group.
- 2026-06-17 18:49 CT: Auth-admin browser clients must resolve the draft domain from `DomainResolverService` when `siteConfig.domain` is not yet available during first-load protected-route checks. This keeps direct reloads of server-cookie protected routes from losing the `X-ZLP-Domain` header before config bootstrap fully settles.
- 2026-06-17 19:08 CT: Direct protected-route reloads with `runtime.authRemote` must be allowed to revalidate against the auth-admin BFF before redirecting, even before the public `runtime.auth` profile is installed in memory. The BFF request may use the public `authRemote.authProfileId` plus the resolved draft domain for `/auth/session/me`, while final authorization still comes from server-side account state and route group checks.
- 2026-06-17 19:36 CT: Express SSR must not pre-redirect protected routes for auth-admin BFF drafts that use `runtime.authRemote` or `runtime.auth.session.mode: "server-cookie"`. In hard reloads, the server cannot authorize the HttpOnly BFF session by itself, so it should render an unprivileged Angular shell and let the browser guard call `/auth/session/me`; traditional non-BFF protected routes can keep the existing same-origin 302 behavior.
- 2026-06-17 19:53 CT: Custom draft-auth submit controls should bind `generic-button.config.loading` to `authForm.<action>.state === "loading"` through `valueInstructions`, provide a user-facing `loadingLabel`, and rely on the button's native disabled + `aria-busy` state to prevent double submits while same-origin auth Lambdas respond. This is UX feedback only; authorization still belongs to server-side session/JWT and tenant/group checks.
- 2026-06-18 19:31 CT: Generic `authFormAction` and `authAdminAction` handlers must treat an existing `*.state === "loading"` status target as an in-flight request and return without sending a second auth/session/admin request. Drafts should still bind visible button loading states, but the handler-level guard is the reusable duplicate-submit protection for slow serverless auth and admin actions.
- 2026-06-17 23:17 CT: Direct reloads of auth-admin BFF protected routes can SSR as an intentionally empty, noindex shell while the browser revalidates the HttpOnly session. Empty `app-root` shells must bootstrap Angular immediately instead of waiting for the static SSR first-paint delay, and duplicate first-load `/auth/session/me` calls should be deduplicated in flight. Do not force Cognito MFA required until auth-admin and draft UI support Cognito challenge flows; TOTP/MFA enforcement needs server-side challenge state and generic draft screens before rollout.
- 2026-06-18 00:21 CT: Cognito TOTP/MFA support is config-driven per auth profile. The browser may receive only sanitized `challenge-required` states and setup material needed by a generic draft page; raw Cognito challenge `Session` values stay server-side behind a short-lived `__Host-zlp_challenge` cookie and `zlp_challenge_csrf`/`X-ZLP-CSRF`. Zoosite uses optional TOTP MFA with generic `/verificar-acceso` and `/configurar-mfa` pages; do not render full `otpauth://` URIs as ordinary visible text or log setup secrets.
- 2026-06-18 00:52 CT: Zoosite Cognito user pool `us-east-1_Pq5OCadbK` now has software-token TOTP MFA enabled with `MfaConfiguration=OPTIONAL`. This does not force MFA for every user, but it lets the generic challenge/setup screens handle Cognito MFA challenges without storing browser tokens or raw Cognito challenge sessions.
- 2026-06-18 01:20 CT: Voluntary TOTP enrollment for already signed-in auth-admin BFF users must stay generic and draft-configured through `authFormAction:startMfaEnrollment` and `authFormAction:verifyMfaEnrollment`. The BFF may temporarily store the Cognito access token server-side behind `__Host-zlp_mfa_enroll` plus `zlp_mfa_enroll_csrf` only because Cognito's software-token APIs require it after password reauthentication; the token, refresh token, raw Cognito session, and setup URI must not be returned to Angular or persisted in draft config.
- 2026-06-18 01:55 CT: Every new `AuthCustomFormService` action exposed to drafts must also be added to the `authFormAction` handler allowlist. Missing allowlist entries silently ignore generic button clicks before loading/error state is written, as happened with voluntary MFA `startMfaEnrollment` and `verifyMfaEnrollment`.
- 2026-06-18 02:26 CT: TOTP setup UIs should prefer the generic `qr-code` component bound to BFF-provided `setup.otpauthUri`, with the manual shared secret only as a wrapped fallback. Do not render the full `otpauth://` URI as visible text, a title, an alt value, analytics metadata, logs, or ordinary draft copy.
- 2026-06-18 03:42 CT: TOTP authenticator display names are server-only profile configuration, not hardcoded app text. Auth profiles may define `mfa.totp.issuer`, `accountLabelTemplate`, and `friendlyDeviceName`; Angular drafts receive only generated setup metadata. Self-service MFA disablement uses the generic `authFormAction:disableMfa` flow and requires active BFF session context, normal CSRF, current password, and current TOTP code before the BFF disables Cognito software-token MFA.
- 2026-06-18 15:52 CT: Draft configs that use runtime data-source mappers must not branch on raw fields hidden by `mapper.fields`. `tools/runtime-data-source-condition-guard.mjs` validates component `condition` strings and `valueInstructions` `when` conditions against mapped item/meta/status fields during app packaging and standalone draft deploy validation, so mapper/condition drift fails before publication.
- 2026-06-18 15:52 CT: Generic admin/support MFA reset is exposed to drafts as `authAdminAction:resetUserMfa` with `runtime.auth.admin.resetUserMfaPathTemplate`. The BFF owns authorization, CSRF, self-reset blocking, Cognito MFA preference reset, target session-version bump, and audit logging; draft UI only renders the admin action and status feedback.
- 2026-06-18 15:52 CT, updated 2026-06-18 20:15 CT: Use `docs/api-driven-config/18-draft-auth-audit-matrix.md` before publishing draft-auth changes. Run `npm run drafts:auth-audit -- --draft-root=drafts\zoositioweb.com.mx --domain=zoositioweb.com.mx --auth-profile-id=staff` for the reusable no-credential static contract check before package upload; it covers route structure, noindex/sitemap treatment, public `runtime.authRemote`, server-cookie session metadata, client/admin/MFA UI wiring, admin lost-device reset wiring, mapper condition safety, and public-payload secret leakage. It does not replace BFF, Angular, public-safety, or browser credential/session QA for client, admin+MFA, pending, suspended, expired, admin reset, and controlled error states.
- 2026-06-18 21:11 CT, updated 2026-06-18 23:05 CT: Future protected draft features such as blogs, dashboards, analytics, uploads, and draft settings must follow `docs/api-driven-config/19-protected-feature-contract.md`: public config exposes only safe routes/auth/data-source/action references and runtime bindings, while tenant ownership, auth-profile binding, groups-to-roles/default-deny policy, DynamoDB/object-storage isolation, Lambda/BFF/JWT authorizer policy, audit policy, error response shape, draft configurability allowlists, and explicit server-only boundaries stay server-only.
- 2026-06-20 01:12 CT: Blog/content-hub articles use draft-like package files in S3-compatible storage for component trees, variables, i18n, media manifests, snapshots, deltas, and published SSR bundles; DynamoDB should hold small metadata, indexes, taxonomy, schedules, moderation, locks, interactions, and hub-sharing records. Drafts compose public/admin blog UIs from generic components and public `runtime.contentHubs` references; server-only hub policy, roles, storage names, auth policy, sanitizer enforcement, and publish validation stay out of browser payloads.
- 2026-06-20 01:31 CT: Content hub backend boundaries start from existing generic service patterns instead of creating blog-only repos: `zoolanding-config-authoring` may own draft-like article package writes, revisions, schedules, taxonomy, hub policy, and publish validation only if an isolated content namespace does not pollute site-package lifecycle; otherwise split to generic `zoolanding-content-authoring`. `zoolanding-config-runtime-read` may own read-only published bundle/slug/taxonomy reads for SSR only if content endpoints stay isolated; otherwise split to generic `zoolanding-content-runtime-read`. `zoolanding-data-dropper-lambda` may accept sanitized `blog_*` analytics events that keep its `appName`/`timestamp` envelope and never include raw PII/comment/form bodies. Keep `zoolanding-image-upload` bounded to grant-protected public image upload; create future generic `zoolanding-feature-media` and `zoolanding-feature-interactions` only when their no-AWS contracts are proven. The local gate is `tools/content-hub-contract-harness.mjs`, which must stay green before real AWS writes.
- 2026-06-24 23:30 CT: `zoolanding-image-upload` must not accept unauthenticated uploads. Public asset uploads require a temporary upload grant stored only as a server-side DynamoDB hash with TTL, scoped domain/kind/page/content-type/size/usage limits, and consumed by the hub upload tool in `zoolandingpage`. Grants, token files, signed URLs, `.env*`, and `.zlp/` state must stay gitignored and out of draft payloads. Abuse notification subscriptions are deployment parameters, not committed repo values.
- 2026-06-20 02:00 CT: Content-builder frontend primitives must stay atomic and draft-composed. Phase 3 adds `generic-table`, `generic-cell`, `generic-rich-text`, and `generic-file-dropzone` to `wrapper-orchestrator`; do not introduce blog-specific Angular managers. `generic-table` emits only row IDs/indexes plus explicit `eventPayloadFields`, never full rows by default. `generic-rich-text` uses `ngx-quill`/Quill only for browser rich editing with Delta JSON/object or plain text; Quill HTML export is not authoritative, and backend publish validation/sanitization remains mandatory. `generic-file-dropzone` validates/selects files but does not own upload authorization or storage.
- 2026-06-20 02:18 CT: Content hub runtime wiring stays generic through `runtime.dataSources` and `runtime.apiActions`, but `kind: "content-hub"` uses a same-origin `ContentHubClientService` with `credentials: include`, draft/hub context headers, and CSRF for mutations instead of the public API proxy client. Public content-hub bindings may carry only safe IDs and allowlisted safe input fields; table/bucket names, upstream URLs, credential refs, tokens, tenant IDs, server policy, authorizer policy, signed URL policy, group-to-role maps, and authorization decisions remain server-only. Content-hub input filtering must normalize camelCase, snake_case, kebab-case, and spaced key variants such as `accessToken`/`access_token`, inspect nested string values for secret-store refs or signed URL markers, and require safe `proxySourceId`/`proxyActionId` values for content-hub bindings.
- 2026-06-20 03:04 CT: Zoosite blog vertical-slice pages stay draft-composed from generic components: public `/blog`, `/blog/:categorySlug`, `/blog/:categorySlug/:articleSlug` routes and admin `/admin/blog*` routes are configured in the draft, while the local seed article package lives under `tools/tests/fixtures/content-hub/` as a no-AWS harness fixture rather than inside the draft tree. Draft route matching now supports `:param` patterns for page resolution and content-hub analytics; route params are not yet a first-class generic component/data-source input. Runtime emits `eventPrefix + "_view"` for configured content-hub article routes, while CTA/component events still come from normal generic component event handlers.
- 2026-06-20 16:25 CT: Zoosite blog admin pages remain draft-composed packages under `drafts/zoositioweb.com.mx/admin-blog*` using generic primitives such as table, search, pagination, toast, rich text, file dropzone, interaction scope, modal, input, text, links, and buttons. Public admin bindings must use `runtime.dataSources[].kind: "content-hub"` with `contentHub.read` and `runtime.apiActions[].kind: "content-hub"` with `contentHub.action`; legacy `kind: "contentHub"`/`remote.contentHub` shapes are not the supported contract. Real permission checks, persistence, publish, scheduling, moderation, and media authorization stay server-side for the backend phase.
- 2026-06-20 17:16 CT: Content hub backend phase starts by extending existing generic lambdas instead of creating blog-only services. `zoolanding-config-authoring` indexes optional draft-package files under `{domain}/content-hubs/{hubId}/...`, rejects server-only/credential-like fields in that namespace, and supports `dev`, `test`, and `production` environment pointers. `zoolanding-config-runtime-read` can resolve `environment=dev` and exposes only allowlisted public `metadata.contentHubs` fields. `zoolanding-data-dropper-lambda` accepts generic blog analytics events only when they include safe `contentHubId` and `articleId`, and rejects obvious PII/credential fields; comments/forms must use moderated feature endpoints, not raw analytics. No real AWS deploy is part of this phase until CI, environment secrets/vars, and explicit deploy approval are complete.
- 2026-06-20 17:54 CT: Content hub backend dev/test stacks are live in `us-east-1`: `zoolanding-config-authoring-dev`, `zoolanding-config-authoring-test`, `zoolanding-config-runtime-read-dev`, `zoolanding-config-runtime-read-test`, `zoolanding-data-dropper-dev`, and `zoolanding-data-dropper-test`. Non-production config storage is managed by `zoolanding-config-authoring` with retained DynamoDB/S3 resources; runtime-read consumes those resources read-only. Non-production analytics raw buckets are managed by `zoolanding-data-dropper-lambda` and retained. Authoring dev/test use server-only `system/deploy-authz.json`; dev derives from the existing deploy authz config by adding `dev` where a rule already allowed `test`.
- 2026-06-21 CT: Protected content-hub browser reads/actions are owned by the new generic `zoolanding-content-hub` BFF, not Angular SSR and not a blog-only Lambda. It exposes only exact same-origin `POST /features/content-hub/read` and `POST /features/content-hub/action`, reuses auth-admin HttpOnly sessions plus CSRF, enforces action-scoped server-side roles, stores article packages/published bundles in its own S3 package bucket, and stores compact article/taxonomy/revision/schedule/media/moderation metadata in DynamoDB. Front-door routing must stay exact-path only for those two endpoints; do not add broad `/features/*` or `/features/content-hub/*` routers. Publishing there creates internal content-hub bundles; public Angular SEO indexes still need the runtime-read/public index bridge before newly published articles automatically appear in public `runtime.contentHubs.publicArticles`.
- 2026-06-21 03:44 CT: Content hub SEO phase may expose only small published public indexes in `runtime.contentHubs.publicArticles` and `publicTaxonomy`: article IDs, locale, public title/summary/path/category/tags/published timestamps, author display label, canonical path, robots, and visible taxonomy labels. SSR can use those indexes for sitemap, RSS/Atom-compatible feed output, basic public search JSON, and article `BlogPosting` metadata, but full authoring state, unpublished content, comments/forms, moderation, locks, table/bucket names, credentials, signed URLs, groups, tenant policy, and raw article component packages stay server-only. Long-term dynamic reads should move to isolated runtime-read content endpoints backed by DynamoDB/S3 metadata when the backend content lifecycle is connected.
- 2026-06-21 04:04 CT: Runtime-read public bundles must not expose operational storage metadata such as config bucket names, S3 prefixes, server-side article ID indexes, or draft sharing allowlists. Public content-hub SEO output should be audited at the actual public surfaces: sitemap XML, RSS/Atom XML, search JSON, and article JSON-LD. Dev/test draft publishes must stay traceable through the protected draft branch flow; if an emergency/manual authoring publish gets ahead of GitHub branches, immediately align the relevant `draft-*` branch or PR and wait for its deploy workflow.
- 2026-06-22 21:52 CT, updated 2026-06-22 22:29 CT: Content-hub front-door routing is now live through `tools/ops/sync-content-hub-front-door.mjs` on the Dokploy/Traefik SSM instance. The dynamic route file is `/etc/dokploy/traefik/dynamic/zoolanding-content-hub.yml`; it must remain exact-path only for `/features/content-hub/read` and `/features/content-hub/action`. Testing uses the shared preview host `test.zoolandingpage.com.mx`; do not include the broken dedicated alias `test.zoositioweb.com.mx` until its CloudFront/DNS target is repaired. Production hosts `zoositioweb.com.mx`/`zoositioweb.com` route to the production stage. Drafts that opt into protected content-hub reads/actions must set `runtime.contentHubs[].publicApiBasePath` to `/features/content-hub` or omit it so the Angular default is used; `/content-hub` is a stale path and will not reach the BFF. Smoke without auth should return `401 Authentication required`, not `404` and not a public payload.
- 2026-06-26 19:26 CT: Parameterized draft detail pages should hydrate generic runtime data sources with `{ "source": "routeParam", "key": "<param>" }`, not by duplicating the same ID in query strings. Zoosite blog admin article detail routes use `/admin/blog/articulos/:id/...` and `content_hub_article_detail.input.articleId` must resolve from route param `id` so edit/preview/SEO/version pages keep metadata when opened from generic table action links.
- 2026-06-22 22:48 CT: Zoosite content-hub admin UI should keep route navigation as draft-configured flex-wrap links or explicit inline CSS grid styles, not Angora grid-template tokens that have not been proven in the deployed runtime. Generic form fields must put visual input sizing on `generic-input.config.inputClasses` and wrapper spacing on `classes`; putting all sizing on wrapper `classes` leaves native controls too small. Content-hub cold-path work should keep read-only paths from initializing unused S3 clients and should cache normalized profile config by config payload plus environment.
- 2026-06-23 00:46 CT: Generic content-hub actions can be triggered from atomic draft UIs. `proxyAction` must build allowlisted inputs from the nearest `interaction-scope` snapshot, generic-table `eventData.rowData`, and direct event data before calling the BFF. `generic-rich-text` and `generic-file-dropzone` are scope-aware primitives; use them instead of draft-specific managers for article bodies and media selection. Content-hub response shortcut refs include article/revision/asset/file/taxonomy/comment/interaction IDs so drafts can show mutation results without reading unsafe raw backend details.
- 2026-06-23 02:32 CT: Content-hub admin list filters should be real draft-composed forms, not standalone `search-box` suggestion widgets. Use `interaction-scope` with `generic-input` fields and `navigateWithScopeQuery` so search/status/pageSize update query params that feed `runtime.dataSources` inputs. Avoid duplicate pagination surfaces: prefer one styled draft pagination control or the table paginator, not both on the same article index.
- 2026-06-23 02:44 CT: Query-param-backed `generic-input` defaults must use component-level `valueInstructions` such as `set:config.value,queryParamOr,q,`. Do not put `{source:"queryParam"}` objects directly in `config.value` for `generic-input`, because the component treats that object as the literal field value and can render `[object Object]`.
- 2026-06-26 16:52 CT: Compact generic-table action UIs should use `actionLabelMode: "tooltip"` plus `actionIconClasses` and keep the visible action header localized through `actionColumnLabel`. Row actions may use same-origin `hrefTemplate` values with row placeholders for link-like navigation, or per-action `eventInstructions` for handler-driven actions; the wrapper forwards per-action instructions instead of forcing component-level instructions. `interaction-scope` fields now resync pristine initial values from query/config updates but preserve dirty user edits, which is required for query-driven selects and paginators.
- 2026-06-26 20:28 CT: Generic table row actions can use `actionButtonStyles` for explicit inline control sizing when a draft needs reliable mobile touch targets and dynamic Angora classes may not be present in the deployed stylesheet. Defaults should stay generic and accessible, while draft-specific sizes such as Zoosite's 52px blog-admin action links belong in draft config.
- 2026-06-26 21:50 CT: Express SSR HTML for draft routes with `route.auth.required` must be non-cacheable even when server-cookie/authRemote profiles intentionally skip server-side redirects and let Angular revalidate through the BFF. Use `Cache-Control: no-store`, `Pragma: no-cache`, `Expires: 0`, and `Vary: Cookie` so protected account/admin pages cannot reuse stale SSR documents from another route or cache private shells.
- 2026-06-26 22:10 CT: Protected SSR shells must also mark `<app-root data-zlp-protected-shell="true">` so the browser bootstrap skips the production static-content delay and keeps the loading curtain active until Angular/BFF session validation finishes. This prevents protected admin routes that SSR only shared chrome from appearing blank after home-to-admin navigation or hard reload.
- 2026-06-27 00:31 CT: Generic draft UIs can now read parameterized route values through `routeParam` and `routeParamOr` value handlers. Use these for detail/action pages such as `/admin/blog/articulos/:id/...` instead of duplicating route IDs in editable inputs or query strings. `generic-table` supports localized Material paginator labels and `hideWhenSinglePage`; draft configs can keep row actions as tooltip-first square links with adequate mobile touch targets. `generic-input` supports `date`, `time`, and `datetime-local` for draft-composed schedule controls.
- 2026-06-27 01:15 CT: `routeParam` and `routeParamOr` must read Angular router params first and then the draft runtime's matched route params. Draft-composed detail pages are often rendered by the app catch-all route, so relying only on Angular `RouterState` drops IDs such as `/admin/blog/articulos/:id/editor`. When an ID is needed for actions but should not be editable, keep the interaction-scope field hidden and render a separate read-only `generic-text` value for users.
- 2026-06-27 02:14 CT: `generic-rich-text` must preserve dirty values from the nearest `InteractionScopeService` when parent config replays stale `config.value`; otherwise draft re-rendering resets active Quill edits. Use query params, not route params, for pages such as `/admin/blog/programados?articleId=...` that do not define `:id` in the route.
- 2026-06-28 02:25 CT: Content-hub article creation/publication now treats category, tags, slug, and public path as first-class product metadata. Drafts may send `articleCategory`, `articleTags`, `articleSlug`, `articlePath`, and `articleVisibility`; the generic content-hub BFF normalizes Spanish-safe slugs, deduplicates comma-separated tags, creates taxonomy metadata, stores `/blog/{categorySlug}/{articleSlug}` by default, and publish should reuse the stored path unless an explicit safe path is provided. Public article detail routes should preserve `draftDomain`/language query context when running on the shared test host, but production canonical Zoosite URLs can rely on the host domain.
- 2026-06-29 01:10 CT: Productizing the content-hub blog is tracked against 12 areas: editorial lifecycle, blog roles/permissions, rich text/component builder stability, visual component catalog/advanced mode, taxonomy UX, media lifecycle, scheduling/revisions, public interactions/moderation, SEO, analytics, operations/audit, and full release QA. Content-hub role policies must be server-side, explicit, action-scoped, wildcard-free, and mapped only to groups allowed by the active auth profile; the harness enforces `hub-admin`, `blog-admin`, `blog-editor`, `blog-publisher`, `blog-reviewer`, `blog-moderator`, `blog-media-manager`, and `blog-analyst` as product roles.
- 2026-06-29 15:53 CT: Zoosite's real Cognito pool and server-only `staff` auth profile must include the dedicated blog groups `zoosite-blog-editor`, `zoosite-blog-publisher`, `zoosite-blog-media`, `zoosite-blog-moderator`, and `zoosite-blog-analyst` alongside `zoosite-admin`/`zoosite-client`. Moderation and analytics admin routes should not rely on admin-only gates when product roles exist; keep those groups explicit in both Cognito and the auth-profile registry before assigning users.
- 2026-06-29 02:38 CT, updated 2026-06-29 13:18 CT: Zoosite Programados uses protected content-hub scheduling contracts: `schedule` for explicit `publish` and `unpublish` scheduling, `scheduleList` for queued schedule reads, and `cancelSchedule` for canceling a queued schedule by stable `scheduleId`. Draft routes to `/admin/blog/programados` must preserve both `articleId` and `revisionId` for publish scheduling. Do not model cancel as unpublish.
- 2026-06-29 02:06 CT: Block 1 editorial lifecycle requires draft-visible `submitReview`, `approveArticle`, `publish`, `unpublishArticle`, and `archiveArticle` actions backed by the generic content-hub BFF. Zoosite wires these through generic buttons/status text in the SEO/editorial page; user-facing status copy must stay generic and must not pass raw backend `.error` text into rendered content.
- 2026-06-28 03:34 CT: Public content-hub runtime data should be bridged into draft variables during `ConfigBootstrapService.load()`. The bootstrap receives the matched draft route path/params from `RuntimeService` and derives `contentHub.publicArticles.items`, `contentHub.categories.items`, `contentHub.tags.items`, and `contentHub.currentArticle` from `runtime.contentHubs.publicArticles/publicTaxonomy`; draft pages should keep rendering those through generic components instead of adding blog-specific Angular page managers.
- 2026-06-28 04:12 CT: Public content-hub article routes must be content-aware, not pattern-only. A path that matches `runtime.contentHubs[].articlePathPattern` renders as an article only when a matching `status: "published"` entry exists in `runtime.contentHubs[].publicArticles`; otherwise draft runtime and SSR must resolve the configured not-found page and analytics must not emit `blog_view`. Article SEO enrichment must also be skipped for SSR 404 responses, even when the public site config still contains other published article indexes for hydration.
- 2026-06-28 05:08 CT: Generic navigation components that preserve draft preview query context must pass Angular SSR `REQUEST.url` into `resolveNavigationTarget`; falling back only to `window.location` makes shared-host SSR links drop `draftDomain`, `debugWorkspace`, and `lang` before hydration. Generic overlay primitives such as dropdown, tooltip, search, and modal should instantiate CDK Overlay/focus-trap services only in the browser, because server-side construction can crash SSR during overlay destroy paths that reference `window`.
- 2026-06-28 05:59 CT: Public content-hub projection in Angular must fail closed if an article-like entry includes `visibility` and it is not `public`. Runtime-read should already omit non-public articles, but SSR/search/feed/bootstrap helpers still defensively require `status: "published"` plus absent-or-`public` visibility before exposing article links, SEO, JSON-LD, or route matches.
- 2026-06-29 00:09 CT: Shared-host testing must resolve content-hub runtime reads against the `test` runtime-read stack, not the production fallback. Express SSR and `ConfigApiService` support environment-specific runtime fallback URLs; generic cards also accept `linkHref` as a backward-compatible alias for `href` so draft-composed public article previews render real SSR anchors that preserve `draftDomain`, `debugWorkspace`, and `lang`.
- 2026-06-29 00:27 CT: Content-hub article SSR SEO must prefer the route-specific runtime bundle `siteConfig` before falling back to the root public index. Newly published/admin-created articles can be present in the article route bundle before the root `publicArticles` index, sitemap, feed, or search JSON have caught up; canonical links and `BlogPosting` JSON-LD should still describe the exact current article when the route bundle exposes a matching `status: "published"` and absent-or-`public` article.
- 2026-06-29 01:44 CT: Content-hub `rolePolicies` must include explicit read permissions for protected reads, not only manage/write permissions. The Angular contract now includes `blog:taxonomy:read`, `blog:media:read`, and `blog:moderation:read` where the generic content-hub BFF requires them, and protected preview reads reuse `blog:article:read`.
- 2026-06-29 14:39 CT: Draft article create/editor surfaces should hydrate category and tag controls from protected `runtime.dataSources` reads instead of static text fields when content-hub taxonomy exists. Use separate literal `taxonomyKind` reads for category/tag, map taxonomy summaries into `{ value, label }` option objects for `generic-input`, and keep static fallback options only as a resilience path while the protected read is loading or empty.
- 2026-06-30 00:35 CT: Content-hub product-readiness smoke follows the editorial lifecycle enforced by the BFF: create article package, approve it with the protected `approveArticle` action, then publish and verify public runtime/search visibility. Do not revert smoke coverage to create-and-publish directly; publication now requires the approved status server-side.
- 2026-06-30 03:00 CT: Product-facing content-hub copy must not expose internal implementation terms such as "content hub", BFF, backend, bundle/package internals, or raw draft variable keys on public/admin Zoosite blog surfaces. Tag-filter routes such as `/blog/tag/:tagSlug` are discovery filters, not canonical article/category pages, so SSR and hydrated Angular metadata must keep them `noindex,nofollow` and self-canonical to the current clean path. Use generic value handlers such as `joinText` for draft-composed labels instead of relying on trailing spaces in `valueInstructions`, because the parser trims arguments.
- 2026-06-30 03:17 CT: Content-hub product-readiness smoke must exercise the real editorial path before product signoff: create, update package content, validate, submit for review, approve, publish, verify runtime bundle/search/public article HTML, schedule, list schedules, and cancel the schedule. Do not reduce it back to create/publish/search because that misses editor persistence, validation, review, and public article rendering.
- 2026-06-30 03:31 CT: After a content-hub smoke article has been published, the scheduling check must schedule `unpublish` with `unpublishAt`; scheduling another `publish` after the article is already `published` is contract-invalid because publish scheduling requires an approved unpublished revision.
- 2026-06-30 04:31 CT: The Zoosite blog/content-hub module should not be called product-complete until testing proves the published runtime bundle matches the promoted draft config and the full admin/public path is green: create, edit, validate, submit review, approve, publish, public article/search/sitemap/feed, schedule/cancel, roles, media, taxonomy, moderation, analytics, SEO, desktop/mobile QA, and sanitized observability. Screens or contracts existing in isolation are not enough for signoff.
- 2026-06-30 05:21 CT: Any Angular `@defer` block that wraps generic runtime components must define an `@error` fallback. Missing `@error` can surface Angular `NG0750` when a deferred chunk fails, so `wrapper-orchestrator`, tab content, app-shell modal/debug hosts, and similar lazy generic surfaces should keep silent or loading fallbacks instead of exposing broken runtime UI.
- 2026-06-30 05:45 CT: Before running the authenticated Zoosite content-hub smoke, run the read-only shared-preview preflight: `npm run ops:public-health -- --hosts=test.zoolandingpage.com.mx --draft-domain=zoositioweb.com.mx --page-path=/blog --runtime-environment=test --expect-content-hub --check-content-hub-unauth --fail-on-aaaa --markdown`. It proves the test host renders the Zoosite draft, runtime-read returns the test bundle with `/features/content-hub`, and the protected content-hub route returns `401` without a session. The live smoke CLI must sanitize backend errors and never print raw cookies, CSRF, tokens, internal URLs, or raw `Invalid id` payloads.
- 2026-06-30 07:08 CT: Protected blog analytics must use the dedicated content-hub read `analyticsSummary`, not `articleList` with empty metric fields. The BFF returns per-article aggregate counts only (`views`, `readProgress`, `ctaClicks`, `reactions`, `comments`, `shares`, `assetDownloads`, and related safe totals) and must not expose raw interaction events, actor hashes, comments, form bodies, email/phone values, metadata, or storage details to the browser. Draft analytics pages should bind to `remote.contentHub.analytics.items` and `remoteStatus.contentHub.analytics`.
- 2026-06-30 07:31 CT: Protected blog actions that require article or revision context must not appear as standalone global buttons unless their interaction scope sends explicit `articleId`/`revisionId`. Keep validation, publication, restore, schedule, and similar mutations on row/detail actions with clear selected-state copy, and make draft contract tests fail on orphan actions such as a list-level `Validar selección` button without payload context.
- 2026-06-30 08:00 CT: Public content-hub SEO endpoints (`/content-hub-search.json`, sitemap, feed) must load the runtime bundle for the public hub route, normally `/blog`, instead of the root `/` bundle. Route-specific runtime projections can contain newly published BFF-backed articles before the root bundle catches up; search/feed/sitemap readiness must prove those endpoints see the same public articles rendered by `/blog`.
- 2026-06-30 08:42 CT: Runtime content-hub public indexes may arrive as either arrays or collection objects with `items`. SSR, `ConfigBootstrapService`, route matching, validators, and schemas must support both shapes for `publicArticles` and `publicTaxonomy`; otherwise live public search, sitemap, feed, and hydrated route matching can silently fall back to stale seed content even when runtime-read returns current articles.
- 2026-06-30 09:41 CT: Public content-hub SEO/runtime reads must pass the effective language into runtime-read and cache runtime site-config lookups by language. Zoosite testing proved `/blog` and route runtime bundles can expose newly published Spanish articles while no-lang runtime reads still return only the seed index; `/content-hub-search.json`, sitemap, feed, and article SEO should therefore use explicit `lang` or refetch with `site.i18n.defaultLanguage`.
- 2026-06-30 13:20 CT: Generic components that receive values through `valueInstructions` must resolve thunk-like dynamic `config.value` entries before hydrating browser controls. `generic-rich-text` specifically must never pass the unresolved function into Quill, because Quill can render the function body as article content; tests should cover both Delta-object and legacy plain-text dynamic values.
- 2026-06-30 13:56 CT: Content-hub product-readiness smoke must verify that the public article route renders the edited rich article body, not just the title, slug, search index, sitemap, or feed entry. A publish flow that exposes only article metadata is not product-ready.
- 2026-06-30 15:19 CT: Browser-side protected draft routes should render the configured page immediately after BFF session/group validation succeeds, then let protected runtime data sources show their own loading states. Do not block `rootComponentsIds` on auth-admin/content-hub data-source completion in the browser; SSR still waits for server data as before, and every protected BFF read/mutation remains server-authorized.
- 2026-06-30 16:18 CT: Moderation/admin mutations for content-hub must be row/detail scoped when they require stable IDs. Comment moderation tables should emit `commentId` through explicit `eventPayloadFields` and attach decisions through row action `eventInstructions`; avoid visible manual ID fields or global buttons that cannot prove selected context. Generic event dispatchers must not log raw `eventData`/scope payloads because comments, forms, and moderation reasons can contain user-provided content.

## Changelog Rules

- Use `changelog/app/YYYY-MM.md` for app/runtime/tooling/deploy history.
- Use `changelog/drafts/YYYY-MM.md` for draft authoring, draft QA, and draft publish history.
- Split a draft into `changelog/drafts/{domain}/YYYY-MM.md` only when the monthly shared file becomes too noisy.
- Changelog entries may be chronological and evidence-heavy; Codex entries must be distilled.
- Keep raw logs out of changelog. Link or summarize sanitized evidence only when useful.

## Closeout Checklist

Before ending a task:

1. Confirm the relevant shared notes were read first and inspect local draft notes when the task depends on an existing draft.
2. Update `Codex.md` only if the task produced a durable project decision or changed a standing rule.
3. Update `changelog/app/` or `changelog/drafts/` only for chronological task history worth retaining.
4. Keep draft-specific scratch notes in `drafts/{domain}/ai_notes/`, `drafts/{domain}/findings/`, or `drafts/{domain}/errors-reports/`.
5. Keep repo-level scratch and raw operational output local-only.
6. Put process logs under `logs/` and avoid leaving `*.log` files in the repo root, `Output/`, `reports/`, `devonly/`, or draft folders.
7. Remove or avoid secrets, credentials, raw env values, signed URLs, and PII from notes/changelogs.
8. Update repo docs if the workflow changed.
9. Audit, fix findings, and rerun the audit at least three times before calling work correct.
10. If draft behavior changed, finish with browser QA on every affected draft route in desktop and mobile viewports.

## Guardrails

- If notes and code disagree, verify against code and docs, then repair the note after verification.
- Do not let one-off debugging output become canonical guidance without distillation.
- Keep canonical notes focused on reusable decisions, constraints, QA rules, and workflows.
